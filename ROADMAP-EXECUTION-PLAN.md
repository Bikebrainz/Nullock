# Nullock roadmap execution plan — drive to exceed Burp on every item

> Authorized 2026-08-02: plan the whole climb, execute in autonomous WAVES across as many sessions as it takes. Each wave: pick the next unclosed batch from PARITY-BACKLOG.md / UI-NAVIGABILITY.md, close with REAL verified code, commit incrementally, flip the matching docs/roadmap/parity.json item + regenerate (parity_report.py --check must stay green), never fake a completion, record honest partials. Verification gates by kind: frontend = bundled @babel/standalone 7.29.0 transform + a functional test of the touched logic; backend = Release build + `ctest --test-dir Build -C Release` + `bash scripts/probe_smoke.sh`; site = whole-site link-check (0 broken) + drift --check. Commit as Bikebrainz, push origin Nullock, verify HEAD==origin.

## Wave 0 — register accuracy (no code): 27 already-present items misclassified as gaps
Verify each against live source, then flip absent/partial/stub -> present/exceeds. Cheap, honesty-improving.
#4,#6,#52,#53,#54,#55,#60,#62,#64,#81,#82,#85,#101,#130,#161,#202,#204,#210,#217,#218,#219,#254,#261,#265,#274,#324,#325

## Wave 1 — surface the biggest hidden features (highest user-visible value)
The 97 orphaned endpoints in UI-NAVIGABILITY.md. Start with: an INSPECTOR tab (/api/inspect + inspector_logic, complete backend, no tab); a unified SCAN LAUNCHER exposing the active-test arsenal (audit/assess/gate + the ~26 per-vuln tests); a REPORTING menu (report/build|html|json); CONTENT-DISCOVERY UI (content/discover, crawler, robots). Each: add tab/menu/button -> invoke endpoint -> render result; transform+functional verify; flip parity.

## Wave 2 — frontend closeable (UI wiring of working backends): 76 items
Batch by tab. Transform+functional verified. Most are hidden-feature surfacing.
#0,#1,#2,#3,#5,#7,#8,#9,#10,#11,#14,#15,#20,#23,#48,#51,#61,#63,#66,#67,#69,#70,#71,#74,#75,#76,#77,#99,#102,#131,#145,#206,#209,#211,#223,#224,#229,#241,#247,#267,#268,#269,#270,#277,#280,#296,#298,#302,#306,#313,#315,#323,#326,#327,#332,#341,#342,#347,#348,#351,#352,#353,#354,#358,#359,#362,#368,#370,#371,#376,#387,#392,#396,#398,#400,#401

## Wave 3 — backend-small closeable: 117 items
Group by subsystem (repeater/sequencer/proxy/intruder/extender) into single build+gauntlet cycles. Add a _logic/control_logic lock where the change guards a fail-silent path.
#12,#13,#16,#19,#21,#28,#29,#31,#32,#33,#34,#35,#36,#37,#39,#40,#41,#42,#44,#45,#47,#59,#78,#79,#83,#86,#87,#88,#91,#93,#98,#122,#123,#125,#127,#128,#133,#135,#144,#146,#148,#149,#150,#152,#153,#154,#156,#158,#162,#163,#165,#176,#178,#179,#180,#181,#183,#185,#186,#188,#194,#195,#196,#203,#208,#216,#222,#225,#230,#234,#235,#238,#239,#245,#248,#249,#252,#253,#257,#258,#259,#264,#271,#273,#279,#281,#283,#284,#286,#289,#292,#293,#294,#299,#300,#304,#305,#307,#308,#316,#317,#320,#333,#337,#343,#345,#350,#355,#356,#357,#366,#385,#393,#399,#402,#403,#405

## Wave 4 — Labs as TryHackMe/HackTheBox scenarios (task #122)
Objective/brief + progressive hints + submit-flag/success-check + difficulty/category + walkthrough tying each step to its Nullock probe; per-lab end-to-end exploit test; tracks/XP gamification; wire into docs/labs + in-app.

## Wave 5+ — the large `absent` builds (one substantial feature per wave, fully verified)
From PARITY-BACKLOG.md, ranked by impact: match-and-replace rule engine, WebSockets history tab, HTTP/2 frame log, SOCKS proxy chaining, intercept response-hold, per-host cert generation UI, sequencer manual-load + full FIPS suite, headless-browser DAST (#12), native CLI+Docker+exit codes (#8). Each gets its own wave: design -> build -> gauntlet -> commit -> roadmap.

## Loop contract
Re-entrant: on each wake, `git fetch origin Nullock`; if a wave is mid-flight continue it, else start the next; always leave the tree clean and HEAD==origin; append a one-line progress note here per wave completed.
- Wave 1 (2026-08-02, 86f094c): INSPECTOR tab added, surfacing /api/inspect. Closed #327 request-attributes, #330 cookies, #332 JWT-decode -> present.

- Wave 1 (2026-08-02, 3db8c7f): PROBE tab added (fingerprint / header-audit / waf-detect / secret-scan on-demand per-URL). Surfaces 4 orphaned endpoints (also flow to Issues); underlying passive capabilities were already present so no parity delta. Orphaned endpoints: 97 -> 92.

- Wave 1 (2026-08-02, 3f89564): TESTS tab added — unified launcher for all 26 active /api/<type>/test checks. Orphaned endpoints: 92 -> 66.

- Wave 1 (2026-08-08, c81109e/3607a42): DISCOVER tab added — content/directory discovery (soft-404 calibrated), robots.txt + sitemap recon, crawler start/stop. Closed #368, #401 -> present. Orphaned endpoints re-audited and corrected: 66 -> 63 (previous 66 baseline undercounted by 1 -- `/api/cache/poison` was never actually closed by TESTS, it has a distinct shape outside the uniform `/<type>/test` contract; UI-NAVIGABILITY.md rewritten in full to match live ui-v2 grep, not just this wave's delta).

- Wave 1 (2026-08-08, 557e5f8): REPORTING tab added — Markdown/HTML report download (POST->blob), JSON posture summary, OpenAPI export/import, CycloneDX SBOM download, workspace push/pull. Closed "Reporting: HTML issue report" (partial->present) and "Reporting: reachable from the UI" (stub->present). Orphaned endpoints: 63 -> 55.

- Wave 2 (2026-08-08, 1074f35): Comparer rebuilt — real item list (paste/load-file/remove/clear, lifted into the app reducer), "Send to Comparer" from Proxy history (request/response) and Repeater (request/response panes), the backend's merged diff split into two scroll-synced side-by-side panels, and a Text/Hex result toggle. Also Ctrl+Space to send in Repeater. Closed #351, #352, #353, #354 (absent -> present). No backend change, no new orphaned-endpoint delta (reused existing /api/compare).

- Wave 2 (2026-08-08, cd03a95/140d355): Repeater message editors gain Raw/Headers/Body/Preview/Hex view tabs (reusing proxy.jsx's renderView/toHexDump), a sandboxed (`sandbox=""`) srcdoc-iframe Render view for the response, in-editor find with wrap-around next/prev + match count, and a selection readout (char count + first-byte dec/hex, non-printing chars labelled). Closed #341, #342 (absent -> present); register-accuracy fix #347 (Ctrl+Space was already shipped in 1074f35, parity.json just never flipped) -> present; #348 and #362 landed real but incomplete (no regex/case-sensitivity; readout lives in Repeater's own panes, not a docked Inspector panel) -> partial, honestly. GitHub Actions ci run green on f8828eb (run 31279648612, https://github.com/Bikebrainz/Nullock/actions/runs/31279648612) -- fully verified, no partials outstanding from this wave.

- Wave 2 (2026-08-09, db0c788/8987a83): continued the Repeater editor thread -- in-editor find gains a case-sensitivity toggle and a regex mode (JS syntax, zero-length-match safe, invalid pattern fails closed instead of throwing); closes #348 (partial -> present). Decoder's Input/Output blocks get a Text/Hex view toggle (reuses proxy.jsx's toHexDump); left honestly partial (#358, absent -> partial) because transcode.cpp's hex-decode/base64-decode already collapse invalid UTF-8 via QString::fromUtf8 before the JSON reaches the UI, so a real non-UTF8 byte is lost server-side before the new view can show it -- fixing that needs a backend wire-format change this cloud session cannot build/verify. #362 (docked Inspector Selection widget) and #359 (docked Inspector panel itself) remain untouched partials, tracked separately. No backend touched this wave. GitHub Actions ci run green on a185ee0 (run 31286362679, https://github.com/Bikebrainz/Nullock/actions/runs/31286362679) -- all 6 jobs (Windows build, Linux build, marketplace/parity drift check, fuzz-corpus replay, CLI syntax, labs syntax) passed; fully verified, no outstanding CI risk from this wave.

- Wave 2 (2026-08-09, d2350aa/8c86b32): docked the Inspector as a live structured panel instead of a separate paste-and-parse tool -- a reusable RepeaterInspectorPanel calls the existing /api/inspect (debounced) and is mounted as an "inspector" view-tab beside raw/headers/body/hex in Repeater's request+response panes, the same toggle in Proxy history's DetailPane request+response panes, and an INSPECTOR/EDIT toggle on Intruder's TEMPLATE editor. Closes #359 (stub->partial reclassification -> present). #362 (Selection widget folded into the same panel) intentionally left untouched/partial -- the readout still lives in Repeater's own panes rather than as a section inside this new panel, an honest remaining gap. No backend touched this wave; verified via bundled-babel transform of both touched files plus a vm-sandboxed render proving the tabs.jsx->proxy.jsx forward reference resolves, and a field-name match against inspector_logic.cpp's JSON keys. README.md + docs/index.html updated (Inspector entry no longer says paste-only). GitHub Actions ci run green on 2a31d3c (run 31292763214, https://github.com/Bikebrainz/Nullock/actions/runs/31292763214) -- all 6 jobs (Windows build, Linux build, marketplace/parity drift check, fuzz-corpus replay, CLI syntax, labs syntax) passed; docs-site run also green (31292763219); fully verified, no outstanding CI risk from this wave.

- Wave 2 (2026-08-09, f872626/a5548cc): Target/Scope batch -- ScopeTab's two glob columns (tabs.jsx) gain click-to-edit, a URL->HOST convert button, and a load-from-file input, plus a shared "include subdomains" checkbox that adds both `host` and `*.host` on Add; proxy.jsx's HTTP-history filter bar gains IN-SCOPE / PARAMS / HIDE-404 toggle chips (a new client-side hostInScope() glob matcher for the first) and right-click add/remove-from-scope on any site-map or history row via a new lightweight context menu. Closes #376, #392, #396, #398 (absent -> present); #392's "requested only" dimension stays trivially-true-for-all-rows and undocumented as a toggle since Nullock has no requested-vs-inferred tracking yet -- called out honestly in parity.json rather than claimed as a real control. No backend touched this wave (all fields already client-side); verified via bundled-babel transform of both touched files, a vm-sandboxed hook-stubbed render of ScopeTab/ProxyTab proving no runtime throw, a Node-side functional test of the pure glob/URL-parsing helpers, and a whole-site relative-link check (0 broken, 78 pages). docs/index.html's Scoped-history card copy updated to name the new filters/scope-add paths. GitHub Actions ci run green on a5548cc (run 31299224895, https://github.com/Bikebrainz/Nullock/actions/runs/31299224895) -- all 6 jobs (Windows build, Linux build, marketplace/parity drift check, fuzz-corpus replay, CLI syntax, labs syntax) passed; docs-site run also green (31299224870); fully verified, no outstanding CI risk from this wave.

- Wave 2 (2026-08-09, 52cf31e/602b6b5): Deep-audit-all batch -- one frontend change (NL.actions.auditAll in real-data.js posting {fromHistory:true} to the existing /api/audit/all, plus a "Deep audit all rows" button in the Findings tab next to "Probe all rows") surfaces the whole default deep-audit sweep cluster from the GUI for the first time. Closes the GUI-reachability gap text on #61/#63 (already present from an earlier wave) and eight scanner register items -- LDAP (A18), XPath (A19), host-header (A22), cache-poisoning (A23), deserialization (A26), NoSQLi (A27), mass-assignment (A28), CORS (A29) -- all already "present" but previously reachable only by curl; gap text updated honestly to reflect the new GUI path rather than bumped to "exceeds". A21 (HTTP request smuggling) intentionally NOT claimed closed: it stays opt-in and excluded from the default sweep for traffic-cost reasons, so the new button doesn't reach it -- left as an honest partial. No backend touched. Verified: bundled-babel transform of both touched files, field-name match of the /api/audit/all response contract ({queued,scopeSkipped,ok,error}) against control_server.cpp:6696-6778, a whole-site relative-link check (0 broken, 78 pages) after docs/roadmap/index.html regeneration, and `python3 scripts/parity_report.py --check` green. README.md + docs/index.html updated with the new sweep. GitHub Actions ci run green on 602b6b5 (run 31306137860, https://github.com/Bikebrainz/Nullock/actions/runs/31306137860) -- all 6 jobs (Windows build, Linux build, marketplace/parity drift check, fuzz-corpus replay, CLI syntax, labs syntax) passed; docs-site run also green (31306137851); fully verified, no outstanding CI risk from this wave.

- Wave 2 (2026-08-09, 80fa0d4): Intercept batch -- InterceptTab gains Send to Repeater/Intruder/Comparer buttons on the currently-held message (new send-to-repeater-raw/send-to-intruder-raw reducer cases reusing the existing repeater/set + intruder/set endpoints, no history round-trip needed) and an eight-button response-modification toolbar (unhide hidden fields, enable disabled fields, remove length limits, remove JS validation, remove all JS, remove <object> tags, HTTPS->HTTP links, strip Secure cookie flag) shown when the held item is a response. Closes #296 and #298, both absent -> partial (not present): #296's Decoder/Scanner pivots are honestly still missing (Decoder isn't lifted into the app reducer the way Comparer is, and there's no ad-hoc scan action anywhere yet), and #298's mods are manual per-message buttons rather than a standing Proxy-Options auto-apply toggle. No backend touched. Verified: bundled-babel transform of both touched files, a Node functional test of all 8 response-mod regex transforms against representative HTML/header fixtures, a field-name match of the intercepted-item JSON contract ({id,kind,host,port,tls,text}) against control_server.cpp:1461-1478, a whole-site relative-link check (0 broken, 78 pages), and `python3 scripts/parity_report.py --check` green. README.md + docs/index.html updated with the new pivot/response-mod capabilities. GitHub Actions ci run green on 80fa0d4 (run 31313634987, https://github.com/Bikebrainz/Nullock/actions/runs/31313634987) -- all 6 jobs (Windows build, Linux build, marketplace/parity drift check, fuzz-corpus replay, CLI syntax, labs syntax) passed; docs-site run also green (31313634979, https://github.com/Bikebrainz/Nullock/actions/runs/31313634979); fully verified, no outstanding CI risk from this wave.

- Wave 2 (2026-08-09, 7dd20d5/37a010d): Intruder batch -- IntruderTab gains a MODE select (sniper/battering-ram/pitchfork/cluster-bomb) in the target row; when pitchfork/cluster-bomb is picked and the template has >1 §marker§, the flat payloads textarea is replaced with a per-position PAYLOAD SETS column editor bound to payloadSets[i] (engine/API already supported this, only the GUI was missing). Adds a RULES bar (payload-processing rule chain: prefix/suffix/case/reverse/match-replace/encode/hash) driven by the live /api/intruder/rule-ops list, +§/CLEAR §/AUTO § buttons on the template editor (AUTO § auto-marks every query-string, form-body, and Cookie value via a new pure autoMarkTemplate() helper, idempotent), and a RESULTS filter/sort bar (2xx-5xx toggle chips, min/max length, regex/negative search, click-to-sort columns preserving the original row index for resend) via two new pure helpers intruderRowVisible()/intruderSortValue(). Closes #0, #1, #2, #3, #10, #11, #14, #15, #23 (register already said present; this closes the GUI-reachability gap) and #5, #20, #48 (partial/absent -> present). No backend touched -- every field consumed (attackType, positions, payloadSets) was already in the /api/snapshot intruder block and the rule-ops/payloadSets contract in control_server.cpp's /api/intruder/set handler; `rules` is UI-only client state (not echoed by the snapshot), matching the existing grepMatch/grepExtract pattern. Verified: bundled-babel transform of both touched files, a Node functional test suite (23 checks) covering autoMarkTemplate (query/cookie/form-body marking, idempotency, CRLF and LF templates), intruderRowVisible (every filter dimension + fail-open on invalid regex), and intruderSortValue (numeric-aware ordering), a hook-stubbed vm-sandboxed render of IntruderTab across sniper/pitchfork-multi-set/empty states proving no runtime throw, confirmation the tabs.jsx NUL byte at offset 47726 was untouched, a whole-site relative-link check (0 broken, 78 pages), and `python3 scripts/parity_report.py --check` green. README.md + docs/index.html updated (new Intruder feature card, MODE/rules/sortable-results language). GitHub Actions ci run green on 37a010d (run 31321818868, https://github.com/Bikebrainz/Nullock/actions/runs/31321818868) -- all 6 jobs (Windows build, Linux build, marketplace/parity drift check, fuzz-corpus replay, CLI syntax, labs syntax) passed; docs-site run also green (31321818860, https://github.com/Bikebrainz/Nullock/actions/runs/31321818860); fully verified, no outstanding CI risk from this wave.

- Wave 2 (2026-08-09, d56b4ae/9bfc482): CSRF PoC batch -- DetailPane (ui-v2/proxy.jsx, shared by the HTTP history table and the Site map) gains a CSRF POC button that calls the already-complete /api/csrf/poc endpoint (RequestExport::csrfPoc -- auto-submitting form for urlencoded/multipart bodies, a credentialed fetch() PoC otherwise) via a new NL.actions.csrfPoc(rowId), and shows the generated HTML in the existing CodecOverlay, extended with an optional note line and a DOWNLOAD button (Blob + object URL) alongside the existing COPY. Closes #224 and #387 (partial -> present) -- both were the same underlying gap (endpoint complete, nothing in the GUI invoked it), reached via one shared component. No backend touched. Verified: bundled-babel transform of both touched files, a vm-sandboxed hook-stubbed render of DetailPane (confirms the button renders with the right title/handler, no throw) and CodecOverlay (confirms the DOWNLOAD button and note line render when passed), a whole-site relative-link check (0 broken, 78 pages), and `python3 scripts/parity_report.py --check` green. README.md + docs/index.html updated with a CSRF PoC feature entry. GitHub Actions ci run green on 9bfc482 (run 31329402279, https://github.com/Bikebrainz/Nullock/actions/runs/31329402279) -- all 6 jobs (Windows build, Linux build, marketplace/parity drift check, fuzz-corpus replay, CLI syntax, labs syntax) passed; docs-site run also green (31329402281, https://github.com/Bikebrainz/Nullock/actions/runs/31329402281); fully verified, no outstanding CI risk from this wave.

- Wave 2 (2026-08-10, c28f16a): Extender/BApp-store batch -- the in-app Marketplace (ui-v2/app.jsx) gains a search input + category chips (ported from the published docs/marketplace/index.html filter model) and a per-item DETAILS panel (author/version/summary/categories/permissions/full sha256, a link to the docs detail page, a raw-source download link, and a prefilled GitHub bug-report link) built entirely from the already-fetched catalog response. Settings tab's Extensions card gains a per-loaded-script modifies-traffic/observe-only badge sourced from bootInfo.extensionGrants (published by the backend since an earlier wave, never read by any frontend until now). Closes #99, #102 (partial -> present) and #131 (partial -> present). No backend touched -- every field consumed was already in the /api/marketplace/catalog response or bootInfo. Verified: bundled-babel transform of ui-v2/app.jsx, a vm-sandboxed hook-stubbed render of Marketplace (idle state, then a ready state with query/category/detailId populated exercising the new filter+detail code paths) and of SettingsTab (Extensions card with extensionGrants populated) proving no runtime throw, a standalone Node functional test of the categories()/visibleItems() filter logic (8 checks) and the mutates-badge logic (5 checks), a field-name match of every field read (id/name/version/author/summary/categories/permissions/sha256/state/installedVersion against control_server.cpp:5388-5400; extensionGrants keyed by fi.fileName() matching extensionScripts, extensions_api.cpp:160/169) confirming nothing invented, a whole-site relative-link check (0 broken, 1051 links across docs/), and `python3 scripts/parity_report.py --check` green. README.md + docs/index.html updated (Extensions section/card describe the new search/detail/badge surfaces). GitHub Actions ci run green on 60e836e (run 31344783088, https://github.com/Bikebrainz/Nullock/actions/runs/31344783088) -- all 6 jobs (Windows build, Linux build, marketplace/parity drift check, fuzz-corpus replay, CLI syntax, labs syntax) passed; docs-site run also green (31344783068, https://github.com/Bikebrainz/Nullock/actions/runs/31344783068); fully verified, no outstanding CI risk from this wave.

- Wave 2 (2026-08-09, 5d9c3c6/0aab7d5): Collaborator batch -- a new COLLABORATOR tab (ui-v2/app.jsx CollaboratorTab, registered in TABS + the tab-render switch) mints out-of-band callback URLs via POST /api/oast/mint (copy-to-clipboard per payload, minted list), polls GET /api/oast/poll on a running since-cursor (manual "poll now" + a 3s auto-poll toggle), and renders the returned hit list with a click-through detail pane (token/time/source IP/method/host header/path/user agent/body bytes/body preview) -- all fields read straight off the endpoint's existing JSON contract. Closes #229 (stub -> present); the previous UI surface was just a status badge ("OAST: {oastDomain}"). Two honest gaps called out in parity.json rather than claimed: DNS-only interactions still never surface (DnsSink retains no hit list, tracked separately under "Collaborator: DNS interaction capture"), and /api/oast/blast (the multi-vector SSRF/XXE/RCE spray) has no button here -- this wave wired the mint-and-watch workflow, not the attack-fire one. No backend touched -- /api/oast/mint and /api/oast/poll were already complete and allowlisted. Verified: bundled-babel transform of both touched files (app.jsx, real-data.js), a hand-rolled hook-dispatcher vm-sandboxed invocation of CollaboratorTab (no jsdom available in this environment; useState/useEffect/useCallback/useRef stubbed for a single render pass) proving the component body and its JSX tree construct without throwing, a field-name match of the mint/poll response contracts (control_server.cpp:3593 mint returns {token,pathUrl,hostUrl,port,baseHost}; control_server.cpp:1927 poll returns {running,port,baseHost,hits:[{id,atMs,token,sourceIp,method,hostHeader,path,bodyBytes,userAgent,bodyPreview}]}) against every field the panel reads, confirmation ui-v2/tabs.jsx was untouched (its NUL byte intact), a whole-site relative-link check (0 broken, 78 pages), and `python3 scripts/parity_report.py --check` green. UI-NAVIGABILITY.md, README.md, and docs/index.html updated (tab count 21->22, orphaned-endpoint count 55->53, new Collaborator feature card). GitHub Actions ci run green on 0aab7d5 (run 31337280077, https://github.com/Bikebrainz/Nullock/actions/runs/31337280077) -- all 6 jobs (Windows build, Linux build, marketplace/parity drift check, fuzz-corpus replay, CLI syntax, labs syntax) passed; docs-site run also green (31337280080, https://github.com/Bikebrainz/Nullock/actions/runs/31337280080); fully verified, no outstanding CI risk from this wave.

- Wave 2 (2026-08-10, de1824b): Intruder generator-dialog batch -- a GENERATOR dialog in the PAYLOADS pane (ui-v2/tabs.jsx IntruderTab) exposes the three payload generators that were already complete in intruder_generators.cpp/control_server.cpp but API-only: numbers (from/to/step/width/hex), dates (from/to/stepDays/format), and a brute forcer (charset/minLen/maxLen), each with a debounced live count+sample preview via /api/intruder/generate before APPLY POSTs the spec to /api/intruder/set{generator} and the full expansion arrives back through the existing snapshot poll. Closes #7, #8, #9 (present-but-API-only -> present with GUI reachability; parity.json state was already "present" so this updates gap/evidence + reconciled rather than flipping a state). No backend touched. Verified: bundled-babel transform of both touched files (tabs.jsx, real-data.js), confirmation the tabs.jsx NUL byte at offset 47726 was untouched, a Node functional test of buildGenSpec's three type branches (numeric coercion, empty-field fallbacks, exact key-set match against control_server.cpp's expandGen), a vm-sandboxed render of IntruderTab with all boolean dialog-open state forced true (exercises the new GENERATOR dialog JSX + its two discovery-fetch effects) proving no runtime throw, and `python3 scripts/parity_report.py --check` green. README.md + docs/index.html updated (Intruder feature line/card mention the generator dialog). GitHub Actions ci run green on efb728a (run 31352965585, https://github.com/Bikebrainz/Nullock/actions/runs/31352965585) -- all 6 jobs (Windows build, Linux build, marketplace/parity drift check, fuzz-corpus replay, CLI syntax, labs syntax) passed; docs-site run also green (31352965584, https://github.com/Bikebrainz/Nullock/actions/runs/31352965584); fully verified, no outstanding CI risk from this wave.

- Wave 2 (2026-08-10, a1aa15d/d23dbbb): Sequencer batch -- a new SEQUENCER tab (ui-v2/app.jsx SequencerTab, registered in TABS + the tab-render switch) wires the token-randomness analyzer (/api/sequencer/analyze, Src/Core/Networking/sequencer_logic.cpp) that was complete and API-only with zero frontend callers -- docs/index.html even advertised the capability with no screen behind it. Manual Load: paste one token per line, LOAD FILE (FileReader), PASTE FROM CLIPBOARD (navigator.clipboard.readText, feature-detected), and a client-side sequencerSampleSummary() pure helper renders count/shortest/longest/mean length BEFORE the ANALYZE NOW call. Analysis renders every field the endpoint returns: score/verdict, Shannon entropy, character-class breakdown, Hamming distance, longest common substring, sequential/monotonic counter detection, and per-position-entropy + bit-level (monobit/two-bit/serial-correlation) tests when the corpus qualifies. Closes the pure-GUI dimension of "Manual load: paste/file/Analyze now" and "Manual load sample summary" (partial -> present); "Sequencer tool tab (dedicated UI)" moves absent -> partial, honestly -- Live Capture stays unbuilt since no backend request-issuing engine exists to drive it. The algorithmic gaps flagged in parity.json for the analysis itself (no per-bit-position FIPS tests, no chi-square character test, per-position keyspace not summed, prefixed-counter detection) are backend C++ work this cloud session cannot build/verify and were left untouched, not claimed. No backend touched -- every field read (n/avgLen/distinctTokens/score/verdict/shannon/charClass/hamming/lcs/sequential/positional/bitLevel) matches sequencer_logic.cpp's analyzeTokens() output exactly. Orphaned-endpoint count: 53 -> 52, tab count: 22 -> 23. Verified: bundled-babel transform of both touched files (app.jsx, real-data.js), confirmation ui-v2/tabs.jsx was untouched (NUL byte still at offset 47726), a Node functional test of sequencerSampleSummary (5 checks), a vm-sandboxed render of SequencerTab across four state branches (empty, populated with positional+bit-level applicable, not-applicable branches, no-data verdict) proving no runtime throw, a whole-site relative-link check (0 broken, 78 pages), and `python3 scripts/parity_report.py --check` green. README.md, UI-NAVIGABILITY.md, and docs/index.html updated (new Sequencer feature card, tab/orphaned-endpoint counts). GitHub Actions ci run green on d23dbbb (run 31362671366, https://github.com/Bikebrainz/Nullock/actions/runs/31362671366) -- all 6 jobs (Windows build, Linux build, marketplace/parity drift check, fuzz-corpus replay, CLI syntax, labs syntax) passed; docs-site run also green (31362671386, https://github.com/Bikebrainz/Nullock/actions/runs/31362671386); fully verified, no outstanding CI risk from this wave.

- Wave 3 (2026-08-10, fef9cc4 / dd9c158 / 045f671 / da103c8): backend batch closing four Repeater/Sequencer items with no progress note logged at the time (backfilled here honestly rather than left undocumented). fef9cc4 adds NetworkingLogic::rewriteHostHeader, syncing Repeater's request `Host:` header to a changed target host unless the header already names a different (deliberately desynced) host -- locked in 5 discriminating networking_logic_test cases. dd9c158/045f671/da103c8 extend Sequencer's FIPS 140-2-style bit-level battery: poker test, runs test, long-runs test, length-bucketed runs, a compression-ratio test, and a sample-size-driven reliability rating on the overall verdict. All four flip their matching parity.json items to present (runs stays partial -- see parity.json for the honest remainder). No frontend touched. GitHub Actions ci run green on e117bba (run 31372938802, https://github.com/Bikebrainz/Nullock/actions/runs/31372938802) -- all 6 jobs (Windows build, Linux build, marketplace/parity drift check, fuzz-corpus replay, CLI syntax, labs syntax) passed, covering the full cumulative diff through da103c8; docs-site run also green (31372938804); fully verified, no outstanding CI risk from this batch.

- Wave 1/2 (2026-08-10, 1c6133b/dd821d4): Collaborator Blast batch -- COLLABORATOR tab gains a Blast section wiring the previously-orphaned POST /api/oast/blast (multi-vector SSRF/blind-XXE/blind-RCE/Log4Shell spray): target URL field, per-class toggle checkboxes, a Fire button, and a rendered list of every fired vector (kind/note/token/callback URL) straight from the endpoint's existing JSON contract. Confirmed callbacks still surface through the tab's existing mint/poll Interactions table and the OAST correlator's auto-confirmed findings -- this wires the attack-fire action, not a new confirmation path. Also corrected UI-NAVIGABILITY.md's orphaned-endpoint tracking, which had drifted from live source: five endpoints (`/api/audit/all`, `/api/csrf/poc`, `/api/intruder/generate`, `/api/intruder/generator-types`, `/api/intruder/rule-ops`) were already wired by earlier waves but never removed from the list. Re-verified every listed endpoint against a fresh literal-string grep of ui-v2/*.jsx and ui-v2/real-data.js; true orphaned count corrected 52 -> 46 of 173 (27%). No backend touched -- every field sent/read matches control_server.cpp's /api/oast/blast handler (control_server.cpp:3619-3829) exactly. Verified: bundled-babel transform of ui-v2/app.jsx, a vm-sandboxed render of CollaboratorTab (hooks stubbed, oastBlast mocked) proving the Blast section and Fire button construct in the element tree with no throw, a whole-site relative-link check (0 broken, 78 pages), and `python3 scripts/parity_report.py --check` green. README.md and docs/index.html updated (Collaborator entry now describes the Blast panel). GitHub Actions ci run green on dd821d4 (run 31375333321, https://github.com/Bikebrainz/Nullock/actions/runs/31375333321) -- all 6 jobs (Windows build, Linux build, marketplace/parity drift check, fuzz-corpus replay, CLI syntax, labs syntax) passed; docs-site run also green (31375333289, https://github.com/Bikebrainz/Nullock/actions/runs/31375333289); fully verified, no outstanding CI risk from this wave.

- Wave 2 (2026-08-10, 5c0c4b4/b4ca3bd): Proxy/Target filter-bar batch -- FilterBar/HistoryTable (ui-v2/proxy.jsx), shared by both the HTTP history table and the Site map, gain a MIME-class select (mimeCategory() bucketing r.mime into html/script/json/xml/css/images/other), an extension show/hide field (pathExtension()/parseExtList()/extFilterMatch()), a case-sensitivity toggle, and negative search via a leading "-" (parseSearchTerm()), on top of the in-scope/params/hide-404 chips and status/method filters shipped in earlier waves. Since Nullock's Site map and HTTP history render through the same FilterBar/HistoryTable pair, one component change closes both #267 and #371 (partial -> present); the honest remainder is an annotation filter and a listener/port filter, both blocked on missing backend stores. No backend touched -- every new predicate operates on fields (mime, path) already in each row. Verified: bundled-babel transform of ui-v2/proxy.jsx, a 26-check Node functional test of the five new pure helpers (mimeCategory/pathExtension/parseExtList/extFilterMatch/parseSearchTerm), a vm-sandboxed render smoke test of ProxyTab proving no throw across FilterBar/HistoryTable/SiteMap/DetailPane, confirmation ui-v2/tabs.jsx was untouched (NUL byte still at offset 47726), a whole-site relative-link check (0 broken, 78 pages, 1051 links), and `python3 scripts/parity_report.py --check` green. README.md and docs/index.html updated (Scoped history feature line/card). GitHub Actions ci run green on b4ca3bd (run 31389193550, https://github.com/Bikebrainz/Nullock/actions/runs/31389193550); docs-site run also green (31389193125, https://github.com/Bikebrainz/Nullock/actions/runs/31389193125); fully verified, no outstanding CI risk from this wave.

- Wave 1 (2026-08-10, 41ced27): JWT toolkit batch -- Inspector tab gains a PARSE / JWT TOOLKIT
  mode toggle. JWT TOOLKIT wires the three previously-orphaned JwtTool/JwtProbe endpoints
  that had zero frontend callers: offline `/api/jwt/analyze` (decode + weakness flags +
  optional HS*-secret wordlist brute-force), `/api/jwt/forge` (alg:none strip or HS256
  re-sign/algorithm-confusion with claim overrides), and a live calibrated `/api/jwt/test`
  acceptance check against a real target (auto-fanned-out carrier or an explicit
  header:/cookie: location) -- a capability Burp only ships via a paid extension. Also fixed
  a latent bug this audit surfaced: the TESTS tab's TEST_TYPES included "jwt", which posted
  to `/api/jwt/test` via the tab's uniform `{url,param?,method?}` contract -- but that
  endpoint requires a `token` field the generic form never collected, so selecting jwt there
  could never have succeeded. Removed it from TEST_TYPES (25 uniform checks remain) now that
  a real token-aware JWT flow exists in Inspector. No parity.json item matches this feature
  (JwtProbe's live-acceptance-test is Nullock-native, beyond Burp's baseline, like the SCANS
  rollups) so no state flip was needed; UI-NAVIGABILITY.md's JWT/OAST/crypto section closes
  (orphaned-endpoint count 37 -> 34) and its stale claim that a pure literal-string grep is
  exhaustive was corrected (`NL.actions.runTest` does build `/api/${type}/test` dynamically).
  No backend touched -- every field read (alg/typ/kid/header/payload/weaknesses[]/
  secretRecovered/secret; token; vulnerable/calibrated/authStatus/rejectStatus/
  requestsSent/hits[]) matches control_server.cpp:6501-6620 exactly. Verified: bundled-babel
  transform of both touched files, confirmation ui-v2/tabs.jsx was untouched (NUL byte still
  at offset 47726), a vm-sandboxed construction of InspectorTab/JwtToolkit/TestsTab proving
  no runtime throw, a whole-site relative-link check (0 broken, 78 pages, 1262 links), and
  `python3 scripts/parity_report.py --check` green. README.md and docs/index.html updated
  (Inspector card, active-tests count 26 -> 25).

- Wave 1 (2026-08-10, 34740a2/f4b6a85): unified scan/audit runner batch -- a new "Assess & audit" section in the SCANS tab (ui-v2/app.jsx ScansTab) wires all 9 previously-orphaned unified scan/audit endpoints: one-shot target Assess (fingerprint + CVE correlation + header/method/TLS, /api/assess), a synchronous Audit run against a single URL with per-tester include checkboxes (/api/audit/run), a Param miner with optional pasted wordlist (/api/paramminer), a Request chain editor for multi-step raw-HTTP {{var}}-extraction flows (/api/chain/run), the capstone Pipeline run orchestrator (/api/pipeline/run), and on-demand Posture grade / Inventory / Compliance coverage / CI gate-check rollups (/api/posture, /api/inventory, /api/compliance, /api/gate). No backend touched -- every field read was verified against control_server.cpp's actual JSON contract for each handler (posture/gate/inventory/compliance/assess/paramminer/chain/pipeline field names all confirmed by direct grep, not invented). Closes the GUI-reachability half of #57 [A9], #63 [A15], and #79 [A31] in parity.json (present -> present with GUI reachability, gap/why/evidence/reconciled updated honestly, the underlying non-GUI caveats -- static CVE DB, XXE absent from TESTS' uniform battery, deep-audit's 60-entry wordlist cap -- left untouched since they're real and unrelated to this batch). The other 6 endpoints (gate/inventory/compliance/chain-run/pipeline-run/posture) have no matching Burp-parity backlog item -- they're Nullock-native rollups/orchestrators beyond Burp's feature set, not parity gaps -- so only UI-NAVIGABILITY.md's orphaned-endpoint count moves (46 -> 37 of 173, 21%), no parity.json state changes for those six. Verified: bundled-babel transform of both touched files (app.jsx, real-data.js), confirmation ui-v2/tabs.jsx was untouched (NUL byte still at offset 47726), a Node functional test of the toggleInclude/chain-steps-JSON-validation/wordlist-parsing pure logic (11 checks), a vm-sandboxed render of ScansTab across both the empty-state and every-result-slot-populated branches proving no runtime throw, a whole-site relative-link check (0 broken, 78 pages, 1051 links), `python3 scripts/parity_report.py --check` green, and marketplace/labs-site drift checks green. README.md and docs/index.html updated (new "Assess & audit" feature entry). GitHub Actions ci run green on f4b6a859 (run 31405024364, https://github.com/Bikebrainz/Nullock/actions/runs/31405024364) -- all 6 jobs (Windows build, Linux build, marketplace/parity drift check, fuzz-corpus replay, CLI syntax, labs syntax) passed; docs-site run also green (31405021694, https://github.com/Bikebrainz/Nullock/actions/runs/31405021694); fully verified, no outstanding CI risk from this wave.

- Wave 1 (2026-08-10, confirm CI for 41ced27/542b343): JWT toolkit wave -- GitHub Actions ci run green on 542b3436 (run 31419820102, https://github.com/Bikebrainz/Nullock/actions/runs/31419820102); docs-site run also green (31419820081, https://github.com/Bikebrainz/Nullock/actions/runs/31419820081); fully verified, no outstanding CI risk from this wave.

- Wave 2 (2026-08-10, 06e1d6d/3e7cae8): Issues/Baseline batch -- backfilled here (the append step was skipped when this batch closed). ISSUES tab gains a Baseline bar (save/status/diff/clear against /api/baseline/*, new-vs-fixed-vs-unchanged counts and lists), a FLAT/GROUPED view toggle (GROUPED calls /api/findings/grouped, bucketing by kind+host with instance count/max CVSS/CWE-OWASP), and a per-finding Triage button (/api/triage/finding, AI or heuristic-fallback impact/fix/false-positive read, rendered inline). Also fixed SEVERITY_COLOR/SEVERITY_ORDER, which had no "critical" entry despite several backend testers emitting that severity. Closes #211 (partial -> present, the Baseline bar closes the GUI-reachability gap) and upgrades #225 (stays partial -- verdict persistence and the unbundled Ollama dependency are the honest remainder, narrowed now that a UI entry point exists). No backend touched. README.md and docs/index.html updated (new Findings feature entry). GitHub Actions ci run green on 3e7cae8 (run 31434724146, https://github.com/Bikebrainz/Nullock/actions/runs/31434724146); fully verified, no outstanding CI risk from this wave.

- Wave 2 (2026-08-11, dc22e4d/49b0490): Issue-detail batch -- clicking a finding in the ISSUES flat list used to jump straight to Proxy (jumpToRow) with no advisory view and nowhere to land for extension-sourced findings (rowId 0). Now it toggles an inline detail pane (ui-v2/app.jsx IssueDetailBody) with three sub-tabs: Advisory (renders the enrichment fields FindingEnricher already attaches -- cwe/owasp/cvssScore/cvssVector/confidence/compliance/fixSummary), Request, and Response (the same raw history bytes proxy.jsx's DetailPane pulls via NL.requestRawById/responseRawById), with every literal occurrence of the finding's evidence string highlighted inline via a new highlightEvidence() pure helper. jumpToRow stays reachable through an explicit "Open in proxy" button in the pane header. Closes #400 (absent -> present); the one honest remainder is that rowId-0 findings still have no request/response to show (the pane says so rather than fetching row 0), tied to item #390's existing rowId-0 gap rather than a new one. No backend touched -- every field read matches control_server.cpp:1311-1328's finding JSON contract exactly. Verified: bundled-babel transform of ui-v2/app.jsx, a 12-check Node functional test of highlightEvidence (empty/no-match/single/multiple/adjacent/case-sensitive/null-input branches), an 8-check vm-sandboxed render smoke test of IssuesTab/IssueDetailBody across advisory/request/response tabs, a rowId-0 finding (confirms no fetch is attempted), and an evidence string absent from the raw text, confirmation ui-v2/tabs.jsx was untouched (NUL byte still at offset 47726), a whole-site relative-link check (0 broken, 78 pages, 1051 links), and `python3 scripts/parity_report.py --check` green. README.md and docs/index.html updated (Findings feature entry mentions the detail pane). Pushed as dc22e4d (feature) + 49b0490 (roadmap); GitHub Actions ci run green on fe2c608 (run 31446611034) and on 49b0490 itself (run 31446556374), docs-site also green (31446556412) -- confirmed this session, fully verified, no outstanding CI risk from that wave.

- Wave 2 (2026-08-11, 5a557ef): Site-map/message-editor batch -- renderView()'s shared 'preview' tab (Proxy history's DetailPane, Site map's same DetailPane, and Repeater's message editor all call the one global function) only ever pretty-printed JSON bodies. Added a DOM-free prettyPrintMarkup() -- splits on tag boundaries, walks lines tracking a nesting depth so closing tags dedent first, and skips depth changes for self-closing/void-HTML/open-close-same-line tags -- wired as the 'preview' fallback when a body starts with '<'. Register-accuracy correction on item #370 (Site map / message editor): re-verified live source and found two of its three original complaints already fixed by earlier waves but never reflected in parity.json -- Inspector has been docked into the message editor's view-tab list since the 2026-08-09 Inspector wave (d2350aa), and the requestRawAt id-1 positional-eviction bug was already replaced by id-keyed NL.requestRawById/responseRawById (real-data.js, with an explanatory code comment). Item #370 stays partial, honestly: the Site Map tree groups by host only (NL.sitemap is {host,count,tls}, no per-path field anywhere in the snapshot -- control_server.cpp:1446-1459), so there is still no leaf node to click for one specific URL; you filter by host on the map, then pick the row in the HTTP history table below. No backend touched. Verified: bundled-babel transform of ui-v2/proxy.jsx, a 13-check Node functional test of prettyPrintMarkup (nesting, self-closing, void tags, XML declaration, empty/whitespace input, deep nesting) and renderView's JSON/XML/plain-body/raw/headers/body branches, a vm-sandboxed render smoke test of DetailPane (null row, then a populated row whose response is XML, confirming the Pretty tab's underlying renderView call produces the indented output with no throw), confirmation ui-v2/tabs.jsx was untouched (NUL byte still at offset 47726), a whole-site relative-link check (0 broken, 78 pages, 971 links), and `python3 scripts/parity_report.py --check` green. README.md and docs/index.html updated (Repeater entry mentions the Pretty view now auto-indenting JSON/XML/HTML). GitHub Actions ci run green on 3e105a8 (run 31455810761, https://github.com/Bikebrainz/Nullock/actions/runs/31455810761) -- confirmed next session as promised: 3e105a8 sits directly on top of 5a557ef with only a roadmap-doc commit in between, so its green run covers 5a557ef's code unchanged; docs-site also green (31455810873); fully verified, no outstanding CI risk from that wave.

- Wave 1 (2026-08-11, 76f025e): GraphQL toolkit batch -- the PROBE tab (ui-v2/app.jsx ProbeTab) gains two buttons wiring the previously-orphaned /api/graphql/schema (introspection dump -> dangerous-mutation-name + sensitive-field-name flagging) and /api/graphql/probe (five active checks -- introspection, field-suggestion leak, alias-amplification, depth-bypass, batched-query bypass; async, findings stream into Issues like every other active test), reusing the tab's existing per-URL run/render pattern. No parity.json item matches GraphQL attack tooling (same as the JWT toolkit/deep-audit-all precedent -- it's a Nullock-native capability beyond Burp's baseline, already claimed in docs/index.html's comparison table on the strength of the backend alone) so no state flip was needed; this wave closes the GUI-reachability gap only. Also corrected UI-NAVIGABILITY.md doc drift carried since the 2026-08-10 Issues/Baseline wave: /api/baseline/{save,status,diff,clear}, /api/findings/grouped, and /api/triage/finding were wired into the ISSUES tab back then but never removed from the orphaned-endpoint list -- re-verified via a fresh literal-string grep against live ui-v2/*.jsx + real-data.js and corrected alongside this wave's own 2-endpoint delta. True orphaned count corrected 34 -> 25 of 173 (14%). No backend touched -- every field read (ok/introspectionEnabled/types/fields/mutationType/dangerousMutations/sensitiveFields/note; queued/target) matches control_server.cpp:6226-6497 exactly. Verified: bundled-babel transform of both touched files (app.jsx, real-data.js), confirmation ui-v2/tabs.jsx was untouched (NUL byte still at offset 47726), a vm-sandboxed render smoke test of ProbeTab across four states (base/empty, graphqlSchema with dangerous mutations + sensitive fields populated, graphqlSchema with introspection disabled, graphqlProbe queued) proving no runtime throw, a whole-site relative-link check (0 broken, 78 pages, 1051 links), `python3 scripts/parity_report.py --check` green, and marketplace/labs-site drift checks green. README.md and docs/index.html updated (new GraphQL toolkit line/card). GitHub Actions ci run green on 76f025e (run 31465790597, https://github.com/Bikebrainz/Nullock/actions/runs/31465790597) -- all 6 jobs (Windows build, Linux build, marketplace/parity drift check, fuzz-corpus replay, CLI syntax, labs syntax) passed; docs-site also green (31465790603); the follow-up roadmap-note commit 523c43d's own ci run is green too (31465874761, no docs-site run triggered since that commit only touched ROADMAP-EXECUTION-PLAN.md, outside the docs-site workflow's path filter) -- confirmed this session via send_later check-in, fully verified, no outstanding CI risk from this wave.

- Wave 3 (2026-08-11, 80e3672/eb944ee, roadmap 9e56004/4c3ca7c): backfilled here honestly (the append step was skipped when this batch closed). 80e3672 adds Sequencer's maxPositionalTransitionV -- for each character position, Cramer's V of the contingency table between consecutive tokens' character at that position, catching a generator whose successive outputs are correlated per-position even when each position's marginal distribution looks uniform (the blind spot per-position entropy alone misses); surfaced as result["transition"], locked in sequencer_test with a +1-shift corpus and a <40-token sentinel case. eb944ee extends the shared transcode engine (Decoder tab + Intruder processing rules) with octal-encode/decode (3-digit octal per byte) and binary-encode/decode (8 bits per byte, space or packed on decode), both rejecting malformed input, plus md4/sha224/sha384/sha3-256 hashes alongside the existing md5/sha1/sha256/sha512 -- ops count 18 -> 26, wired into the Decoder UI op list (ui-v2/app.jsx). Both commits' own log claims "Full gauntlet green (92/92 + 156/156)" from a build this cloud session cannot itself run; taken on trust from the commit record rather than re-verified locally, but GitHub Actions ci run green on 4c3ca7c (run 31474905215, https://github.com/Bikebrainz/Nullock/actions/runs/31474905215) covers the full stack through both feature commits, confirmed this session. Flips both matching parity.json items to present.

- Wave 2 (2026-08-11, 8efc7d2/986e204): Authz-test batch -- DetailPane (ui-v2/proxy.jsx, shared by the HTTP history table and the Site map) gains an AUTHZ TEST button opening a modal identity editor (name + raw "Header: value" lines per identity, add/remove) that replays the captured row as each identity via the already-complete /api/authz-test (control_server.cpp:4592, Burp Auth Analyzer equivalent) through a new NL.actions.authzTest(rowId, identities), rendering a per-identity status/body-size table plus a divergent/consistent verdict banner (the server still files its own authz-divergence finding into Issues independently). Closes the GUI-reachability half of #78 [A30] -- state was already "present" so no flip, gap/why/evidence/reconciled updated honestly; the real remaining caveats (manual-header identities, naive status/size divergence heuristic) are left in the gap text, not claimed fixed. No backend touched. UI-NAVIGABILITY.md's orphaned-endpoint count corrected 25 -> 24 of 173 (14%). Verified: bundled-babel transform of both touched files (proxy.jsx, real-data.js), confirmation ui-v2/tabs.jsx was untouched (NUL byte still at offset 47726), a 7-check Node functional test of the parseHeaderLines pure helper (empty/single/CRLF-multi/no-colon/leading-colon/whitespace-trim/blank-line-skip), a vm-sandboxed render of AuthzTestOverlay proving no runtime throw, a field-name match of the /api/authz-test request/response contract against control_server.cpp:4584-4691 (confirmed independently by a parallel research agent before implementation), a whole-site relative-link check (0 broken, 78 pages, 971 links), and `python3 scripts/parity_report.py --check` green. README.md and docs/index.html updated (new Authz test feature entry). Pushed as 8efc7d2 (feature) + 986e204 (roadmap); HEAD verified == origin/Nullock. GitHub Actions ci run green on e5a48d9 (run 31478221613, https://github.com/Bikebrainz/Nullock/actions/runs/31478221613) -- e5a48d9 sits directly on top of 986e204/8efc7d2 with only a roadmap-doc commit in between, so its green run covers this wave's code unchanged; docs-site also green on 986e204 (run 31478161406, https://github.com/Bikebrainz/Nullock/actions/runs/31478161406) -- confirmed this session via send_later check-in, fully verified, no outstanding CI risk from this wave.

- Wave 0 (2026-08-11, 942da87): register-accuracy audit -- all 27 listed items (#101,#130,#4,#6,#202,#204,#210,#217,#218,#219,#254,#261,#265,#274,#324,#325,#52,#53,#54,#55,#60,#62,#64,#81,#82,#85,#161) were already graded present/exceeds in parity.json (a prior pass had flipped `state` but never recorded the audit itself), so this batch's real work was re-verifying each claim against live source (symbol/line spot-checks across every referenced file: extensions_api.cpp, intruder_engine.cpp, intruder.cpp, oast_server.cpp, oast_correlator.cpp, cert_authority.cpp, proxy_server.cpp/.hpp, intercept.hpp, intercept_logic.hpp, transcode.cpp, passive_scanner.cpp/.hpp, finding_enricher.cpp, session_manager_logic.cpp/.cpp, control_server.cpp, app.cpp, ui-v2/app.jsx, ui-v2/proxy.jsx, ui-v2/tabs.jsx) -- all 27 confirmed true, then adding the missing audited_state+reconciled trail to 25 of them. The other 2 (extender [A7] extension security model, [B28] BApp Store browse+install) had genuinely stale gap/why/evidence text left over from before later waves shipped (rebuildEngine's full-engine-replace on reload(), the marketplace's 14-entry catalog with search/category filter and per-item DETAILS panel from the 2026-08-10 c28f16a wave) -- refreshed honestly; states were already correct (exceeds/present) so no flip. No code touched. Verified: `python3 scripts/parity_report.py --check` green, a whole-site relative-link check (0 broken, 78 pages, 971 links). Pushed as 942da87 (register) + a same-session follow-up backfilling the self-referential commit hash into the 27 reconciled blocks (that placeholder problem -- can't know your own commit hash before committing -- resolved the same way this file's own log has resolved it before: a hash-backfill commit right after). HEAD verified == origin/Nullock. This is a docs-only change (parity.json + docs/roadmap/index.html + this file); no C++/frontend touched, so no CI risk expected, but this session cannot itself build the C++ side to confirm -- will confirm the next GitHub Actions run is green before calling this fully closed.

- Wave 0 (2026-08-11, roadmap cf8b6f3): CI confirmed green on cf8b6f3 -- ci run 31507871036 (https://github.com/Bikebrainz/Nullock/actions/runs/31507871036) and docs-site run 31507871013 (https://github.com/Bikebrainz/Nullock/actions/runs/31507871013) both completed with conclusion=success, checked directly via the GitHub Actions API this session. No C++/frontend was touched by this batch as expected. Wave 0 register-accuracy audit (942da87/cf8b6f3) is fully verified and closed, no outstanding risk.

- Wave 1 (2026-08-11, 9b3269b): SCANS-tab recon batch -- the Assess & audit section (ui-v2/app.jsx ScansTab) gains three single-target Section blocks wiring previously-orphaned recon endpoints that were complete and API-only: Exposure scan (POST /api/exposure/scan -- curated .git/.env/actuator/backup path probe, confirmed by content signature), Service CVE correlation (POST /api/servicevulns/scan -- banner-grab a host's ports against a curated CVE table), and JS recon (POST /api/jsrecon/scan -- mines same-origin JS bundles for API endpoints, hardcoded secrets, and exposed source maps). No backend touched -- every field read (probed/catchAll/hitCount/hits; portsProbed/hitCount/hits; scripts/endpointCount/sourceMaps/secretCount) matches control_server.cpp's actual JSON contract for each handler exactly. No parity.json item matches any of the three (Nullock-native capabilities beyond Burp's baseline already claimed in docs/index.html's comparison table on the strength of the backend alone -- same precedent as the GraphQL/JWT-toolkit/deep-audit-all waves), so this closes GUI-reachability only; UI-NAVIGABILITY.md's orphaned-endpoint count corrected 24 -> 21 of 173 (12%). Verified: bundled-babel transform of both touched files (app.jsx, real-data.js), a vm-sandboxed hook-stubbed render of ScansTab proving no runtime throw and confirming all three new Section titles construct in the element tree, a whole-site relative-link check (0 broken, 78 pages, 1051 links), and `python3 scripts/parity_report.py --check` green. README.md and docs/index.html updated (Active scanner / Version->CVE / new JS-recon feature lines, Assess & audit card). Pushed as 9b3269b; HEAD verified == origin/Nullock. GitHub Actions ci run green on 9b3269b (run 31492030559, https://github.com/Bikebrainz/Nullock/actions/runs/31492030559); docs-site (pages) run also green on 9b3269b (run 31492030525, https://github.com/Bikebrainz/Nullock/actions/runs/31492030525) -- confirmed this session via send_later check-in; the roadmap-note commit deda47d touches only this file (outside any build/docs-site path filter) so its own ci run carries no code risk regardless of outcome; fully verified, no outstanding CI risk from this wave.

- Wave 2 (2026-08-11, 58346ff): Notes/Update-check batch -- ScopeTab (ui-v2/tabs.jsx) gains a Notes card (textarea bound to scope.notes, dirty-tracked against mid-typing snapshot polls, saved on blur/Ctrl+Enter/a SAVE button) wiring the previously-dead NL.actions.scopeSetNotes -> POST /api/scope/notes -> ProjectStore::setNotes path via a new "scope-set-notes" reducer case in app.jsx; closes #206 (partial -> present) -- still one project-wide blob, no per-host/per-issue/per-request notes, called out honestly as the remainder. App() (ui-v2/app.jsx) gains a small dismissible UpdateBanner reading the now-surfaced NL.update (real-data.js applySnapshot), showing current/latest version + a release-notes excerpt + a VIEW RELEASE button (Qt.openUrlExternally when embedded, matching the existing CA-folder convention), dismissal remembered per latest version via localStorage; closes #223 (stub -> present) -- the backend UpdateChecker/root["update"] packing was already complete with zero frontend readers. No backend touched. Verified: bundled-babel transform of both touched files (app.jsx, tabs.jsx) plus a syntax check of real-data.js, confirmation ui-v2/tabs.jsx's intentional NUL byte was untouched (same fuzz-payload-string context, just shifted offset from the earlier insertion), a vm-sandboxed functional test suite (reducer's scope-set-notes case updates state and calls the action; UpdateBanner renders null when unavailable and a populated div tree when available; ScopeTab constructs with the Notes textarea present, no throw), a whole-site relative-link check (0 broken, 78 pages, 1051 links), and `python3 scripts/parity_report.py --check` green. README.md and docs/index.html updated (new Engagement notes / Update check feature entries). Pushed as 58346ff (feature) + this roadmap-note commit; HEAD verified == origin/Nullock. Will confirm the next GitHub Actions ci run is green before calling this fully closed (this cloud session cannot build the C++ side itself).

- Wave 2 (2026-08-11, confirm CI for 58346ff/7cd3f66): Notes/Update-check wave -- GitHub Actions ci run green on 7cd3f66 (run 31523819176, https://github.com/Bikebrainz/Nullock/actions/runs/31523819176) -- all 6 jobs (Windows build+regression-tests+smoke, Linux build, marketplace/parity drift check, fuzz-corpus replay, CLI syntax, labs syntax) passed; docs-site (pages) run also green on the same commit (run 31523819199, https://github.com/Bikebrainz/Nullock/actions/runs/31523819199) -- confirmed this session via three send_later check-ins as the Windows and Linux builds each took ~13 minutes; fully verified, no outstanding CI risk from this wave.

- Wave 1 (2026-08-11, a015fc3): TLS-inspection batch -- the SCANS tab's Assess & audit section gains a "TLS / certificate inspection" Section wiring the previously-orphaned /api/tls/inspect (live host:port TLS connect, flags expired/self-signed/weak-key/hostname-mismatch/legacy-protocol config, CWE-295; findings also file into Issues), matching the existing exposure-scan/service-CVE/JS-recon single-target input pattern. No parity.json item matches TLS-cert-inspection-as-a-scanner (same Nullock-native precedent as GraphQL/JS-recon) so no state flip; README's pre-existing "TLS audit" line was already claiming this capability on the backend alone, now corrected to note GUI reachability. /api/request/curl was deliberately left orphaned -- confirmed it's functionally superseded by the client-side COPY AS exporter (proxy.jsx renderRequestAs, already closing parity #306 at "exceeds" with 9 formats + redaction), so wiring the backend endpoint too would add a redundant, weaker path rather than close a real gap. UI-NAVIGABILITY.md orphaned count corrected 21 -> 20 of 173 (12%). No backend touched -- every field read (connected/protocol/cipher/subject/daysToExpiry/findingCount/findings[]) matches control_server.cpp:7341-7382 exactly. Verified: bundled-babel transform of both touched files (app.jsx, real-data.js), confirmation ui-v2/tabs.jsx was untouched (NUL byte still present), a 6-check functional test of the port-parsing pure logic (empty/valid/zero/overflow/non-numeric/high-port), a vm-sandboxed render smoke test of ScansTab proving no runtime throw, a whole-site relative-link check (0 broken, 78 pages, 1051 links), `python3 scripts/parity_report.py --check` green, and marketplace/labs-site drift checks green. README.md and docs/index.html updated. Pushed as a015fc3; HEAD verified == origin/Nullock. Will confirm the next GitHub Actions ci run is green before calling this fully closed (this cloud session cannot build the C++ side itself).

- Wave 1 (2026-08-11, confirm CI for a015fc3/6896f17): TLS-inspection wave -- GitHub Actions ci run green on a015fc3 (run 31538397919, https://github.com/Bikebrainz/Nullock/actions/runs/31538397919); docs-site (pages) run also green on a015fc3 (run 31538397918, https://github.com/Bikebrainz/Nullock/actions/runs/31538397918); the roadmap-note commit 6896f17's own ci run is green too (run 31538447684, https://github.com/Bikebrainz/Nullock/actions/runs/31538447684) -- confirmed this session via two send_later check-ins; fully verified, no outstanding CI risk from this wave.

- Wave 1 (2026-08-12, 1b5c530): WS Repeater batch -- the Proxy tab's HTTP HISTORY pane-head gains a WS REPEATER overlay wiring the previously-orphaned WebSocket-injection pair (GET /api/ws/sessions, POST /api/ws/send) into ui-v2/proxy.jsx (WsRepeaterOverlay + NL.actions.wsSessions/wsSend in real-data.js): a live-session dropdown (polled every 2s, showing host:port and up/down frame counts), direction (up/down) + opcode (text/binary/close/ping/pong) selectors, a payload editor, SEND, and a one-click RESEND LAST. Closes parity #270 partial -> present; UI-NAVIGABILITY.md orphaned count 20 -> 18 of 173 (10%). No backend touched -- both response shapes read (sessions[].id/host/port/framesUp/framesDown; send response ok/queued) verified against control_server.cpp:2009-2024 and :5689-5735 by reading the handlers directly. Verified: bundled-babel transform of both touched files, confirmation ui-v2/tabs.jsx was untouched (NUL byte intact, file not in the diff), a Node functional test of the request-building/resend-override logic, `python3 scripts/parity_report.py --check` green. README.md and docs/index.html updated with a WS Repeater card/line. Pushed as 1b5c530; will confirm the next GitHub Actions ci run is green before calling this fully closed (this cloud session cannot build the C++ side itself).

- Wave 1 (2026-08-12, confirm CI for 1b5c530/2015e41): WS Repeater wave -- GitHub Actions ci run green on 1b5c530 (run 31550468828, https://github.com/Bikebrainz/Nullock/actions/runs/31550468828); the roadmap-note commit 2015e41's own ci run is green too (run 31550585394, https://github.com/Bikebrainz/Nullock/actions/runs/31550585394), and its docs-site (pages) run is green as well (run 31550585370, https://github.com/Bikebrainz/Nullock/actions/runs/31550585370) -- 1b5c530 touched only ui-v2/ so no pages run was expected or triggered for it; confirmed this session via two send_later check-ins as the Windows and Linux builds ran their usual ~13-20 minutes; fully verified, no outstanding CI risk from this wave.

- Wave 0/3 (2026-08-11/12, backfilled here honestly -- a prior run closed these but skipped this file's required append step): abb6559 fixes HIGH #1 of the Src/Core/Networking security review (PR #1, archived at c6d8c9a as `reviews/2026-08-11-mad-httpsec.md`) -- the header-audit redirect follower gated on hostname only, so an https->http downgrade or port change re-emitted captured Cookie/Authorization headers over cleartext and bound verdicts to the pre-redirect URL; a new pure isSameOriginRedirect() predicate (header_audit.hpp/.cpp, header_logic.cpp) requires exact scheme+host+port equality before following, locked by 9 cases in header_audit_test. a5b3cb6 fixes HIGH #2 -- the JWT probe stripped secondary credentials only on the no-token calibration shot, so a forged-token shot kept a session Cookie and a cookie-authenticated endpoint was misreported as a signature/algorithm bypass; the strip now applies to both shots (jwt_probe_logic.cpp), with jwt_probe_test's buggy-behavior-pinning assertion corrected to assert the fix. Findings #3-#11 (MEDIUM/LOW) remain open in the review doc for future waves. Separately: 0f0d772 fixed site nav (nine hand-written docs/ pages had a stale nav bar missing Extensions/Labs/Roadmap links, restored depth-correctly); 5d45642/f1fa81b added and closed #239 (XML issue report, GET /api/report/xml, absent -> present, 144/409); 2f27079/d512e8c added and closed #186 (Sequencer sample-size warning + FIPS 140-2 20,000-bit threshold guidance, partial -> present, 145/409). Both HIGH-fix commits claim "Full gauntlet green (92/92 + 156/156)" from a build this cloud session cannot itself run, taken on trust from the commit record; GitHub Actions ci run green on every one of these commits individually, confirmed this session via the Actions API: abb6559 (run 31555424978), 0f0d772 (run 31555533748), a5b3cb6 (run 31555773050), c6d8c9a (run 31555802997), f1fa81b (run 31556603839), d512e8c (run 31558348779) -- fully verified via CI, no outstanding risk from this backfilled batch.

- Wave 1 (2026-08-12, 3e338f5): Detection-templates batch -- the SCANS tab's Assess & audit section gains a "Detection templates" Section wiring the previously-orphaned nuclei-style template scanner (GET /api/template/list, POST /api/template/run): a bundled-template picker (id/name/severity/description from templates/detections/*.json) plus a custom-JSON template editor, both firing against a target URL and rendering match/request-count/severity, with matches also filing into Issues per the endpoint's existing behavior. No parity.json item matches this Nullock-native capability (same precedent as the JWT/GraphQL-toolkit waves -- README and docs/index.html already claimed the backend template engine on its own strength, now corrected for GUI reachability plus a new comparison-table row), so this closes orphaned-endpoint count only: UI-NAVIGABILITY.md corrected 18 -> 16 of 173 (9%). No backend touched -- every field read (ok/count/templates[]; ok/matched/matchedCount/requests/capped/results[]/templateId/name/severity) matches control_server.cpp:3431 (list) and :3457 (run) exactly. Verified: bundled-babel transform of both touched files (app.jsx, real-data.js), a vm-sandboxed hook-stubbed render of ScansTab proving the new section constructs with no throw, confirmation ui-v2/tabs.jsx was untouched (NUL byte intact, file not in the diff), a whole-site relative-link check (0 broken, 78 pages, 1078 links), and `python3 scripts/parity_report.py --check` green. README.md and docs/index.html updated (Template scanner line, new comparison-table row). This batch rebased onto a concurrent wave (65f4035/2ed9673, Sequencer wrapped-counter detection closing #153) that landed on origin mid-session; rebased cleanly, all local checks (transform/render/parity/link-check) re-run and green post-rebase. Pushed as 3e338f5; HEAD verified == origin/Nullock. Will confirm the next GitHub Actions ci run is green before calling this fully closed (this cloud session cannot build the C++ side itself).

- Wave 1 (2026-08-12, confirm CI for 3e338f5/b081f32): Detection-templates wave -- GitHub Actions ci run green on 3e338f5 (run 31560433383, https://github.com/Bikebrainz/Nullock/actions/runs/31560433383) and on b081f32 (run 31560528422, https://github.com/Bikebrainz/Nullock/actions/runs/31560528422); docs-site (pages) run also green on 3e338f5 (run 31560433354, https://github.com/Bikebrainz/Nullock/actions/runs/31560433354) -- b081f32 touched only this file, outside the pages workflow's path filter, so no pages run was expected or triggered for it, matching prior precedent; fully verified, no outstanding CI risk from this wave.

- Wave 3 (2026-08-12, backfilled here honestly -- a prior run closed these but skipped this file's required append step, same pattern noted in earlier backfills): seven small Intruder/Sequencer backend closures landed between the Detection-templates wave and this entry, each its own feature commit + roadmap commit, all with GitHub Actions ci run green (confirmed this session via the Actions API against every listed commit): 211b9c3/05a1ad7 fix Sequencer's corpus-decode mislabel (an odd-length hex corpus fell through to base64 and was decoded+labelled wrong) and non-conformer intolerance (one bad token used to zero out the whole corpus's bit-level analysis; now skipped and counted, not fatal) -- closes #152 (147/409). 233729a/50b507e advertise the already-executing Decode payload-processing rules (base64/base64url/url/hex/html-decode) in Intruder's rule-ops list, previously hidden by a deliberate-but-incomplete comment -- closes #40 (148/409). ce02c64/331747b add the Null payloads generator (N empty payloads, bypassing parseSet's empty-line drop) -- closes #33 (149/409). 1735b1f/89580e7 add the Character frobber generator (code-point-safe +1 bump per position, surrogate-range-aware) -- closes #34 (150/409). 60d5bc4/9ddc4cc add the Character blocks generator (whole-string repeat by multiplier, not truncate) -- closes #32 (151/409). 6a4188d/f8c0f13 add the Case modification generator (Burp's five case options incl. the Propername/ProperName distinction, code-point-safe) -- closes #29 (152/409). 66eb638/5a3ed41 add Sequencer positional-length-tolerance diagnostics (modalWidth/atModalWidth/offWidth/skipReason always reported, so a variable-length corpus is distinguishable from a too-small one) -- closes #144 (153/409). Every commit's own log claims "Full gauntlet: 92/92 ctest + 156/156 probe_smoke" from a build this cloud session cannot itself run, taken on trust from the commit record; independently confirmed via the GitHub Actions API this session that ci is green on all fourteen commits (211b9c3, 05a1ad7, 233729a, 50b507e, ce02c64, 331747b, 1735b1f, 89580e7, 60d5bc4, 9ddc4cc, 6a4188d, f8c0f13, 66eb638, 5a3ed41) plus docs-site green where triggered -- fully verified via CI, no outstanding risk from this backfilled batch.

- Wave 2 (2026-08-12, 223f9ec): Session handling rules batch -- the SESSIONS tab gains a Session handling rules editor wiring the previously-orphaned macro engine (/api/session-rules/set, /api/session-rules/clear-vars; session_rules.hpp -- Burp's session-handling-rules/macro equivalent: extract a value from a matched response by header/cookie/JSON-path/regex, store it as a variable, inject it into every subsequent matched request by header/cookie/body/URL). A named-rule form (host/path glob, extract-from + key + variable, inject-into + key + template) with add/edit/delete and a per-rule enable/disable checkbox sits above a live captured-variables readout with a Clear button; three pure helpers (sessionRuleUpsert/sessionRuleRemoveAt/sessionRuleToggle) build the next full array since the backend only exposes a whole-list SET, not per-rule CRUD. Closes parity item 155 ("Session handling rules: named, ordered, individually enable/disable-able rules in a rules editor", partial -> present); the honest remainder is unchanged from before -- no cross-restart persistence (tracked separately, still absent) and no manual drag-to-reorder (matches the sibling Match & Replace RULES tab's own precedent of no reorder UI despite an unused ruleMove backend action). No backend touched -- every field read/written matches control_server.cpp:1587-1611 (snapshot) and :3869-3895 (/api/session-rules/set, /clear-vars) exactly. Verified: bundled-babel transform of both touched files (app.jsx, real-data.js), confirmation ui-v2/tabs.jsx was untouched (NUL byte intact, file not in the diff), a 16-check Node functional test of the three pure array helpers, a vm-sandboxed render smoke test of SessionsTab across a populated-rules state and a bare/no-sessionRules state proving no runtime throw, a whole-site relative-link check (0 broken, 78 pages, 998 links), and `python3 scripts/parity_report.py --check` green. This batch rebased cleanly onto a concurrent wave (e8b8695/e5fee25, Intruder Character substitution generator closing #28) that landed on origin mid-session -- also skipped this file's append step, noted here rather than left silent; post-rebase total is 409 features, 155 at parity (38%). README.md and docs/index.html updated (Session rules line + a new Session handling rules feature card). Pushed as 223f9ec (feature) + this roadmap-note commit; HEAD verified == origin/Nullock. Will confirm the next GitHub Actions ci run is green before calling this fully closed (this cloud session cannot build the C++ side itself, though this batch touched no C++).

- Wave 2 (2026-08-12, confirm CI for 223f9ec/43767fc): Session handling rules wave -- GitHub Actions ci run green on 43767fc (run 31570736555, https://github.com/Bikebrainz/Nullock/actions/runs/31570736555); docs-site (pages) run also green on 43767fc (run 31570736559, https://github.com/Bikebrainz/Nullock/actions/runs/31570736559); ci run green on the feature commit's own head e5fee25 predecessor chain confirmed clean throughout -- fully verified, no outstanding CI risk from this wave. Fast-forwarded local to origin (683bd64, #35 Bit flipper) before pushing this note; that and #165 (1c21e73, Cookie jar transport safety) are further concurrent Wave-3 backend closures from other sessions, both already CI-green per the Actions API, to be folded into this file's progress log in a future run.

- Wave 3 (2026-08-12, backfilled here honestly, folding in the two closures flagged as pending in the entry above): 2f60292/683bd64 add Intruder's Bit flipper payload generator (single-bit XOR sweep across every byte of a seed value, matching Burp's option) -- closes #35 (157/409). 1f250cf/1c21e73 fix a real security bug in the cookie jar: Secure-flagged cookies were gated on `port==443`, not on whether the connection actually negotiated TLS, so a Secure cookie captured over a real HTTPS connection on a non-standard port (or proxied/CONNECT-tunneled setups where 443 isn't literally the wire port) could still be replayed in cleartext, and conversely a plain port-443 HTTP dev listener could leak a Secure cookie it should never have carried; the gate now checks the request's actual TLS state -- closes #165, present -> present with the transport-safety gap actually fixed rather than just documented (was reclassified present->partial by an earlier adversarial-verify pass specifically for this bug). Then two register/foundation items, also backend, also unbuilt/unverified locally: c37a99d/14b0dd4 lay foundation for #194 (cookie jar domain scope) -- CapturedCookie gains domain/hostOnly fields, parseSetCookie's attribute switch gains a `domain` arm, and a pure domainMatches() (RFC 6265 5.1.3 exact-or-dot-suffix) is added alongside the existing pathMatches -- but injectInto is NOT yet gated on it (the PARITY-BACKLOG plan's step 4), so a domain cookie still doesn't correctly ride a different subdomain than the one that set it; left honestly at absent -> partial, not present -- wiring the injectInto gate is the natural next Wave-3 pickup for #194. All four commits' own logs claim a full local gauntlet from a build this cloud session cannot run; GitHub Actions ci was green on 2f60292/683bd64/1f250cf/1c21e73 and on 14b0dd4 (confirmed via the Actions API this session, before this session's own commits queued behind them). Separately, eight more test-hardening commits (df48f1c..37a49f9, "Tests: lock ...") added no parity-affecting behavior -- they lock existing passive-scanner/JWT-analyze behavior against regression (previously-untested leaked-secret/security-header/subdomain-takeover/stack-trace/cloud-storage/DOM-XSS/PII/JWT-severity detectors) -- all CI-green per the Actions API; two of that same run surfaced and fixed real false-negatives, 8f55a35 (stack-django's dead-row fix: the Django 404 DEBUG-page fingerprint literally could not fire) and 850d611 (outbound-PII gate was over-suppressing public 172.x hosts as if they were RFC1918-private), documented in 7b05cd2's CHANGELOG entry; CI for 8f55a35/850d611/7b05cd2 was still in_progress as this note was being written (queued behind this session's own df50bbc build) -- will confirm green next run per this file's standing convention for backend changes this cloud session cannot itself build.

- Wave 2 (2026-08-12, df50bbc): Decoder "Send to Decoder" batch -- Proxy history / Site map's DetailPane (ui-v2/proxy.jsx) gains a DECODER dropdown (request/response) beside the existing COMPARER one; Repeater's request/response message-editor panes (ui-v2/tabs.jsx RepeaterTab) gain a DEC button beside CMP; the Intercept queue's held-message toolbar (ui-v2/tabs.jsx InterceptTab) gains a DEC button beside CMP. All three dispatch a new `send-to-decoder` reducer action that bumps a client-only `decoder.seedNonce` slot in the app reducer's state; DecoderTab (ui-v2/app.jsx) now takes a `decoder` prop and a `useEffect` keyed on `seedNonce` overwrites its own local input (clearing any stale output/error/chain from a prior run) on every send, including a repeat send of identical text -- the same hand-off shape Comparer already used, just without Comparer's persistent item list since Decoder only ever holds one active buffer. Closes parity #323's affordance half (partial -> present); the item's other named gap -- the Proxy pane's own CodecBar selection-decode tools being a second, divergent JS implementation instead of a call to `/api/transcode` -- is left honestly untouched in the gap text as unrelated follow-up, not silently claimed fixed. No backend touched. Verified: bundled-babel transform of all three touched files (app.jsx, proxy.jsx, tabs.jsx), confirmation ui-v2/tabs.jsx's intentional NUL byte was untouched (1 byte, same fuzz-payload-string context, offset shifted by the inserted lines as expected), a vm-sandboxed hook-stubbed render/functional suite (6 checks: the reducer's send-to-decoder case bumps seedNonce on both a fresh send and a same-text repeat send; DecoderTab renders unseeded with no throw; DecoderTab renders seeded and its local `input` hook slot is provably overwritten with the seed text, proving the effect actually fires and not just that the component doesn't throw; DetailPane renders with onSendDecoder wired; RepeaterTab renders with the new sendToDecoder path; InterceptTab renders and dispatches through its new sendToDecoder), a whole-site relative-link check (0 broken, 78 pages, 1078 links), and `python3 scripts/parity_report.py --check` green (409 features, 158 at parity = 39%). README.md and docs/index.html updated (Decoders line in the README table; the Intercept-&-edit and Repeater feature cards in docs/index.html now name the Decoder pivot). Pushed as df50bbc (feature, includes parity.json + docs) + this roadmap-note commit (which also backfills df50bbc into parity.json item #323's self-referential `reconciled.commit`, following this file's established pattern for that unavoidable ordering problem); HEAD verified == origin/Nullock via two `git rev-parse` calls before and after push. Will confirm the next GitHub Actions ci run is green before calling this fully closed (this cloud session cannot build the C++ side itself, though this batch touched no C++).

- Catch-up + Wave 1 (2026-08-12, d77f1b3): between df50bbc (the append above) and this run's start, 34 further commits landed on origin from other sessions, each with its own "docs: reconcile roadmap" commit flipping the matching parity.json item as it closed (so parity.json itself never drifted, only this file's append log fell behind) -- mostly small Wave-3 Intruder/Decoder generator and processing-rule closures (Character substitution, Substring/Reverse substring, Username generator, Illegal Unicode, propername case, cookie-jar expiry, regex Match/replace, ECB block shuffler payload type). Before starting new work this run confirmed the chain landed clean: `python3 scripts/parity_report.py --check` passed (409 features, in sync) and GitHub Actions ci was green on the HEAD of that chain (fc72a0f, run 31593367146, conclusion=success) via the Actions API. This run's own new work: the SCANS tab's Assess & audit section gains an "HTTP/3 detection" Section wiring the previously-orphaned /api/http3/detect (reads the Alt-Svc response header for advertised h3/h3-* protocol support, files an info finding when advertised) -- the same single-target-probe pattern as TLS inspection/exposure-scan/service-CVE-correlation/JS-recon. No parity.json item matches this Nullock-native capability (same precedent as those sibling probes) so this closes GUI-reachability only. Also corrected UI-NAVIGABILITY.md's stale tracking: an earlier session-handling-rules wave wired /api/session-rules/set and /api/session-rules/clear-vars into the SESSIONS tab but never updated this doc; re-verified every listed orphaned endpoint via a fresh literal-string grep of live ui-v2/*.jsx + ui-v2/real-data.js, true orphaned count corrected 16 -> 13 of 173 (8%). No backend touched -- every field read (ok/error/advertisesHttp3/http3Versions/altSvc/protocols[].id/.authority/.maxAge/.isHttp3) matches control_server.cpp's /api/http3/detect handler exactly. Verified: bundled-babel transform of both touched files (app.jsx, real-data.js), a vm-sandboxed hook-stubbed render of ScansTab in both its default empty state and with http3Res forced populated via a targeted useState-call-index override (proving the new conditional branches construct with no throw and produce the expected advertised-protocol/Alt-Svc text), confirmation ui-v2/tabs.jsx was untouched (NUL byte intact, file not in the diff), a whole-site relative-link check (0 broken, 78 pages, 1078 links), and `python3 scripts/parity_report.py --check` green (409 features, in sync). README.md and docs/index.html updated (Recon feature line, new HTTP/3 comparison-table row, Assess & audit card description). Pushed as d77f1b3; HEAD verified == origin/Nullock via two `git rev-parse` calls before and after push. Will confirm the next GitHub Actions ci run is green before calling this fully closed (this cloud session cannot build the C++ side itself, though this batch touched no C++).

- Wave 1 (2026-08-12, confirm CI for d77f1b3/0f75631): HTTP/3 detection wave -- GitHub Actions ci run green on d77f1b3 (run 31597455882, conclusion=success) and on 0f75631 (run 31597504452, conclusion=success), checked directly via the GitHub Actions API this session. No C++ was touched by this batch as expected; fully verified, no outstanding CI risk from this wave.

- Wave 1 (2026-08-12, ea7d3e5): H2 Frame Log batch -- the Proxy tab's HTTP HISTORY pane-head gains an H2 FRAME LOG button opening a new H2FrameLogOverlay (ui-v2/proxy.jsx) wiring the previously-orphaned GET /api/h2/streams (per-stream summary: conn/streamId/method/path/status/bytesIn/bytesOut/framesIn/framesOut/closed) and GET /api/h2/events?since=<cursor> (a live-tailing raw frame feed: ts/conn/type/flags/streamId/bytes/errorCode, capped client-side at the last 500 tailed), polled every 2s while open -- modeled directly on the existing WS Repeater overlay's pattern. Closes parity #277's GUI-reachability gap (state stays present; the H2EventLog capture itself was already complete and fed from real nghttp2 frame callbacks on both MITM legs, only the UI affordance was missing -- "Burp has no frame log at all" per the item's own gap text). No backend touched -- both response shapes read match control_server.cpp:1961 (streams) and :1984 (events) exactly, both already allowlisted in isReadPath (:1669-1670). UI-NAVIGABILITY.md orphaned-endpoint count corrected 13 -> 11 of 173 (6%) after a fresh literal-string grep of every listed endpoint against live ui-v2/*.jsx + ui-v2/real-data.js. Verified: bundled-babel transform of ui-v2/proxy.jsx, confirmation ui-v2/tabs.jsx was untouched (not in the diff, NUL byte intact), a vm-sandboxed construction of both H2FrameLogOverlay (empty-state streams/events) and WsRepeaterOverlay (proving the shared file edit didn't regress the existing overlay) with no runtime throw, a whole-site relative-link check (0 broken, 78 pages, 1078 links -- unchanged from the prior wave's baseline since no new pages were added), `python3 scripts/parity_report.py --check` green (409 features, in sync), and marketplace/labs-site drift checks green (16/51 pages respectively). README.md and docs/index.html updated (H2 Frame Log feature card, new comparison-table row). Pushed as ea7d3e5 (feature, includes parity.json + docs) + this roadmap-note commit (which also backfills ea7d3e5 into parity.json item #277's self-referential `reconciled.commit`, following this file's established pattern for that ordering problem); HEAD verified == origin/Nullock via two `git rev-parse` calls. Will confirm the next GitHub Actions ci run is green before calling this fully closed (this cloud session cannot build the C++ side itself, though this batch touched no C++).

- Wave 1 (2026-08-12, confirm CI for ea7d3e5/7cc6c01): H2 Frame Log wave -- GitHub Actions ci run green on ea7d3e5 (run 31613113147, conclusion=success) and on 7cc6c01 (run 31613167741, conclusion=success), checked directly via the GitHub Actions API this session. No C++ was touched by this batch as expected; fully verified, no outstanding CI risk from this wave.

- Wave 1 (2026-08-12, 4678e4f): SCANS/Settings orphaned-endpoint batch -- the port scanner (SCANS tab) gains an "Import nmap XML" file-picker next to its existing "Export nmap XML" (wiring POST /api/portscan/import-nmap, a saved or externally-run nmap `-oX` scan feeds the same PortResult pipeline as a live scan), a new "Port scan → findings" Section in the Assess & audit block promotes the port scanner's current results into the shared findings list (wiring POST /api/portscan/to-findings, idempotent on re-post -- exposed db/remote-admin/mgmt-API/cleartext/file-share plus banner→CVE correlation), and Settings' Extensions card gains an "Install bundled" button (wiring POST /api/extensions/install-builtins, copies the extensions shipped with the repo into the user's extensions dir and reloads). No parity.json item matches any of the three (same GUI-reachability-only precedent as the GraphQL/JS-recon/TLS-inspection waves -- these are complete backends that simply had zero ui-v2 callers), so this closes UI-NAVIGABILITY.md's orphaned-endpoint count only: 11 -> 8 of 173 (5%), leaving `/api/cookies`, `/api/cve/overlay`, `/api/cve/overlay/clear`, `/api/cve/sync`, `/api/intruder/multi`, `/api/project/templates` (plus the two intentionally-orphaned `/api/cache/poison` and `/api/request/curl`) as the honest remainder for a future wave. No backend touched -- every field read (imported; openPorts/emitted/skippedDuplicates/bySeverity/findings; installed/destDir) matches control_server.cpp's handlers exactly (extensions/install-builtins:5424, portscan/to-findings:9333, portscan/import-nmap:9662). Verified: bundled-babel transform of both touched files (app.jsx, real-data.js), a vm-sandboxed hook-stubbed render of ScansTab (empty and populated port-scan states, exercising both the new Import button and the Port scan → findings Section) and SettingsTab (exercising the new Install-bundled button) proving no runtime throw, confirmation ui-v2/tabs.jsx was untouched (NUL byte intact at offset 50840, file not in the diff), a whole-site relative-link check (0 broken, 1078 links), and `python3 scripts/parity_report.py --check` green (409 features, in sync). README.md and docs/index.html updated (Recon/Extensions lines, Assess & audit card). Pushed as 4678e4f; HEAD verified == origin/Nullock via two `git rev-parse` calls. Will confirm the next GitHub Actions ci run is green before calling this fully closed (this cloud session cannot build the C++ side itself, though this batch touched no C++).

- Wave 1 (2026-08-12, confirm CI for 4678e4f/781d26d): SCANS/Settings orphaned-endpoint wave -- GitHub Actions ci run green on 4678e4f (run 31628445131, conclusion=success) and on 781d26d (run 31628480208, conclusion=success), checked directly via the GitHub Actions API this session. No C++ was touched by this batch as expected; fully verified, no outstanding CI risk from this wave.

- Wave 1/2 (2026-08-12, 69ab754): Cookie jar / CVE overlay / project templates batch -- Sessions tab gains a Cookie Jar section (GET /api/cookies: full per-host inventory with Path, resolved Expires/session state, and httpOnly/Secure/SameSite coverage percentages), Scans tab gains a "CVE overlay" section (GET /api/cve/overlay, POST /api/cve/overlay/clear, POST /api/cve/sync: push extra service CVEs directly for air-gapped use or sync a feed URL, live entry count, Clear button), and Settings' Projects card gains a template picker + "Create from template" button (GET /api/project/templates, POST /api/project/create-from-template). Closes the missing-attribute half of parity #162's gap (state held at partial -- per-cookie add/edit/remove still has no backend route, a real absent capability, not just unwired) and the GUI-reachability half of parity #208's gap (state held at partial -- the template schema's `rules`/`extensionsEnabled` fields are still silently dropped server-side, a separate real backend gap). `/api/intruder/multi` intentionally left orphaned: same field set as the already-wired async Intruder flow (/api/intruder/set + /api/intruder/start), so a second GUI path would be a redundant, weaker duplicate -- same precedent as /api/request/curl. UI-NAVIGABILITY.md's true orphaned-endpoint count corrected 8 -> 3 of 173 (2%), all three now intentional (`/api/cache/poison`, `/api/request/curl`, `/api/intruder/multi`). No backend touched -- every field read matches control_server.cpp's existing JSON contracts exactly (cookies :4954-4993, cve/overlay :4421-4429, cve/sync :4431-4559, project/templates :4998-5030, create-from-template :5031-5073). Verified: bundled-babel transform of both touched files (app.jsx, real-data.js), a vm-sandboxed render smoke test of SettingsTab/SessionsTab/ScansTab in both empty and fixture-populated states (fixtures built directly from each handler's JSON contract, substituted into the source before transform to force the populated-branch JSX) proving no runtime throw, confirmation ui-v2/tabs.jsx was untouched (NUL byte intact at offset 50840, file not in the diff), a whole-site relative-link check (0 broken, 1289 links), marketplace/labs-site drift checks green, and `python3 scripts/parity_report.py --check` green (409 features, in sync). README.md and docs/index.html updated (Version->CVE, Engagement notes, new Cookie jar card). Pushed as 69ab754; HEAD verified == origin/Nullock via two `git rev-parse` calls before and after push. Will confirm the next GitHub Actions ci run is green before calling this fully closed (this cloud session cannot build the C++ side itself, though this batch touched no C++).

- Wave 1/2 (2026-08-12, confirm CI for 69ab754/42c864e): Cookie jar/CVE overlay/project templates wave -- GitHub Actions ci run green on 69ab754 (run 31643361470, conclusion=success) and on 42c864e (run 31643409661, conclusion=success); docs-site (pages) run also green on 69ab754 (run number 157, conclusion=success) and on 42c864e (run number 158, conclusion=success), checked directly via the GitHub Actions API this session. No C++ was touched by this batch as expected; fully verified, no outstanding CI risk from this wave.

- Wave 2 (2026-08-13, 4983339): Inspector Selection-widget batch -- RepeaterInspectorPanel (ui-v2/tabs.jsx) gains a permanent "Selection" section (character count + first selected byte's decimal/hex value, non-printing bytes labelled \n/\t/\r/space via the existing repeaterDisplayChar/repeaterSelectionStats helpers) rendered ahead of the parsed-view content, replacing a bottom-strip readout that vanished whenever Inspector itself was the active view-tab. The same selection tracking was wired into Proxy history/Site map's DetailPane (ui-v2/proxy.jsx), which previously had zero selection-aware code outside the codec bar's grab-whole-or-selected helper -- new reqSel/respSel state, onSelect/onMouseUp/onKeyUp handlers on both textareas, a bottom RepeaterSelectionReadout strip matching Repeater's own UX, and `sel` passed into both RepeaterInspectorPanel calls. Closes parity #362 (partial -> present); honest remainder called out in the gap text: Intruder's INSPECTOR/EDIT template-editor toggle still doesn't track selection, so its Inspector panel always shows "nothing selected" -- a narrower surface, not a missing capability. No backend touched. Verified: bundled-babel transform of both touched files, confirmation ui-v2/tabs.jsx's NUL byte was untouched (still present, offset shifted to 51699 by the inserted lines as expected), an 11-check Node functional test of repeaterSelectionStats/repeaterDisplayChar (empty/no-selection/length/first-char/dec/hex/tab/space/CR/plain-char branches), a vm-sandboxed render smoke test of RepeaterInspectorPanel (with and without a populated `sel`) and of DetailPane with the new selection wiring, both proving no runtime throw, a whole-site relative-link check (0 broken, 78 pages, 1078 links), marketplace/labs-site drift checks green, and `python3 scripts/parity_report.py --check` green (409 features, 168 at parity = 41%). README.md and docs/index.html updated (Inspector entry now names the Selection widget). Pushed as 4983339 (feature) + 5a7a632 (commit-hash backfill); HEAD verified == origin/Nullock via two `git rev-parse` calls before and after push. Will confirm the next GitHub Actions ci run is green before calling this fully closed (this cloud session cannot build the C++ side itself, though this batch touched no C++).

- Wave 2 (2026-08-13, confirm CI for 4983339/5a7a632): Inspector Selection-widget wave -- GitHub Actions ci run green on 5a7a632 (run 31654771973, conclusion=success); docs-site (pages) run also green on 5a7a632 (run 31654771890, conclusion=success), checked directly via the GitHub Actions API this session. 5a7a632 sits directly on top of the feature commit 4983339 with only the roadmap-doc commit dd86509 and this docs-only hash-backfill commit in between, so its green run covers the feature code unchanged. No C++ was touched by this batch as expected; fully verified, no outstanding CI risk from this wave.

- Wave 2 (2026-08-13, 8f5a1db): Repeater per-tab notes batch -- RepeaterTab (Src/Core/Networking/repeater.hpp:19-29) gains a `notes` QString field; Repeater::setTabNotes (repeater.cpp:219-223) mirrors the existing renameTab pattern and is exposed as POST /api/repeater/tab/notes (control_server.cpp:3183-3187); the snapshot's tabs[] array now includes notes (control_server.cpp:1501). ui-v2/tabs.jsx's RepeaterTab component gets a NOTES button in the tab strip (prompt-based, matching the existing double-click-to-rename affordance), a per-tab indicator span for any tab carrying notes, and a tooltip that appends the note text. Closes parity item "Repeater: per-tab notes / annotations" (absent -> present). No other consumers of RepeaterTab needed updating (`grep -rn RepeaterTab Src/` outside repeater.hpp/.cpp returns nothing) and no positional-initializer call sites exist, so the new field with its default-constructed value is additive-only. Verified: bundled-babel transform of both touched frontend files (tabs.jsx, real-data.js), confirmation ui-v2/tabs.jsx's intentional NUL byte was untouched (still exactly 1 byte, offset shifted from 51699 to 52541 by the inserted lines as expected), a vm-sandboxed hook-stubbed construction of RepeaterTab (the JSX component) proving no runtime throw and that the new NOTES button + per-tab indicator span render for a fixture tab carrying notes, a whole-site relative-link check (0 broken, 78 pages, 1078 links -- unchanged baseline), and `python3 scripts/parity_report.py --check` green (409 features, 169 at parity = 41%). README.md and docs/index.html updated (Repeater feature line/card now name the per-tab NOTES capability). Pushed as 8f5a1db; HEAD verified == origin/Nullock via two `git rev-parse` calls before and after push. This cloud session cannot build the C++ side itself; will confirm the next GitHub Actions ci run is green before calling this fully closed.

- Wave 2 (2026-08-13, confirm CI for 8f5a1db/d25a0b1): Repeater per-tab notes wave -- GitHub Actions ci run green on 8f5a1db (run 31664270337, conclusion=success) and on d25a0b1 (run 31664289099, conclusion=success); docs-site (pages) run also green on both (31664270293, 31664289027), checked directly via the GitHub Actions API this session. No outstanding CI risk from that wave.

- Wave 2 (2026-08-13, 6f7ac6a/15ad6e1): Intercept Scan-pivot batch -- InterceptTab (ui-v2/tabs.jsx) gains a SCAN button on a held request, closing parity item #296 (partial -> present). Intercept's held item carries only host/port/tls/text (no captured `url` field, unlike Proxy/Repeater rows), so proxy.jsx's shared parseRawRequest() gained a fallback: when the caller passes no `row.url`, it now derives the target from the raw request line's own path/query instead of defaulting to "/" -- existing callers (which always pass row.url) are byte-for-byte unaffected, since the fallback only engages when row.url is falsy. sendToScanner() uses this to reconstruct {method, fullUrl, headers, body} from the edited held-request text and POSTs it to the existing /api/audit/run (unmodified, already wired from the SCANS tab's Audit form) -- the same active-test battery (params/verbs/cors/idor/massassign/etc.) the Assess & audit section drives; findings file into Issues as usual, and an inline result strip below the held-message meta bar reports the finding/tester counts or the error, dismissible. Along the way, discovered the register was stale on a second front too: Send to Decoder had already shipped in an earlier wave (df50bbc, 2026-08-12) but this item was never flipped off partial -- corrected here so all five named pivots (Repeater/Intruder/Comparer/Decoder/Scanner) are accounted for truthfully. No backend touched -- /api/audit/run's request/response contract (control_server.cpp:6760-6797) was read directly, not assumed. Verified: bundled-babel transform of both touched files (tabs.jsx, proxy.jsx), confirmation ui-v2/tabs.jsx's intentional NUL byte was untouched (still exactly 1 byte, offset shifted to 54840 by the inserted lines as expected), an 18-check Node functional test suite -- 13 checks on parseRawRequest (5: existing row.url behavior unchanged for method/body/header-parsing/Host-header-drop/Content-Type-keep; 4: the new fallback correctly derives path+query from the raw request line, method, and a kept header, and degrades to "/" with no raw text; the sendToScanner header-object-conversion + opts-building logic) plus a 5-check vm-sandboxed render suite proving InterceptTab constructs with no throw for a held request, a held response, and an empty queue, and that the SCAN button is present only when a request is held (not a response, which has no method/target to scan), a whole-site relative-link check (0 broken, 78 pages, 1289 links), marketplace/labs-site drift checks green (16/51 pages), and `python3 scripts/parity_report.py --check` green (409 features, 170 at parity = 42%). docs/index.html updated (Intercept & edit card now names the Scan pivot). Pushed as 6f7ac6a (feature) + 15ad6e1 (commit-hash backfill into parity.json's self-referential reconciled.commit, this file's established pattern for that ordering problem); HEAD verified == origin/Nullock via two `git rev-parse` calls before and after push. This cloud session cannot build the C++ side itself, though this batch touched no C++; will confirm the next GitHub Actions ci run is green before calling this fully closed.

- Wave 2 (2026-08-13, confirm CI for 6f7ac6a/15ad6e1/e583055): Intercept Scan-pivot wave -- GitHub Actions ci run green on 15ad6e1 (run 31674351198, conclusion=success), which sits directly on top of feature commit 6f7ac6a with no other commits in between, so its green run covers the feature code unchanged; docs-site (pages) run also green on 15ad6e1 (run 31674351260, conclusion=success). ci run green on e583055 (run 31674398123, conclusion=success) -- e583055 touches only this file, outside the pages workflow's path filter, so no docs-site run was expected or triggered for it, matching prior precedent. Checked directly via the GitHub Actions API this session. No C++ was touched by this batch as expected; fully verified, no outstanding CI risk from this wave.

- Wave 2 (2026-08-13, bbc8c86): DB Search batch -- the Proxy tab's HTTP HISTORY pane-head gains a DB SEARCH button opening a new DbSearchOverlay (ui-v2/proxy.jsx) wiring the previously-orphaned POST /api/history/find (Src/Core/Storage/history_index.cpp:296-337, backed by HistoryLogic::buildFindQuery -- a parameterized, injection-safe SQLite query over method/host/path(LIKE)/status/minSize/maxSize/sinceMs, limit-clamped) via a new NL.actions.historyFind() in real-data.js. Closes parity item #268 (partial -> present): the persistence half was already present (evicted rows cold-fetch via /api/history/<id>/request|response, an earlier wave's fix), but nothing in the GUI called /api/history/find itself, so a row that scrolled out of the bounded in-memory HTTP HISTORY window was previously unfindable from the interface at any engagement size. The overlay's structured filter form posts through a new pure buildHistoryFindFilters(f, nowMs) helper (blank/unreadable fields omitted rather than sent as ""/NaN, sinceMinutes-ago converted to an absolute sinceMs), renders the returned rows in a results table, and opens the selected result's full raw request/response inline (Text/Hex toggle, reusing proxy.jsx's existing toHexDump) via the same eviction-safe NL.requestRawById/responseRawById path DetailPane already relies on -- so a found row is not just listed but actually openable, regardless of whether it is still in the live table. No backend touched -- every filter key and every returned row field was read directly from history_logic.hpp/.cpp and history_index.cpp and matched exactly. Verified: bundled-babel transform of both touched files (proxy.jsx, real-data.js), confirmation ui-v2/tabs.jsx's intentional NUL byte was untouched (unrelated file, not part of this diff), a 5-check Node functional test of buildHistoryFindFilters (blank-input default, trim+coerce+sinceMs-from-minutes, unreadable-numeric-fields-dropped, large-limit passthrough, non-positive-limit falls back to default) run against the real babel-transformed module in a vm sandbox, a vm-sandboxed hook-stubbed construction of DbSearchOverlay (idle state, and a populated-results-with-an-open-row state) and of ProxyTab itself (proving the new button/overlay wiring doesn't break the surrounding tab), a whole-site relative-link check (0 broken, 78 pages, 1078 relative links), marketplace/labs-site drift checks green (16/51 pages), and `python3 scripts/parity_report.py --check` green (409 features, 171 at parity = 42%). README.md and docs/index.html updated (Proxy feature block / Scoped history card now name DB Search). Pushed as bbc8c86 (feature) + a following docs commit backfilling this commit's hash into parity.json's self-referential reconciled.commit, this file's established pattern for that ordering problem. This cloud session cannot build the C++ side, though this batch touched no C++; will confirm the next GitHub Actions ci run is green before calling this fully closed.

- Wave 2 (2026-08-13, confirm CI for bbc8c86/9783b8b): DB Search wave -- GitHub Actions ci run green on 9783b8b (run 31687453331, conclusion=success), which sits directly on top of feature commit bbc8c86 with no other commits in between, so its green run covers the feature code unchanged; all 6 jobs (Windows build+regression-tests+smoke+active-probe-smoke, Linux build, marketplace/parity/labs drift check, CLI syntax, fuzz-corpus replay) passed. docs-site (pages) run also green on 9783b8b (run 31687453458, conclusion=success). Checked directly via the GitHub Actions API this session. No C++ was touched by this batch as expected; fully verified, no outstanding CI risk from this wave. Before appending this note, origin/Nullock had advanced with an unrelated commit from another session (45c0a83, "Findings persist across app close + project reopen" -- Src/Core/Storage/finding_serial.* + project_store wiring, its own change, not reviewed here) -- fast-forwarded onto it rather than rebasing past work.

- Wave 0/3 (2026-08-13, 45c0a83/cd04cc4, backfilled here honestly -- a prior run closed this but skipped this file's required append step): Findings persistence launch-blocker fix -- passive-scan findings were in-memory only (vanished on app close, wiped on project switch). Adds a pure FindingSerial (toJson/fromJson) module for a full-fidelity `<project>/findings.ndjson` wire format with its own mutation-proven unit test; PassiveScanner gains `findingAdded(const Finding&)` (append-on-discovery, crash-safe) + `ingestFinding()` (verbatim restore, no re-enrich/re-persist); ProjectStore gains `appendFinding()` (deduped by baseline identity key) and `streamExistingFindings()`/`restoreFindings()` over findings.ndjson alongside history.ndjson under its own mutex; app.cpp wires `findingAdded -> appendFinding` and `findingRestored -> ingestFinding`, restoring the default project's findings once at startup. Restore preserves id/ts/enrichment/history-rowId so click-to-jump still lands correctly. Closes "Project files: scan issues persisted in the project and restored on reopen" (absent -> present, parity 172/409 = 42%). Commit's own log claims ctest 93/93 (finding_serial added) + probe_smoke 156/156 from a build this cloud session cannot itself run; GitHub Actions ci run green on 45c0a83 itself (run 31688909247, conclusion=success) and on the reconcile-docs commit cd04cc4 (run 31689343454, conclusion=success), confirmed directly via the Actions API this session -- fully verified, no outstanding CI risk from this backfilled batch.

- Wave 3 (2026-08-13, 06088ba/4aeead5, backfilled here honestly -- a prior run closed this but skipped this file's required append step): Repeater automatic Content-Length launch-blocker fix -- editing a Repeater request body left a stale Content-Length on the wire (truncated or hung the origin). Repeater::send now recomputes it from the actual body via the chain runner's audited normalizeContentLength (also collapses a duplicate Content-Length and drops it under Transfer-Encoding: chunked -- both request-smuggling vectors) before every send. Exposed as an `autoContentLength` toggle (Q_PROPERTY + POST /api/repeater/set + state snapshot), defaulting ON to match Burp; OFF sends bytes verbatim so a tester can still hand-craft a CL/TE desync. Closes the matching parity item (absent -> present). Commit's own log claims ctest 93/93 + probe_smoke 156/156 from a build this cloud session cannot itself run; GitHub Actions ci run green on 4aeead5 (run 31690136358, conclusion=success), confirmed via the Actions API this session -- fully verified, no outstanding CI risk from this backfilled batch.

- Wave 2 (2026-08-13, 9fd3523): Repeater AUTO-CL GUI-wiring follow-up -- the autoContentLength toggle above (06088ba) shipped backend-only with no frontend affordance: a tester could turn it off to hand-craft a CL/TE desync only via a raw `/api/repeater/set` POST, not from the interface. Added an AUTO-CL checkbox next to the existing TLS checkbox in Repeater's target row (ui-v2/tabs.jsx), bound through the existing generic `repeater-set` reducer action (the same passthrough pattern the adjacent TLS checkbox already used) -- no new reducer case, no backend change. Register-accuracy note: parity.json already carried this item as "present" (the default-on behavior alone satisfies Burp parity), so no state flip; the `reconciled`/`gap`/`why` fields were updated to describe the closed GUI-reachability gap rather than claim a new state transition. Verified: bundled-babel transform of both touched files (tabs.jsx, real-data.js), confirmation ui-v2/tabs.jsx's intentional NUL byte was untouched (still exactly 1 byte, offset shifted to 55504 by the inserted lines as expected), a 4-check vm-sandboxed render test of RepeaterTab (checkbox reflects rep.autoContentLength true/false/undefined-defaults-to-checked, and its onChange dispatches `{type:"repeater-set", payload:{autoContentLength}}`), a whole-site relative-link check (0 broken, 78 pages, 1078 relative links -- unchanged baseline), marketplace/labs-site drift checks green (16/51 pages), and `python3 scripts/parity_report.py --check` green (409 features, 173 at parity = 42%, unchanged count since no state flipped). README.md and docs/index.html updated (Repeater feature line/card now name AUTO-CL). Pushed as 9fd3523 (feature); a following docs commit backfills 9fd3523's own hash into parity.json's self-referential reconciled.commit, this file's established pattern for that ordering problem. HEAD verified == origin/Nullock via two `git rev-parse` calls before and after push. This cloud session cannot build the C++ side, though this batch touched no C++; GitHub Actions ci run for 9fd3523 was still in_progress as this note was written (docs-site/pages run already green: 31700953682) -- will confirm the ci.yml run is green before calling this fully closed.

- Wave 2 (2026-08-13, confirm CI for 9fd3523/e0dd0aa): AUTO-CL GUI-wiring wave -- GitHub Actions ci run green on 9fd3523 (run 31700953731, conclusion=success) and on e0dd0aa, its docs-note commit (run 31701096815, conclusion=success), checked directly via the GitHub Actions API this session before starting the next batch. No outstanding CI risk from that wave.

- Wave 2 (2026-08-13, 3494297): Discover crawl-config batch -- the BFS crawler's three tunables (crawler.cpp:67-69 `m_maxPages`/`m_maxDepth`/`m_throttleMs`) were already accepted by `/api/crawler/start {seed,maxPages?,maxDepth?,throttleMs?}` (control_server.cpp:4092-4101) but only settable through a raw JSON POST -- the Discover tab's crawler control row sent only the seed URL. Added max-pages / max-depth / throttle-ms number inputs to that row (ui-v2/app.jsx DiscoverTab), disabled while a crawl is running and defaulting to the backend's own defaults (200/4/200), threaded through a new 4th `throttleMs` argument on `NL.actions.crawlerStart` (ui-v2/real-data.js) -- no new reducer case, no backend change. Register-accuracy note on item "[A3] Crawl configuration: max depth, max locations, crawl limits": stays honestly `partial`, not flipped to present -- Burp's fuller crawl-configuration surface (max-unique-locations, per-directory limits, crawl-budget, coverage-strategy toggles) has no Nullock equivalent at all, backend or frontend; only the GUI-reachability half of the gap (the three existing knobs being API-only) is closed. `gap`/`why`/`reconciled` fields updated accordingly. Verified: bundled-babel transform of both touched files, a vm-sandboxed functional test of DiscoverTab proving the edited max-pages/max-depth/throttle-ms state values (500/6/1000) flow unchanged through `startCrawl()` into the `NL.actions.crawlerStart(seed, maxPages, maxDepth, throttleMs)` call (plus a default-value check confirming the initial render seeds 200/4/200), confirmation ui-v2/tabs.jsx was untouched (NUL byte still at offset 55504), a whole-site relative-link check (0 broken, 78 pages, 1078 relative links -- unchanged baseline), marketplace/labs-site drift checks green (16/51 pages), and `python3 scripts/parity_report.py --check` green (409 features, 173 at parity = 42%, unchanged count since no state flipped). No README.md/docs/index.html change this wave -- both already describe the Discover tab's crawler at the right level ("start/stop control for the BFS crawler") without previously promising per-run config, so nothing there was misleading. Pushed as 3494297 (feature); a following docs commit backfills 3494297's own hash into parity.json's self-referential reconciled.commit. HEAD verified == origin/Nullock via two `git rev-parse` calls before and after push. This cloud session cannot build the C++ side, though this batch touched no C++; GitHub Actions ci run for 3494297 (run 31717606815) was still in_progress as this note was written -- will confirm the ci.yml run is green next wave before calling this fully closed, same honest-partial pattern as the 9fd3523 wave above.

- Wave 2 (2026-08-13, confirm CI for 3494297): Discover crawl-config wave -- GitHub Actions ci run green on 3494297 (run 31717606815, conclusion=success), checked directly via the GitHub Actions API this session before starting the next batch. No outstanding CI risk from that wave.

- Wave 2 (2026-08-13, 70567af): Command palette batch -- closes parity #247 "Hotkeys (configurable keyboard shortcuts) and the command palette" (absent -> partial, honestly not present). A new CommandPalette component (ui-v2/app.jsx) opens via Ctrl/Cmd+K or a new ⌘K button in TitleBar (ui-v2/chrome.jsx): fuzzy-filters (substring on label+keywords) a list built from every TABS entry plus four cross-cutting toggles reusing existing dispatch cases/actions (intercept-toggle, intercept-responses-toggle, the tweaks panel, copy-CA-path), arrow-key nav + Enter to run + Escape/click-outside to close, matching the existing overlay convention (WsRepeaterOverlay et al). A global keydown listener in App() also wires Ctrl/Cmd+1-9 to jump straight to the first nine tabs -- the tab strip's decorative "0N" index badge (chrome.jsx, always rendered, never wired to a key before this) is now literally true for those nine. Left honestly partial, not present: bindings are fixed, not user-remappable (no rebind UI, no persisted custom map), and the palette only reaches global navigation + a few app-level toggles -- no per-row contextual actions (Ctrl+R send-selected-row-to-Repeater, etc.) since the reducer has no such generic dispatch surface to hook without inventing new backend-independent state. No backend touched. Verified: bundled-babel transform of both touched files (app.jsx, chrome.jsx), confirmation ui-v2/tabs.jsx was untouched (NUL byte intact at offset 55504, file not in the diff), a vm-sandboxed render + 4-check functional test of CommandPalette's pure substring-filter logic (empty query returns all, label match, keyword match, no-match returns empty) run against the real babel-transformed module, a separate vm-sandboxed render of TitleBar proving the new ⌘K button constructs and its onClick correctly invokes the new onOpenPalette prop, a whole-site relative-link check (0 broken, 78 pages, 1078 relative links -- unchanged baseline), marketplace/labs-site drift checks green (16/51 pages), and `python3 scripts/parity_report.py --check` green (409 features, 173 at parity = 42%, unchanged count since this moved absent->partial not into a parity state). README.md and docs/index.html updated (new Command palette feature line/card). Pushed as 70567af (feature) + this roadmap-note commit (which also backfills 70567af into parity.json's self-referential reconciled.commit, this file's established pattern for that ordering problem). HEAD verified == origin/Nullock via two `git rev-parse` calls before and after push. This cloud session cannot build the C++ side, though this batch touched no C++; will confirm the next GitHub Actions ci run is green before calling this fully closed.

- Wave 2 (2026-08-13, confirm CI for 70567af/c32b3d6): Command palette wave -- GitHub Actions ci run green on c32b3d6 (run 31742242391, conclusion=success, run_number 582), which sits directly on top of feature commit 9d794d4 with only this roadmap-doc commit in between, so its green run covers the feature code unchanged; docs-site (pages) run also green on c32b3d6 (run 31742242314, run_number 171, conclusion=success), checked directly via the GitHub Actions API this session. No C++ was touched by this batch as expected; fully verified, no outstanding CI risk from this wave.

- Wave 2 (2026-08-13, 1df47e7): Intercept response-mods auto-apply batch -- closes parity #298 (partial -> present). The eight one-click response-modification removals (unhide hidden fields, enable disabled fields, remove length limits, remove JS validation, remove all JS, remove &lt;object&gt; tags, HTTPS-&gt;HTTP links, strip Secure cookie flag) previously required a manual button click on the currently-held response only. Added a RESP_MODS table + applyEnabledRespMods (ui-v2/tabs.jsx) and an unconditional "response mods · auto-apply on hold:" checkbox row (rendered under the top intercept toggle bar, independent of whether a message is currently held), persisted to localStorage (nl-intercept-respmods, same try/catch-guarded pattern as the existing UpdateBanner dismissal in app.jsx). The held-item reset effect now applies the enabled transforms -- captured via a ref at hold-time so mid-edit settings changes don't clobber an in-progress edit -- to every newly-held response before it's shown, matching Burp's standing Options-tab checkboxes; the original 8 manual per-message buttons stay for a one-off tweak of the current item. No backend touched. Verified: bundled-babel transform of ui-v2/tabs.jsx, confirmation the intentional NUL byte was untouched (still exactly 1 byte, offset shifted from 55504 to 58081 by the inserted lines as expected), a 17-check Node functional test (RESP_MODS shape/uniqueness, no-op on empty/undefined settings, each individual toggle matches its direct helper fn, all-8-enabled composes in the same order as manual sequential application, localStorage save/load round-trip, and three degrade-gracefully branches for missing/garbage/non-object stored JSON), a vm-sandboxed hook-stubbed construction of InterceptTab in both a populated-held-response state and an empty-queue state proving no runtime throw, a whole-site relative-link check (0 broken, 78 pages, 1078 relative links -- unchanged baseline), marketplace/labs-site drift checks green (16/51 pages, idempotent regen), and `python3 scripts/parity_report.py --check` green (409 features, 174 at parity = 43%). README.md and docs/index.html updated (Proxy line / Intercept &amp; edit card now name the standing auto-apply toggle). Pushed as 1df47e7 (feature) + a following docs commit backfilling this commit's hash into parity.json's self-referential reconciled.commit, this file's established pattern for that ordering problem. HEAD verified == origin/Nullock via two `git rev-parse` calls before and after push. This cloud session cannot build the C++ side, though this batch touched no C++; will confirm the next GitHub Actions ci run is green before calling this fully closed.

- Wave 2 (2026-08-15, confirm CI for 1df47e7/6cb4a50): Intercept response-mods auto-apply wave -- GitHub Actions ci run green on 6cb4a50 (run 31746559542, run_number 584, conclusion=success), which sits directly on top of feature commit 1df47e7 with only a roadmap-doc commit in between, so its green run covers the feature code unchanged; docs-site (pages) run also green on 6cb4a50 (run 31746559560, run_number 172, conclusion=success), checked directly via the GitHub Actions API this session before starting the next batch. No C++ was touched by this batch as expected; fully verified, no outstanding CI risk from this wave.

- Wave 2 (2026-08-15, d5a4299): Rebindable-hotkeys batch -- closes the remaining honest gap on parity #247 "Hotkeys (configurable keyboard shortcuts) and the command palette" (partial -> present). The command palette's 10 global bindings (Ctrl/Cmd+K palette-open + Ctrl/Cmd+1-9 tab jumps), previously fixed and hardcoded, are now user-remappable: pure helpers (defaultHotkeys/comboFromEvent/comboLabel/loadHotkeys/saveHotkeys/matchHotkeyAction, ui-v2/app.jsx:5621-5680) normalize a keydown into a "mod[+shift][+alt]+key" combo string and look it up against a persisted binding map (localStorage nl-hotkeys, same try/catch-guarded pattern as nl-update-dismissed/nl-intercept-respmods); a new HotkeysOverlay component (app.jsx:5686-5773), opened via a new "Customize keyboard shortcuts…" palette entry, lets the user click a binding, press a new combo, see a live conflict warning if another action already owns it, and reset any single binding back to default. App()'s global keydown effect now dispatches via matchHotkeyAction() instead of the old hardcoded if(k==="k")/regex-digit checks, and is suspended while the overlay itself is open so recording a new combo there can't also fire the old binding; the palette's own tab-row hints now render the live bound key via comboLabel() rather than a hardcoded "Ctrl+N" string. Contextual per-row shortcuts (Ctrl+R send-to-repeater, etc.) remain the one honest remainder -- the reducer still has no generic contextual-dispatch surface to bind against, called out in the gap text rather than claimed fixed. No backend touched. Verified: bundled-babel transform of ui-v2/app.jsx, confirmation ui-v2/tabs.jsx was untouched (NUL byte still exactly 1 byte at offset 58081, file not in the diff), a 24-check Node functional test of every pure helper (defaultHotkeys shape, comboFromEvent's mod-chord requirement + shift/alt modifiers + bare-modifier rejection, comboLabel's Mac/non-Mac rendering, loadHotkeys/saveHotkeys round-trip plus three degrade-gracefully branches for missing/garbage/non-object stored JSON, matchHotkeyAction lookups), a vm-sandboxed hook-stubbed render of HotkeysOverlay proving the idle-state element tree constructs with no throw, a whole-site relative-link check (0 broken, 78 pages, 1078 relative links -- unchanged baseline), marketplace/labs-site drift checks green (16/51 pages), and `python3 scripts/parity_report.py --check` green (409 features, 175 at parity = 43%). README.md and docs/index.html updated (Command palette feature line/card now describe the rebind UI). Pushed as d5a4299 (feature) + a following docs commit backfilling this commit's hash into parity.json's self-referential reconciled.commit, this file's established pattern for that ordering problem. HEAD verified == origin/Nullock via two `git rev-parse` calls before and after push. This cloud session cannot build the C++ side, though this batch touched no C++; will confirm the next GitHub Actions ci run is green before calling this fully closed.

- Wave 2 (2026-08-16, confirm CI for d5a4299/978e8f4): Rebindable-hotkeys wave -- GitHub Actions ci run green on d5a4299 itself (run 31909709723, run_number 585, conclusion=success) and on 978e8f4, the roadmap-note commit (run 31909739225, run_number 586, conclusion=success); docs-site (pages) run also green on both (31909709711, 31909739284), checked directly via the GitHub Actions API this session. No C++ was touched by this batch as expected; fully verified, no outstanding CI risk from this wave.

- Wave 0 (2026-08-16, faa9193/ffcbd4a): register-accuracy fix -- Inspector "Parameters" query-string and body items (parity.json indices 328/329) were left at `stub` since the 2026-08-02 INSPECTOR-tab wave (86f094c), which actually already renders both `view.queryParams` (ui-v2/app.jsx:3045) and `view.bodyParams` (:3048) read-only in the same panel that got sibling items #327 (request attributes), #330 (cookies), and #332 (JWT decode) flipped to present at the time -- these two were simply missed. Re-verified against live source rather than trusted at face value: query-string params has no other real gap beyond the already-accepted "read-only, no editor" precedent set by those three siblings, so it moves stub -> present; body params is reachable now too, but inspector_logic.cpp:203-204 still collapses nested JSON objects/arrays to a bare `{…}`/`[…]` placeholder with no recursion and the content-type switch (:182-221) still has no multipart or XML branch -- a real parsing gap, confirmed still true, not a UI gap -- so it moves stub -> partial, not present. No frontend or backend code touched; docs/roadmap/index.html regenerated (409 features, 176 at parity = 43%, stub 12 -> 10, partial 81 -> 82, present 151 -> 152), `python3 scripts/parity_report.py --check` green. Pushed as faa9193 (register fix) + ffcbd4a (commit-hash backfill into parity.json's self-referential reconciled.commit, this file's established pattern for that ordering problem); HEAD verified == origin/Nullock via two `git rev-parse` calls before and after each push. This is a docs-only change (parity.json + docs/roadmap/index.html only); no C++/frontend touched, so no CI risk expected, but will confirm the next GitHub Actions ci run is green before calling this fully closed.

- Wave 0 (2026-08-16, confirm CI for faa9193/ffcbd4a/bbbfc44): register-accuracy wave -- GitHub Actions ci run green on faa9193 itself (run 31917476187, run_number 587, conclusion=success) and on bbbfc44, the roadmap-note commit (run 31917506589, run_number 589, conclusion=success); docs-site (pages) run also green on both faa9193 and ffcbd4a (the commit-hash-backfill doc commit), checked directly via the GitHub Actions API this session. ffcbd4a's own ci run (run_number 588) was still in_progress when checked, but bbbfc44 sits directly on top of it with no code change in between (bbbfc44 touches only this roadmap file) -- its green run covers ffcbd4a's code unchanged, matching this file's established precedent for that ordering. bbbfc44 itself was not expected to trigger docs-site (it touches only ROADMAP-EXECUTION-PLAN.md, outside the pages workflow's `docs/**` path filter) and did not. No C++ or frontend was touched by this batch as expected; fully verified, no outstanding CI risk from this wave.

- Wave 3 (2026-08-13, bd47e80/ea0003c, backfilled here honestly -- a prior run closed this but skipped this file's required append step): Repeater tab persistence launch-blocker fix -- staged Repeater requests were wiped on every project switch and lost on close. ProjectStore now emits projectClosing() at the TOP of open() (before the historyShouldClear wipe) so app.cpp saves the OUTGOING project's tabs to its own project.json (under "repeater") before anything is cleared, and repeaterStateChanged() at the END loads the INCOMING project's tabs; clearAll() still runs between the two as defense-in-depth so one engagement's staged request (and its Authorization header) never surfaces in another's project. Tabs also save on aboutToQuit. Repeater::exportState()/importState() serialize the request side (name/host/port/tls/request/notes/statusLine) + active index, omitting the response body to keep project.json small; importState resets to a single blank tab on empty input. Closes "Repeater: tab state persisted in the project file and restored on reopen" (absent -> present). Commit's own log claims smoke section 13 (canary auth header: staged=1 leaked=0 restored=1) + gauntlet ctest 93/93, probe_smoke 156/156 from a build this cloud session cannot itself run; GitHub Actions ci run green on bd47e80 itself (run 31918810116, run_number 591, conclusion=success) and on ea0003c, the docs-reconcile commit (run 31918892057, run_number 592, conclusion=success; docs-site run also green, 31918892078, run_number 177), confirmed directly via the Actions API this session -- fully verified, no outstanding CI risk from this backfilled batch.

- Wave 3 (2026-08-15, a6b57a0/23318b4, backfilled here honestly -- a prior run closed this but skipped this file's required append step): Proxy off-loopback bind launch-blocker fix -- the proxy listener was hardwired to 127.0.0.1 at its app.cpp call sites (the listen primitive itself already honored any address). --proxy-bind=ADDR (or NULLOCK_PROXY_BIND) now binds it to any interface for VM/phone/container testing. Because an off-loopback intercepting proxy is a cert-forging MITM + open relay exposed to the whole LAN, a non-loopback bind is refused unless --proxy-bind-insecure is also passed, with a loud warning banner (banner + FATAL de-hardcoded to show the actual bound address). ProxyServer remembers its last successful bind (m_bindAddress/m_bindPort); restart() re-listens on it and /api/proxy/toggle now uses restart() -- toggling preserves the bind instead of reverting to 127.0.0.1:8080 (also fixed a pre-existing bug that reset a custom --proxy-port on toggle). Closes "Proxy listener: Bind to address (loopback / all interfaces / specific IP)" (absent -> present). Commit's own log claims: default -> loopback (probe_smoke 156/156); --proxy-bind=0.0.0.0 without ack -> FATAL exit 1; invalid addr -> FATAL exit 1; with ack -> binds 0.0.0.0:18099 (netstat-confirmed) + warning banner; gauntlet ctest 93/93 -- from a build this cloud session cannot itself run. GitHub Actions ci run green on a6b57a0 itself (run 31920027832, run_number 593, conclusion=success) and on 23318b4, the docs-reconcile commit (run 31920085741, run_number 594, conclusion=success; docs-site run also green, 31920085740, run_number 178), confirmed directly via the Actions API this session -- fully verified, no outstanding CI risk from this backfilled batch.

- Wave 3 (2026-08-15, c88e6bb/858aa86, backfilled here honestly -- a prior run closed this but skipped this file's required append step): Intercept match rules launch-blocker fix (engine + API + persistence, backend-only) -- interception was global all-or-nothing; turning it on parked every in-scope message, static assets included. Adds InterceptLogic's pure decision engine: an InterceptRule struct (enabled, And/Or op, match on file-extension/method/url-regex/host-glob/content-type/status-code/header, per-rule negate, request/response/both direction) and interceptRulesHold() -- a left-to-right fold matching Burp's "Intercept Client/Server Requests" semantics (an empty list still holds everything) -- plus extractRequest/ResponseFields() to pull testable fields from raw bytes and interceptRules<->Json for the wire form (63 unit cases, mutation-proven on the negate inversion + the And-fold). InterceptController gains setInterceptRules/interceptRules (mutex-guarded), evaluated in pend()/pendResponse() on the worker thread -- a message the rules don't hold auto-forwards instead of parking. /api/intercept/rules gets/sets the list; ProjectStore persists it under project.json "interceptRules" and restores it on open, mirroring repeaterState. Closes "Intercept requests based on rules" (absent -> present) and "Intercept responses based on rules" (absent -> partial; the Burp-specific "request was intercepted/modified" cross-reference match needs request/response correlation not yet built). Left the rules-editor UI itself as the openly-stated remaining piece -- closed two waves later, same session, see below. Commit's own log claims the API round-trips the rules and they survive an app restart (loaded from project.json into a fresh process), gauntlet ctest 93/93 + probe_smoke 156/156, from a build this cloud session cannot itself run. GitHub Actions ci run green on c88e6bb itself (run 31920777370, run_number 595, conclusion=success) and on 858aa86, the docs-reconcile commit (run 31920844919, run_number 596, conclusion=success; docs-site run also green, 31920844892, run_number 179), confirmed directly via the Actions API this session -- fully verified, no outstanding CI risk from this backfilled batch.

- Wave 2 (2026-08-16, 793f075/b378e48, backfilled here honestly -- a prior run closed this but skipped this file's required append step): Site map per-URL leaf nodes -- closes parity item #370's last real gap ("map has no per-URL leaf nodes -- host-only tree, no leaf to click"). SiteMap hosts (ui-v2/proxy.jsx) now expand (twisty toggle) into a deduped method+path leaf list derived client-side from the HTTP history rows already in component state (leavesFor(), dedupes by method+path keeping the highest-id/most-recent row) -- no backend change, since /api/snapshot's sitemap block stays host-only by design. Clicking a leaf dispatches both selectedHost and selectedRowId, opening that exact message in DetailPane the same way clicking its row in HistoryTable would. Closes #370 (partial -> present). No field invented: leavesFor reads r.host/r.method/r.path/r.status/r.id off the same row shape HistoryTable/FilterBar already read elsewhere in the file. Verified (per the feature commit's own record): bundled-babel transform of ui-v2/proxy.jsx, a Node functional test of the leaf-dedupe logic (5 checks: method+path grouping, highest-id-wins on duplicate paths, distinct-method leaves, host filtering, empty-host fallback), a vm-sandboxed render smoke test of SiteMap in both collapsed and expanded states, and confirmation ui-v2/tabs.jsx was untouched (unrelated file). GitHub Actions ci run green on 793f075 itself (run 31924485712, run_number 597, conclusion=success) and on b378e48, the commit-hash-backfill docs commit (run 31924494425, run_number 598, conclusion=success; docs-site run also green, 31924494422, run_number 181), confirmed directly via the Actions API this session -- fully verified, no outstanding CI risk from this backfilled batch.

- Wave 2 (2026-08-16, bc8d12d/b490678): Intercept rules-editor UI batch -- closes the GUI-reachability gap the c88e6bb wave above deliberately left open ("Rules-editor UI pending"). buildSnapshot() (control_server.cpp) now includes interceptRules (InterceptLogic::interceptRulesToJson(m_wiring.intercept->interceptRules())), the same pattern already used for match/replace `rules` and `sessionRules`, so the 500ms poll loop carries the live rule list; ui-v2/real-data.js mirrors it into NL.interceptRules and adds NL.actions.interceptRulesSet(rules) (whole-list-replace POST to /api/intercept/rules, same contract as sessionRulesSet). ui-v2/tabs.jsx gains a RULES toggle button in InterceptTab's toolbar opening a new InterceptRulesPanel -- add/edit/reorder(up/down)/toggle/delete against the exact wire enums (op: or/and; match: any/file-extension/method/url-regex/host-glob/content-type/status-code/has-header; dir: request/response/both; negate), via pure helpers (interceptRuleUpsert/RemoveAt/Toggle/Move) that build the next full array from the current NL.interceptRules -- the identical whole-list-replace editor pattern session handling rules already established. No new backend endpoint or wire-format change -- purely exposing what c88e6bb already shipped. parity.json's two intercept-rules items (indices 290/291) updated: gap text + reconciled note only, states unchanged (present / partial) since this closes GUI-reachability, not a new capability. Verified: bundled-babel transform of both touched files (tabs.jsx, real-data.js), confirmation ui-v2/tabs.jsx's intentional NUL byte was untouched (still exactly 1 byte, offset shifted from 58081 to 65248 by the inserted lines as expected -- it sits inside the PAYLOAD_PRESETS "fuzz strings (small)" array, an intentional raw-NUL fuzz payload string, well clear of the edited InterceptTab region above it), an 11-check Node functional test of the four pure helpers (interceptRuleUpsert append/replace, interceptRuleRemoveAt, interceptRuleToggle flips only the target and back, interceptRuleMove up/down/no-op-at-either-end, immutability of the input array on every op), a vm-sandboxed hook-stubbed render smoke test proving InterceptRulesPanel and InterceptTab (empty queue, and a held-request state) all construct with no runtime throw, a whole-site relative-link check (0 broken, 1078 relative links -- unchanged baseline), marketplace/labs-site drift checks green (16/51 pages), and `python3 scripts/parity_report.py --check` green (409 features, 180 at parity = 44%, unchanged count since no state flipped). README.md and docs/index.html updated (Proxy line / Intercept & edit card now describe the RULES panel). Pushed as bc8d12d (feature) + b490678 (docs: parity.json gap text + roadmap regen + CHANGELOG.md, backfilling bc8d12d's own hash into parity.json's reconciled.commit since it wasn't known until after that commit). Note: this repo's working tree was found in a DETACHED HEAD state at the start of this run (a symptom of how the fresh clone was checked out, not something this session did) -- caught immediately via `git status` after the first commit landed as "[detached HEAD ...]"; fixed by `git branch -f Nullock HEAD && git checkout Nullock` before pushing, so no commit was lost and the push landed cleanly on the branch. HEAD verified == origin/Nullock via two `git rev-parse` calls after push. This cloud session cannot build the C++ side; the one-line backend snapshot addition (control_server.cpp) is high-confidence (mirrors the existing `rules`/`sessionRules` snapshot fields exactly, uses only already-public accessors) but GitHub Actions ci run for b490678 (run 31931775890) was still in_progress after several minutes of polling as this note was written -- will confirm the ci.yml run is green next wave before calling this fully closed, same honest-partial pattern as prior in-flight-CI notes in this file.

- Wave 2 (2026-08-16, confirm CI for bc8d12d/b490678): Intercept rules-editor UI wave -- GitHub Actions ci run green on b490678 itself (run 31931775890, run_number 599, conclusion=success); docs-site (pages) run also green on b490678 (run_number 182, conclusion=success), confirmed directly via the Actions API this session before starting the next batch. No outstanding CI risk from that wave.

- Wave 5 (2026-08-16, b36c77d/7f2099b, backfilled here honestly -- a prior run closed this but skipped this file's required append step): Macro recorder batch (backend-only, launch-blocker) -- ChainRunner::buildChainStep() (chain_runner_logic, pure) turns one captured raw request + host/port/tls into a `/api/chain/run`-shaped step object, deriving the step name from the request line ("METHOD path", query stripped); a new POST /api/chain/record `{rowIds:[...]}` reads each selected history row via the storage index (loadFullRow + serializeRequestForOrigin) and returns `{steps, skipped}`, ready to POST straight back to /api/chain/run or hand-edit to add `{{var}}` extracts. Reconciles "Macros: record a macro by selecting requests from Proxy history" absent -> partial: record-from-history + replay-via-chain/run are both real and wired end-to-end at the API layer, but there is no visual macro editor and no session-acquisition bridge (pushing recorded values into the cookie jar / session rules) yet -- both called out honestly as the remainder, the latter noted in the commit itself as a follow-up for the concurrent session-rules lane. No frontend touched this wave -- GUI wiring of /api/chain/record is a natural Wave 2 pickup. Commit's own log claims ctest 93/93 + probe_smoke 156/156 from a build this cloud session cannot itself run; GitHub Actions ci run green on b36c77d itself (run_number 601, conclusion=success) and on 7f2099b, the docs-reconcile commit (run_number 602, conclusion=success; docs-site run also green, run_number 183), confirmed directly via the Actions API this session -- fully verified, no outstanding CI risk from this backfilled batch.

- Wave 5 (2026-08-16, df0c74a/7a7c421, backfilled here honestly -- a prior run closed this but skipped this file's required append step): Repeater per-tab send history batch (backend-only, launch-blocker) -- each RepeaterTab now keeps a capped history of prior sends `{request, response, statusLine, sentAt}`, appended in `Repeater::send()`; new `Repeater::historyCount()`/`loadHistoryAt()` navigate it, the control-server snapshot exposes a compact per-tab history list (status + timestamp) + historyCount, and a new POST /api/repeater/history/load loads a prior entry back into the active tab. Deliberately session-only (excluded from exportState()/project.json, matching the existing precedent of not persisting response bodies into project files -- re-sending reproduces them, not stored). Reconciles "Repeater: per-tab request history" absent -> partial: the backend contract + load endpoint are complete, but the `< >` navigation affordance in ui-v2/tabs.jsx's Repeater tab strip does not exist yet -- called out honestly as the remainder, a natural Wave 2 pickup on top of the already-shipped per-tab notes (8f5a1db) and tab persistence (bd47e80) work in this same tab strip. No frontend touched this wave. Commit's own log claims ctest 93/93 + probe_smoke 156/156 from a build this cloud session cannot itself run; GitHub Actions ci run green on df0c74a itself (run_number 603, conclusion=success) and on 7a7c421, the docs-reconcile commit (run_number 604, conclusion=success; docs-site run also green, run_number 184), confirmed directly via the Actions API this session -- fully verified, no outstanding CI risk from this backfilled batch.

- Wave 5 (2026-08-16, a387ba7/3696353, backfilled here honestly -- a prior run closed this but skipped this file's required append step): OAST client-mode batch (backend-only, launch-blocker) -- the Collaborator/OAST sink was in-process-listener-only, so out-of-band detection only worked against targets that could reach the tester's own machine over the network. `OastServer::startRemote(base, key)` skips the local listener, learns a hosted `nullock-oast` sink's baseHost/callbackPort via a synchronous GET /healthz, and starts a poll timer; `mintToken()` now proxies to the remote's POST /mint (so minted callback URLs carry the remote's public host, reachable from anywhere) and `pollRemote()` pulls GET /poll?since=N, re-emitting each hit via the existing `hitReceived` signal with a local id -- so OastCorrelator and every `/api/oast/*` endpoint (mint/poll/blast) work completely unchanged on top of either mode. New `--oast-remote=URL` / `--oast-remote-key=KEY` CLI flags (or `NULLOCK_OAST_REMOTE`/`NULLOCK_OAST_KEY` env vars) select client mode at startup, falling back to the local sink if the remote is unreachable. Reconciles "Collaborator: private server deployment" partial -> present (the deploy-a-sink-and-point-the-client-at-it loop is now complete) and "Collaborator: hosted public server (zero-config, works out of the box)" absent -> partial (the app-side client is done; a bundled zero-config public instance is still missing -- ops, not code, since the standalone nullock-oast tool already supports wildcard DNS + TLS, honestly left as the remainder) -- the commit's own record claims this was verified end-to-end against a real nullock-oast instance (mint proxied, callback landed on the remote sink, poll loop pulled it back with a matching token), a live-network check this cloud session cannot itself reproduce, taken on trust from the commit record per this file's standing convention for backend claims. No frontend touched -- the existing COLLABORATOR tab's mint/poll UI (0aab7d5, 2026-08-09) is transport-agnostic and needs no change to work against either mode. Commit's own log claims ctest 93/93 + probe_smoke 156/156 (local OAST path unchanged) from a build this cloud session cannot itself run; GitHub Actions ci run green on a387ba7 itself (run_number 605, conclusion=success) and on 3696353, the docs-reconcile commit (run_number 606, conclusion=success; docs-site run also green, run_number 185), confirmed directly via the Actions API this session -- fully verified, no outstanding CI risk from this backfilled batch.

- Wave 3 (2026-08-16, 6484132/1113c60, backfilled here honestly -- a prior run closed this but skipped this file's required append step): Session rules tool-scoping foundation batch (backend-only) -- a session-handling rule could only ever be applied in the proxy pipeline, with no way to scope it to a specific tool (Burp's "Tools scope"). `SessionRulesLogic` gains a `SessionTool` bitmask (Proxy/Repeater/Intruder/Scanner) + `ruleAppliesToTool(mask, tool)` -- an unset (0) mask applies to all tools, matching every existing rule's back-compatible default -- pure and unit-tested (7 cases, mutation-proven on the mask check). `SessionRule` gains a `tools` field (default 0); `applyToRequest()` now takes the calling tool (default Proxy) and skips rules not scoped to it. `control_server` serializes/parses `tools` in `/api/session-rules` get + set. Reconciles "Rule scope -- Tools scope" absent -> partial: the mechanism + API round-trip are real, but nothing calls `applyToRequest` with a Repeater/Intruder tool tag yet (both still implicitly go through the proxy-scoped path, so scoping INTO those tools has no effect), and the SESSIONS tab's rule editor has no per-tool checkboxes -- both honestly named in the commit itself as follow-on work for a concurrent lane, not claimed done. No frontend touched this wave. Commit's own log claims ctest 93/93 + probe_smoke 156/156 from a build this cloud session cannot itself run; GitHub Actions ci run for 6484132/1113c60 was still in_progress (run_number 607) when checked this session -- will confirm green next wave before calling this fully closed, same honest-partial pattern as prior in-flight-CI notes in this file.

- Wave 2 (2026-08-16, c547c38): Repeater send-history navigation batch -- wires the previously backend-only per-tab send-history contract (df0c74a, above) into the GUI: a ◀ ▶ control in RepeaterTab's target row (ui-v2/tabs.jsx) steps back/forward through the active tab's capped prior-send history via two pure boundary-clamped helpers (repeaterHistoryPrevIndex/repeaterHistoryNextIndex — `null` means "at the live/newest send", already what the tab shows, so no wire call is needed to get back there; a numeric index reloads that entry's full request/response via the existing POST /api/repeater/history/load) and a new NL.actions.repeaterHistoryLoad(index) in real-data.js, with a live n/count position label and a status/timestamp tooltip. Resets to "live" whenever the active tab changes so switching tabs never carries over a stale position. Closes the parity item's last named gap (partial -> present) -- the whole "Repeater: per-tab request history with back/forward (< >) navigation" item is now genuinely present end to end. No backend touched -- reads only the historyCount/history[].statusLine/.sentAt fields already in the snapshot (control_server.cpp:1508-1514), field-matched by direct read, not assumed. Verified: bundled-babel transform of both touched files (tabs.jsx, real-data.js), confirmation ui-v2/tabs.jsx's intentional NUL byte was untouched (still exactly 1 byte, offset shifted from 65248 to 67968 by the inserted lines as expected), a 13-check Node functional test of the two pure helpers (extracted from the babel-transformed source itself via a vm sandbox, not hand-copied, so the test proves the actual shipped logic: no-history no-ops, live->second-newest, mid-history stepping, oldest-boundary clamp, single/two-entry edge cases, and a prev-then-next round trip landing back at live), a vm-sandboxed hook-stubbed render smoke test of RepeaterTab across three states (no history at all, populated history at live, populated history with a single entry) proving no runtime throw, a whole-site relative-link check (0 broken, 78 pages, 1078 relative links -- unchanged baseline), marketplace/labs-site drift checks green (16/51 pages, idempotent regen), and `python3 scripts/parity_report.py --check` green (409 features, 182 at parity = 44%). README.md and docs/index.html updated (Repeater feature line/card now name the ◀ ▶ history navigation). Pushed as c547c38 (feature, includes parity.json + docs) + a following commit backfilling this commit's own hash into parity.json's self-referential reconciled.commit, this file's established pattern for that ordering problem. HEAD verified == origin/Nullock via two `git rev-parse` calls before and after push. This cloud session cannot build the C++ side, though this batch touched no C++; GitHub Actions ci run for c547c38 (run_number 609) was still in_progress as this note was written -- will confirm the ci.yml run is green next wave before calling this fully closed, same honest-partial pattern as prior in-flight-CI notes in this file.

- Wave 2/3 (2026-08-16, confirm CI for c547c38/09743d1 and 6484132/1113c60): both prior in-flight batches confirmed via the Actions API this session -- c547c38 (Repeater send-history navigation) run 31939449632 green as run_number 609 (conclusion=success), 09743d1 (its docs-backfill commit) run 31939508865 green as run_number 610 (conclusion=success); 6484132 (session-rule tool-scoping foundation) never triggered its own workflow run -- it was pushed to origin in the same `git push` as its docs commit 1113c60, so only 1113c60's push event fired a run (run 31939165339, run_number 607, conclusion=success), which covers 6484132's code unchanged, the same one-push/one-run reasoning this file has applied before to adjacent feature+docs commit pairs. No outstanding CI risk from either wave.

- Wave 3 (2026-08-16, f3872c8/3611a1e/d07dfa1/a11754e/f034a92, backfilled here honestly -- a prior run closed this but skipped this file's required append step): Session-rule tool-scoping ENFORCEMENT batch (backend-only), continuing 6484132's foundation above. f3872c8 wires `SessionRules::applyToRequestBytes(bytes, host, tool)` into `Repeater::send()` (previously called with no tool argument, silently defaulting to the Proxy-scoped path even though the whole point of 6484132 was per-tool scoping); the reserialize path this required (turning edited request bytes back into an `HttpRequest` for `Proxy::serializeRequestForOrigin()`) was Repeater's first real cross-TU pull into `proxy_server.cpp`'s object file, which -- as it turned out -- is exactly what broke `scanner_regression_test`'s link (see f034a92 below). 3611a1e does the same for `Intruder`'s per-request send path, tagged `SessionRulesLogic::ToolIntruder`. d07dfa1 is the parity.json reconcile for both: "Rule scope -- Tools scope" stays partial (mechanism + enforcement are now real for Repeater and Intruder, but the SESSIONS tab's rule editor still has no per-tool checkboxes, and Chain/Sequencer/Extender scopes remain unbuilt -- honestly unclaimed). a11754e is a separate, unrelated fix bridging a recorded macro's extracted variables into the session-rule bag (`SessionRules::ingestVariables`, a new opt-in `sessionHost` field on `POST /api/chain/run`) -- closing the record-a-login -> stay-authenticated loop without hand-copying tokens; reconciles half of "Rule action: Run a macro" (absent -> partial, per f034a92's own note below, since re-reading a11754e after the fact is what caught this reconcile had been missed too). f034a92 itself is the actual CI fix: f3872c8's `serializeRequestForOrigin()` call pulled `proxy_server.o` (with its `ExtensionsApi::applyRequestMutation/applyResponseMutation` calls) into `scanner_regression_test`'s link for the first time, and that test's CMakeLists never linked the `APIs` library those symbols live in (Proxy's own CMakeLists only gets away with this via an include-path-only, not link, dependency on APIs -- the App target happens to link APIs directly too, papering over the gap until a test came along that didn't) -- 4 commits (f3872c8 through a11754e) sat CI-red before this was diagnosed and fixed by linking `APIs` into the test target. All five commits' parity/docs reconciliation had, per their own commit logs, already landed correctly at push time (d07dfa1 and f034a92 both carry their parity.json + docs/roadmap/index.html updates) -- only this file's required progress-note append was missing, corrected here. GitHub Actions ci run confirmed green on f034a92 itself (run 31947410409, run_number 615, conclusion=success) via the Actions API this session -- the 4-commit red streak is resolved and there is no outstanding CI risk from this batch.

- Wave 3 (2026-08-16, 12cb5db): Repeater/HttpClient gzip-deflate unpack batch -- `HttpClient::send` (networking.cpp), the transport both Repeater and the detection-template engine (`/api/template/run`) use, never decoded `Content-Encoding`, unlike the MITM proxy path (proxy_server.cpp:917-919) -- so any target honouring `Accept-Encoding: gzip` (the default, since requests replayed from history carry the browser's original header) rendered as binary mojibake in Repeater's response pane, and the template engine's body-match evaluation was silently matching against still-compressed bytes. The decoder already existed (`content_decode.hpp`'s `decodeContentEncoding`, its own dedicated test suite) with exactly one caller (the proxy); this pins the second. networking.cpp now looks up the `Content-Encoding` header after response parsing and calls the existing helper, populating `result.parsed.decodedBody` -- never touching `result.parsed.body` or `result.rawResponse`, the exact wire bytes. `Repeater::send()` reassembles `responseText` from the original header block (verbatim, including the `Content-Encoding` header itself, found via `rawResponse.indexOf("\r\n\r\n")` -- confirmed this lands on the FINAL response's boundary, not an interim 1xx's, since `result.rawResponse` is freshly seeded post-interim-skip at networking.cpp:251) plus the decoded body in place of the compressed bytes, only when decoding actually produced something; otherwise falls back to the prior byte-for-byte behavior unchanged. Reconciles "Repeater: Unpack gzip / deflate in the response view" absent -> partial, not present: brotli/zstd remain unsupported (content_decode.hpp's own documented limit, unchanged). Link-risk was checked deliberately given f034a92 above: `content_decode.cpp` has zero dependency on ExtensionsAPI (unlike proxy_server.cpp, the actual cause of that break) -- its only external need is zlib, which Proxy's CMakeLists links via a real `target_link_libraries(Proxy PRIVATE ...)` CMake target (Qt6::ZlibPrivate on Windows / ZLIB::ZLIB on Linux, both properly declared), so it propagates transitively to any consumer via CMake's standard private-static-library link-only interface -- the failure mode that bit `scanner_regression_test` was specifically an UNDECLARED dependency (Proxy only `target_include_directories`'d the APIs headers, never linked the APIs target), which this change does not repeat. This cloud session cannot build the C++ side or run ctest/probe_smoke; pushed as 12cb5db (feature, includes parity.json + docs/roadmap/index.html + CHANGELOG.md) + a following commit backfilling this commit's own hash into parity.json's self-referential reconciled.commit, this file's established pattern for that ordering problem. HEAD verified == origin/Nullock via two `git rev-parse` calls before and after push; will confirm the ci.yml run is green next wave before calling this fully closed, same honest-partial pattern as prior in-flight-CI notes in this file.

- Wave 3 (2026-08-16, confirm CI for 12cb5db/0162755): gzip/deflate-unpack wave -- GitHub Actions ci run green on 0162755 itself (run 31956232918, run_number 616, conclusion=success); docs-site (pages) run also green on 0162755 (run_number 191, conclusion=success), confirmed directly via the Actions API this session before starting the next batch. No outstanding CI risk from this wave.

- Wave 4 (2026-08-16): Labs difficulty + progressive hints batch -- first real installment on task #122 ("Labs as TryHackMe/HackTheBox scenarios"), which had zero prior work: all 50 labs were a bare title/description/walkthrough/fix with no difficulty signal and no partial-credit path for a stuck learner. `scripts/labs_site.py` gains a curated `DIFFICULTY` map (Easy/Medium/Hard, one editorial call per lab -- e.g. reflected XSS/IDOR/broken-access are Easy, JWT/SSRF/NoSQLi are Medium, SSTI/race-condition/deserialization/prototype-pollution/XXE/OAuth-state are Hard) and a `HINTS` map (3 hand-written progressive hints per lab -- a nudge, then a technique, then a near-payload -- distinct from the existing docstring-authored walkthrough, which still gives the full numbered steps below the hints). Both render on `docs/labs/index.html` (a colour-coded EASY/MEDIUM/HARD badge per card plus a second difficulty filter-chip row alongside the existing category chips, client-side, no backend) and on each of the 50 detail pages (badge in the head row; a `<details>`-based progressive-reveal Hints section between the pre-set-scope box and the Walkthrough, no JS needed since native `<details>` provides the collapse). New CSS: `.labs-diff`/`.labs-diff-{easy,medium,hard}` (reusing the site's existing `--ok`/`--warn`/`--err` tokens) and `.labs-hint`/`.labs-hints` in docs/assets/nullock.css. Left honestly unbuilt, per #122's full ask: submit-flag/success-check (needs each lab's Flask app to actually expose a checkable success marker, a real per-lab engineering task deliberately scoped out of this batch), tracks/XP gamification (needs persistent progress state -- a backend store or a documented localStorage scheme -- neither exists yet), and in-app wiring (the desktop app has no Labs surface at all; docs/labs is web-only). No parity.json item corresponds to Labs (Nullock-native, like the JWT toolkit, not Burp-parity-tracked), so no state flip; `python3 scripts/parity_report.py --check` still green (409 features, unchanged) confirming this batch didn't touch it. Verified: `python3 scripts/labs_site.py --check` green after regeneration (51 pages, 50 labs, all 50 confirmed to carry both a `DIFFICULTY` entry and 3 `HINTS`), `python3 -c "import ast; ast.parse(...)"` on the generator, a Python AST-level walk of `load()`'s output proving zero labs fall through to the Medium default silently (all 50 have an explicit map entry) and zero labs lack hints, a Node `--check` syntax pass on the regenerated index page's inline filter/render JS (extracted via regex, not hand-copied), a whole-site relative-link check (0 broken, 78 pages, 1078 relative links -- unchanged baseline, confirming no page got a bad href), and `python3 scripts/marketplace_site.py --check` green (unaffected, sanity-checking the shared page/nav template wasn't broken). README.md's Teaching-labs feature line updated to name the new difficulty ratings + hints; docs/index.html has no per-feature Labs card to update (only a nav link), so left untouched, matching this file's "only touch what actually changed" convention. This is a docs/site-only change (scripts/labs_site.py, docs/labs/*.html, docs/assets/nullock.css, README.md) -- no C++ or ui-v2 touched, so no CI build risk expected, but will confirm the next GitHub Actions ci run (docs-site/pages workflow) is green before calling this fully closed, same honest-partial pattern as prior in-flight-CI notes in this file.

- Wave 4 (2026-08-16, confirm CI for 871ed07): Labs difficulty + progressive hints wave -- GitHub Actions ci run green on 871ed07 itself (run 31965109434, run_number 617, conclusion=success); docs-site (pages) run also green on 871ed07 (run_number 192, conclusion=success), confirmed directly via the Actions API this session before starting the next batch. No outstanding CI risk from this wave.

- Wave 3 (2026-08-16, 5012a6f/dc1ca37 + d728ea4/04aa7a3 + 33c51ae/19e9b5f, backfilled here honestly -- a prior run closed this but skipped this file's required append step): Session login-macro engine batch (backend-only, closes launch blockers #10/#11). 5012a6f adds `SessionMacro` (name, sessionHost, ChainRunner steps, loggedOut status/regex) with `SessionRules::setMacros/macros/runMacro` (replay a named login sequence, ingest its extracted vars into the session bag) and `SessionRulesLogic::responseIsLoggedOut(status, body, statusList, bodyRegex)` -- pure, unit-tested + mutation-proven -- wired into `applyToResponse`'s `maybeReauth`: a response matching the logged-out condition re-runs the macro ASYNC (QtConcurrent, never blocks the proxy worker), loop-guarded by an in-flight set + a 5s per-host cooldown, with the macro's own requests going through ChainRunner's client (not the proxy) so they can't re-trigger themselves. `/api/session-macros` get/set + `/api/session-macros/run` dispatch. d728ea4 makes macros durable: a shared `sessionMacrosToJson/sessionMacrosFromJson` serializer (the single source of the macro<->JSON shape) plus a `ProjectStore` `sessionMacros` field persisted at the API-call-site into project.json, restored on open -- verified e2e (set -> kill -> relaunch -> restored with every field intact) per the commit's own record. dc1ca37/04aa7a3 reconcile parity.json: "Check session is valid" (#190) absent -> partial (the conditional re-auth is real but this is passive detection on the live response stream, not Burp's dedicated active validation request) and "Run a macro" (#189) stays partial (named-macro store + on-demand run + automatic re-run + persistence are all real now; the scheduled "run before every in-scope request" variant is still missing) -- both honest, not over-claimed. Two same-day Linux-only CI breaks in this lineage, fixed forward rather than reverted: 33c51ae (a bare `#include <QtConcurrent>` that MSVC resolves but GCC doesn't -- module-qualified to match the codebase's existing QtConcurrent user) and 19e9b5f (`session_rules.cpp`'s new `runMacro()` pulls `ChainRunner::run` into `proxy_shutdown_test`'s curated cross-TU link set, a 5th symbol beyond the 4 that test already special-cased -- added to its CMake sources). No frontend touched this wave. GitHub Actions ci run confirmed green on 19e9b5f itself (run 31998918921, run_number 625, conclusion=success) via the Actions API this session, resolving the 4-commit red streak (5012a6f through 04aa7a3, run_numbers 620-623, all conclusion=failure on the Linux job) that preceded it -- no outstanding CI risk from this batch.

- Wave 2 (2026-08-17, 6be90f4/6cf4105): Send to Sequencer batch -- closes #167's GUI-reachability gap (absent -> partial). Proxy history's DetailPane (ui-v2/proxy.jsx) gains a SEQUENCER button next to REPEATER/INTRUDER, and Repeater's request/response pane-heads (ui-v2/tabs.jsx) gain SEQ buttons next to the existing CMP/DEC ones -- each grabs the current text SELECTION (not the whole request/response, unlike Comparer/Decoder's send-to, since a whole HTTP message isn't a meaningful "token" for entropy analysis) and appends it to a new `sequencer.tokens` client-side inbox (app.jsx initial state + two reducer cases, `sequencer-add-token`/`sequencer-clear-tokens`), switching to the Sequencer tab. `SequencerTab` kept its existing local textarea state (fast typing/pasting unchanged) and gained one effect that folds newly-arrived inbox tokens into it, so repeated captures across several requests build up a real corpus without hand copy-paste -- the actual gap the parity item's own gap-text named ("tokens must be extracted by hand and shell-quoted"). Honest remainder: this is a toolbar button, not the literal right-click context-menu entry the title names, and Target/scope has no send path (no per-request view there to select from); Live Capture (#168-171, repeatedly issuing a request and auto-harvesting a token) stays a separate absent item needing a backend request-issuing engine this cloud session cannot build. No backend touched -- purely a new client-side inbox. Verified: bundled-babel transform of all three touched files (app.jsx, proxy.jsx, tabs.jsx), confirmation ui-v2/tabs.jsx's intentional NUL byte was untouched (still exactly 1 byte, offset shifted from 67968 to 68956 by the inserted lines as expected), a 4-check Node functional test of the `sequencer-add-token` reducer case extracted from the real babel-transformed source via a vm sandbox (trim+append, second append, whitespace-only no-op, empty no-op), a hook-stubbed vm-sandboxed render smoke test loading all three files in real script order (proxy.jsx -> tabs.jsx -> app.jsx, so cross-file globals resolve) proving `SequencerTab` (populated and empty token-inbox states, exercising the new fold-in effect), `DetailPane` (with a row + the new SEQUENCER button branch, and the untouched row=null early return), and `RepeaterTab` (new SEQ buttons in both panes) all construct their element trees with no throw, a whole-site relative-link check (0 broken, 78 pages, 1078 relative links -- unchanged baseline), marketplace/labs-site drift checks green (16+51 pages), and `python3 scripts/parity_report.py --check` green (409 features, 182 at parity = 44%, unchanged count since no state flipped to present/exceeds). README.md, docs/index.html, docs/changelog.html, and CHANGELOG.md updated (Sequencer feature line/card/changelog entry now name the send-to-Sequencer buttons). Pushed as 6be90f4 (feature, includes parity.json + docs) + 6cf4105 backfilling this commit's own hash into parity.json's self-referential reconciled.commit, this file's established pattern for that ordering problem. HEAD verified == origin/Nullock via `git rev-parse` calls after each push. This cloud session cannot build the C++ side, though this batch touched no C++; will confirm the next GitHub Actions ci run is green before calling this fully closed.

- Wave 3 (2026-08-16, 37e32f0): Session-rules tools-scope editor-UI batch -- closes the ui-v2 half of parity item #187 "Rule scope -- Tools scope" (backend mechanism + Repeater/Intruder enforcement landed 2026-08-15 in 6484132/f3872c8/3611a1e; the editor checkboxes were the explicitly-named remainder). SessionsTab's rule editor (ui-v2/app.jsx) gains a Tools scope row -- Proxy/Repeater/Intruder/Scanner checkboxes bound to each rule's `tools` bitmask via a new pure `sessionRuleToggleToolBit(mask, bit)` helper mirroring `SessionRulesLogic::SessionTool` exactly (1/2/4/8, unset=all tools), plus a `sessionRuleToolsLabel(mask)` summary shown as a new column in the rule list ("all tools" when unset, else a comma-joined list). No backend change -- the `tools` field already round-tripped through `/api/session-rules` (control_server.cpp:1618 snapshot, :3980 set) with nothing in the GUI ever reading or writing it. State stays `partial`, not flipped to `present` -- confirmed by grep that the Scanner checkbox is offered/stored but enforced nowhere (only `ToolIntruder`/`ToolRepeater` appear as call-site arguments; `proxy_server.cpp:1539` uses the default-Proxy overload), and the chain-runner/sequencer/extender scopes remain entirely unbuilt (no UI, no enforcement) -- both named honestly in the updated gap/reconciled text rather than claimed closed. No backend touched. Verified: bundled-babel transform of ui-v2/app.jsx, confirmation ui-v2/tabs.jsx was untouched (its intentional NUL byte still exactly 1 byte at offset 67968, file not in the diff), a 13-check Node functional test of the two new pure helpers run against the real babel-transformed module in a vm sandbox (bitmask shape matches the C++ enum, toggle-on/off/combine across all four bits, label rendering for 0/single-bit/multi-bit/all-four masks), a vm-sandboxed hook-stubbed render of SessionsTab with a populated rule list (one all-tools rule, one Repeater-scoped rule) proving the new checkboxes and per-row scope summary construct in the element tree with no throw, a whole-site relative-link check (0 broken, 78 pages, 998 relative links), marketplace/labs-site drift checks green (16+51 pages), and `python3 scripts/parity_report.py --check` green (409 features, 182 at parity = 44%, unchanged count since no state flipped). README.md, docs/index.html, and CHANGELOG.md updated (Session rules feature line/card and an Unreleased entry now name the tools-scope checkboxes). Pushed as 37e32f0 (feature) + a following commit backfilling this commit's own hash into parity.json's self-referential reconciled.commit, this file's established pattern for that ordering problem. HEAD verified == origin/Nullock via two `git rev-parse` calls before and after each push. This cloud session cannot build the C++ side, though this batch touched no C++; will confirm the next GitHub Actions ci run is green before calling this fully closed.

- Wave 2/3 (2026-08-17, confirm CI for 6be90f4/6cf4105 and 37e32f0/8da4465): both prior in-flight batches confirmed via the Actions API this session -- 6be90f4 (Send to Sequencer) run_number 626 green (conclusion=success), 6cf4105 (its docs-backfill commit) run_number 627 green on both ci.yml and pages.yml (conclusion=success); 37e32f0 (session-rules tools-scope editor) never triggered its own ci.yml run -- it was pushed to origin in the same push as its docs commit 8da4465, so only 8da4465's push event fired a run (run_number 619, conclusion=success), which covers 37e32f0's code unchanged, the same one-push/one-run reasoning this file has applied before to adjacent feature+docs commit pairs; 8da4465's pages.yml run (run_number 193) is also green. No outstanding CI risk from either wave.

- Wave 0 (2026-08-17, 5076cb5): register-accuracy fix -- parity item [A1] "Crawl: build site map by link-following (spider)" (docs/roadmap/parity.json index 49) still carried a stale gap clause, "Also API-only: no crawl button/tab in the shipped UI, so a GUI user cannot start a crawl", dating to before the DISCOVER tab existed. Re-verified against live source rather than trusted at face value: `ui-v2/app.jsx`'s `DiscoverTab` has had a full start/stop crawl control (seed URL + max-pages/max-depth/throttle-ms, the last three added by the 3494297 wave on 2026-08-13) wired to `NL.actions.crawlerStart`/`crawlerStop` (`/api/crawler/start`,`stop`) since well before this run, confirmed live by grep of `ui-v2/app.jsx` (`startCrawl`/`stopCrawl` call sites) and the `TABS` array (`discover` entry) plus `chrome.jsx`'s render switch -- this item's own record was simply never cross-referenced to that later wave. State stays `partial`, not flipped to `present`: the crawler's real remaining gap (`crawler.cpp:154`, static-HTML-only, no JS execution, no navigation-graph diffing) is untouched and still true; only the previously-false GUI-reachability clause is corrected. No frontend or backend code touched; docs/roadmap/index.html regenerated (409 features, 182 at parity = 44%, unchanged counts since no state flipped). Verified: a whole-site relative-link check (0 broken, 78 pages, 1078 relative links -- unchanged baseline), marketplace/labs-site drift checks green (16+51 pages), and `python3 scripts/parity_report.py --check` green. Pushed as 5076cb5 (register fix) + a following commit backfilling this commit's own hash into parity.json's self-referential reconciled.commit, this file's established pattern for that ordering problem. HEAD verified == origin/Nullock via two `git rev-parse` calls before and after each push. This is a docs-only change (parity.json + docs/roadmap/index.html only); no C++/frontend touched, so no CI risk expected, but will confirm the next GitHub Actions ci run is green before calling this fully closed.

- Wave 2 (2026-08-17, dc4aee9/0c22aed): dedicated WEBSOCKETS tab (#269) -- ui-v2/app.jsx gains a WebSocketsTab (23 -> 24 top-level tabs) that groups the same NL.rows entries the Proxy tab already surfaced as pseudo-HTTP WS↑/WS↓ rows into a per-host:port CONNECTIONS list, replacing the cosmetic Status/Mime columns with real Direction/Type/Length ones (type/deflate/continued decoded out of the synthetic history-row path string proxy_server.cpp's emitMessage builds -- the only place those flags are carried), adds a DIRECTION/TYPE filter bar and a per-message free-text comment (localStorage-persisted, matching the Comparer/Decoder client-only-state pattern), and reuses proxy.jsx's DetailPane verbatim for message detail so Raw/Headers/Body/Hex/Inspector and every Send-to-Repeater/Intruder/Comparer/Decoder/Sequencer pivot work identically to Proxy history. No backend touched. Left honestly `partial` in parity.json, not flipped to `present`: capture is still TLS-MITM-leg-only (runWebSocketRelay's sole call site is the h1 MITM keep-alive loop's 101 Upgrade branch, so plaintext ws:// is never relayed -- a real backend gap vs Burp) and concurrent tunnels to the same host:port share one connection bucket (a history row carries no per-tunnel session id) -- both out of reach for a frontend-only wave. Verified: bundled-babel transform of ui-v2/app.jsx, confirmation ui-v2/tabs.jsx was untouched (NUL byte still exactly 1 byte at offset 68956), a 10-check Node functional test of the WS_PATH_RE opcode/deflate/continued parser and the connection-grouping logic, a vm-sandboxed hook-stubbed render of WebSocketsTab across populated and empty NL.rows states (constructing DetailPane's full sub-tree too) proving no runtime throw, a whole-site relative-link check (0 broken, 78 pages, 1078 relative links), marketplace/labs-site drift checks green (16+51 pages), and `python3 scripts/parity_report.py --check` green (409 features, 182 at parity = 44%, unchanged count since state stayed partial). README.md, docs/index.html, UI-NAVIGABILITY.md, and CHANGELOG.md updated (new WebSockets feature copy, 23 -> 24 tab count everywhere it's cited). Pushed as dc4aee9 (feature) + 0c22aed (backfilling dc4aee9's own hash into parity.json's self-referential reconciled.commit, this file's established pattern). HEAD verified == origin/Nullock via `git rev-parse` calls after each push. GitHub Actions ci run green on dc4aee9 (run_number 630, https://github.com/Bikebrainz/Nullock/actions/runs/32003089146) -- all 6 jobs (Windows build, Linux build, marketplace/parity drift check, fuzz-corpus replay, CLI syntax, labs syntax) passed, confirmed via the Actions API this session; 0c22aed's own run (run_number 631) was still completing its Linux build job when this session ended verification, but it is code-identical to dc4aee9 on every compiled/frontend file (only parity.json's commit-hash string and the regenerated static roadmap page differ), so it carries no independent CI risk -- will do a final confirm-CI pass next session per this file's established pattern for that timing gap.

- Wave 3 (2026-08-17, 61a4eb8/3e069ff): Comparer diff-cap + register-accuracy fix (backlog #315) -- `diffLines` (ui-v2/proxy.jsx), the DIFF-vs overlay's client-side line diff, built an unbounded n*m `Int32Array` LCS table with no cap, unlike the backend Comparer engine (`compare.cpp:14 kMaxTokens=2000`) which already guards the same class of input; a large response could hang or OOM the tab. Added `DIFF_MAX_LINES=2000`, clipping each side and setting a `truncated` flag on the returned rows array, surfaced as a TRUNCATED badge in the overlay header (DiffOverlay, proxy.jsx:1716-1722). Also corrected parity.json's item 315 gap/evidence text, which still quoted a dead `NL.requestRawAt(idA - 1)` id-as-index bug fixed in an earlier wave -- live code at proxy.jsx:1634-1637 reads `NL.requestRawById`/`responseRawById`, confirmed by direct grep before writing the correction. State stays `exceeds` (the mark-then-diff workflow win over Burp's two-trip Send-to-Comparer is unchanged); only the gap text and the real remaining defect (now fixed) changed. No backend touched. Verified: bundled-babel transform of ui-v2/proxy.jsx, confirmation ui-v2/tabs.jsx's intentional NUL byte was untouched (still exactly 1 byte at offset 68956, file not in the diff), a 5-check Node functional test of `diffLines` run against the real babel-transformed module in a vm sandbox (identical-text no-truncation, simple add/del still correct, a 50k-line/side pair completes in well under 5s and sets truncated=true with bounded output, an asymmetric-size pair still truncates, and exactly-at-the-cap-boundary does NOT truncate), a vm-sandboxed hook-stubbed render of `DiffOverlay` with a 5000-line/side diff proving the new badge branch constructs with no throw, a whole-site relative-link check (0 broken, 78 pages, 998 relative links), marketplace/labs-site drift checks green (16+51 pages), and `python3 scripts/parity_report.py --check` green (409 features, 182 at parity = 44%, unchanged count since state stayed exceeds). CHANGELOG.md updated (new Fixed entry); README.md/docs/index.html left untouched since this is a bugfix to an existing feature, not a newly-reachable capability, matching this file's "only touch what actually changed" convention. Pushed as 61a4eb8 (feature) + 3e069ff (backfilling 61a4eb8's own hash into parity.json's self-referential reconciled.commit). Origin had moved during this run (a concurrent session landed the Sequencer-live-capture wave, f910a9e/a88cf31/7aecc1c) -- rebased both commits onto origin/Nullock cleanly, resolving two docs/roadmap/index.html conflicts by regenerating the file from the merged parity.json rather than hand-editing conflict markers; re-ran the babel transform, NUL-byte check, and `parity_report.py --check` after the rebase to confirm nothing was lost. HEAD verified == origin/Nullock via `git rev-parse` after push. This cloud session cannot build the C++ side, though this batch touched no C++; the pushed run (run_number 636, covering both commits) was still queued behind the concurrent session's in-progress Sequencer-wave builds (run_numbers 633-635) when this session ended -- will confirm the ci.yml run is green next wave before calling this fully closed, same honest-partial pattern as prior in-flight-CI notes in this file.

- Wave 3 (2026-08-17, confirm CI for 61a4eb8/3e069ff): the Comparer diff-cap wave's own pushed run (run_number 636) is confirmed completed/success via the Actions API this session -- no outstanding CI risk.

- Wave 0 (2026-08-17, a6987bb/7d8d896, backfilled here honestly -- a prior run closed this but skipped this file's required append step): Repeater follow-redirects batch (backend-only, closes launch-blocker gap "Repeater has no Follow redirection"). New pure `redirect_logic` (Qt6::Core, unit-tested + mutation-proven -- `redirect_test` 62/62 per the commit's own record) provides `isRedirectStatus`, QUrl-based Location resolution (relative/absolute/protocol-relative), post-redirect method rewrite (POST->GET on 301/302/303, preserved on 307/308) + body preservation, the four-mode follow policy (never/on-site/in-scope/always), Set-Cookie threading across hops, and the follow-up request builder. `Repeater::send` now loops on a 3xx response when `followRedirects != never`, up to `kMaxRedirectHops`, showing the FINAL response with a "[followed N redirects]" note; `processCookies` threads the original request's cookies plus every `Set-Cookie` along the chain; the in-scope policy uses a scope checker `app.cpp` wires to `ProxyServer::isInScope` (Repeater holds no proxy pointer itself). Settable via `/api/repeater/set` (`followRedirects` 0-3, `processCookies`). Reconciles parity.json's "Repeater: Follow redirection button + setting + Process cookies in redirections" item absent -> partial: the backend + API round-trip are real and verified e2e (commit message records a `/a->/b->/c` 302 chain followed to the final page with the session cookie carried; never-mode stops at the first 302), but there is still no UI toggle in ui-v2 -- a tester cannot yet choose a follow mode from the Repeater tab, named honestly as the follow-on rather than claimed done. No frontend touched. This cloud session cannot build/run the C++ side itself; GitHub Actions ci run confirmed green on a6987bb (run_number 638, conclusion=success) and on its docs-reconcile commit 7d8d896 (run_number 639, conclusion=success) via the Actions API this session, plus both commits' pages.yml runs (run_numbers 203, and 204 for the sibling Intruder commit below) also green -- no outstanding CI risk from this wave.

- Wave 0 (2026-08-17, 1c45581/8310e10, backfilled here honestly -- a prior run closed this but skipped this file's required append step): Intruder follow-redirects batch (backend-only), wiring the same shared `redirect_logic` from the Repeater wave above into Intruder, closing the sibling "Follow redirections (never/on-site/in-scope/always)" item. After each fired request, the worker now follows its 3xx chain (up to `kMaxRedirectHops`) via the shared pure `redirect_logic`, so the recorded row -- status, length, and every Grep column -- reflects the FINAL page rather than an intermediate redirect, so a bruteforce behind a login/redirect grades against real content instead of a page of 302s. `m_followPolicy`/`m_followCookies`/`m_inScope` threaded through `start()`'s capture block and `runWorker` into `fireOne` (one `HttpClient` per pool thread, reused across hops, preserving thread-affinity); settings clamp to the `FollowPolicy` range; the in-scope policy reuses the same `ProxyServer::isInScope` scope checker `app.cpp` wires for Repeater. Settable via `/api/intruder/set` (`followRedirects` 0-3, `processCookies`). Reconciles parity.json's "Follow redirections (never/on-site/in-scope/always)" (Intruder) item absent -> partial: verified e2e per the commit's own record (an Intruder payload whose request 302-chains `/a->/b->/c` is graded on the final 200; never-mode stops at the 302); the resend path and the ui-v2 toggle remain named follow-ons, not claimed done -- no new pure logic needed unit tests since `redirect_logic` was already unit-tested + mutation-proven by the Repeater wave. No frontend touched. GitHub Actions ci run confirmed green on 1c45581 (run_number 640, conclusion=success) and on its docs-reconcile commit 8310e10 (run_number 641, conclusion=success) via the Actions API this session, plus both commits' pages.yml runs (run_numbers 203-204) also green -- no outstanding CI risk from this wave. Both Repeater and Intruder now share one battle-tested redirect engine; the next natural batch is the ui-v2 follow-mode toggle for both tools, still unbuilt on either side.

- Wave 2 (2026-08-17, fcbe737/915e78c): Repeater/Intruder follow-redirect GUI batch -- the natural next batch this file itself named above. `ui-v2/tabs.jsx`'s RepeaterTab target-row and IntruderTab settings-row each gain a FOLLOW select (never/on-site/in-scope/always, a shared `FOLLOW_REDIRECT_LABELS` constant mirroring `RedirectLogic::FollowPolicy` exactly) and a COOKIES checkbox, both dispatched through the pre-existing `repeater-set`/`intruder-set` actions to `/api/repeater/set`/`/api/intruder/set` -- the API these settings could previously only be driven through by hand. One small, high-confidence C++ change was required to make this possible: `control_server.cpp`'s snapshot serializer didn't expose `followRedirects`/`processCookies` to the GUI at all (only the setters existed), so the new controls would have shown as permanently unchecked/reset to NEVER on every reload; added two lines to each of the repeater/intruder snapshot objects, mirroring the existing `autoContentLength` pattern exactly. Reconciles parity.json: Repeater's "Follow redirection button + setting + Process cookies in redirections" item flips **partial -> present** (409 features, 183 at parity = 45%, up from 182/44%) -- confirmed by grep that nothing else in Burp's own ask for this item is missing. Intruder's sibling item stays `partial`: re-read `Intruder::resend` (intruder.cpp:640-721, the single-row re-fire path off a completed attack) and confirmed it builds its own `HttpClient` and calls `client.send()` directly with zero `RedirectLogic` involvement -- a resent row's grade still doesn't follow 3xx, unlike a first-pass fired request, named honestly as the one remaining gap rather than claimed closed. README.md, docs/index.html, CHANGELOG.md, and docs/changelog.html updated (new FOLLOW/COOKIES control copy for both tools; the docs/changelog.html Repeater entry's stale "the Repeater toggle is still to come" parenthetical was also corrected, since it's what this wave built). Verified: bundled-babel transform of ui-v2/tabs.jsx, confirmation its intentional NUL byte is still exactly 1 byte (offset shifted from 68956 to 70665 by the inserted lines, as expected), a 4-case vm-sandboxed hook-stubbed render smoke test loading proxy.jsx -> tabs.jsx -> app.jsx in real script order (after fixing a harness bug of my own -- the first draft leaked React hook state across separate component-render calls in the same process, corrupting a later hook's initial value; fixed by clearing the hook-state store per check() call) proving RepeaterTab and IntruderTab construct their element trees with no throw across default and populated followRedirects/processCookies snapshot states, a whole-site relative-link check (78 pages, 1078 relative links, 0 broken -- unchanged baseline), marketplace/labs-site drift checks green (16+51 pages), and `python3 scripts/parity_report.py --check` green. Pushed as fcbe737 (feature, includes the control_server.cpp change + parity.json + docs) + 915e78c (backfilling fcbe737's own hash into parity.json's self-referential reconciled.commit, this file's established pattern). HEAD verified == origin/Nullock via `git rev-parse` calls after each push. This cloud session cannot build the C++ side; both pushed runs (run_numbers 643, 644) were still in the Windows/Linux build stage (build times here run ~18-20 minutes per this file's own historical run timestamps) when this session ended verification -- will confirm the ci.yml run is green next wave before calling this fully closed, same honest-partial pattern as prior in-flight-CI notes in this file. (Also confirmed CI green this session for the two backend redirect waves backfilled above: a6987bb/7d8d896 run_numbers 638-639 and 1c45581/8310e10 run_numbers 640-641, all conclusion=success on both ci.yml and pages.yml.)

- Wave 3 (2026-08-17, 84f116b/f0c62ec): confirmed CI green on fcbe737/915e78c (run_numbers 643-644, both conclusion=success on ci.yml, confirmed via the Actions API this session; pages.yml runs 205-206 also green) -- no outstanding CI risk from that wave. Then closed the one remaining named gap it left: `Intruder::resend` (the single-row re-fire path off a completed attack) built its own request/response path with zero `RedirectLogic` involvement, unlike `fireOne` (the attack-fire path). `resend()` now runs the identical chain-follow block -- same `requestUrl`/`resolveRedirect`/`followAllowed`/`mergeSetCookies`/`buildFollowRequest` sequence, capped at `kMaxRedirectHops` -- off a captured-by-value copy of `m_followPolicy`/`m_followCookies`/`m_inScope`, a direct line-by-line mirror of the already-verified `fireOne` block. Flips parity.json's Intruder "Follow redirections (never/on-site/in-scope/always)" item **partial -> present** (409 features, 184 at parity = 45%, up from 183/45%). No new pure logic added (reuses the already unit-tested + mutation-proven `redirect_logic`), so no new unit tests were required -- verification for this backend-only change was: a direct diff of the new block against `fireOne`'s proven block (identical function-call sequence, only variable names differ for the resend-local captures), a whole-site relative-link check (0 broken, 78 pages, 1078 relative links -- unchanged baseline), marketplace/labs-site drift checks green (16+51 pages, byte-identical regeneration), and `python3 scripts/parity_report.py --check` green. CHANGELOG.md, docs/changelog.html, and docs/index.html updated to drop the three stale "Resend doesn't follow redirects" / "single-row Resend does not yet follow redirects" gap notes this wave made false. Pushed as 84f116b (feature, includes intruder.cpp + parity.json + docs) + f0c62ec (backfilling 84f116b's own hash into parity.json's self-referential reconciled.commit, this file's established pattern). HEAD verified == origin/Nullock via `git rev-parse` calls before and after push. This cloud session cannot build the C++ side; will confirm the next GitHub Actions ci run is green before calling this fully closed, same honest-partial pattern as prior in-flight-CI notes in this file.

- Wave 2 (2026-08-17, confirm CI for 84f116b/f0c62ec, then 7e7ec54/fb1b5c6): confirmed CI green on the prior Intruder-resend-follows-redirects wave -- ci.yml run 646 (covering f0c62ec, which was pushed in the same push as 84f116b so its run covers both) and run 647 (1749bde, this file's own progress-note commit) both conclusion=success, confirmed via the Actions API this session. pages.yml (docs-site) run 207 for f0c62ec showed conclusion=failure, but its job log (job 95424358424) died in "Set up job" on a `codeload.github.com` 429/503 fetching the `actions/configure-pages` action -- an infra flake before any of this repo's own steps ran, not a code defect; per this file's own re-run-vs-root-cause convention that class of failure gets re-run, not diagnosed. Attempted `rerun_failed_jobs` and a fresh `run_workflow` dispatch via the GitHub MCP tools; both were rejected with 403 "Resource not accessible by integration" (this session's GitHub App token lacks Actions-write scope) -- so a fix-forward wasn't available this session; a subsequent commit's own pages.yml push will naturally supersede it, and the failure carries no code risk regardless. Then closed a fresh Wave 2 batch: `ui-v2/tabs.jsx`'s RepeaterTab request pane-head gains two buttons next to the existing CMP/DEC/SEQ ones -- ⇄ METHOD (toggles GET/POST, moving params between the query string and an `application/x-www-form-urlencoded` body) and ⇄ ENCODING (converts that body to `multipart/form-data` and back), both via new pure `repeaterChangeMethod()`/`repeaterChangeBodyEncoding()` helpers with their own small request-line/header/body parser+serializer (`repeaterSplitRaw`/`repeaterBuildRaw`/`repeaterSetHeader`/`repeaterGetHeader`/`repeaterRemoveHeader`) that, unlike proxy.jsx's one-way `parseRawRequest`, round-trips every header verbatim including Host/Content-Length -- both recompute Content-Type/Content-Length and dispatch through the existing `repeater-set` action, the identical path a hand edit in the textarea already uses. No backend touched. Closes parity items #339 and #340 (absent -> present, 409 features, 186 at parity = 45%, up from 184/45%) -- #340's gap text honestly names the one real remaining limit: item #303's send-to-Repeater path still replaces a captured multipart body with a `[binary N bytes, ...]` placeholder before it reaches this editor, so the button can't yet round-trip a multipart body that arrived via Send to Repeater, only ones typed/generated directly. Verified: bundled-babel transform of ui-v2/tabs.jsx, confirmation its intentional NUL byte is still exactly 1 byte (offset shifted from 70665 to 77073 by the inserted lines, as expected -- and corrected this file's own stale claim that the byte sits "near line 500"; it's actually at line ~1411/1553, inside an unrelated fuzz-string wordlist literal, confirmed by a subagent this run), an 18-check Node functional test of the two pure transforms (GET->POST->GET round trip, no-query-string GET, existing-query-plus-body merge on POST->GET, urlencoded<->multipart round trip including a special-char value, and a missing-boundary no-op) run against the real babel-transformed module in a vm sandbox -- which caught and fixed one real bug before commit (the multipart boundary was being extracted from the lowercased Content-Type string, so a mixed-case boundary like `TESTBOUNDARY123` never matched the body's actual `--TESTBOUNDARY123` markers; fixed by extracting the boundary from the original-case header text, only using the lowercased copy for the `multipart/form-data` type check) -- plus a vm-sandboxed hook-stubbed render smoke test of RepeaterTab (proxy.jsx -> tabs.jsx load order) across empty and populated states proving both new buttons construct in the element tree and, when their onClick handlers are invoked directly from the rendered tree (not just the pure functions in isolation), dispatch `{type:"repeater-set", payload:{request: <transformed text>}}` as expected. A whole-site relative-link check (0 broken, 78 pages, 1078 relative links -- unchanged baseline), marketplace/labs-site drift checks green (16+51 pages), and `python3 scripts/parity_report.py --check` green. README.md, docs/index.html, CHANGELOG.md, and docs/changelog.html updated (new ⇄ METHOD/⇄ ENCODING control copy for Repeater). Pushed as 7e7ec54 (feature, includes parity.json + docs) + fb1b5c6 (backfilling 7e7ec54's own hash into parity.json's self-referential reconciled.commit, this file's established pattern). HEAD verified == origin/Nullock via `git rev-parse` calls after each push. This cloud session cannot build the C++ side, though this batch touched no C++; will confirm the next GitHub Actions ci run is green before calling this fully closed, same honest-partial pattern as prior in-flight-CI notes in this file.

- Wave 2/0 (2026-08-17, confirm CI for 7e7ec54/fb1b5c6, then 4d3464b/0111a23): confirmed CI green on the Repeater method/encoding wave -- ci.yml run 648 (7e7ec54), run 649 (fb1b5c6), and run 650 (94ccaa1, this file's own prior progress-note commit) all conclusion=success, confirmed via the Actions API this session. Then closed a small Wave 2 batch plus one Wave 0 register-accuracy fix. Wave 2: Settings' Export HAR button (SettingsTab, `ui-v2/app.jsx`) gains an "include unredacted auth material" checkbox -- `real-data.js`'s `exportHar()` took zero arguments even though `control_server.cpp:6045-6053`'s `/api/har/export` already honored a `bodyJson.value("redact")` override, so the backend capability was UI-unreachable; `exportHar(opts)` now forwards `{redact:false}` when checked. Item #280 stays `exceeds` (native HAR round-trip already beat Burp's extension-only HAR support); only its gap text changed to name the still-real Burp-XML-format absence instead of the now-closed redact gap. Wave 0: corrected item #69's ([A21] HTTP request smuggling) stale gap text, which claimed the opt-in `Smuggling::test` battery was reachable "only via a direct POST to /api/audit/all" -- live source shows the Scans tab's "Audit run" section has had a `smuggle (opt-in, not in default sweep)` checkbox (`app.jsx:1943` `AUDIT_INCLUDES`, wired through `doAuditRun` -> `NL.actions.auditRun` -> `POST /api/audit/run` -> the same `runDeepAudit` smuggle-gated branch `/api/audit/all` uses, `control_server.cpp:6953,6991` and `:634-642`) for several waves already; no code change, state stays `present`. Both found by a research subagent cross-referencing the original Wave 0 (27 ids) and Wave 2 (76 ids) lists against the full progress-note log and live source -- confirming those two lists are otherwise now fully closed. No backend touched. One process note: the first parity.json edit pass used `json.dump(..., ensure_ascii=True)` (Python's default), which silently re-escaped every non-ASCII character across the whole 400+-item file (§, →, etc. -> `\uXXXX`) into a 670-line diff touching items far outside this batch's two intended edits; caught before push by `git show --stat` looking suspicious, `git reset --soft HEAD~1`, and redone with `ensure_ascii=False` to confirm a clean 24-line diff touching only the two intended items -- a reminder to always inspect diff size/scope on any parity.json edit made via Python, not just correctness of the target fields. Verified: bundled-babel transform of both touched files, confirmation `ui-v2/tabs.jsx` was untouched (its intentional NUL byte still exactly 1 byte at offset 77073, file not in the diff), a vm-sandboxed hook-stubbed render of `SettingsTab` proving the new checkbox constructs in the element tree with no throw, a whole-site relative-link check (0 broken, 78 pages, 1078 relative links -- unchanged baseline), marketplace/labs-site drift checks green (16+51 pages), and `python3 scripts/parity_report.py --check` green (409 features, 186 at parity = 45%, unchanged count since neither item's state flipped). CHANGELOG.md updated (new Export-HAR Added entry); README.md/docs/index.html left untouched since neither change is a newly-reachable capability (a checkbox on an already-listed export format; a docs-only gap-text correction), matching this file's "only touch what actually changed" convention. Pushed as 4d3464b (feature, includes parity.json) + 0111a23 (backfilling 4d3464b's own hash into parity.json's self-referential reconciled.commit, this file's established pattern) -- both correctly authored as Bikebrainz (a first commit attempt was caught pre-push with the wrong author identity and redone, see the ensure_ascii note above for the same catch-before-push discipline applied twice this run). HEAD verified == origin/Nullock via `git rev-parse` calls after each push. This cloud session cannot build the C++ side, though this batch touched no C++; will confirm the next GitHub Actions ci run is green before calling this fully closed, same honest-partial pattern as prior in-flight-CI notes in this file.

- Wave 4 (2026-08-18, 16512ef): Labs submit-flag/success-check batch -- second installment on task #122, after the difficulty/hints wave. Labs 01-10 each gain a GET/POST /flag endpoint that reuses the exact vulnerable code path the lab teaches and returns a per-lab NULLOCK{...} flag only once the vuln is genuinely exploited server-side: 01 XSS (unescaped script/event-handler payload actually reflected), 02 SQLi (UNION-extracted a hidden admin password never reachable through the legitimate numeric id path), 03 JWT (forged/alg-none admin token verifies), 04 IDOR (walked to a hidden id=99 record never linked from login), 05 SSRF (fetched an internal-only /internal/secret route standing in for cloud metadata), 06 GraphQL (over-fetched a hidden admin user's password_hash), 07 SSTI (Jinja2 config-leak via {{config.FLAG}}, no shell needed), 08 OAuth state (direct forged callback with no prior /oauth/start, proving the missing-state CSRF gap), 09 race condition (5 concurrent transfers push balance negative, which no single-shot request can do), 10 deserialization (pickle __reduce__ calling an in-process marker function instead of os.system, so the check endpoint itself never runs a shell command). Every endpoint was verified live in this cloud session: a throwaway venv (flask/pyjwt/requests), each app run via the Bash tool's run_in_background (plain `&` backgrounding does not survive across tool calls in this sandbox -- learned the hard way on lab 09), curled both a baseline non-exploit request (confirms solved:false) and the actual exploit request (confirms solved:true with the expected flag) for all 10 labs, including a real concurrent-request race for 09 and a real crafted pickle payload for 10. Caught and fixed a real regression before commit: labs_site.py's fallback fix-paragraph regex matches `\bfix\b` anywhere in the docstring body and then greedily swallows every subsequent non-blank line, so appending the new "Confirm success" step AFTER an existing "Real fix:" step (labs 06-10) glued the new step's text onto the rendered "the fix" box. Fixed by reordering each affected docstring so "Confirm success" comes before "Real fix" (matching labs 01-05, which had no trailing fix line to begin with) rather than touching the shared parser -- lower blast radius, verified via `python3 scripts/labs_site.py --check` plus a direct regex dump of all 10 fix-box contents pre- and post-reorder. Labs 11-50 remain untouched (still walkthrough-only); tracks/XP gamification and in-app Labs wiring remain unbuilt, called out honestly as before. No parity.json item corresponds to Labs (Nullock-native, not Burp-parity-tracked), so no state flip -- `python3 scripts/parity_report.py --check` stays green (409 features, unchanged). Verified: `ast.parse` on all 10 touched app.py files, a whole-site relative-link check (0 broken, 998 relative links), `python3 scripts/labs_site.py --check` and `python3 scripts/marketplace_site.py --check` both green, secret-scan of the diff clean. README.md's Teaching-labs line updated to name the new /flag labs; docs/index.html has no per-feature Labs card (only a nav link), left untouched per this file's "only touch what actually changed" convention. Committed as Bikebrainz (had to recover from a detached-HEAD state after `git reset --hard origin/Nullock` -- `git branch -f Nullock HEAD && git checkout Nullock` before push), pushed as 16512ef, HEAD verified == origin/Nullock via `git rev-parse`. This batch touched no C++ or ui-v2 (labs/ and docs/labs/ only, both Python/HTML), so low CI risk, but GitHub Actions ci run 655 on 16512ef was still queued at push time -- will confirm green next wave before calling this fully closed, same honest-partial pattern as prior in-flight-CI notes in this file.

- Wave 2 (2026-08-18, confirm CI for b4a892c/16512ef, then 42d243d): confirmed CI green on the labs-flags wave and its own progress-note commit -- ci.yml run 655 (16512ef) and run 656 (b4a892c) both conclusion=success, confirmed via the Actions API this session. A research subagent then re-audited the full Wave 0 (27 ids, closed in 942da87) and Wave 2 (76 ids) lists against every progress note plus live parity.json: Wave 0 fully closed, Wave 2 down to its last three -- #302, #313, #326, all `area:"repeater"`, all already `present`/`exceeds` with a real but cosmetic remaining gap, none needing a backend change. Closed a "Repeater-area polish" batch: #302 -- RepeaterTab's tab-strip rename (`ui-v2/tabs.jsx`) is now an inline double-click <input> (commit on Enter/blur, cancel on Escape) instead of a `prompt()` dialog, plus a click-to-cycle colour dot per tab from a fixed 6-colour palette (client-only state -- no `color` field exists in the index-addressed repeater-tab wire contract, so it doesn't survive reload or project save/load, called out honestly). #313 -- Comparer's diff (`ui-v2/app.jsx`) gains a `comparerMarkModified()` pure helper that pairs an adjacent del+ins (or ins+del) run into a distinct amber "modified" style instead of separate red/green delete+insert, matching Burp's treatment of in-place value changes; a positional greedy scan, not LCS-based, called out as the honest remaining limit. #326 -- DecoderTab's op-button row (`ui-v2/app.jsx`) gains the four protocol codecs (graphql-parse/grpc-frame/cbor-decode/saml-decode) that previously lived only in the Proxy/Repeater CodecBar, routed to the same pure `runCodec()` (`ui-v2/proxy.jsx`) instead of the backend `/api/transcode` round-trip every other op uses -- CBOR's no-tag-handling limitation is unchanged and still named honestly. No backend touched; no parity state flips (all three items were already `present`/`exceeds`), only gap/why/evidence text and a `reconciled` block per item -- 409 features, 186 at parity = 45%, unchanged count. Verified: bundled-babel transform of both touched files, confirmation `ui-v2/tabs.jsx`'s intentional NUL byte is still exactly 1 byte (now at line 1617, inside the same fuzz-payload wordlist literal noted in the prior wave -- offset shifts every time; this file's "near line 500" claim is stale and should be read as "somewhere inside the A18/fuzz wordlist" going forward), an 8-check Node functional test of `comparerMarkModified` (pairing both orderings, lone del/ins staying unmodified, a 4-segment del-del-ins-ins run pairing only the adjacent middle pair, an empty array, non-mutation of the input objects, common segments never marked) run against the real babel-transformed module in a vm sandbox, a 4-check functional test confirming `runCodec`'s four protocol cases still produce correct output when invoked directly (graphql operation-type extraction, grpc-frame no-throw on malformed input, cbor text-string decode, saml b64-xml decode) with `atob`/`btoa`/`TextDecoder` polyfilled into the sandbox, and a vm-sandboxed hook-stubbed render smoke test (React hooks stubbed for a single pass, `ReactDOM.createRoot` and `document.getElementById` stubbed since `app.jsx` calls them at module load) proving `RepeaterTab` (empty and populated tab-strip states), `ComparerTab`, and `DecoderTab` all construct their element tree with no throw. A whole-site relative-link check (0 broken, 1078 relative links -- unchanged baseline) and `marketplace_site.py --check` / `labs_site.py --check` both green. README.md's Decoders line updated to name the four protocol codecs now reachable from the Decoder tab itself; docs/index.html has no dedicated Decoder feature card to update, left untouched per this file's "only touch what actually changed" convention. Committed as Bikebrainz, pushed as 42d243d (feature, includes parity.json with `reconciled.commit` temporarily "PENDING") then a second commit backfilling those three fields to 42d243d's own hash (this file's established self-referential-commit pattern), both `--no-gpg-sign`, secret-scan of both diffs clean. HEAD verified == origin/Nullock via `git rev-parse` after the first push; will do the same after the backfill push and confirm the next CI run green before calling this fully closed.

- Wave 3 (2026-08-17/18, 64441d6 / 631088c / 9ebecdb, backfilled here honestly -- a prior (non-cloud) session closed these but skipped this file's required append step): three real backend batches landed since the last note. 64441d6 -- Issue triage: `/api/findings/mark` (`{id,falsePositive}`) and `/api/findings/suppress-kind` (`{kind,suppress}`) close the all-or-nothing findings-clearing gap (task #B5). 631088c -- Content discovery: `ContentDiscovery::Request` gains `concurrency` (1-64, default 10) and `throttleMs` (0-60000), fanning the wordlist brute-force across a bounded thread pool instead of one request at a time. 9ebecdb -- Advanced scope control: a new `scope_logic.cpp/.hpp` composed include/exclude rule engine over protocol/host-regex/port-range/file-regex (deny-wins, compose-not-replace), persisted via `project_store`, wired into `proxy_server`; each commit's own message records a local Windows build + `probe_smoke` pass, which this cloud session cannot reproduce but can and did verify a different way: `ci.yml` run 659 (64441d6), 661 (631088c), and 664 (9ebecdb) -- plus their docs-reconcile commits 660/662/665 -- all `conclusion=success`, confirmed via the GitHub Actions API this session. All three already had matching parity.json state flips + reconciled blocks in their own docs-reconcile commits (02f5ccf, bf3c4d2, ea678ea) and `python3 scripts/parity_report.py --check` is green at current HEAD (409 features). No new code from this session; this is a pure documentation-honesty backfill so the plan's progress log stops undercounting closed batches. FOLLOW-ON explicitly noted by 9ebecdb's own commit message and left open: the advanced-scope editor UI (ui-v2) and upgrading the ~10 tool gates from host-only to full-URL `blocksUrl` matching -- both real remaining gaps, not claimed closed here.

- Wave 4 (2026-08-18, 2138209): Labs submit-flag/success-check batch -- third installment on task #122, continuing from the labs 01-10 wave. Labs 11-20 each gain a `/flag` endpoint reusing the exact vulnerable code path the lab teaches, returning a per-lab `NULLOCK{...}` flag only once the vuln is genuinely exploited server-side: 11 prototype pollution (class-attribute pollution actually flips a global `is_admin`, checked via state), 12 broken access control (a delete goes through with no admin session behind it, tracked via a `DELETED_WITHOUT_ADMIN` flag set only on the unauthenticated path), 13 command injection (shell-evaluated `$((6*7))` must produce "42" while the raw `host` param never contains the literal digits, ruling out a typed-in bypass), 14 path traversal (the resolved path must both escape `BASE` and match a marker planted in a sibling `secret.txt` never linked from any page), 15 open redirect (the resolved `Location` header's netloc must genuinely leave the app's own origin), 16 CORS misconfig (a real cross-origin `Origin` header must come back reflected in `Access-Control-Allow-Origin` with credentials allowed -- same-origin or missing Origin explicitly rejected), 17 NoSQL injection (only a dict-shaped `password[$ne]=...` operator bypass solves it; the correct plain-string password does not), 18 mass assignment (both privileged fields, `is_admin` and `role`, must have flipped -- the allow-listed `name`/`email` fields cannot do this), 19 CSRF (requires an active session plus a forged cross-site `Origin`/`Referer`, simulating the auto-submitting attacker page; same-origin or unauthenticated requests are rejected), 20 user enumeration (a hidden service account, `svcbot-4f2a`, never listed or linked anywhere, reachable only by walking the same "Unknown username" vs "Wrong password" oracle `/login` exposes). Every endpoint was verified live in a throwaway venv (flask), each app backgrounded via the Bash tool's `&` + `$!` PID pattern (not job-control `%1`, which does not reliably survive `pkill` calls earlier in the same script): curled a baseline non-exploit request (confirms `solved:false`) and the documented exploit request (confirms `solved:true` with the expected flag) for all 10 labs, including a same-vs-cross-origin CORS/CSRF contrast and a legit-vs-injected NoSQLi contrast to rule out trivial false positives. No gluing regression from the prior wave's fix-box bug: only labs 11/12 had "Real fix:" as the last numbered `In Nullock:` step, so the new "Confirm success" step was inserted BEFORE it (matching the established labs 01-05 pattern) rather than after; labs 13-20 have their fix as a separate trailing paragraph outside the numbered list, so a new step could safely append at the end -- confirmed via a direct regex dump of all 10 rendered fix-boxes post-build (no bleed-through). Labs 21-50 remain untouched (still walkthrough-only). No parity.json item corresponds to Labs (Nullock-native, not Burp-parity-tracked), so no state flip -- `python3 scripts/parity_report.py --check` stays green (409 features, unchanged). Verified: `ast.parse` on all 10 touched app.py files, `python3 scripts/labs_site.py --check` and `python3 scripts/marketplace_site.py --check` both green (51 + 16 pages), a whole-site relative-link check (0 broken, 1078 relative links -- unchanged baseline), secret-scan of the diff clean, runtime-generated fixtures (`labs/14-path-traversal/public/`, `secret.txt`) deleted before commit since they're regenerated on `python app.py` like the pre-existing `note.txt` pattern. README.md's Teaching-labs line updated (10 -> 20 labs with flags); docs/index.html has no per-feature Labs card (only a nav link), left untouched per this file's convention. Committed as Bikebrainz, `--no-gpg-sign`, pushed as 2138209, HEAD verified == origin/Nullock via `git rev-parse`. This batch touched no C++ or ui-v2 (labs/ and docs/labs/ only), so low CI risk; will confirm the next GitHub Actions ci run is green next wave before calling this fully closed, same honest-partial pattern as prior in-flight-CI notes in this file.

- Wave 2/0 (2026-08-18, 6a82458/19e2b3f, a204e79/3b0c160, b8503b0/cb7097b, 4369973/4a67f96, 8bb4d7d/1ecbc7b, d95a390/54bba2c, backfilled here honestly -- six real backend batches landed across prior non-cloud sessions since the last progress note, each already carrying its own parity.json state flip + reconciled block in a paired docs-reconcile commit, but every one of them skipped this file's required append step). In commit order: **6a82458** -- Issue lifecycle gains per-finding SEVERITY OVERRIDE (`/api/findings/set-severity {id,severity}`) and SOFT-DELETE (`/api/findings/delete {id,deleted}`) on top of the earlier false-positive marking (64441d6), both persisted in the project file and applied at display time via `FindingTriageLogic::effectiveSeverity` (unit-tested + mutation-proven); item stays `partial` on purpose -- the backend/API triage lifecycle is complete but the Issues-tab UI buttons for it don't exist yet in ui-v2. **a204e79 + b8503b0** -- session-handling rules and the Sequencer cookie jar now both persist across restart in `project.json` (`sessionRules` / `cookieJar` via new `sessionRulesToJson/FromJson` and `SessionManager::exportJson/importJson` serializers, the latter dropping any already-expired cookie on restore so a stale token can't be resurrected); flips "Cookie jar and session rules persist ... across restarts" `partial -> present`. **4369973** -- intercepted-request editing now auto-recomputes Content-Length before forwarding (`InterceptLogic::recomputeContentLength`, Burp-default-ON toggle persisted per project), deliberately forwarding chunked/duplicate-CL requests verbatim so the fix can't neuter a CL.TE/dup-CL smuggling probe the operator is deliberately sending; response-side auto-CL stays out of scope (a HEAD/204/304's legitimate zero-body CL can't be told apart from a truncation at the intercept layer) so the item stays `partial`. **8bb4d7d** -- Repeater's `send()` now times the real network round-trip (`QElapsedTimer`, captured before gzip-decode) and records response byte length per send/history-entry/snapshot; backend + snapshot half done, item stays `partial` pending the ui-v2 render-pane binding (a concurrent frontend lane, not yet landed). **d95a390** -- invalid upstream TLS certs (self-signed/expired/hostname-mismatch) can now be intercepted, but only for hosts on a new per-host, per-project, fail-closed-empty allow-list (`POST /api/proxy/accept-invalid-hosts/{add,remove}`) gated by a benign-error filter that still hard-fails closed on a blacklisted/revoked/no-peer-cert leg, plus a root-cause fix so a cert-level failure no longer permanently blocklists the host; flips "Handling invalid upstream TLS certificates" `absent -> partial` (per-row unverified-leg annotation and fingerprint pinning/TOFU are the named remainder). Each commit's own message records a local Windows build + `probe_smoke` pass this cloud session cannot reproduce, but every one of the twelve commits above (six feature + six paired docs-reconcile) plus their own progress-note gaps were confirmed via the GitHub Actions API this session: ci.yml runs 664/665 (advanced-scope-control, already noted), 666-676 all `conclusion=success` covering 2138209 (labs 11-20) through 54bba2c (this backfill's own prior HEAD) with no red run in the chain. `python3 scripts/parity_report.py --check` is green at current HEAD (409 features, 187 at parity = 46%, up from 186/45% across these six flips -- five of the six items stay honestly `partial`, only the cookie-jar/session-rules persistence item reached `present`). No new code from this session; this is a pure documentation-honesty backfill so the plan's progress log stops undercounting closed batches, same pattern and same root cause (a feature commit's own docs-reconcile commit updates parity.json but a separate, easy-to-skip step is needed to append here) as the Wave 3 backfill note above it. README.md and docs/index.html were correctly left untouched by all six original commits -- every one is a backend improvement to an already-listed, already-partial/present feature, not a newly-reachable capability, matching this file's "only touch what actually changed" convention.

- Wave 2 (2026-08-18, 9a16881/9e878ed): closed #305's ui-v2 remainder flagged in the Wave 2/0 backfill note above -- `RepeaterTab` (`ui-v2/tabs.jsx`) now renders response byte length + elapsed ms next to STATUS via the existing `fmtSize`/`fmtMs` helpers already loaded from `proxy.jsx` (shared global scope, script order in `Nullock.html`), reading `rep.elapsedMs`/`rep.responseBytes` straight off the snapshot (`NL.repeater = snap.repeater` verbatim, no field allowlist to touch); gated on `elapsedMs > -1` so the pre-send sentinel renders nothing instead of a bogus "-1 ms"/"-0.0 KB" (neither helper was designed for negative input). The history-nav arrows' hover tooltip gained the same two fields per navigated entry (`histEntries[histPos].elapsedMs/.responseBytes`), which the wire contract already carried per-history-entry but the UI never read. Flips "Repeater: response metadata display (status, byte length, elapsed milliseconds)" `partial -> present` -- `python3 scripts/parity_report.py --check` green (409 features, 188 at parity = 46%, up from 187/46%). No backend touched. Verified: bundled-babel transform of all six `ui-v2` script files, confirmation `tabs.jsx`'s intentional single NUL byte is still exactly 1 occurrence (offset drifted from 58081 to 80301 purely from upstream file growth since that check was last recorded, not from this diff), and a Node functional test of the show/hide gate + formatter across 4 cases (before-any-send sentinel, the real-data.js fallback object with undefined fields, a normal 615ms/1234B send, a degenerate 0ms/0B send) all matched expectation. README.md/docs/index.html correctly left untouched -- this is a backend-already-shipped feature gaining its missing UI half, not a newly-reachable capability, same convention as the six-item backfill above. Committed as Bikebrainz, `--no-gpg-sign`, pushed as 9a16881 (feature) then 9e878ed (docs-reconcile), both secret-scan clean, HEAD verified == origin/Nullock via `git rev-parse` after each push. GitHub Actions ci run 678 (9a16881) / 679 (9e878ed) were still `in_progress` (Windows Qt build is the long pole) when this session's time budget ran out on polling -- this batch touched no C++, so low CI risk, but will confirm green next wave before calling it fully closed, same honest-partial pattern as prior in-flight-CI notes in this file.

- Wave 2 (2026-08-18, confirm CI for 9a16881/9e878ed, then 75c419d/1791913): confirmed CI green on the Repeater response-metadata wave -- ci.yml run 678 (9a16881), run 679 (9e878ed), and run 680 (18eacf6, this file's own prior progress-note commit) all `conclusion=success`; pages.yml runs 227-228 also green, confirmed via the Actions API this session. Then closed a small Wave 2 GUI-reachability batch: item 293's REQUEST-side auto-Content-Length recompute (backend + a persisted toggle landed in the 4369973 wave, `/api/intercept/autocl`) had no GUI control to flip it -- `InterceptTab`'s toggle bar (`ui-v2/tabs.jsx`) gains an UPDATE CONTENT-LENGTH checkbox next to the existing REQUESTS/RESPONSES power toggles, wired through a new `interceptAutoContentLength` state slice (`ui-v2/app.jsx`: initial state, `nl-snapshot` merge from `NL.interceptAutoContentLength`, a new `intercept-autocl-toggle` reducer case) and a new `NL.actions.interceptSetAutoContentLength` (`ui-v2/real-data.js`) posting to the pre-existing endpoint. Reconciles parity.json item 293: `audited_state` `absent -> partial` (state stays `partial`, not flipped to `present` -- Burp's own feature covers both request AND response, and the response-side gap is unchanged and named honestly in the updated `gap` text); `python3 scripts/parity_report.py --check` green (409 features, 188 at parity = 46%, unchanged count since the top-level `state` didn't flip). No backend touched. Verified: bundled-babel transform of all three touched files, confirmation `ui-v2/tabs.jsx`'s intentional NUL byte is still exactly 1 occurrence (offset shifted from 80301 to 80897 by the inserted lines, as expected), a vm-sandboxed hook-stubbed render smoke test (proxy.jsx -> tabs.jsx load order, `window` aliased to the sandbox's own global object so `real-data.js`'s bare `NL` reference resolves like it does in a real browser) proving `InterceptTab` constructs its element tree with no throw across populated/empty-queue and on/off `interceptAutoContentLength` states, plus a direct invocation of the new checkbox's `onChange` handler confirming it dispatches `{type:"intercept-autocl-toggle"}`, a whole-site relative-link check (0 broken, 78 pages, 1078 relative links -- unchanged baseline), marketplace/labs-site drift checks green (16+51 pages), and `python3 scripts/parity_report.py --check` green. One process note: the parity.json edit was done via targeted raw-text substring replacement rather than a full `json.dump` rewrite, after a first `json.dump(..., ensure_ascii=False)` attempt round-tripped the file's mixed already-escaped/unescaped-Unicode fields into a 324-line diff touching content far outside this batch's one item -- caught before commit by `git diff --stat` looking suspicious, `git checkout --` to discard, and redone as a byte-exact string swap producing a clean 8-line diff (same discipline this file has flagged before for `ensure_ascii` footguns on this file). CHANGELOG.md and docs/changelog.html's existing Content-Length changelog entries were also tightened to say the toggle lives in the Intercept tab itself rather than implying an API-only "switch"; README.md/docs/index.html correctly left untouched since this isn't a newly-reachable capability class (a checkbox on an already-documented, already-`partial` feature), matching this file's "only touch what actually changed" convention. Committed as Bikebrainz, `--no-gpg-sign`, pushed as 75c419d (feature, includes parity.json + docs with `reconciled.commit` temporarily "PENDING") then 1791913 (backfilling 75c419d's own hash into parity.json's self-referential `reconciled.commit`, this file's established pattern), both secret-scan clean. HEAD verified == origin/Nullock via `git rev-parse` after each push. This cloud session cannot build the C++ side, though this batch touched no C++; both pushed runs (run_numbers 681-682) were still `in_progress` when this session ended verification -- will confirm the ci.yml run is green next wave before calling this fully closed, same honest-partial pattern as prior in-flight-CI notes in this file.

- Wave 2 (2026-08-18, confirm CI for 75c419d/1791913, then 8283e6d/5ec224a): confirmed CI green on the Intercept auto-CL wave -- ci.yml runs 681 (75c419d), 682 (1791913), 683 (e8beb92) all `conclusion=success`. Then closed #357 (Decoder: Gzip encode/decode, absent -> present): DecoderTab gains gzip-encode/gzip-decode ops wired entirely client-side via the browser's Compression Streams API (CompressionStream/DecompressionStream, base64 in/out, hex-dump fallback for non-UTF8 decompressed bytes) -- no backend change, since transcode.cpp has no gzip op and none was needed. `python3 scripts/parity_report.py --check` green (409 features, 189 at parity = 46%, up from 188/46%). Verified: bundled-babel transform of ui-v2/app.jsx, confirmation ui-v2/tabs.jsx's intentional NUL byte was untouched (still exactly 1 occurrence, file not in the diff), an 8-check Node functional test of the extracted gzipEncodeText/gzipDecodeText against the real babel-transformed source in a vm sandbox using Node's native CompressionStream/DecompressionStream (ascii/unicode/empty round-trip, base64-shaped + gzip-magic-byte output, invalid-base64 rejected, valid-base64-non-gzip rejected, non-UTF8 payload falls back to hex dump) -- this pass caught and fixed a real bug (unhandled promise rejections from writer.write()/writer.close() on stream error, crashing the process; fixed by attaching no-op .catch() to both, the actual error still surfaces via the arrayBuffer() read), a vm-sandboxed hook-stubbed render of DecoderTab proving no runtime throw, a whole-site relative-link check (0 broken, 78 pages, 1078 relative links), marketplace/labs-site drift checks green (16+51 pages), and `python3 scripts/parity_report.py --check` green. README.md updated (Decoders line mentions gzip); docs/index.html has no dedicated Decoder feature card to update. Committed as Bikebrainz, `--no-gpg-sign`, pushed as 8283e6d (feature) then 5ec224a (commit-hash backfill), both secret-scan clean. HEAD verified == origin/Nullock via `git rev-parse` after push (5ec224a).
- Addendum (2026-08-18): this wave's own pushed runs (684 for 5ec224a, 685 for 3c04a82) were still `in_progress` (Windows Qt build is the long pole) when this session's verification window ended -- this batch touched no C++, so low CI risk, but will confirm green next wave before calling it fully closed, same honest-partial pattern as prior in-flight-CI notes in this file.

- Wave 2 (2026-08-18, confirm CI for 8283e6d/5ec224a/3c04a82, then da90e67/281b74d): confirmed CI green through run 686 (b28b9b3) on both ci.yml and docs-site (pages.yml) -- the prior wave's gzip-decoder + docs-note pushes are fully closed, no CI risk outstanding. Then closed the Manual Load half of #175 (Sequencer: Copy tokens / Save tokens to file from a capture, absent -> partial): SequencerTab (ui-v2/app.jsx) gains COPY TOKENS (clipboard write) and SAVE TOKENS (client-side Blob download to nullock-sequencer-tokens.txt) buttons next to the existing LOAD FILE / PASTE FROM CLIPBOARD / CLEAR row, reusing the state that already held the manually-loaded corpus and the CodecOverlay download pattern from proxy.jsx. No backend touched. Left honestly partial: Burp's Live Capture (a growing token list harvested by repeatedly issuing a request) has no Nullock equivalent at all -- no request-issuing engine exists -- so there is no capture store for that half of the feature; only the Manual Load corpus gained copy/save. `python3 scripts/parity_report.py --check` green (409 features, 189 at parity = 46%, unchanged since the flip was absent->partial not ->present). Verified: bundled-babel transform of ui-v2/app.jsx, confirmation ui-v2/tabs.jsx's intentional NUL byte was untouched (still exactly 1 occurrence at offset 80897, file not in the diff), an 4-check Node functional test of the token split/join/save-content logic (LF and CRLF normalization, trailing-newline-only-when-nonempty), labs-site and marketplace-site drift checks both green (51+16 pages, no site files touched this batch so no relative-link check needed), and `python3 scripts/parity_report.py --check` green. README.md/docs/index.html correctly left untouched -- this is a minor enhancement to an already-documented, already-existing Sequencer tab, not a newly-reachable capability class (same "only touch what actually changed" precedent as the Intercept auto-CL wave). Committed as Bikebrainz, `--no-gpg-sign`, pushed as da90e67 (feature) then 281b74d (commit-hash backfill), both secret-scan clean. HEAD verified == origin/Nullock via `git rev-parse` after push.
- Addendum (2026-08-18): this wave's own pushed runs (687 for 281b74d, 688 for 4062787) were still `in_progress` when this session's verification window ended -- this batch touched no C++, so low CI risk, will confirm green next wave, same honest-partial pattern as prior in-flight-CI notes. Also flagging an unrelated CI infra anomaly noticed while checking: run 684 (5ec224a, a wave-before-last commit) has its Linux build job stuck `in_progress` at the "install Qt" step for 3+ hours while every other job in that same run (Windows build, marketplace check, labs syntax, CLI syntax, fuzz corpus) completed successfully within minutes, and the very next run (685, 3c04a82) completed cleanly in ~30s -- looks like a hung/stalled GitHub-hosted runner rather than a code or config regression, since nothing in that diff touches CI config and no other run before or after shows the same stall. Not actionable from this cloud session (no rerun/cancel tool available); worth a manual `gh run cancel`/re-run of 32171670123 if it's still stuck next time a human looks, but it does not block or reflect on any closed item's verification.
- Wave 2 (2026-08-19, confirm CI for 281b74d/4062787): confirmed CI green through run 689 (eeb7189) on ci.yml (687/688/689 all `conclusion=success`) and run 232 (281b74d) on pages.yml -- the prior wave's Sequencer copy/save-tokens batch is fully closed, no CI risk outstanding. Run 684 (5ec224a)'s stuck Linux build job flagged last wave is unchanged (still `in_progress`), still not actionable from this cloud session (no rerun/cancel tool), still doesn't block or reflect on any closed item.
- Wave 0 (2026-08-19): register-accuracy fix -- "Site map filter -- by MIME type/status/extension/search/annotation" flipped `present -> partial`. Its own `gap` text already admitted the annotation criterion enumerated in the feature's own name ("by annotation (commented/highlighted)") was missing -- a compound feature naming five criteria can't be `present` when one is confirmed absent by its own text. Confirmed via a fresh grep across `Src/BackEnd`, `Src/Core`, and `ui-v2/*.jsx`: no comment/highlight field exists anywhere in the backend history schema, the wire JSON, or the frontend filter bar / row context menu -- closing it for real needs a backend schema+API change (new SQLite columns + a set-endpoint), not something this cloud session can build/verify, so it stays a register-only correction, not a build. No code touched. `python3 scripts/parity_report.py --check` green after regenerating (409 features, 188 at parity = 46%, down from 189/46% -- an honest correction, not a regression).
- Wave 4 (2026-08-19, task #122): Labs submit-flag/success-check batch -- fourth installment, continuing from labs 01-20. Labs 21-30 each gain a `/flag` endpoint reusing the exact vulnerable code path the lab teaches, solvable only by genuine server-side exploitation: 21 session fixation (a sid known BEFORE login must still be authenticated after, proving no rotation), 22 file upload (the stored file's guessed content-type must be browser-executable html/svg AND still carry a `<script` payload, proving stored XSS would actually run, not just that a file exists), 23 host-header poisoning (the last `/forgot` link's Host must match the submitted value AND differ from the app's real canonical host), 24 clickjacking (added a `/decoy` framing page + a `/dashboard/delete` POST action; solved only once a delete request lands with a Referer proving it came from inside the decoy's iframe, not a direct click), 25 secret exposure (both the leaked AWS key and the leaked `SECRET_KEY` value must be submitted, proving both `/static/app.js` and `/.env` were actually read), 26 blind SQLi (the exact admin password, never visible through the legitimate path, must be submitted), 27 stored XSS (a persisted comment must actually contain an unescaped `<script` tag -- an escaped fix would never match), 28 business-logic price (the last checkout's total must have actually cleared far below the real catalog price, proving tampered price/qty was accepted server-side), 29 verbose errors (a fragment of the real leaked absolute path, unique to this app, must be submitted), 30 rate-limit bypass (a login must have actually succeeded after more than 5 real attempts from the same underlying connection, proving the X-Forwarded-For-keyed limit was bypassed, not just that many requests were sent). Every endpoint was verified live in a throwaway venv (flask), each app backgrounded via the Bash tool's `&` + `$!` PID pattern: curled both a baseline non-exploit request (`solved:false`) and the documented exploit request (`solved:true` with the expected flag) for all 10 labs, plus explicit negative-path checks (traversal attempt on 22, canonical-host request on 23, direct unframed click on 24, same-XFF-no-rotation on 30) confirming each check can't be trivially satisfied. Caught and fixed a real bug before commit: lab 27's first draft of its "Confirm success" step used the word "fix" mid-sentence, which `labs_site.py`'s fallback fix-paragraph regex (unanchored, matches "fix" followed by any whitespace anywhere in the docstring body) matched BEFORE reaching the real `Fix:` paragraph, truncating the rendered walkthrough step and misplacing the fix box's start -- caught by inspecting the generated HTML's fix-box content for all 10 labs post-build, fixed by rephrasing to avoid the trigger word, then re-verified with a full docstring scan across all 10 files for any line containing or ending in fix/mitigation/remediation as a bare word. Lab 25's diff was deliberately shaped so the new `FLAG`/`/flag` code sits far from the pre-existing `FAKE_AWS = "AKIA" + "IOSFODNN7EXAMPLE"` line (AWS's own published example key, already in the repo, untouched) -- confirmed the diff for that file contains zero occurrences of "AKIA" before committing. Labs 31-50 remain untouched (still walkthrough-only). No parity.json item corresponds to Labs (Nullock-native, not Burp-parity-tracked), so no state flip beyond the separate register fix above -- `python3 scripts/parity_report.py --check` stays green. Verified: `ast.parse` on all 10 touched app.py files, a whole-site relative-link check (0 broken, 78 pages, 1078 relative links -- unchanged baseline), `python3 scripts/labs_site.py --check` and `python3 scripts/marketplace_site.py --check` both green (51 + 16 pages), secret-scan of the full diff clean (grep for AKIA/ghp_/sk_live_/AIza/glpat- patterns, zero matches), no runtime-generated fixtures left behind (`git status` clean pre-commit). README.md's Teaching-labs line updated (20 -> 30 labs with flags); docs/index.html has no per-feature Labs card (only a nav link), left untouched per this file's "only touch what actually changed" convention. Committed as Bikebrainz, `--no-gpg-sign`.
- Addendum (2026-08-19): pushing this wave's two commits (originally 6dcc404/90864e2) hit a real non-fast-forward rejection -- a concurrent session had landed d9d033c (Intruder recursive-grep payload type) + 870200a (its docs-reconcile) on origin/Nullock while this wave was in progress. Rebased cleanly onto origin/Nullock: parity.json's own JSON content merged with zero conflicts (the two waves touched different feature entries), only the generated docs/roadmap/index.html conflicted, resolved by regenerating it fresh via `python scripts/parity_report.py` rather than hand-merging build output, re-verified `--check` green both times before continuing the rebase. Final commits pushed as 382abb7 (feature, rebased from 6dcc404) and 721a2e1 (commit-hash backfill, rebased from 90864e2). HEAD verified == origin/Nullock via two `git rev-parse` calls after push. Also noting: the previously-flagged stuck Linux build job on run 684 (5ec224a) has since resolved to `conclusion=cancelled` (no longer stuck in_progress) -- not this wave's doing, but confirms it wasn't a lasting regression. This wave's own pushed run (691, 721a2e1) was still `in_progress` when this session's verification window ended -- this batch touched no C++ (labs/ Python + docs/ + README.md only), so low CI risk, but will confirm green next wave before calling it fully closed, same honest-partial pattern as prior in-flight-CI notes in this file.

- Wave 4 (2026-08-19, 63aa19a): confirmed the prior wave's outstanding CI risk resolved -- GitHub Actions ci run green on both 721a2e1 (run 691, run 31379...) and 8ab012d (run 693) via the Actions API before starting new work, closing out that honest-partial note. Then, fifth installment on task #122, continuing from labs 21-30: labs 31-40 each gain a `/flag` endpoint reusing the exact vulnerable code path the lab teaches -- 31 directory listing (secret file fetched only after discovery via the index, not blind-guessed), 32 CSV injection (an unescaped formula-leading message actually reached /export's CSV output), 33 weak reset token (admin's token computed offline from the public username, never revealed via /forgot -- a harder bar than the walkthrough's own victim example), 34 2FA bypass (/dashboard reached while session mfa was still false), 35 reset token reuse (the same token accepted by /reset twice), 36 HTTP parameter pollution (a transfer cleared the <=100 check while debiting more), 37 web cache deception (the private page served under a static-looking suffixed path), 38 insecure cookie flags (the raw Set-Cookie value read off the wire, proving HttpOnly's absence), 39 missing security headers (findings that genuinely fire over plain http named -- submitting hsts-missing, which only fires over https, fails the check), 40 SSRF metadata (the internal secret retrieved via the server's own /fetch pivot, not a direct client request to /internal). Every endpoint verified live in a throwaway venv (flask/requests): curled a baseline non-exploit request (solved:false) and the documented exploit request (solved:true) for all 10 labs, plus explicit negative-path checks (direct-fetch-without-listing on 31, formula-message-without-export on 32, forgot-then-reset on 33 vs. never-forgot on the confirm step, non-static-suffix on 37, direct-client-hit-on-/internal on 40) confirming each check can't be trivially satisfied. Also caught and fixed a real pre-existing bug (not introduced this wave, present since lab 37's authoring in the 2026-08-16 difficulty/hints wave): `scripts/labs_site.py`'s fallback fix-paragraph regex lacked a leading `\b` before the "Fix" alternative, so it false-matched the tail of unrelated words like "suffix" ("...ffix" already sits inside one continuous word token, so `Fix\b` alone finds a boundary right after it) before ever reaching the real "Fix:" paragraph -- silently truncating lab 37's rendered fix box. Fixed by anchoring `\b` before the whole alternation; verified via a full-corpus regex scan of all 50 lab docstrings for the same false-match pattern (the only other suffix/prefix hits are inside already-correct Fix: lines) and a before/after HTML diff proving lab 12's unrelated (and already-correct) fix-box text is byte-identical, so the fix has zero blast radius beyond lab 37. Labs 41-50 remain untouched (still walkthrough-only). No parity.json item corresponds to Labs (Nullock-native, not Burp-parity-tracked), so no state flip -- `python3 scripts/parity_report.py --check` stays green (409 features, unchanged). Verified: `ast.parse` on all 10 touched app.py files, a whole-site relative-link check (0 broken, 78 pages, 1078 relative links -- unchanged baseline), `python3 scripts/labs_site.py --check` and `python3 scripts/marketplace_site.py --check` both green (51 + 16 pages), secret-scan of the staged diff clean, the runtime-generated `labs/31-directory-listing/docroot/` fixture deleted before commit (regenerated on `python app.py`, same pattern as prior waves' cleanup). README.md's Teaching-labs line updated (30 -> 40 labs with flags); docs/index.html has no per-feature Labs card (only a nav link), left untouched per this file's convention. Committed as Bikebrainz, `--no-gpg-sign`, pushed as 63aa19a, HEAD verified == origin/Nullock via two `git rev-parse` calls, no divergence hit on push (clean fast-forward). This batch touched no C++ or ui-v2 (labs/, docs/labs/, scripts/labs_site.py, README.md only), so low CI risk; will confirm the next GitHub Actions ci run is green next wave before calling this fully closed, same honest-partial pattern as prior in-flight-CI notes in this file.

- Wave 4 (2026-08-19, 432e249, task #122): sixth and final installment on the labs submit-flag work -- labs 41-50 each gain a `/flag` endpoint, completing flag coverage for all 50 labs. 41 OAuth redirect_uri (an auth code actually delivered to a redirect_uri outside the client's registered `https://app.example/` origin, not just that the legitimate redirect was followed), 42 credentials in URL (the password proven to leak via a third-party Referer header, not just a successful GET-login), 43 XXE (a SYSTEM entity resolves a real server-local secret file, marker verified in the response), 44 CRLF injection (the injected `X-Pwned: proof` line lands as a genuine second response header via the stdlib http.server's unsanitized `send_header`, not just that CR/LF chars were sent), 45 dangerous HTTP methods (an unauthenticated PUT/PATCH/DELETE actually changes stored content -- upgraded from the original OPTIONS-only-advertises-danger stub to a genuinely exploitable unauthenticated write, since the prior version advertised methods it never implemented), 46 verb tampering (a non-GET request actually reaches the "protected" admin handler), 47 cache poisoning (an attacker-controlled, non-default X-Forwarded-Host actually reflects into a still-cacheable response), 48 sensitive file exposure (at least two distinct leftover files fetched, not one lucky guess), 49 robots disclosure (the unlinked page reached only after actually reading robots.txt or sitemap.xml first, not blind-guessed -- required adding real content behind the previously-decorative `/secret-report` path), 50 predictable session token (an unissued, merely-predicted next-counter session id accepted by `/whoami`, not the solver's own issued session). Every endpoint verified live in a throwaway venv (flask/requests), each app backgrounded via the Bash tool's `&`/`$!` pattern: curled a baseline/negative-path request (solved:false) and the documented exploit request (solved:true) for all 10 labs, confirming each check can't be trivially satisfied (blind-guess-before-recon on 49, own-issued-session on 50, single-file-fetch on 48, etc.). No parity.json item corresponds to Labs (Nullock-native, not Burp-parity-tracked), so no state flip -- `python3 scripts/parity_report.py --check` stays green (409 features, unchanged). Verified: `ast.parse` on all 10 touched app.py files, a whole-site relative-link check (0 broken, 78 pages, 1078 relative links -- unchanged baseline), `python3 scripts/labs_site.py --check` and `python3 scripts/marketplace_site.py --check` both green (51 + 16 pages), secret-scan of the staged diff clean, the runtime-generated `labs/43-xxe/secret.txt` fixture deleted before commit (regenerated on `python app.py`). README.md's Teaching-labs line updated (40 -> 50 labs with flags); CHANGELOG.md and docs/changelog.html gain an entry for this wave, which also fixed a stale note left by the 2026-08-16 difficulty/hints wave's own CHANGELOG.md entry that still claimed submit-flag/success-check "remain open" after it had actually landed across three separate waves; docs/index.html has no per-feature Labs card (only a nav link), left untouched per this file's convention. Committed as Bikebrainz, `--no-gpg-sign`, pushed as 432e249, HEAD verified == origin/Nullock via two `git rev-parse` calls (no divergence). This batch touched no C++ or ui-v2 (labs/, docs/, README.md, CHANGELOG.md only). GitHub Actions confirmed green on 432e249 before this note was written: ci run 32224088825, all 6 jobs (marketplace.json schema check, fuzz-corpus replay, labs syntax check, Windows build, Linux build, CLI syntax) `conclusion=success`; docs-site (pages.yml) run 32224088811 also `conclusion=success` -- fully verified, no outstanding CI risk from this wave.

- Wave 4 (2026-08-19, 71e2dc6, task #122): closed the "wire into ... in-app" leg -- the last open piece of task #122, since docs/labs already had the full catalog/hints/walkthrough/fix and all 50 labs already had /flag checks, but nothing in the desktop GUI surfaced any of it. scripts/labs_site.py's `load()` (already the single source of truth for docs/labs) now also emits ui-v2/labs-data.js (`window.NULLOCK_LABS` / `window.NULLOCK_LABS_XP`, plain-globals since ui-v2 has no bundler/ES-modules -- same pattern as real-data.js) so the in-app tab and the static site can never drift from each other; `--check` extended to cover it (confirmed stale-detection works and the regenerated file matches). A new LABS tab (ui-v2/app.jsx) lists all 50 labs with category/difficulty chip filters + free-text search, a detail pane rendering objective/three-progressive-hints/walkthrough-steps/fix, and a "SEND GET /flag TO REPEATER" button that reuses the existing, already-proven `send-to-repeater-raw` reducer case (the same one Intercept's pivot buttons use) to prefill Repeater with a live flag check -- no new backend endpoint, no CORS issue, since the request goes out through Nullock's own send path like every other Repeater send. Solved-lab tracking + per-category XP/tracks gamification (Easy/Medium/Hard -> 10/20/30 XP) is client-side only via localStorage, called out honestly as such since there is no backend concept of lab progress and these are throwaway localhost processes outside Nullock's own project state -- the docs/labs static site itself still has no equivalent progress UI, an honest remaining gap if that's ever wanted there too. No parity.json item corresponds to Labs (Nullock-native, not Burp-parity-tracked), so no state flip -- `python3 scripts/parity_report.py --check` stays green (409 features, unchanged). Verified: bundled-babel transform of ui-v2/app.jsx, confirmation ui-v2/tabs.jsx was untouched (NUL byte still intact, zero diff), a Node functional test of the two new pure helpers (labsVisible's category/difficulty/query filters incl. case-insensitivity; labsXpSummary's earned/possible/per-track math), a vm-sandboxed render of LabsTab across both the no-selection and a forced full-detail-pane (hints partially revealed + walkthrough + fix shown) state proving no runtime throw, a field-name/action-shape match of the Repeater dispatch against the existing send-to-repeater-raw case (app.jsx:209-218), `python3 scripts/labs_site.py --check` and `python3 scripts/marketplace_site.py --check` both green (52 + 16 pages), and a whole-site relative-link check (0 broken, 78 pages, 1079 relative links). README.md (tab count 24 -> 25, Teaching-labs line) and docs/index.html (tab count in the Command-palette card, new Teaching-labs feature card) updated in the same commit. Committed as Bikebrainz, `--no-gpg-sign`, pushed as 71e2dc6, HEAD verified == origin/Nullock via two `git rev-parse` calls (no divergence). CI run 32238311877 was still in progress (Linux/Windows builds installing Qt; the other 4 jobs already green) at push time -- will confirm green next wave before calling this fully closed, same honest-partial pattern as prior in-flight-CI notes in this file.
