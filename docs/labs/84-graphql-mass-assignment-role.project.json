{
  "name": "Lab 84 -- GraphQL Mass Assignment: the REST profile update allow-lists its fields, the GraphQL mutation for the same record merges whatever the client sends.",
  "notes": "Local lab on port 5084. Copy this preset to project.json in a fresh directory, then launch NullockApp --project=<absolute-directory>. Start the lab separately with python app.py. Advanced scope restricts traffic to localhost or 127.0.0.1 on this port.",
  "inScope": [
    "localhost",
    "127.0.0.1"
  ],
  "outOfScope": [],
  "advancedScope": [
    {
      "enabled": true,
      "include": true,
      "host": "(localhost|127\\.0\\.0\\.1)",
      "portFrom": 5084,
      "portTo": 5084,
      "protocol": 0,
      "file": ""
    }
  ]
}
