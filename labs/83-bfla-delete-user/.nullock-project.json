{
  "name": "Lab 83 -- Broken Function Level Authorization: the destructive admin action never got the same role check its sibling admin views did.",
  "notes": "Nullock lab target. Scope is pre-set to the lab host so active probes run only here. Start the lab (python app.py) then run the steps in app.py.",
  "inScope": ["http://localhost:5083/*", "http://127.0.0.1:5083/*"],
  "outOfScope": []
}
