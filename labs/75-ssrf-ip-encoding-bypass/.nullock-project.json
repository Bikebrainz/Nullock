{
  "name": "Lab 75 -- SSRF via IP-address encoding bypass (decimal/octal/hex host, literal-string blocklist).",
  "notes": "Nullock lab target. Scope is pre-set to the lab host so active probes run only here. Start the lab (python app.py) then run the steps in app.py.",
  "inScope": ["http://localhost:5075/*", "http://127.0.0.1:5075/*"],
  "outOfScope": []
}
