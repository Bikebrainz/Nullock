{
  "name": "Lab 78 -- SSRF into an internal Docker Engine API (unauthenticated control-plane exposure).",
  "notes": "Nullock lab target. Scope is pre-set to the lab host so active probes run only here. Start the lab (python app.py) then run the steps in app.py.",
  "inScope": ["http://localhost:5078/*", "http://127.0.0.1:5078/*"],
  "outOfScope": []
}
