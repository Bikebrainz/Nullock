{
  "name": "Lab 85 -- Race Condition (Limit Overrun): a single-use gift code's redeemed check races its own write, so concurrent redemptions all get credited.",
  "notes": "Nullock lab target. Scope is pre-set to the lab host so active probes run only here. Start the lab (python app.py) then run the steps in app.py.",
  "inScope": ["http://localhost:5085/*", "http://127.0.0.1:5085/*"],
  "outOfScope": []
}
