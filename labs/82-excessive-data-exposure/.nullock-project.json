{
  "name": "Lab 82 -- Excessive Data Exposure: the profile API returns the whole DB row, the page only renders two fields of it.",
  "notes": "Nullock lab target. Scope is pre-set to the lab host so active probes run only here. Start the lab (python app.py) then run the steps in app.py.",
  "inScope": ["http://localhost:5082/*", "http://127.0.0.1:5082/*"],
  "outOfScope": []
}
