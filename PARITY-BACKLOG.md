# Nullock-vs-Burp parity — re-triage backlog (2026-08-02, verify-backed)

> Second-pass re-verification of all 409 items. Most items now carry BOTH a triage verdict and an adversarial verify verdict. Items marked `verified` were upheld by a second agent that tried to refute them; `REFUTED` were killed by that pass; `no-verdict` (21 items, mostly target/platform) hit the session wall and still need a second opinion. Still: re-derive before acting, but the `verified` set is high-confidence.

- 409 re-triaged | 221 closeable candidates | verify-upheld: 200 | refuted: 4

## Reclassifications (verify status)
| id | area | from→to | verify | feature |
|--|--|--|--|--|
| 359 | repeater | stub→partial | verified | Inspector: docked structured side panel in the message editors |
| 144 | sequencer | stub→partial | verified | Live capture option: "Ignore token length differences" |
| 165 | sequencer | present→partial | verified | Cookie jar transport safety (do not replay a Secure cookie over cleartext) |
| 273 | proxy | present→partial | verified | 'Don't send items to Proxy history or live tasks, if out of scope' (as an option) |
| 17 | intruder | present→partial | REFUTED | Grep - Extract |
| 40 | intruder | absent→partial | verified | Processing rule: Decode |
| 98 | extender | stub→absent | verified | [A4] Extension auto-reload during development (Auto-reload checkbox; Ctrl/Cmd-click to reload a single extension) |
| 119 | extender | absent→partial | REFUTED | [B17] Extension access to sitemap and proxy history; adding issues to the sitemap |
| 123 | extender | absent→partial | verified | [B21] Extension unloading handler (registerUnloadingHandler) — clean teardown |
| 128 | extender | stub→partial | verified | [B26] Installed tab: Add… dialog (choose extension type, file, output/error destination) |
| 131 | extender | absent→partial | verified | [B29] BApp Store: search box and category filtering |
| 132 | extender | absent→partial | REFUTED | [B30] BApp Store: ratings/popularity, 'Last updated' column and sorting |
| 211 | platform | present→partial | verified | Scan-to-scan issue delta (new vs fixed findings between runs) |
| 231 | platform | stub→partial | REFUTED | Logger (records HTTP from ALL tools + tool attribution, not just proxy) |
| 241 | platform | stub→absent | no-verdict | Reporting: reachable from the UI (Report menu / right-click > report selected issues) |
| 401 | target | absent→partial | no-verdict | Content discovery: dedicated GUI (vs Burp's discovery window) |

## Refuted closeable claims (adversarial pass killed these)

- **#330 [repeater] Inspector: "Cookies" — request cookies and response Set-Cookie with attributes** — verified_state=stub is CORRECT. Backend confirmed: inspector_logic.cpp:174 request cookies parsePairs(';',formDecode=false) opaque-safe; :251-257 Set-Cookie name/value parsed but attributes=segs.mid(1).join(';') is one unparsed blob (no Secure/HttpOnly/SameSite flags, no warnings). closeable_now is OVER-OPTIMISTIC/dishonest. The plan says "render it in THE SHARED INSPECTOR PANEL" — no such panel exists. grep 'api/inspect' across the whole tree returns ZERO .jsx/.js callers (only CHANGELOG/PARITY-BACKLOG/parity.json + control_server.cpp:9442 itself); no queryParams/setCookies/bodyParams consumer in ui-v2. The Inspector backend ships to nobody. The attribute-parse itself is ~1hr, but delivering a user-visible missing-flag warning first requires building the entire Inspector UI panel from scratch (NL.actions.inspect + side-panel component) — separate scaffolding tracked as backlog item 359 and shared with 328/331. Not an honest hours-to-a-day close.
- **#349 [repeater] Repeater: send from a Repeater tab to Intruder / Comparer / Decoder / Sequencer / Scanner** — verified_state=absent is CORRECT: reducer app.jsx:49-91 has only send-to-repeater/send-to-intruder, both keyed on action.row (a history row via NL.requestRawById), no from-Repeater case; Repeater control row tabs.jsx:180-249 = PROTO/HOST/:port/TLS/STATUS/CLEAR/SEND + NEW/DUP only. But closeable_now is OVER-OPTIMISTIC. Only the Intruder leg is a real day. (1) Sequencer does not exist at all — no 'sequencer' tab id in enum app.jsx:10-18, no SequencerTab component; 'send to Sequencer' has no destination. (2) ComparerTab (app.jsx:1440) and DecoderTab (app.jsx:1536) are self-contained local-useState components rendered with zero props (app.jsx:2646-2651) — no injection path; the plan itself admits these depend on unbuilt items 351/352/358. (3) No send-to-scanner reducer case; only passive scanner + TCP port scanner (app.jsx:1105,1191). The '~a day' estimate silently drops Sequencer+Scanner and presupposes receiver refactors — a hidden multi-item build, not a frontend day.
- **#56 [scanner] [A8] Passive audit: secrets / sensitive data in URL and body** — REFUTED. Claim's evidence ("no passive body-secret call in passive_scanner.cpp") and plan (build SecretScanner-on-body into checkResponse) are both wrong — that work already ships. passive_scanner.cpp:579-634, inside PassiveScanner::checkResponse (fn :174, invoked passively via onResponseReceived :135), scans the response body (<=4MB, statusCode==200 guard at :582) against a 10-pattern battery: AWS key/secret :590-594, GitHub PAT/App :595-598, Slack :599, Stripe :601, SendGrid :603, Mapbox :605, Google API :607, PEM key :609, each reported high-severity 'leaked-* found in response body' :630. Plus URL-query secrets :299-316, Authz-over-HTTP :318-324, JWT-in-URL :985, storage-of-secrets :1118-1127. verified_state 'present' is correct (feature covers URL AND body passively — arguably exceeds), but closeable_now:true is invalid: nothing to close, the proposed hours-of-work already exists. SecretScanner (control_server.cpp:770-786, /api/secrets/scan) is an additional richer API battery, not the sole body path.
- **#57 [scanner] [A9] Passive audit: tech/CMS fingerprint with CVE correlation** — REFUTED. Register premise ("passive fingerprints carry NO CVE; correlation is a separate /api/assess-only call") is false against live source. passive_scanner.cpp:1567-1621 does inline CVE correlation in the SAME per-response function as flagCms(1411): builds CveDatabase::HttpFingerprint, sniffs version via bodyVerRx (1581-1585), iterates cveKinds (1589-1594), calls CveDatabase::lookupByFingerprint (1605), emits cve-correlated findings with CVSS-derived severity + vector/affected/fix (1615-1619). Comment 1571-1572 claims it beats Burp Pro. assessWebTarget path also confirmed (control_server.cpp:432-436, CveDatabase::lookup). So closeable_now=true is dishonest — the claimed hours-of-work (attach CVE after passive fingerprint) is ALREADY shipped; nothing to close. State understated: dual-path, version-gated, GUI-reachable -> exceeds. Caveat unchanged: correlation is against static cve_database.cpp + runtime OverlayCve; did not verify a live NVD/OSV feed refreshes it, but that does not gate the feature existing.

## Closeable candidates (grouped by kind → effort; verify status per item)

### roadmap-only (27)

- **#101 [extender] (minutes) [verified] — [A7] Security model around third-party code (Burp: in-process, full JVM privileges, no sandbox, no permission model)**
  - plan: Confirmed exceeds and already reconciled — register only. Nullock has a real permission model Burp lacks: default-deny modify-requests/modify-responses grants parsed from the script (ExtensionPerms::isAllowed, enforced at onRequest registration and in doMutateResponse per-handler, extensions_api.cpp:36-59,489-491). reload() now DESTROYS and rebuilds the QJSEngine (rebuildEngine, extensions_api.cpp:93-107 / reload :121-146), so an uninstalled/observe-only script's globals and prototype patches no longer persist. Remaining real gap (tracked separately as task #115): NO per-extension execution-time bound — grep for setInterrupted/isInterrupted/watchdog/deadline in ExtensionsAPI/ is empty, so a runaway handler freezes the main event loop, including the removal endpoint. That hardening (a watchdog thread calling QJSEngine::setInterrupted) is a separate ~1-day build, not required to keep the 'exceeds' verdict.
  - evidence: extensions_api.cpp:93-107 rebuildEngine fresh QJSEngine per reload; :36-59 grant-gated registration; :489-491 mayMutate discard; grep setInterrupted/watchdog/deadline ExtensionsAPI/ = empty (no time bound)
- **#130 [extender] (minutes) [verified] — [B28] BApp Store: browse installable extensions inside the tool, one-click install**
  - plan: STALE prior gap ('No UI at all — uncommitted and unbuilt'). The in-app store now exists and is committed: a full Marketplace React component (ui-v2/app.jsx:223-385) mounted in SettingsTab (app.jsx:639). It fetches the merged catalog (NL.actions.marketplaceCatalog -> control_server.cpp:5354), renders per-extension cards with name/version/summary/categories/sha256, and offers Install/Update/Remove buttons. The two-step consent gate is fully wired: first install call sends confirmMutating:false, the server refuses traffic-mutating extensions with needsConfirmation:true (control_server.cpp:5455), and the UI shows the consent panel (app.jsx:315-340) built from permissions read out of the DOWNLOADED+verified bytes. Catalog now holds 14 entries (extensions/marketplace.json), not three. With sha256 pinning + bytes-derived consent this arguably EXCEEDS Burp's BApp store. Action: just mark the register present/done.
  - evidence: ui-v2/app.jsx:223-385 Marketplace component + :639 mount; control_server.cpp:5354 /catalog, :5418 /install (needsConfirmation :5455), :5467 /uninstall; extensions/marketplace.json = 14 ids
- **#4 [intruder] (minutes) [verified] — Payload positions (§marker§) + insertion**
  - plan: Fully present at Burp parity for manual marking: findMarkers pairs §…§ (inner text = per-position default), applyPayloads splices by marker index, GUI shows a live insertion-point count. No work needed beyond confirming the register; a convenience 'insert §…§' button is optional and overlaps item 5.
  - evidence: intruder_engine.cpp:15-32 findMarkers (U+00A7 pairs, keeps default text), 134-148 applyPayloads (null combo entry → marker default); tabs.jsx:620 '(template.match(/§/g)?.length\|\|0)/2 INSERTION POINT'.
- **#6 [intruder] (minutes) [verified] — Payload type: Simple list**
  - plan: Fully present including GUI: newline-split parseSet drops blanks/trims, ui-v2 has the payloads textarea plus a curated preset menu (SET/+ADD). At Burp parity for the simple-list case. Register only.
  - evidence: intruder.cpp:20-27 parseSet; tabs.jsx:699-704 payloads textarea; PAYLOAD_PRESETS tabs.jsx:339-447 with preset dropdown 648-697.
- **#202 [platform] (minutes) [verified] — Collaborator: HTTP interaction capture**
  - plan: No code change. Register stays 'present'. HTTP OAST capture is fully functional: handleClient parses request line + headers with slowloris/body deadlines, mints an OastHit and appends to a bounded ring (m_hits, capped at kMaxHits, oldest evicted), which is listable via hitsSince(sinceId). Wired live in the app.
  - evidence: Src/Core/APIs/Oast/oast_server.cpp:111 handleClient; :176-189 hit fields (token/sourceIp/method/hostHeader/path/bodyPreview); :191-195 m_hits ring append + cap; :52-58 hitsSince(); wired at Src/App/app.cpp:925 `Nullock::Core::OastServer oast;` + :939 hitReceived connect.
- **#204 [platform] (minutes) [verified] — Collaborator: automatic scan-check integration and interaction-to-issue correlation**
  - plan: No code change. OastCorrelator::onHit resolves a registered token to its origin, dedupes via m_confirmed, derives the finding kind from the probe kind (`<kind>-confirmed`), escalates log4shell/rce to critical, and calls scanner->reportFinding with rowId/host/url. True-positive-by-construction correlation is wired.
  - evidence: Src/Core/APIs/Oast/oast_correlator.cpp:30-47 onHit token->origin + confirmed dedupe; :55-57 `baseKind + "-confirmed"`; :76-78 severity critical for log4shell/rce; :80-81 m_scanner->reportFinding(origin.rowId, ...).
- **#210 [platform] (minutes) [verified] — Reporting/exports beyond Burp: SARIF, CycloneDX SBOM, Postman, OpenAPI, HAR, nmap XML, machine-readable master JSON**
  - plan: No code change. Multiple export formats Burp lacks are implemented and GET-allowlisted: nmap-xml, SARIF v2, CycloneDX 1.5 SBOM, Postman collection, OpenAPI export, report/json master bundle. SARIF/Postman/nmap-xml also have UI buttons. Register stays 'exceeds'. (Minor optional polish: SBOM/OpenAPI/report-json lack dedicated UI buttons but are reachable — a frontend nicety, not a gap.)
  - evidence: Src/BackEnd/Control/control_server.cpp:5790 export/nmap-xml, :5848 export/sarif, :5917 export/sbom (CycloneDX 1.5), :6051 export/postman, :4670 /api/openapi/export, :5267 report/json; allowlisted at :1687 `p.startsWith("/api/export/")`; UI buttons ui-v2/app.jsx:603/994/1263.
- **#217 [platform] (minutes) [verified] — Headless operation (run without a display, for containers/CI)**
  - plan: No code change. --headless (and implied by --scan) selects a QCoreApplication instead of QGuiApplication, skips the QML window/style, and runs an event loop with a CLI exit path. Fully functional for containers/CI. Register stays 'present'.
  - evidence: Src/App/app.cpp:578 `--scan ... implies headless`; :582 `const bool headless = hasFlag(argc, argv, "--headless") \|\| !scanUrl.isEmpty();`; QCoreApplication branch selected when headless (app.cpp headless path).
- **#218 [platform] (minutes) [verified] — CI integration with meaningful exit codes (fail the build on findings at/above a threshold)**
  - plan: No code change. runGateScan performs a preflight probe, evaluates findings against a fail-on threshold via CiGate::evaluate, and returns a real process exit code; unreachable/opt-out cases fail CLOSED (not scanning is not passing). Also exposed as GET /api/gate?fail-on=. Register stays 'exceeds'.
  - evidence: Src/BackEnd/Control/control_server.cpp:9655 `int runGateScan(const QString &url, const QString &failOn, bool ndjson)`; :4256 `Nullock::Core::CiGate::evaluate(severities, failOn)`; :4265 `{ "exitCode", g.exitCode }`; :9697 `CiGate::unreachable(failOn, ...)` fail-closed preflight.
- **#219 [platform] (minutes) [verified] — Machine-readable live event stream for automation**
  - plan: Nothing to build; register entry is correct. The --ndjson stream is real and thoughtfully hardened: query-string stripping by default (--ndjson-include-query to opt in), monotonic model.lastId() (not rowCount) so rows survive the 10k-window eviction, and a main-thread context object on both connects so worker-thread emissions are queued and one-object-per-line stays intact under load. Emits response and finding events on stdout, jq-friendly. Burp has no equivalent free live NDJSON tap. Keep as exceeds.
  - evidence: Src/App/app.cpp:586 `const bool ndjsonOut = hasFlag(argc,argv,"--ndjson")`; :1075 `if (ndjsonOut)`; :1084 `includeQuery`; :1097-1099 responseReceived connect with `&model` context object; :1110 `e["rowId"] = model.lastId();`; :1123 `QTextStream(stdout) << QJsonDocument(e).toJson(Compact)`; :1126+ findingsChanged connect.
- **#254 [proxy] (minutes) [verified] — CA private key hardening on disk**
  - plan: Prior gap text is STALE -- both sub-gaps are now fixed (tasks #110/#111). Leaf keys DO get lockdownPrivateKeyFile now on fresh mint (cert_authority.cpp:300-303) and on disk-reuse (:212-214), and every lockdown failure now WARNs instead of returning silently (:150-156, :166-169, :212-214, :300-303); the Windows path returns rc==ERROR_SUCCESS and POSIX returns setPermissions (:81-89, [[nodiscard]]). Verdict stays 'exceeds' (per-user DACL/chmod beyond Burp). Action: update the roadmap register to drop the 'leaves/*.key inherit default permissions' and 'errors return without logging' notes -- they no longer hold.
  - evidence: cert_authority.cpp:300-303 (leaf key lockdown on mint), :212-214 (lockdown on reuse), :48 [[nodiscard]] bool, :150-156 & :166-169 (warn on failure), :81-89 (rc/setPermissions returned)
- **#261 [proxy] (minutes) [verified] — Intercept client requests: hold, edit, forward, drop in flight**
  - plan: Confirmed at parity: pend() holds outbound requests (proxy_server.cpp:305-312), drop disconnects, forward returns edited/original bytes (intercept.hpp:94-108, intercept_logic resolveForwardBytes). The kMaxPendingIntercepts=256 backpressure cap (intercept_logic.hpp:48) is a deliberate, documented anti-DoS choice (auto-forward on overflow), not a defect. 'No interception rules' is a distinct roadmap item (Group B #10), not part of this feature. Action: leave as present; update register. If desired, the cap could be made configurable in minutes, but it is not a parity gap.
  - evidence: proxy_server.cpp:305-312 (pend on request, drop path), intercept.hpp:94-108 (forward/drop/pend API), intercept_logic.hpp:48 (kMaxPendingIntercepts=256, documented rationale :39-47)
- **#265 [proxy] (minutes) [verified] — Match and replace: per-rule host scoping**
  - plan: Confirmed exceeds: MatchReplaceRule.hostGlob (proxy_server.hpp:85) supports '*.example.com' wildcards, compiled to an anchored regex (proxy_server.cpp:1415-1421) and enforced per-rule (:1412-1413 hostAll gate). Burp has no per-rule host scoping on match/replace, so this is genuinely above parity. Nothing to build; register-only.
  - evidence: proxy_server.hpp:85 (hostGlob), proxy_server.cpp:1415-1421 (glob->regex compile), :1412-1413 (hostAll gate)
- **#274 [proxy] (minutes) [verified] — HTTP/2 support to the server (ALPN h2 upstream, h2-to-h1 translation)**
  - plan: Present and working, nothing to build — update the register only. Upstream ALPN advertises h2+http/1.1 (proxy_server.cpp:459-462), negotiation with an unexpected-ALPN bailout (proxy_server.cpp:520-526), and a keep-alive h1-client->h2-upstream bridge via H2Client::send (proxy_server.cpp:615-693). Known and acceptable limitation: the browser sees serializeResponse's re-framed bytes rather than upstream wire bytes, and it only runs inside a successful MITM tunnel (inherits the openssl dependency + markMitmBlocked stickiness). Neither reduces it below Burp parity.
  - evidence: proxy_server.cpp:459-462 (h2 ALPN upstream); 520-526 (negotiation/bailout); 615-693 (h2->h1 bridge, session-reused H2Client::send at :645).
- **#324 [repeater] (minutes) [verified] — Decoder: hash-algorithm identification from a digest**
  - plan: Already shipped AND now reachable from the UI (prior audit undercounted: it thought there was no Decoder tab). identifyHash covers bcrypt/md5crypt/sha256crypt/sha512crypt/argon2 prefixes + hex-length classification, exposed via /api/transcode op=identify and the DecoderTab 'identify hash' button. Just confirm the roadmap register says present/exceeds; no code work.
  - evidence: Src/Core/Networking/transcode.cpp:287-308 identifyHash(); control_server.cpp:9426-9430 op==identify branch returns candidates; ui-v2/app.jsx:1626-1627 'identify hash' button calls run('identify').
- **#325 [repeater] (minutes) [verified] — Decoder: JWT decode with security analysis**
  - plan: Shipped and reachable: jwtDecode pretty-prints header+payload and flags alg=none (forgeable) and HS* (brute-forceable), always noting the signature is unverified. Wired through /api/transcode op=jwt-decode and the DecoderTab 'jwt-decode' button. Roadmap register only.
  - evidence: Src/Core/Networking/transcode.cpp:152-175 jwtDecode() with alg=none/HS warnings; control_server.cpp:9431-9434 apply() branch; ui-v2/app.jsx:1548,1629-1630 jwt-decode op button.
- **#52 [scanner] (minutes) [verified] — [A4] Passive audit: missing security headers (CSP/HSTS/XFO/XCTO/RP/Permissions-Policy/COOP/COEP/CORP)**
  - plan: Confirmed present and live. The core five (CSP/HSTS/XFO/XCTO/RP) are a rule table (passive_scanner.cpp:223-243) with HSTS correctly gated on TLS; the modern isolation headers (Permissions-Policy/COOP/COEP/CORP) are separate addFinding calls (passive_scanner.cpp:417-432). PassiveScanner::onResponseReceived is wired to every proxy response (app.cpp:764,994), so these fire automatically on captured traffic — not API-gated. Just fix the register.
  - evidence: passive_scanner.cpp:224 missing-csp; :226 missing-hsts; :228 missing-xfo; :230 missing-xcto; :232 missing-rp; :239 HSTS TLS-gate; :417 missing-permissions-policy; :422 missing-coop; :427 missing-coep; :432 missing-corp; app.cpp:764 connect(...PassiveScanner::onResponseReceived)
- **#53 [scanner] (minutes) [verified] — [A5] Passive audit: cookie security flags + prefixes**
  - plan: Confirmed present. Per-cookie HttpOnly/Secure(TLS-gated)/SameSite flags (passive_scanner.cpp:254-265, capped against finding-spam) plus __Secure-/__Host- prefix violations and broad-path-no-samesite (passive_scanner.cpp:462/481/490). Runs passively on live traffic. Roadmap-only.
  - evidence: passive_scanner.cpp:254 cookie-no-httponly; :259 cookie-no-secure; :263 cookie-no-samesite; :462 cookie-secure-prefix-violation; :481 cookie-host-prefix-violation; :490 cookie-broad-path-no-samesite
- **#54 [scanner] (minutes) [verified] — [A6] Passive audit: CORS misconfiguration**
  - plan: Confirmed present. Basic ACAO:* + Allow-Credentials:true (high) and plain ACAO:* (info) at passive_scanner.cpp:288-297, plus the extended battery: origin-reflection (high), null-origin, methods-wildcard, headers-wildcard at 782-801. All passive on live responses. Roadmap-only.
  - evidence: passive_scanner.cpp:288 cors-wildcard-creds; :293 cors-wildcard; :782 cors-origin-reflection (high); :788 cors-null-origin; :796 cors-methods-wildcard; :801 cors-headers-wildcard
- **#55 [scanner] (minutes) [verified] — [A7] Passive audit: information disclosure**
  - plan: Confirmed present and broad. Server version leak + X-Powered-By (passive_scanner.cpp:273/279), internal-ip-leak (:711), html-comment-leak (:744), source-map-exposed (:573), exposed-dev-file (:682), phpinfo-output critical (:947), plus verbose 4xx SQL/framework error leaks (:1131-1157). Passive/live. Roadmap-only.
  - evidence: passive_scanner.cpp:273 server-version-leak; :279 x-powered-by; :711 internal-ip-leak; :744 html-comment-leak; :573 source-map-exposed; :682 exposed-dev-file; :947 phpinfo-output; :1137-1147 verbose-sql/php/debug err
- **#60 [scanner] (minutes) [verified] — [A12] Active audit: reflected XSS**
  - plan: Present. Dedicated XssReflected module (control_server.cpp:734 XssReflected::test) plus GUI probe battery unencoded-canary reflection (control_server.cpp:2190 reports reflected-xss). Reflected-only by design; stored/DOM is out of scope here and belongs to the DAST item (A10/task #12), not this row. Roadmap-only.
  - evidence: control_server.cpp:734 XssReflected::test(xrr); control_server.cpp:2190 report('high','reflected-xss',...); xss_reflected.cpp module present
- **#62 [scanner] (minutes) [verified] — [A14] Active audit: path traversal / LFI**
  - plan: Present. Dedicated PathTraversal module (control_server.cpp:754 PathTraversal::test) plus GUI per-row probe battery with multiple traversal encodings (%2f, double-encoded, backslash, /proc/self/environ) confirmed by file signatures root:x: / [fonts] / PATH= (control_server.cpp:2418-2438). Roadmap-only.
  - evidence: control_server.cpp:754 PathTraversal::test(ptr); control_server.cpp:2420-2424 LFI payload table; :2431 signature match report('critical','lfi',...)
- **#64 [scanner] (minutes) [verified] — [A16] Active audit: server-side template injection**
  - plan: Present, and the prior GAP is STALE/WRONG — SSTI IS in the GUI per-row probe battery. control_server.cpp:2441-2487 fires engine-specific {{7*7}}-style payloads for Jinja2/Twig, ERB, Freemarker, Velocity, Smarty and confirms on the '49' signature, reporting ssti-jinja-twig/ssti-erb/etc. That battery is GUI-reachable via the per-row PROBE button (proxy.jsx:328 probeRow) and 'Probe all rows' (app.jsx:1011 probeAll). The dedicated Ssti module is ALSO available via /api/audit and /api/ssti/test (control_server.cpp:797). So a GUI-only user DOES run SSTI. Action = delete the stale 'API-only / GUI user never runs SSTI' gap text; no code change. Roadmap-only.
  - evidence: control_server.cpp:2451-2467 GUI-battery SSTI payloads (Jinja2/ERB/Freemarker/Velocity/Smarty); :2477 signature '49' report('critical',s.kind,...); proxy.jsx:328 probeRow; app.jsx:1011 probeAll; control_server.cpp:797 Ssti::test (API path)
- **#81 [scanner] (minutes) [verified] — [A33] Issue severity model (High/Medium/Low/Info)**
  - plan: Nothing to build. Finding::severity carries a 5-level ladder (info\|low\|medium\|high\|critical) that meets and slightly exceeds Burp's 4 levels (adds 'critical'). It flows through the enricher (defaultScoreForSeverity), the /api/report exec-summary severity breakdown, and SARIF/report exporters. Just keep the roadmap entry marked present.
  - evidence: Src/Core/Networking/passive_scanner.hpp:25 `QString severity; // "info"\|"low"\|"medium"\|"high"\|"critical"`; finding_enricher.cpp:441 defaultScoreForSeverity(f.severity); control_server.cpp:5084 report severity breakdown
- **#82 [scanner] (minutes) [verified] — [A34] Issue confidence model (Certain/Firm/Tentative)**
  - plan: Present at parity. Finding::confidence uses the Burp taxonomy confirmed/firm/tentative (== Certain/Firm/Tentative). It is per-finding: a scanner that ran its own proof (OAST callback, time-based reproduce) sets it explicitly and the enricher preserves it; confidenceForKind() only fills a default when empty (finding_enricher.cpp:459-487). The prior 'no per-evidence, no user override' note is stale/overstated: confidence IS per-finding-evidence (OAST vs heuristic diverge). A user override is beyond Burp parity. Keep marked present.
  - evidence: Src/Core/Networking/finding_enricher.cpp:459 `if (f.confidence.isEmpty()) f.confidence = confidenceForKind(f.kind);`; :463-487 confidenceForKind maps oast-callback/time-based->confirmed, suspect/heuristic->tentative; passive_scanner.hpp:46 `QString confidence;`
- **#85 [scanner] (minutes) [verified] — [A37] Out-of-scope handling during scans**
  - plan: Present. blocksScope() gates every active surface: /api/audit/all skips out-of-scope urls and history rows and returns scopeSkipped count; probe/repeater/intruder/authz-replay all guard with blocksScope before firing. Line numbers shifted from the prior audit (file is 9772 lines now) but the controls hold. Keep marked present.
  - evidence: control_server.cpp:1196 `blocksScope(host){ return proxy && !host.isEmpty() && !proxy->isInScope(host); }`; :6717 `if (blocksScope(uu.host())) { ++scopeSkipped; return; }`; :6744 history-row scope skip; :2090 probe scope 403; :3006/:3130/:3333/:4615 audit/repeater/intruder/authz guards
- **#161 [sequencer] (minutes) [verified] — Cookie jar honors cookie path scope**
  - plan: Correct as implemented and remains so: pathMatches (session_manager_logic.cpp:94-116) resolves dot-segments on the request path (removeDotSegments :81-92), scopes an absent/relative cookie Path to '/', and enforces the RFC 6265 directory-prefix + '/'-boundary rule; it is applied in injectInto at session_manager.cpp:132, which is reached only from the proxy inject path (proxy_server.cpp:1530). Nothing to build for correctness -- keep the register at 'present'. The only caveat (the cookie's Path is captured at parseSetCookie session_manager_logic.cpp:67 but never shown in the viewer, app.jsx:2113-2131 renders name/value/flags only) is subsumed by item 162's UI work; surfacing a Path column there makes a path-suppressed cookie diagnosable.
  - evidence: session_manager_logic.cpp:94-116 (pathMatches, RFC-correct), :81-92 (removeDotSegments), :67 (Path captured); session_manager.cpp:132 (used in injectInto); proxy_server.cpp:1530 (sole inject caller); ui-v2/app.jsx:2113-2131 (no Path column).

### frontend (76)

- **#99 [extender] (hours) [verified] — [A5] BApp Store: extension detail page (full description, author, version, source-code link, 'Report a bug')**
  - plan: Prior evidence is now STALE. Per-extension detail pages DO exist: docs/marketplace/e/{aws_sigv4,crumbs,dom_taint,...}.html (14 pages), each with rendered source, version/author, and a github.com bug-report link (dom_taint.html:25-26,304). The catalog XSS the prior audit flagged is FIXED — index.html now builds every card via el()/textContent + appendChild (docs/marketplace/index.html:78-84,114-145), no innerHTML of untrusted fields. Remaining gap is only the lack of an IN-APP detail view. Closeable now (frontend): the in-app marketplace panel already fetches the full catalog (/api/marketplace/catalog returns name/summary/version/author/sha256/permissions, control_server.cpp:5397), so add a detail modal in the Marketplace JSX component that renders those fields plus a link to docs/marketplace/e/<id>.html and the raw .js source URL. Changelog data does not yet exist (would need a per-entry changelog field) — leave that out of the MVP.
  - evidence: docs/marketplace/e/*.html (14 detail pages) with source + github bug link (dom_taint.html:25-26); docs/marketplace/index.html:78-84,114-145 textContent/appendChild (XSS fixed); control_server.cpp:5397 catalog exposes fields in-app
- **#102 [extender] (hours) [verified] — [A8] Per-extension declared capability visibility (Burp shows only what the extension registered)**
  - plan: Confirmed partial — a pure wiring gap. The backend already publishes bootInfo.extensionGrants as a {filename: [capabilities]} object (control_server.cpp:1240-1244 from scriptGrants()), but NO frontend reads it: grep for extensionGrants across ui-v2/ returns only nothing in the JSX, and the Extensions card renders only Folder / Loaded / the log tail (app.jsx:641-662). The only user-visible grant signal is a transient log line that ages out of the 30-line tail. Closeable now (frontend): in app.jsx read b.extensionGrants and render a per-script row/badge in the Extensions card — flag an extension as 'modifies traffic' when its list contains modify-requests/modify-responses, else 'observe-only' (mirror the marketplace card's modifies/observe badge at docs/marketplace/index.html:126-128). No backend change needed.
  - evidence: control_server.cpp:1240-1244 bootInfo['extensionGrants']=grants published; ui-v2/app.jsx:641-662 Extensions card renders Folder/Loaded/log only; grep extensionGrants ui-v2/ = absent
- **#131 [extender] (hours) [verified] — [B29] BApp Store: search box and category filtering**
  - plan: STALE evidence. The PUBLISHED catalog website (docs/marketplace/index.html:44-192) now has a full search input (name/summary/author/category), clickable category chips (state.cat filter, filtered() at :99-113), and a name/updated sort segmented control — a complete reference implementation. But the IN-APP store (ui-v2/app.jsx Marketplace) still renders state.items unfiltered with no search/category controls, which is the surface that matters for Burp BApp-store parity. Close: port the ~40 lines of filter/sort logic from docs/marketplace/index.html:76-113 into the Marketplace component — add useState for query+category+sort, derive category list from items[].categories (already present in each item), filter/sort client-side before the state.items.map at app.jsx:342. Pure frontend, all data already client-side, reference code already written.
  - evidence: ui-v2/app.jsx:342 state.items.map with no predicate + no <input> in the component; docs/marketplace/index.html:46 search input, :91-113 categories()/filtered(), :183-190 sort handlers
- **#0 [intruder] (hours) [verified] — Attack type: Sniper**
  - plan: Engine+API complete and Sniper is the GUI default, so it works today. To close the register/parity gap add an attack-type <select> in the ui-v2 IntruderTab header (tabs.jsx ~529, replacing the hardcoded 'sniper mode' span) bound to intruder.attackType, dispatch {type:'intruder-set', payload:{attackType}}. Also add attackType to the intruder-start POST in app.jsx (~110-118) so a chosen type is guaranteed set before start. This single picker also unlocks items 1-3.
  - evidence: intruder_engine.cpp:75-84 (Sniper combo build), parseAttackType 36-43; control_server.cpp:3209-3216 parses attackType (int or name); tabs.jsx:529 header hardcoded 'INTRUDER · sniper mode'; no attack-type control anywhere in ui-v2 (grep 'sniper\|pitchfork\|battering' returns only the label).
- **#1 [intruder] (hours) [verified] — Attack type: Battering ram**
  - plan: Capability present (engine + API); only unreachable from the GUI. Closed by the same attack-type <select> as item 0 (option value 1 = battering-ram). No backend change — control_server /api/intruder/set already accepts it. Verify by picking it and confirming all markers get the same payload per request.
  - evidence: intruder_engine.cpp:67-73 (BatteringRam fills every position with same p); control_server.cpp:3207-3216; tabs.jsx has no selector — only single payloads textarea at 699-704.
- **#11 [intruder] (hours) [verified] — Processing rule: Add suffix**
  - plan: Present engine+API; closed by the same rule-chain editor as item 10 (suffix is one op in the dropdown). No backend change.
  - evidence: intruder_rules.cpp:14 suffix; operations() 59-69 lists it; control_server.cpp:3247-3261.
- **#14 [intruder] (hours) [verified] — Processing rule: Encode (URL/HTML/Base64/hex)**
  - plan: Core encode set present via Transcode delegation (base64/base64url/url/hex/html/unicode-escape/rot13). Closed for GUI by the item-10 rules editor exposing these ops. The minor missing Burp variants (selective 'URL-encode these characters' with a char-list arg, ASCII-hex-with-separator) are a small follow-up backend op if wanted, but the named feature is present.
  - evidence: intruder_rules.cpp:42-46 delegates to Transcode::apply; operations() 63-66 lists base64-encode/base64url-encode/url-encode/hex-encode/html-encode/unicode-escape/rot13.
- **#15 [intruder] (hours) [verified] — Processing rule: Hash**
  - plan: Present engine+API (md5/sha1/sha256/sha512 via Transcode). Only GUI missing; closed by the item-10 rules editor. No backend change.
  - evidence: intruder_rules.cpp:67-68 operations() lists md5/sha1/sha256/sha512; delegated through Transcode::apply at 45-46.
- **#20 [intruder] (hours) [verified] — Results table: filtering**
  - plan: Confirmed only a single hardcoded 'hide 404s' checkbox. All result fields (status/size/ms/extracted/payload/err) are already client-side in intruder.results, so a proper filter bar is pure frontend. In tabs.jsx IntruderTab add local state + controls: status-class toggles (2xx/3xx/4xx/5xx), min/max length, a regex-highlight/negative-search box over payload+extracted, and replace the line 741 `if(hide404 && r.status===404) return null` with a composed predicate. No backend change needed.
  - evidence: tabs.jsx:714-718 single hide404 checkbox; tabs.jsx:741 `if(hide404 && r.status===404) return null;` — no status-range/length/regex/invert controls anywhere in IntruderTab.
- **#23 [intruder] (hours) [verified] — Send to Intruder from other tools**
  - plan: Confirmed: on send-to-intruder the last `=value` on a line is auto-wrapped in §payload§ (app.jsx:77) and the tab switches to intruder. Interactive position marking DOES partly exist — the template textarea is editable so a user can hand-place §…§ markers (tabs.jsx:626). Optional parity nicety (frontend-only, hours): add a 'mark selection' button that wraps the current textarea selection in § markers, matching Burp's Add-§ button. Low impact; core send works.
  - evidence: app.jsx:71-91 case 'send-to-intruder' sets host/port/tls/template + tab:'intruder'; app.jsx:77 auto-marks last `=value`; tabs.jsx:626 editable template textarea allows manual marker placement.
- **#48 [intruder] (hours) [verified] — Results table: sortable columns**
  - plan: Pure frontend, tabs.jsx IntruderTab. Confirmed: header <th> cells (line 734) have no onClick and rows render intruder.payloads.map((p,i)=>...) in fixed index order (line 737). Add sortKey/sortDir state; build a derived array rows = intruder.payloads.map((p,i)=>({p, r: intruder.results[i]\|\|default, i})); sort by the chosen key (#, payload, status, size, ms, matched, extracted, error) numeric-aware for status/size/ms; render tbody from the sorted array while keeping the ORIGINAL index i for the key and the intruderResend(i) button (line 757) so resend still targets the right model row. Make each <th> an onClick toggling key/dir with an arrow indicator. Respect existing hide404 filter. Highest-value closeable item (high impact, no backend touch).
  - evidence: tabs.jsx:734 header <th> cells have no onClick; :737 intruder.payloads.map((p,i)) renders in fixed index order; :757 resend uses index i.
- **#206 [platform] (hours) [verified] — Notes (free-text engagement notes stored in the project)**
  - plan: Backend + client stub are ready; only the editor UI is missing. project_store setNotes()/saveMetadata() persist a project-wide notes blob, and real-data.js already exposes scopeSetNotes(notes) -> POST /api/scope/notes, but NO UI calls it (app.jsx:598 renders notes as a read-only single-line Row). Add a <textarea> notes editor (there are existing textarea patterns at app.jsx:1498/1601) bound to scope.notes with an onBlur/save-button calling NL.api.scopeSetNotes. Per-host/per-issue/per-request notes remain a larger schema change and stay out of scope for this slice.
  - evidence: Src/Core/Storage/project_store.cpp:346-349 setNotes; :299 `o["notes"] = m_meta.notes` persisted; ui-v2/app.jsx:598 read-only Row (first line, 60 chars); ui-v2/real-data.js:139 `scopeSetNotes(notes){ return post("/api/scope/notes", { notes }); }` (defined, uncalled — grep of ui-v2 finds no invoker).
- **#209 [platform] (hours) [verified] — Reporting: HTML issue report**
  - plan: Generator is complete (POST /api/report/html builds a self-contained styled HTML report from findings/scope/notes) but delivery is missing: no UI button anywhere and the endpoint is NOT in the isReadPath allowlist, so it requires POST and a plain download link (GET) won't reach it. Two-line close: (1) add `\|\| p == "/api/report/html"` to isReadPath (it is genuinely read-only — no mutation), (2) add a 'Download HTML report' button in ui-v2/app.jsx next to the existing SARIF/Postman/nmap export buttons pointing an <a href>/download at /api/report/html. Alternatively keep POST and do a fetch->blob->download in JS.
  - evidence: Generator: Src/BackEnd/Control/control_server.cpp:5089 `if (path == "/api/report/html")`. Not allowlisted: isReadPath at :1662-1690 lists /api/report/json (:1683) but NOT /api/report/html. No UI: grep of ui-v2 for report/html returns nothing (only export/postman :603, export/sarif :994, export/nmap-xml :1263 have buttons).
- **#211 [platform] (hours) [verified] — Scan-to-scan issue delta (new vs fixed findings between runs)**
  - plan: Downgraded from present: the diff engine is sound within a single process (identity key kind\|host\|url\|summary, baseline persisted to disk, NEW/FIXED/unchanged computed) but it is API-only (no UI anywhere — grep finds only CSS 'baseline'), and the CURRENT side reads scanner->findings(0) which is in-memory with NO reload path (no findings.json load exists), so reopening a project and hitting /api/baseline/diff before re-scanning reports every baselined issue as FIXED. Closeable slice (hours, frontend): add a Baseline panel in ui-v2 that calls /api/baseline/save + /api/baseline/diff and renders new/fixed, plus guard the empty-current case (surface hasBaseline+currentCount==0 as 'no current scan' rather than 'all fixed'). Full cross-restart persistence of live findings is a larger follow-up.
  - evidence: Src/BackEnd/Control/control_server.cpp:4189-4218 baseline/diff: current keys from `m_wiring.scanner->findings(0)` (in-memory), FIXED = baseline keys absent from current; no findings persistence/reload anywhere (grep for loadFindings/findings.json in Src/Core+Src/BackEnd returns nothing); no UI (grep ui-v2 'baseline' hits only flexbox alignItems).
- **#223 [platform] (hours) [verified] — Update check / new-version notification**
  - plan: Backend is complete: UpdateChecker runs async (gated by --no-update-check / NULLOCK_NO_UPDATE) and control_server packs root["update"] = {available,currentVersion,latestVersion,releaseUrl,releaseNotes,publishedAt} into /api/snapshot. But NO frontend reads snapshot.update (grep of ui-v2 for latestVersion/releaseUrl/releaseNotes outside the extension-marketplace 'update-available' badge is empty). To close: in ui-v2/app.jsx snapshot handler, read data.update; if update.available render a dismissible banner/badge linking releaseUrl with releaseNotes tooltip. Pure frontend — the data is already on the wire. Files: ui-v2/app.jsx (snapshot consumer + a banner component).
  - evidence: control_server.cpp:1554-1563 `if (m_wiring.updates){... upd["latestVersion"]=u.latestVersion; upd["releaseUrl"]=u.releaseUrl; ... root["update"]=upd; }`. ui-v2 grep for latestVersion/releaseUrl/'update.available' (excluding marketplace/extension 'updated:'/'update-available' badge) returns nothing — no consumer of snapshot.update.
- **#224 [platform] (hours) [verified] — Engagement tool: Generate CSRF PoC**
  - plan: Generator and endpoint both exist and are good: POST /api/csrf/poc {id} or {method,url,contentType,body} -> RequestExport::csrfPoc() which emits auto-submit forms for GET/urlencoded, a multipart scaffold, and a credentialed fetch() PoC for other content-types (with a CORS-preflight caveat note). The only gap is discoverability: no right-click/button and no NL.actions entry. To close: add a 'Generate CSRF PoC' item to the proxy-row/Repeater context menu (like the existing COPY-AS exporters) that POSTs the selected row id to /api/csrf/poc and shows the returned HTML in a copy/download modal. Files: ui-v2/proxy.jsx (row context menu) and/or ui-v2/real-data.js NL.actions, wired to /api/csrf/poc.
  - evidence: control_server.cpp:3905-3938 `// POST /api/csrf/poc { id } \| { method,url,... }` ... `const QString html = RequestExport::csrfPoc(method,url,contentType,body,note);`. Src/Core/Networking/request_export.cpp:71-95 GET/urlencoded/multipart/fetch branches real. ui-v2 grep for csrf/poc shows only unrelated header-rule placeholders (app.jsx:809 'csrf strip', :834/:838 X-CSRF-Token) — no generate action.
- **#241 [platform] (hours) [no-verdict] — Reporting: reachable from the UI (Report menu / right-click > report selected issues)**
  - plan: The backend report generators are fully implemented and working (report/html :5089, report/build :5001, report/json :5267), but there is ZERO UI affordance -- grep for report/Report across every ui-v2/*.jsx returns nothing; app.jsx only wires /api/export/postman\|sarif\|nmap-xml (:603/:994/:1263). Reclassified stub->absent because no UI scaffold exists at all; the report is only reachable via curl -H 'X-Nullock-UI: 1'. To close (hours, frontend-only): add a 'Generate report' action to NL.actions in real-data.js using the existing post() helper (real-data.js:113, which already sets X-Nullock-UI:1), read the response as a blob and trigger a download (report/html and report/build are POST-only -- not in isReadPath at :1661-1692 -- so a plain <a href> GET will 405; must fetch-POST-then-blob). Add a button in app.jsx's findings/export area. report/json is already GET+allowlisted if a link is preferred for that one.
  - evidence: control_server.cpp:5001/:5089/:5267 (report backends present); ui-v2 report grep empty; app.jsx:603/994/1263 (only export buttons); real-data.js:113 (post() sets X-Nullock-UI:1); allowlist :1661-1692 omits report/html+build
- **#268 [proxy] (hours) [verified] — HTTP history persistence + search at engagement scale**
  - plan: Backend is complete: HistoryIndex::find (Src/Core/Storage/history_index.cpp:296-337) is a parameterized, injection-safe SQL filter (method/host/path/status/minSize/maxSize/sinceMs/limit) exposed at POST /api/history/find (control_server.cpp:3987-3998); individual rows openable by id via /api/history/full/<id> (control_server.cpp:3865+). The only gap is UI wiring. Add NL.actions.historyFind(filters) => post('/api/history/find', filters) in ui-v2/real-data.js (next to search() ~line 150), then add a filter bar to ui-v2/proxy.jsx that renders the returned metadata rows and, on click, loads full bytes via the existing NL.requestRawById/responseRawById(id) path. No backend change needed.
  - evidence: history_index.cpp:296-337 (complete SQL find); control_server.cpp:3987-3998 (/api/history/find wired); control_server.cpp:1416-1444 (snapshot serializes only the in-memory ProxyModel window, capped 10'000); real-data.js:150-155 exposes only /api/search, no historyFind action; grep of ui-v2/*.jsx for history/find = 0 hits. /api/search oldest-500 cap already fixed (control_server.cpp:1762-1763; commit 07e43be).
- **#270 [proxy] (hours) [verified] — WebSocket message injection into a live connection (Burp WebSocket Repeater)**
  - plan: Injection backend is complete and thread-safe: WsRepeater registers each live tunnel (proxy_server.cpp:1038-1069, frames written on the owning worker via the frameQueued signal), POST /api/ws/send accepts {sessionId,direction,opcode,payload} (control_server.cpp:5689-5735) and GET /api/ws/sessions lists live sessions with framesUp/Down counts (control_server.cpp:2009-2024). Add NL.actions.wsSessions() and wsSend(sessionId,direction,opcode,payload) to real-data.js and a WS Repeater panel in proxy.jsx: session dropdown from /api/ws/sessions, a payload editor, direction+opcode selectors, and a resend-last button. Frame history would need a small backend buffer; the editor/inject/resend loop is frontend-only over the existing API.
  - evidence: ws_repeater backend registered at proxy_server.cpp:1038-1069; /api/ws/send control_server.cpp:5689-5735; /api/ws/sessions control_server.cpp:2009-2024 (summary counts only, no per-frame history); zero ws/send or ws/sessions references in ui-v2/*.jsx or real-data.js.
- **#280 [proxy] (hours) [verified] — Save/export proxy history items (HAR export + import)**
  - plan: Exceeds Burp confirmed: full HAR export AND import (path or raw object) at control_server.cpp:5735-5766, where Burp's HAR is extension-only. The gap is a UI affordance: exportRedact defaults ON and the UI's exportHar() posts no redact flag (real-data.js:169), so a colleague's HAR may lack the auth material to reproduce the bug — but the backend already honors a body {redact:false} (control_server.cpp:5742-5743). Add an 'include secrets / unredacted' checkbox to the export UI that posts redact:false, and surface a clear warning. Optional separate small backend generator for Burp-XML interop (not required for close).
  - evidence: control_server.cpp:5735-5748 (/api/har/export honors body redact, default from exportRedact() ON); 5749-5766 (/api/har/import path or raw har); real-data.js:169 exportHar() posts no redact flag.
- **#296 [proxy] (hours) [verified] — Intercept Action menu: send held message to Repeater/Intruder/Comparer/Decoder/Scanner**
  - plan: Confirmed: the intercept action row is DROP/FORWARD only (tabs.jsx:313-322), whereas the history DetailPane already has 'Send to Repeater' (proxy.jsx:264). The current pending exposes text/host/port/tls (intercept.hpp Q_PROPERTYs). Plan: add a 'Send to Repeater' button in the tabs.jsx intercept panel that reuses the SAME dispatch/control path proxy.jsx already calls, seeded from `current` (host/port/tls + editedText). Repeater ingest exists today, so that pivot is a few hours of frontend. Intruder/Comparer/Decoder targets can follow as their ingest endpoints allow; Repeater alone gets the core parity. Mostly JSX; possibly a thin control shim if the existing send-to-repeater endpoint needs raw-bytes input.
  - evidence: tabs.jsx:318-321 (DROP/FORWARD only); proxy.jsx:264 (existing 'Send to Repeater' on history — the reusable path)
- **#302 [repeater] (hours) [verified] — Repeater: tab renaming (double-click) and auto-naming**
  - plan: At functional parity; only cosmetic gaps. Rename works via double-click -> browser prompt() (tabs.jsx:116-119, bound onDoubleClick at :141) -> repeaterTabRename -> Repeater::renameTab (repeater.cpp:202-207). Auto-naming from the request line confirmed (autoTabName repeater.cpp:43-51) but applied only in addTabFromHistory/loadFromHistory; a plain addTab still gets `tab N` (repeater.cpp:156-157). To close the cosmetic gap: in tabs.jsx replace the prompt() with an inline contentEditable/input on double-click, and add optional per-tab colour/highlight labels (store a colour on RepeaterTab + emit in the snapshot). Low impact.
  - evidence: D:/dev/Nullock/ui-v2/tabs.jsx:116-119 `const next = prompt("Rename tab", cur); ... NL.actions.repeaterTabRename(i,next)` + :141 onDoubleClick; Src/Core/Networking/repeater.cpp:202-207 renameTab, :43-51 autoTabName, :156-157 plain addTab `tab %1`; endpoint control_server.cpp:3167-3171.
- **#306 [repeater] (hours) [verified] — Repeater: "Copy as curl" / copy request to other tooling**
  - plan: Confirmed: the 9-target COPY AS menu (curl/wget/httpie/powershell/fetch/sqlmap/postman/nuclei/burp-raw) lives only in the proxy detail pane (proxy.jsx:289-314) and calls renderRequestAs(kind,row,req) which needs a history `row` for scheme/host/port/URL (parseRawRequest proxy.jsx:406-412). It is unreachable from the Repeater where the request was actually finished. Close by synthesizing a pseudo-row from the active RepeaterTab (host/port/tls known; parse the method+path from the first request line) and reusing renderRequestAs, then add the same COPY AS dropdown to the repeater UI (tabs.jsx / repeater pane). Pure frontend; this is exactly tracked task #121.
  - evidence: D:/dev/Nullock/ui-v2/proxy.jsx:289-314 COPY AS menu -> :302 renderRequestAs(k,row,req); :406-412 parseRawRequest needs row for URL; :463 renderRequestAs(kind,row,raw); no COPY AS in tabs.jsx repeater UI.
- **#313 [repeater] (hours) [verified] — Comparer: colour-coded diff (added/deleted/unchanged)**
  - plan: At parity: three coalesced classes eq/ins/del from push() (compare.cpp:106-120), rendered green/red/strike in ComparerTab segStyle (app.jsx:1474-1480, 1527-1528). Optional Burp nicety (a distinct 'modified' class for in-place value changes vs a delete+add pair): post-process the segments array in ComparerTab to fold an adjacent del+ins pair into a `mod` render class. Pure frontend, low impact.
  - evidence: D:/dev/Nullock/Src/Core/Networking/compare.cpp:106-120 push/eq/ins/del; ui-v2/app.jsx:1474-1480 segStyle, :1526-1528 segment render.
- **#315 [repeater] (hours) [verified] — Comparer: compare two proxy-history items directly**
  - plan: Still exceeds Burp (MARK/DIFF two history rows directly, proxy.jsx:335-353, DiffOverlay :620). STALE-VERDICT CORRECTION: the prior audit's id-vs-index defect (`requestRawAt(idA-1)`) is FIXED — DiffOverlay now fetches by id via NL.requestRawById(idA)/responseRawById(idA) which hit /api/history/<id>/request (proxy.jsx:631-634; real-data.js:96-109), so it no longer diffs the wrong pair after eviction. Two residual defects remain: (1) a second, divergent diff engine — client-side diffLines (proxy.jsx:585-607) with different tokenization from the server Compare — two engines to maintain; (2) the real bug still open: diffLines allocates an UNBOUNDED n*m Int32Array DP table (proxy.jsx:590) with no token cap, so a large response will hang/OOM the tab. Closeable now: add a line cap (mirror kMaxTokens, clip A/B and show a truncated badge) to diffLines. Pure frontend, hours. Unifying onto the server engine is the larger, separate cleanup.
  - evidence: D:/dev/Nullock/ui-v2/proxy.jsx:335-353 MARK/DIFF, :620-638 DiffOverlay uses requestRawById/responseRawById (by id, FIXED), :585-607 diffLines, :590 `new Int32Array(m+1)` unbounded n*m; real-data.js:96-109 by-id fetch.
- **#327 [repeater] (hours) [verified] — Inspector: "Request attributes" (method, path, HTTP version/protocol)**
  - plan: Engine + endpoint are complete and correct; the sole defect is zero UI consumers (no inspect() in real-data.js). Add NL.actions.inspect(raw,kind) posting to /api/inspect, and an Inspector side-panel in app.jsx that renders view.method/path/version read-only alongside the Repeater request pane. That closes the reachability defect in hours. Inline editing of method/path/version (rewriting the raw request line) is a separate, larger write-back effort shared with 328/331.
  - evidence: inspector_logic.cpp:157-168 method/target/version/path extraction; control_server.cpp:9442-9454 /api/inspect live; NO inspect action in ui-v2/real-data.js actions (verified 140-226), no Inspector component in app.jsx.
- **#332 [repeater] (hours) [verified] — Inspector: JWT decoding of tokens found in headers and cookies**
  - plan: collectJwts auto-discovers Bearer/cookie/Set-Cookie JWTs and the endpoint already returns view.jwts[] with alg/typ/header/payload/where; the ONLY gap is nobody calls /api/inspect. Render view.jwts in the shared Inspector panel (see 327). Pure frontend, hours. (Standalone Decoder JWT already ships, so the delivered JWT story is non-zero.)
  - evidence: inspector_logic.cpp:125-149 collectJwts (Bearer strip + cookie scan); inspectRequest :228-230 / inspectResponse :266-268 out[jwts]; control_server.cpp:9442-9454 returns view; no inspect() in real-data.js.
- **#341 [repeater] (hours) [verified] — Repeater: response "Render" tab (renders the HTML response)**
  - plan: Confirmed absent: response pane is a single readOnly textarea, no view tabs/iframe. Add a 'Render' view to the response pane in tabs.jsx that strips headers and injects the body into a sandboxed <iframe sandbox srcdoc={body}> (no allow-same-origin, so it renders in a null origin and cannot self-XSS the control server). Frontend-only, hours; ties in with 342's view-tab scaffolding.
  - evidence: tabs.jsx:236-243 response pane is one <textarea className="txt readonly" readOnly>; no iframe/srcdoc; proxy preview (proxy.jsx:1102-1108) only pretty-prints JSON, no render.
- **#342 [repeater] (hours) [verified] — Repeater: message editor views — Pretty / Raw / Hex for request and response**
  - plan: Confirmed absent in Repeater (two bare textareas) but the exact primitives already exist in the proxy pane: renderView(raw,view) and toHexDump(s) (64 KiB cap). Lift both into a shared module, add a view-tab row (raw/pretty/hex, plus preview/render on the response) above each Repeater pane in tabs.jsx with local useState, mirroring proxy.jsx:349-366. Frontend, hours. High value (binary/gzipped responses currently unreadable in Repeater).
  - evidence: tabs.jsx:222-243 both Repeater panes are bare textareas with no view tabs; proxy.jsx:349-366 view-tab rows; renderView at proxy.jsx:1093, toHexDump at proxy.jsx:1113.
- **#347 [repeater] (hours) [verified] — Repeater: keyboard shortcut to send (Ctrl+Space / configurable hotkeys)**
  - plan: The core Ctrl+Enter/Ctrl+Space-to-send is minutes of frontend work. In ui-v2/tabs.jsx RepeaterTab the request textarea (lines 228-233) has no onKeyDown; add onKeyDown={e => { if ((e.ctrlKey\|\|e.metaKey) && (e.key==="Enter"\|\|e.key===" ")) { e.preventDefault(); send(); } }} (send() already defined at tabs.jsx:106). No ctrlKey/metaKey handlers exist anywhere in ui-v2 today (only vendor/react-dom). A full *configurable* hotkey layer is larger, but that is not needed for parity on the core send loop.
  - evidence: tabs.jsx:216-218 SEND is onClick-only; tabs.jsx:228-233 request textarea has no key handler; grep of ui-v2 shows ctrlKey/metaKey only in vendor/react-dom.development.js, none in app/tabs/proxy jsx.
- **#352 [repeater] (hours) [verified] — Comparer: "Send to Comparer" from any Burp message**
  - plan: Reducer (app.jsx:49-101) has no comparer case; proxy DetailPane send-to buttons are REPEATER/INTRUDER only. Minimal version (hours): add a `send-to-comparer` reducer case that switches tab to comparer and fills the next empty slot (A then B) with the row's raw message, plus a COMPARER button in the proxy DetailPane next to REPEATER/INTRUDER (proxy.jsx ~:264-265 / :1225,1250 pattern). Richer version appends to the item list from 351. Frontend only.
  - evidence: app.jsx:49-101 reducer cases (no comparer); proxy.jsx:1250 onSendIntruder dispatch pattern; DetailPane exposes REPEATER/INTRUDER send-to only.
- **#354 [repeater] (hours) [verified] — Comparer: Text / Hex toggle on the results view**
  - plan: ComparerTab control bar (app.jsx:1512-1514) has only words/lines/chars. Add a Text/Hex toggle that, in Hex mode, renders A and B through the existing toHexDump (proxy.jsx:1113) before diffing/displaying. Honest caveat: the differ is QString-based and blobs arrive as text, so a hex view is faithful for text but cannot recover true binary bytes (the bytes are already lossy — same A11/A19 root cause). Frontend affordance is hours; full binary fidelity would additionally require backend byte-preservation (separate, larger).
  - evidence: app.jsx:1512-1514 Comparer buttons words/lines/chars only; toHexDump lives at proxy.jsx:1113 and is not imported into app.jsx.
- **#358 [repeater] (hours) [verified] — Decoder: Text / Hex view toggle per block**
  - plan: DecoderTab (app.jsx:1536-1583+) renders input/output textareas with no view control. Add a Text/Hex toggle over the output that renders through toHexDump (proxy.jsx:1113) in Hex mode. Honest caveat: the backend transcode returns QString via QString::fromUtf8 for hex/base64 decode, so the raw bytes are already collapsed before the view sees them — the hex toggle is faithful for text output but a truly binary-correct hex view requires the backend to return a byte buffer (base64 of bytes) rather than a lossy QString (larger, backend change). Frontend affordance alone is hours.
  - evidence: app.jsx:1536-1583 DecoderTab input/output textareas + button bar (smart decode / identify hash / 18 OPS), no view toggle; toHexDump proxy.jsx:1113 not imported; transcode.cpp hexDecode/base64Decode return QString::fromUtf8 (lossy).
- **#362 [repeater] (hours) [verified] — Inspector: "Selection" widget — character count and ASCII decimal/hex values for highlighted text**
  - plan: proxy.jsx:244-248 grabFrom already reads textarea selection (selectionStart/selectionEnd) but only runs a transform into an overlay — no char count, no ASCII readout. Add a small self-contained widget: on selection change in the Repeater/message textareas, compute selection length and, for a short selection, list each char's decimal + hex code (including non-printing bytes shown as e.g. 0x0d) via a simple JS map over the selected string's code units. Render in a small popover or in the Inspector panel (359). Frontend, hours. Medium confidence on exact byte semantics for multibyte/UTF-16 chars (present code units, note the caveat).
  - evidence: proxy.jsx:244-248 grabFrom selection -> runCodec transform overlay only; sole selectionStart use in ui-v2; no char-count / ASCII widget anywhere.
- **#61 [scanner] (hours) [verified] — [A13] Active audit: OS command injection (incl. blind/OAST)**
  - plan: Present — and the prior gap ('output-channel only, no blind') is partly STALE. Output-channel: CmdInjection module with command-substitution arithmetic-proof across 9 chaining techniques (cmd_injection.cpp:19-32,100-125) + GUI per-row probe (control_server.cpp:2362 uid= check). BLIND/OAST IS implemented: /api/oast/blast fires curl-to-sink RCE payloads in 5 shell forms (control_server.cpp:3779-3812 rce-oast) plus Log4Shell JNDI (:3719-3749) — arguably exceeds Burp. The only real gap: the OAST blast has NO GUI trigger (ui-v2 shows the OAST domain at app.jsx:1714 but no blast button). Closeable in hours: add an 'OAST blast' action in the history/issues UI calling /api/oast/blast {rce:true,ssrf:true,xxe:true,log4shell:true}. Frontend.
  - evidence: cmd_injection.cpp:20-31 9 techniques; :117-122 command-substitution product proof (no time/blind in module); control_server.cpp:2362 GUI-probe uid=; control_server.cpp:3779 wantRce rce-oast curl-to-sink; :3719 log4shell; ui-v2 has no oast/blast button (only app.jsx:1714 oastDomain display)
- **#63 [scanner] (hours) [verified] — [A15] Active audit: XXE injection**
  - plan: Present and strong (arguably exceeds Burp): classic external-entity + XInclude(parse=text) payloads with baseline + inert-DOCTYPE control subtraction to defeat error-page false positives (xxe_injection.cpp:21-89), plus blind OAST XXE via external entity to the sink (control_server.cpp:3754-3771). Reachable via /api/audit (XML-gated, control_server.cpp:656-669), the dedicated /api/xxe/test (:7551), and /api/oast/blast. Prior gap holds only in that there is NO GUI trigger and it is not in the per-row probe battery (which is query-param based; XXE needs an XML body). Closeable in hours: add a GUI 'XXE test' button on rows with an XML content-type wired to /api/xxe/test (or include the OAST XXE in the blast button from A13). Frontend.
  - evidence: xxe_injection.cpp:21-44 classic+XInclude payloads; :70-80 inert-control subtraction; control_server.cpp:659 XML content gate; :7551 /api/xxe/test; :3754 blind xxe-oast; ui-v2 has no XXE trigger
- **#66 [scanner] (hours) [verified] — [A18] Active audit: LDAP injection**
  - plan: Feature is present: full LdapInjection::test runs in the default deep-audit sweep (control_server.cpp:685-693) and via /api/ldapi/test (7671). Prior gap 'not in GUI probe battery' is now STALE - the per-row GUI probe DOES fire an ldap-injection-suspect heuristic (control_server.cpp:2516-2531). Only remaining gap is that the high-fidelity tester is reachable only by API. Close by adding an 'Audit all rows (deep)' button in app.jsx (next to 'Probe all rows', ~line 1007) + an auditAll action in real-data.js posting /api/audit/all {fromHistory:true} - that endpoint's default sweep already runs ldapi. One frontend change closes this for the whole API-only cluster (66/67/70/71/74/75/76/77) at once.
  - evidence: control_server.cpp:689 LdapInjection::test (deep audit); :2531 report("medium","ldap-injection-suspect") GUI probe; :7671 /api/ldapi/test; xpath_injection.cpp:100 confirms Core testers populate real hits
- **#67 [scanner] (hours) [verified] — [A19] Active audit: XPath injection**
  - plan: Present: XpathInjection::test (error-signature + engine-specific boolean classifier, xpath_injection.cpp:100 / xpath_logic.cpp) runs in the deep-audit default sweep (control_server.cpp:695-703) and via /api/xpathi/test (7719). Prior gap 'API-only; not in GUI' STILL HOLDS - unlike ldap/nosql, there is NO xpath entry in the per-row probe battery (grep of 2318-2880 shows none). Close via the same shared 'Deep audit all rows' GUI button (see id 66), which invokes /api/audit/all whose default sweep already includes xpath. Alternatively add an xpath block to the per-row probe loop.
  - evidence: control_server.cpp:699 XpathInjection::test (deep audit); :7719 /api/xpathi/test; xpath_injection.cpp:100 result.hits.append; NO xpath match in per-row probe 2318-2880
- **#69 [scanner] (hours) [verified] — [A21] Active audit: HTTP request smuggling (CL.TE / TE.CL)**
  - plan: Present. Full Smuggling::test runs opt-in in the deep-audit sweep (control_server.cpp:635-643, gated on include contains 'smuggle' because timing probes are slow) and via /api/smuggle/test (7469-7486). Prior gap 'API-only; a GUI-only user cannot run smuggling' is now STALE - the per-row GUI probe hand-crafts and fires a CL+TE desync probe reporting http-smuggling-clte-suspect (control_server.cpp:2734-2771). Remaining nit: the full multi-variant tester still needs the opt-in API. Close via the shared deep-audit GUI button (id 66) passing include:['smuggle'], or expose a 'smuggling (slow)' checkbox.
  - evidence: control_server.cpp:635 opt-in smuggle gate; :639 Smuggling::test; :2771 report("high","http-smuggling-clte-suspect") GUI per-row probe; :7479 /api/smuggle/test
- **#70 [scanner] (hours) [verified] — [A22] Active audit: Host header injection / poisoning**
  - plan: Present. HostHeader::test runs in the default deep-audit sweep (control_server.cpp:610-630, Host-line->Location is high, URL-context reflection is medium) and via /api/hostheader/test (8439). Prior gap 'API-only; not in GUI' MOSTLY HOLDS - the per-row GUI probe only fires an X-Forwarded-Host cache-poison variant (control_server.cpp:2779-2803), not host-header-injection proper. Close via the shared 'Deep audit all rows' GUI button (id 66); its default sweep already runs hostheader.
  - evidence: control_server.cpp:615 HostHeader::test; :621 fromHostLine && where=='Location'->high; :8439 /api/hostheader/test; :2801 GUI probe is cache-poison-xfh only (not host-header-injection)
- **#71 [scanner] (hours) [verified] — [A23] Active audit: web cache poisoning**
  - plan: Present. Full CachePoison::test runs in the default deep-audit sweep with a confirmed/cacheable/unkeyed severity ladder (control_server.cpp:595-608) and via /api/cache/poison (8276). Prior gap 'API-only' is now STALE - the per-row GUI probe fires an X-Forwarded-Host cache-poison probe (control_server.cpp:2779-2803). Remaining: full unkeyed-header battery is API-only. Close via shared deep-audit GUI button (id 66).
  - evidence: control_server.cpp:600 CachePoison::test; :602-605 confirmed/cacheable/unkeyed ladder; :2801 report("high","cache-poison-xfh") GUI probe; :8276 /api/cache/poison
- **#74 [scanner] (hours) [no-verdict] — [A26] Active audit: insecure deserialization**
  - plan: Present. DeserProbe::test runs in the default deep-audit sweep with per-format kind labeling (control_server.cpp:717-728, kindForFormat) and via /api/deser/test (7821). Prior gap 'API-only; not in GUI' STILL HOLDS - no deser entry exists in the per-row probe battery. Close via the shared 'Deep audit all rows' GUI button (id 66); its default sweep already runs deser.
  - evidence: control_server.cpp:721 DeserProbe::test; :726-727 kindForFormat; :7821 /api/deser/test; NO deser match in per-row probe 2318-2880
- **#75 [scanner] (hours) [verified] — [A27] Active audit: NoSQL injection**
  - plan: Present. Full NoSqlInjection::test runs in the default deep-audit sweep (control_server.cpp:645-653) and via /api/nosqli/test (7522). Prior gap 'API-only; not in GUI' is now STALE - the per-row GUI probe fires a nosql-injection-suspect heuristic (control_server.cpp:2489-2507). Remaining: full operator-injection tester is API-only. Close via shared deep-audit GUI button (id 66).
  - evidence: control_server.cpp:649 NoSqlInjection::test; :2507 report("medium","nosql-injection-suspect") GUI probe; :7522 /api/nosqli/test
- **#76 [scanner] (hours) [verified] — [A28] Active audit: mass assignment / auto-binding**
  - plan: Present. MassAssign::test(defaultFields) runs in the deep-audit sweep, gated on a non-empty body (control_server.cpp:559-569) and via /api/massassign/test (8870). Prior gap partially STALE - the per-row GUI probe fires a mass-assignment-suspect heuristic (control_server.cpp:2540-2554). Genuine remaining limits (default field names only, no app-model awareness) are inherent to blind mass-assignment testing (Burp has the same limitation). Close the GUI-reachability via the shared deep-audit button (id 66).
  - evidence: control_server.cpp:566 MassAssign::test; :559 gated on !t.body.isEmpty(); :2554 report("high","mass-assignment-suspect") GUI probe; :8870 /api/massassign/test
- **#77 [scanner] (hours) [verified] — [A29] Active audit: CORS active probing (attacker-origin reflection)**
  - plan: Present. CorsTester::test runs in the default deep-audit sweep, tracking worst-probe severity and using the real probe kind rather than a hardcoded credentialed label (control_server.cpp:524-542) and via /api/cors/test (8804). Prior gap 'API-only; no GUI trigger' STILL HOLDS - no cors entry in the per-row probe battery. Close via the shared 'Deep audit all rows' GUI button (id 66); its default sweep already runs cors.
  - evidence: control_server.cpp:528 CorsTester::test; :533-541 worst-severity + real-kind labeling; :8804 /api/cors/test; NO cors match in per-row probe 2318-2880
- **#368 [target] (hours) [verified] — Site map populates from the crawler / automated crawl**
  - plan: The engine, the model wiring AND the control endpoints already exist — only the UI affordance is missing. Add NL.actions.crawlerStart(seed,{maxPages,maxDepth,throttleMs}) and crawlerStop() to ui-v2/real-data.js POSTing /api/crawler/start and /api/crawler/stop (control_server.cpp:4002-4013), then add a seed input + START/STOP CRAWL control (Recon tab or the Proxy site-map pane). Crawl results already flow into ProxyModel/site map via app.cpp wiring, so they appear automatically. (Host-granularity of the output is item 366.)
  - evidence: Engine real: crawler.hpp start/stop. Control endpoints NOW present: control_server.cpp:4002 `/api/crawler/start` -> m_wiring.crawler->start(...), cpp:4012 `/api/crawler/stop`. But grep for crawl across ui-v2/*.jsx and ui-v2/real-data.js returns nothing — no button, no action wired.
- **#376 [target] (hours) [verified] — Scope UI actions: Add / Edit / Remove / Paste URL / Load-from-file on both tables**
  - plan: Pure ui-v2/tabs.jsx work in the two scope columns: (1) Edit — click a glob to load it into the input and remove-then-re-add on save; (2) Paste-URL — a button that parses a URL and derives a *.host (or host) glob via new URL(); (3) Load — an <input type=file> that splits lines and dispatches scope-add-in/out per non-empty line. All dispatch through the existing scope-add/scope-remove reducer actions.
  - evidence: tabs.jsx:48-67 (in) and 76-95 (out) provide only ADD (button + Enter) and a per-row × remove (scope-remove-in/out). No edit, paste-URL, or file-load control present.
- **#387 [target] (hours) [no-verdict] — Engagement tools > Generate CSRF PoC**
  - plan: Backend is complete and unused — wire it into the DetailPane. In ui-v2/proxy.jsx, add a 'CSRF PoC' entry to the existing COPY-AS menu (~275-355) that POSTs /api/csrf/poc {id: row.id} (control_server.cpp:3908) and shows result.html in the existing overlay component; add a 'preview in browser' via a Blob URL. Add NL.actions.csrfPoc(id) in real-data.js. Full parity (technique selector, GET/POST/multipart variants) can follow, but basic right-click PoC generation is the closeable win.
  - evidence: Endpoint present and careful: control_server.cpp:3908-3940 `/api/csrf/poc` accepts {id} or explicit fields, loads the full row, calls RequestExport::csrfPoc, returns {html,note}. No ui-v2 caller (grep for csrf/poc in ui-v2 finds only unrelated header placeholders and real-data.js:118 CSRF-guard header).
- **#392 [target] (hours) [no-verdict] — Site map filter by request type: in-scope only / requested only / parameterized only / hide not-found**
  - plan: Confirmed absent: ProxyFilterModel exposes only hostSubstring/statusClass/methodFilter/hiddenCount (proxy_filter_model.hpp:17-20), and ui-v2 HistoryTable filters on host/search/statusClass/methodFilter (proxy.jsx:22-54). But every primitive for a pragmatic-complete version already exists CLIENT-SIDE: rows carry r.params (proxy.jsx:34) and r.status, and the scope list is available via scopeAdd/inScopeList. Add four toggle chips in proxy.jsx: parameterized-only (r.params>0), hide-not-found (r.status!==404), in-scope-only (client-side glob-match r.host against the scope list), and requested-only (trivially all, since nothing is ever inferred). Pure frontend, no backend change. 'Requested vs inferred' is moot until inference (394) lands but the filter is satisfiable now. ParamsRole (proxy_model.hpp:21) exists if the Qt UI ever needs it too.
  - evidence: proxy_filter_model.hpp:16-20 (three filter dims only); ui-v2/proxy.jsx:22-54 (host/search/status/method filters); proxy.jsx:34 rows carry params; proxy_model.hpp:21 ParamsRole exists (unused as filter).
- **#396 [target] (hours) [no-verdict] — Scope: 'Include subdomains' checkbox on a prefix rule**
  - plan: Confirmed absent: ProjectMeta scope is flat QStringList inScope/outOfScope (project_store.hpp:17-18), grep includeSubdomains/subdomain finds only recon-engine hits, none in scope code. `*.example.com` already works as a wildcard glob (proxy_server.cpp ~1317 glob->regex), so the honest complete fix is a UI convenience: an 'Include subdomains' checkbox next to the scope-add input in tabs.jsx that, when checked, adds BOTH `example.com` and `*.example.com` via the existing scopeAddIn action (real-data.js scopeAddIn -> /api/scope/in/add). Frontend-only, complete, verifiable in hours; also fix the misleading placeholder tabs.jsx:62 'e.g. *.acme.corp'. A proper per-rule flag would require the rule struct from item 397, so use the two-entry approach.
  - evidence: project_store.hpp:17-18 (flat QStringList scope); grep includeSubdomains\|subdomain in Src -> only recon hits (control_server.cpp:1358 etc), none in scope; real-data.js scopeAddIn -> /api/scope/in/add exists.
- **#398 [target] (hours) [no-verdict] — 'Add to scope' / 'Remove from scope' from a right-click context menu on any site map / Proxy history item**
  - plan: Confirmed absent: grep onContextMenu/contextmenu across ui-v2/*.jsx,*.js -> no matches (there is NO context-menu infrastructure). Map rows are selection-only (proxy.jsx:193-196 onClick=onSelect(e.host); app.qml:176-183 MouseArea toggles hostSubstring). But the backend scope endpoints ALREADY exist and are wired: real-data.js scopeAddIn/scopeRemoveIn/scopeAddOut/scopeRemoveOut -> /api/scope/in\|out/add\|remove (control_server.cpp:3049-3064). So this is pure frontend: add an onContextMenu handler to history/site-map rows in proxy.jsx that opens a small menu (or even a shift-click) calling scopeAddIn(host)/scopeRemoveIn(host). Complete and verifiable in hours. Building the reusable context-menu component is the only real work.
  - evidence: grep onContextMenu\|contextmenu ui-v2 -> empty; proxy.jsx:193-196 rows onClick-only; app.qml:176-183 MouseArea toggle-only; control_server.cpp:3049-3064 scope add/remove endpoints exist; real-data.js scopeAddIn/scopeRemoveIn wrappers present.
- **#2 [intruder] (day) [verified] — Attack type: Pitchfork**
  - plan: Engine (zip-by-index) and full multi-set API exist; the missing piece is a per-position set editor in ui-v2 (single textarea only fills set 0). Build a set editor that renders N payload boxes driven by intruder.positions (positionCount from snapshot), each POSTing via /api/intruder/set payloadSets[i]. Primitives all exist server-side (setPayloadSets, setPayloadSetAt, syncSetsToPositions in intruder.cpp:181-235). Frontend-only, ~1 day for a clean N-column editor + the attack-type picker.
  - evidence: intruder_engine.cpp:86-104 (minLen zip); control_server.cpp:3221-3233 accepts payloadSets (array or newline string per position); snapshot exposes positions/payloadSets at control_server.cpp:1514-1521; tabs.jsx:699-704 is a single textarea → set 0 only.
- **#3 [intruder] (day) [verified] — Attack type: Cluster bomb**
  - plan: Same closure as Pitchfork: reuses the per-position multi-set editor + attack-type picker. Engine odometer/cartesian product is complete and capped by maxResults; API identical. No backend work. Ship items 2 and 3 together in the one frontend set-editor build.
  - evidence: intruder_engine.cpp:105-129 (odometer cartesian product, skips empty positions, maxResults cap); control_server.cpp:3221-3233 payloadSets wiring; ui-v2 exposes no per-position editor.
- **#5 [intruder] (day) [verified] — Auto-mark payload positions (Add/Clear/Auto §)**
  - plan: Confirmed partial: the only auto-marker is send-to-intruder which regexes just the LAST param value (app.jsx:77); the Intruder tab has no Add/Clear/Auto buttons and the discovery template hardcodes 'GET /§§'. Add to the TEMPLATE pane header (tabs.jsx ~616-628): 'Add §' (wrap current textarea selection), 'Clear §' (strip all § from template), and 'Auto §' (JS request parser marking every query-param value, form-body value, and cookie value). Frontend-only; the substitution engine already consumes whatever markers exist. ~1 day for a robust auto-marker.
  - evidence: app.jsx:77 tmpl=req.replace(/(=)([^&\s\n]*)$/m,'$1§payload§') marks only the last param; tabs.jsx:471-478 discovery hardcodes GET /§§; no Add/Clear/Auto controls in IntruderTab (tabs.jsx:616-705).
- **#7 [intruder] (day) [verified] — Payload type: Numbers (range/step/hex/padded)**
  - plan: Engine + API complete and overflow-safe; only GUI is missing (no generator control anywhere in ui-v2). Add a 'Generator ▾' dialog in the PAYLOADS pane offering numbers/dates/brute. For numbers: from/to/step/width/hex fields → POST /api/intruder/generate for a live count+sample preview, then POST /api/intruder/set {generator:{type:'numbers',...}} on apply. Discovery of types via /api/intruder/generator-types. Frontend-only, ~1 day covering all three generators (items 7-9).
  - evidence: intruder_generators.cpp:23-39 numbers() (from/to/step/width/hex, stops before qint64 wrap); control_server.cpp:3183-3199 expandGen, 3310-3325 /generate preview, 3326-3331 /generator-types; grep of ui-v2 for 'generator' returns only vendor libs.
- **#8 [intruder] (day) [verified] — Payload type: Dates**
  - plan: Present engine+API; no GUI. Closed by the same generator dialog as item 7 (dates tab: from/to ISO, stepDays, format string). API path /api/intruder/set {generator:{type:'dates',...}} already wired. No backend change.
  - evidence: intruder_generators.cpp:76-87 dates() via QDate::addDays, kMaxCount cap; control_server.cpp:3194-3197 expandGen dates branch.
- **#9 [intruder] (day) [verified] — Payload type: Brute forcer**
  - plan: Present engine+API (code-point-correct enumeration, ~6.4M char-volume cap); no GUI. Same generator dialog (brute tab: charset, minLen, maxLen). Use /api/intruder/generate preview so the user sees the count/cap before applying. No backend change.
  - evidence: intruder_generators.cpp:41-74 brute() over charset.toUcs4(), kMaxChars=kMaxCount*64 cap; control_server.cpp:3191-3193 expandGen brute branch.
- **#10 [intruder] (day) [verified] — Processing rule: Add prefix**
  - plan: Rule present in engine+API; no GUI rules editor. Build a payload-processing rule-chain editor in the IntruderTab: an ordered list of {op,arg} rows (op dropdown populated from GET /api/intruder/rule-ops), POST via /api/intruder/set {rules:[...]}. One editor closes items 10,11,13(partial),14,15. Frontend-only, ~1 day; wire attackType/rules into intruder-start POST so they persist a fresh run.
  - evidence: intruder_rules.cpp:13 prefix; control_server.cpp:3247-3261 builds rule chain from JSON, 3304-3308 /rule-ops lists op names; no rules editor in ui-v2 (app.jsx RULES tab at 718+ is the match/replace proxy rules, unrelated to intruder payload rules).
- **#229 [platform] (day) [verified] — Collaborator client (dedicated panel: mint N payloads, copy, poll now, view interaction detail)**
  - plan: Backend endpoints exist and are allowlisted: POST /api/oast/mint (token+pathUrl+hostUrl), GET /api/oast/poll?since=<id> (returns full HTTP hit records: id,atMs,token,sourceIp,method,hostHeader,path,bodyBytes,userAgent,bodyPreview), POST /api/oast/blast. UI is only a status badge (app.jsx:1714 'OAST: {oastDomain}'), no panel, no NL.actions entry. To close: build a Collaborator tab in ui-v2 that calls /api/oast/mint (copy-to-clipboard), polls /api/oast/poll with a running `since` cursor, renders a hit list + a detail pane from the fields already returned, and a 'poll now' button. Caveat/known limit: DNS callbacks are NOT surfaced by /api/oast/poll (only OastServer HTTP hits), so DNS-only interactions stay invisible until #230/DNS wiring — call that out in the panel. Files: new ui-v2 tab + tabs.jsx/app.jsx registration.
  - evidence: control_server.cpp:1927 `if (path=="/api/oast/poll")` returns hits[] with bodyPreview etc (1935-1954); :1669 `\|\| p=="/api/oast/poll"` in isReadPath; :3593 `/api/oast/mint`; :3619 `/api/oast/blast`. poll only iterates m_wiring.oast->hitsSince — no DNS merge. ui-v2 oast grep = only badge at app.jsx:1714-1715.
- **#247 [platform] (day) [no-verdict] — Hotkeys (configurable keyboard shortcuts) and the command palette**
  - plan: The entire keyboard surface is per-input Enter handlers and Escape-to-close (proxy.jsx:622-624, :782; app.jsx per-field onKeyDown) -- ctrlKey/metaKey/hotkey grep over ui-v2/*.jsx returns nothing, so there is no global shortcut layer and no palette. This is frontend-only and the primitives exist: NL.actions (real-data.js:128) is already a named action registry. Add a single window-level keydown dispatcher in app.jsx mapping defaults (Ctrl+R send-to-repeater, Ctrl+Shift+R, Ctrl+O open, Ctrl+K command palette), and a palette component that lists/filters NL.actions and invokes the selected one. Solid default-hotkeys + palette is ~1 day; user-configurable bindings (persisted rebinds) are a stretch that depends on the missing settings store (item 237).
  - evidence: ui-v2/proxy.jsx:622-624 (Escape-only keydown), :782; app.jsx per-field Enter handlers; ctrlKey/metaKey grep over ui-v2/*.jsx -> empty; NL.actions registry at real-data.js:128
- **#267 [proxy] (day) [verified] — HTTP history filter bar (in-scope only / hide items without responses / MIME / status / regex + negative + case-sensitive search / extension show-hide / annotation / listener)**
  - plan: Confirmed: FilterBar exposes only host, status-class, method, and a plain (always case-insensitive, no-negation) search + deep-search toggle (proxy.jsx:113-145, filter logic :23-53). Missing MIME filter, extension show/hide, hide-without-responses, in-scope-only, negative search, case-sensitivity. NOTE the prior 'oldest 500' evidence is STALE: deep body search now scans newest-first with an honest truncated flag (control_server.cpp:1818-1844, task #120). Closeable frontend-first: MIME filter (over existing r.mime), extension show/hide (derive from r.path), hide-without-responses (r.status truthy), negative search (leading '-'), case-sensitivity toggle -- all pure ui-v2 predicates in HistoryTable's useMemo (:23-53), ~day. In-scope-only needs a scope flag surfaced per row (backend has isInScope) and listener/annotation filters need the port (already in row) + an annotations store; those are small follow-ons.
  - evidence: proxy.jsx:113-145 (FilterBar: host/status/method/search only), :23-53 (table filter predicates), control_server.cpp:1818-1844 (search now newest-first + honest truncated, not oldest-500)
- **#269 [proxy] (day) [verified] — WebSockets history (separate tab, direction, length, comment, filtering)**
  - plan: WS frames are captured only on the MITM leg and surfaced as synthetic pseudo-HTTP history rows (emitMessage, proxy_server.cpp:1074-1099: method WS↑/WS↓, path a human string, resp.statusCode 101), and the only UI is two method-filter entries (proxy.jsx:114). Build a dedicated WebSockets view in proxy.jsx that filters rows where method starts with 'WS', renders direction/opcode/length/deflate columns from the existing synthetic fields, and adds a WS-only filter set. Per-message comment is the one piece needing a small backend store (a comment map keyed by row id + a POST endpoint); the tab/columns/filter themselves are pure frontend over data already emitted. Note capture is TLS-MITM-only (runWebSocketRelay is reached solely from the 101 branch at proxy_server.cpp:750-757); cleartext ws:// upgrades are not framed.
  - evidence: proxy_server.cpp:1074-1099 (emitMessage synthesizes HttpRequest/Response for each WS message); proxy.jsx:17-18 + 114 (WS↑/WS↓ are cosmetic method-filter classes only); runWebSocketRelay call site proxy_server.cpp:754-755 (MITM 101 only); no ws tab/columns in any ui-v2/*.jsx.
- **#298 [proxy] (day) [verified] — Response modification presets (unhide fields, enable disabled, remove JS/limits/<object>, HTTPS->HTTP, strip Secure)**
  - plan: Confirmed: none of the eight one-click removals ship; the only response-body mutation path is a user-authored MatchReplaceRule::RespBody regex (proxy_server.cpp match/replace apply). Tractable BECAUSE the RespBody/RespHeader regex rule engine already runs on every response: ship the eight presets as canned rules the user toggles (e.g. 'remove disabled=' -> RespBody regex; 'strip Secure' -> RespHeader Set-Cookie rewrite; 'HTTPS->HTTP links' -> RespBody). Plan: a preset registry (each preset = a fixed match/replace tuple) + eight checkboxes in the Match/Replace UI that add/remove those rules. Mostly frontend + a small preset table; ~1 day. Regex approximations match Burp's own approach (no DOM). Confidence medium: a couple presets (remove all JS) are coarse by nature.
  - evidence: grep for the eight removals -> only detection strings (header_logic.cpp:404, passive_scanner.cpp:458); MatchReplaceRule::RespBody is the sole body-mutation path (proxy_server.cpp ~1537 region)
- **#323 [repeater] (day) [verified] — Decoder: "Send to Decoder" from other tools / decode a selection in place**
  - plan: A canonical, backend-backed Decoder workbench now EXISTS (app.jsx:1536 DecoderTab -> NL.actions.transcode -> /api/transcode), so the prior 'not in the Decoder tab' claim is stale. What is still missing is (a) any 'Send to Decoder' affordance from Repeater/Proxy/Comparer and (b) unifying the proxy pane's second, divergent JS codec set. Fix: (1) route proxy.jsx CodecBar (proxy.jsx:747 onward) through /api/transcode instead of its own JS codecs so the two decoders stop disagreeing; (2) add a context/toolbar action in tabs.jsx Repeater panes and proxy detail that copies the current selection into the Decoder tab's input (set a shared window.NL.decoderSeed + switch tab). Pure frontend; backend transcode already canonical.
  - evidence: ui-v2/app.jsx:1536-1635 DecoderTab (backend-backed, 18 ops + smart + identify); ui-v2/real-data.js:224 transcode(); divergent JS codec set still lives in proxy pane ui-v2/proxy.jsx:744-757 CodecBar tools[] operating on selection; no 'send to decoder' action anywhere in real-data.js actions list (140-226).
- **#326 [repeater] (day) [verified] — Decoder: additional codecs beyond Burp (rot13, unicode escape, GraphQL, gRPC framing, CBOR, SAML)**
  - plan: rot13 + unicode-escape/unescape are backend ops now surfaced in the Decoder tab (exceeds Burp). GraphQL/gRPC/CBOR/SAML decoders still live ONLY as JS in the proxy pane (proxy.jsx ~933/963/1003-1033), so you cannot paste a gRPC frame or COSE blob into the workbench, and CBOR is self-declared partial (no tag handling). To close the gap: lift those four JS decoders into a shared module and add them as client-side ops in DecoderTab's OPS list (or add /api/transcode ops). Frontend port of existing code; add CBOR major-type-6 tag handling for full CBOR.
  - evidence: Backend rot13/unicode: transcode.cpp:109-143 registered at :234-235, in DecoderTab OPS ui-v2/app.jsx:1547. Protocol decoders still proxy-only: ui-v2/proxy.jsx:754-757 tools[] includes graphql/grpc/cbor/saml; CBOR partial comment proxy.jsx:1013-1014 'No tag handling.'
- **#348 [repeater] (day) [verified] — Repeater: search / highlight within the request and response editors**
  - plan: The two panes are plain <textarea> with only a LINES counter (tabs.jsx:226,240,228-233,242). A functional find (search box per pane with case + regex toggle, live match count, and next/prev that scrolls+selects via textarea.setSelectionRange) is achievable in ~a day of frontend work, giving most of Burp's value. TRUE inline highlight-all is the one blocker: a textarea cannot render colored spans, so highlight-all needs an absolutely-positioned mirror <div> overlay behind the textarea (or swapping to a contenteditable/editor component) — treat that as the stretch, not the MVP. No backend change needed.
  - evidence: tabs.jsx:222-243 both panes are plain textareas with ph-count LINES only; existing search is row-scoped via NL.actions.search (proxy.jsx history), not in-pane.
- **#351 [repeater] (day) [verified] — Comparer: item list — Paste / Load from file / Remove / Clear, sortable, pick any two**
  - plan: ComparerTab (app.jsx:1440-1534) is two useState textareas (a/b) + 3 mode buttons + inline diff. The backend diff already exists (/api/compare, NL.actions.compareBlobs). Build an item-list frontend: a React state array of items {label,text}, a table with Paste (from clipboard/textarea), Load-from-file (<input type=file> + FileReader.readAsText), Remove, Clear, and two radio/select columns to pick A and B, feeding the existing compareBlobs call. Persist the array in localStorage (respecting the preview-seeding caution in memory — stash/restore real telemetry keys). Substantial but self-contained frontend, ~a day. No backend change.
  - evidence: app.jsx:1440-1445 ComparerTab useState(a/b/mode/result/err); :1498-1506 two textareas placeholder first/second blob; :1512-1514 three mode buttons; no file input, item table, or persistence.
- **#353 [repeater] (day) [verified] — Comparer: two synchronized side-by-side result panels with "Sync views" scrolling**
  - plan: Comparer result is a single inline segment stream (app.jsx:1525-1531 result.segments.map). The aligned two-pane rendering primitive already exists: proxy.jsx:708-731 DiffOverlay uses a `display:grid; gridTemplateColumns:1fr 1fr` rows.map layout. Port that layout into ComparerTab, feeding it the /api/compare segment/row output (may need a small adapter from the compare segment shape to left/right rows, or reuse the DiffOverlay row engine). Add sync-scroll by mirroring onScroll scrollTop between the two panes (small). Frontend, ~a day; medium confidence because the two code paths use different diff shapes.
  - evidence: app.jsx:1525-1531 single inline segment stream; proxy.jsx:708-731 DiffOverlay two-column grid rows.map exists and is reusable.
- **#359 [repeater] (day) [verified] — Inspector: docked structured side panel in the message editors**
  - plan: Reclassify stub->partial: the engine is a COMPLETE implementation, not scaffold — /api/inspect is live (control_server.cpp:9442-9454) over inspectRequest/inspectResponse (inspector_logic.cpp:153-271) returning method/target/path/query/queryParams/headers/cookies/contentType/bodyParams/bodyKind/jwts (request) and version/status/reason/headers/setCookies/contentType/bodySize/bodyPreview/jwts (response). The ONLY missing piece is UI: grep of ui-v2 for 'inspect' returns nothing. Closeable read-only: add an NL.actions.inspect(raw,kind) call and a collapsible Inspector side panel component in the Repeater request/response panes (tabs.jsx RepeaterTab) that POSTs the current textarea content and renders the sections. ~a day, frontend only. (Write direction is item 360, separate.)
  - evidence: control_server.cpp:9442-9454 /api/inspect returns full view; inspector_logic.cpp:153-271 complete impl; ui-v2 grep for inspect/inspector returns zero non-vendor hits (no delivered UI).
- **#51 [scanner] (day) [verified] — [A3] Crawl configuration: max depth, max locations, crawl limits**
  - plan: Three knobs exist and are correctly bounded (crawler.cpp:67-69 maxPages qBound 1..5000, maxDepth 0..10, throttleMs 0..60000) but are settable ONLY via the /api/crawler/start JSON body (control_server.cpp:4006-4008 reads maxPages/maxDepth/throttleMs). Closeable in ~a day: (1) frontend — expose the three knobs in the new crawl panel from A1; (2) backend-small — add a maxUniqueLocations bound and a per-host page cap as two more qBound'd fields on Crawler::start + parse them in control_server.cpp:4005. Full 'coverage-strategy' toggles (breadth vs depth ordering) would be more, but a solid, complete config surface with the extra location cap is realistic in a day since the frontier/visited counters already exist (crawler.cpp m_seenUrls, m_visited).
  - evidence: crawler.cpp:67 'm_maxPages = qBound(1, maxPages, 5000); m_maxDepth = qBound(0, maxDepth, 10); m_throttleMs = qBound(0, throttleMs, 60'000);'; control_server.cpp:4006-4008 maxPages/maxDepth/throttleMs from bodyJson; no UI form (ui-v2 has no crawler controls)
- **#145 [sequencer] (day) [verified] — Manual load: paste / file / "Analyze now"**
  - plan: Backend is complete: POST /api/sequencer/analyze { tokens:[...] } (control_server.cpp:4697-4703) fully analyzes any token list. Only clients are the bash CLI (bin/nullock:713-725, argv tokens only -- no `-` stdin, no file, contrast cmd_chain which reads a file) and there is NO Sequencer surface in ui-v2 (grep of app.jsx: zero Sequencer/sequencer hits). Closeable: (1) add a Sequencer tab to ui-v2/app.jsx -- a <textarea> for newline-delimited paste + a file <input type=file> reading .text() + an 'Analyze now' button POSTing to /api/sequencer/analyze via NL.actions, rendering score/verdict/shannon/positional/bitLevel. (2) trivially extend cmd_sequencer to accept `-`/@file (read stdin or file, split on newlines). The analysis engine and endpoint already exist, so this is pure client wiring.
  - evidence: control_server.cpp:4697-4703 (endpoint exists); bin/nullock:713-725 (argv-only CLI, no stdin/file); ui-v2/app.jsx grep = no Sequencer UI (RulesTab at :722 is Match&Replace).
- **#370 [target] (day) [verified] — View full request/response with the message editor (Pretty/Raw/Hex, Inspector)**
  - plan: The Inspector backend is finished and unused — wire it in. Add an INSPECT tab/panel to DetailPane (proxy.jsx) that POSTs the raw req/resp to /api/inspect (control_server.cpp:9442, backed by inspector_logic) and renders the returned structured view (request line/status, headers, cookies, query+body params, decoded JWTs). Add XML/HTML pretty-print branches to renderView() (proxy.jsx:1110) alongside the existing JSON-only 'preview'. Caveat to verify while there: requestRawById/responseRawById past the 10k ProxyModel window can return the wrong row — guard or bound it. Map-node entry point is item 366.
  - evidence: Editor tabs proxy.jsx:359 (raw\|headers\|body\|hex, req) and :373 (raw\|headers\|body\|preview\|hex, resp). 'preview' is JSON-only pretty at proxy.jsx:1110-1116; no XML/HTML. Inspector endpoint EXISTS: control_server.cpp:9442 `/api/inspect` + include inspector_logic.hpp:23 — but grep of ui-v2 for inspect/inspector finds no caller.
- **#371 [target] (day) [no-verdict] — Site map filter — by MIME class, status family, file extension, search (regex/case/negative), annotation**
  - plan: Frontend-only for most of it — the data is already in each row. In proxy.jsx FilterBar/HistoryTable (22-53,113): add a MIME-category <select> mapping r.mime to HTML/Script/XML/CSS/Images/Other; an extension show/hide field derived from r.path; a leading-! negative-search convention; and a case-sensitivity checkbox that skips the toLowerCase() at proxy.jsx:26/36. The deep-search /api/search regex is force-CaseInsensitive (control_server.cpp:1783) — expose a flag on that call too. Annotation filter (commented/highlighted) is blocked on a missing annotation store — out of scope here.
  - evidence: proxy.jsx:47-51 filters only statusClass+methodFilter; MIME lives only inside the free-text blob proxy.jsx:31-35 (substring includes). Deep search regex forced CaseInsensitiveOption\|MultilineOption at control_server.cpp:1783-1785. No MIME/extension/annotation/negative/case controls anywhere in the bar.
- **#400 [target] (day) [no-verdict] — Issue detail view with Advisory / Request / Response sub-tabs and per-instance evidence highlighting**
  - plan: Confirmed absent: clicking a finding navigates AWAY (app.jsx:1060-1061 onClick=jumpToRow(f.rowId) -> app.jsx:958-960 dispatch {tab:'proxy', selectedRowId}); no advisory component exists. But the advisory DATA is already on every finding — the Finding struct carries cwe, owasp, cvssScore, cvssVector, compliance, fixSummary, evidence (passive_scanner.hpp:22-38), all populated by the enricher. Closeable in ~1 day, frontend: build an issue-detail pane in app.jsx with Advisory (render the existing cwe/owasp/cvss/fix/compliance fields), Request, and Response sub-tabs (pull the pair from history by rowId, same source proxy.jsx already renders). Two honest caveats: per-instance evidence HIGHLIGHTING inside req/resp is extra effort, and extension findings with rowId 0 have no request/response to show (ties to item 390). Core advisory+req/resp pane is a clean day.
  - evidence: app.jsx:1060-1061 onClick jumpToRow, :958-960 jumpToRow navigates away; passive_scanner.hpp:22-38 Finding carries cwe/owasp/cvssScore/cvssVector/compliance/fixSummary/evidence + rowId (:24).
- **#401 [target] (day) [no-verdict] — Content discovery: dedicated GUI (vs Burp's discovery window)**
  - plan: Reclassify absent->partial. The three engines are finished AND server-side endpoints exist: /api/content/discover (control_server.cpp:2998,8489), /api/robots/scan (:2996,7093), /api/crawler/start+stop (:4002,4012). There IS a partial UI affordance — tabs.jsx:556 a 'DISCOVERY' button (setupDiscovery, tabs.jsx:460-473) that pre-fills an Intruder template + curated wordlist. So a discovery workflow is reachable, but it drives the INTRUDER, not the dedicated content_discovery engine (with soft-404 calibration) / crawler / robots, which have NO client wrappers — real-data.js has reconDns/reconWordlist/etc but no contentDiscover/crawlerStart/robots. Closeable in ~1 day, frontend: add contentDiscover/crawlerStart/robots wrappers in real-data.js hitting the existing endpoints, and a discovery panel to launch + show hits. Backend already done.
  - evidence: control_server.cpp:2998/8489 /api/content/discover, :2996/7093 /api/robots/scan, :4002/4012 /api/crawler/start\|stop (all exist); ui-v2/real-data.js action list has NO contentDiscover/crawlerStart/robots; tabs.jsx:556 DISCOVERY button + :460-473 setupDiscovery pre-fills Intruder only.
- **#277 [proxy] (days) [verified] — HTTP/2 frame-level visibility (Burp has no frame log at all)**
  - plan: Backend capture is complete and exceeds Burp (which has zero frame logging): H2EventLog is fed on both legs (http2_client.cpp:153, h2_server.cpp:111) and fully exposed via GET /api/h2/streams (control_server.cpp:1957-1978) and GET /api/h2/events?since=<ms> (control_server.cpp:1980-2007, with a frame-type decode table). The only gap is that nothing polls them — API-only. Add NL.actions.h2Streams()/h2Events(since) to real-data.js and an H2 frame-log view in proxy.jsx: a stream table from /api/h2/streams and a live frame stream that polls /api/h2/events with the returned cursor. Pure frontend.
  - evidence: control_server.cpp:1957-1978 (/api/h2/streams complete); 1980-2007 (/api/h2/events with since cursor + type table); H2EventLog fed at http2_client.cpp:153 and h2_server.cpp:111; no h2/events or h2/streams reference in any ui-v2 file.

### backend-small (117)

- **#40 [intruder] (minutes) [verified] — Processing rule: Decode**
  - plan: MISCLASSIFICATION. The decode capability already EXECUTES: applyRule delegates any non-local op to Transcode::apply (intruder_rules.cpp:45-46), and Transcode::apply supports base64-decode/base64url-decode/url-decode/html-decode/hex-decode/jwt-decode (transcode.cpp:243-248). So passing op="base64-decode" as a rule already decodes today. Only the discovery list operations() deliberately omits them (comment at intruder_rules.cpp:56-58). To reach Burp parity: append the decode op names to the operations() literal (line 59-69) — no engine change needed. Alternatively keep the deliberate exclusion and record it as an intentional roadmap decision. Because the mechanism works but is hidden, verified_state is partial, not absent.
  - evidence: intruder_rules.cpp:45-46 fallthrough to Transcode::apply(op,value); transcode.cpp:229-235 ops() and :243-248 apply() implement base64/url/html/hex/jwt decode; intruder_rules.cpp:56-58 comment excludes them from the advertised list only.
- **#98 [extender] (hours) [verified] — [A4] Extension auto-reload during development (Auto-reload checkbox; Ctrl/Cmd-click to reload a single extension)**
  - plan: Reclassify stub->absent: the auto-reload FEATURE has zero code. No QFileSystemWatcher anywhere (grep empty), no auto-reload toggle, no single-extension reload. What exists is only a GLOBAL manual Reload (reload() rebuilds the whole engine, extensions_api.cpp:121-146; POST /api/extensions/reload control_server.cpp:5778; app.jsx:643 Reload button) — that is the reload primitive, not the auto-reload feature. Closeable now: add a QFileSystemWatcher on extensionsDir() in ExtensionsApi (watch the dir + added files), debounce ~300ms, call reload() on change; guard it behind a bool m_autoReload toggled by a new control endpoint; surface an 'Auto-reload' checkbox in the Extensions card in app.jsx. Single-extension reload collapses to reload-all given the shared engine, so no per-script engine work is needed. Reuses the existing reload() primitive end to end. Medium confidence only on the 'stub vs absent' label call.
  - evidence: grep QFileSystemWatcher/autoReload in Src/ ui-v2/ = empty; extensions_api.cpp:121 reload() global rebuild; control_server.cpp:5778 /api/extensions/reload; ui-v2/app.jsx:643 single global Reload btn
- **#122 [extender] (hours) [verified] — [B20] Extension utilities: api.utilities() — Base64/URL/hex/gzip codecs, ByteArray, crypto helpers**
  - plan: CLOSEABLE. Every primitive is already in Qt6::Core. Add a small QObject (e.g. ExtensionsUtilBridge) exposed as nullock.utils with Q_INVOKABLEs: base64Encode/Decode (QByteArray::toBase64/fromBase64), urlEncode/Decode (QUrl::toPercentEncoding/fromPercentEncoding), hexEncode/Decode (QByteArray::toHex/fromHex), md5/sha1/sha256 (QCryptographicHash), hmacSha256 (QMessageAuthenticationCode), and gzip/gunzip. NOTE for gzip: do NOT vendor zlib — per the repo's known Qt6Core Z_PREFIX quirk, link Qt6::ZlibPrivate (z_-prefixed symbols) as already done elsewhere. Register the util object in rebuildEngine() alongside `nullock` (extensions_api.cpp:105-106). Add a util_logic pure TU + test for the codec round-trips. This immediately lets aws_sigv4.js drop its hand-rolled ES5 SHA-256/HMAC.
  - evidence: extensions_api.cpp:105-106 (`nullock` is the ONLY global); aws_sigv4.js inlines SHA-256/HMAC for lack of a codec API; all needed Qt primitives (QCryptographicHash/QMessageAuthenticationCode/QByteArray/QUrl/Qt6::ZlibPrivate) available
- **#123 [extender] (hours) [verified] — [B21] Extension unloading handler (registerUnloadingHandler) — clean teardown**
  - plan: STALE PRIOR EVIDENCE — reclassify absent->partial. The gap's core claim ('the QJSEngine is never recreated, so an uninstalled script's globals persist in shared scope until restart') is NO LONGER TRUE: reload() now calls rebuildEngine(), which destroys the engine and constructs a fresh one, re-publishing `nullock` (extensions_api.cpp:93-107, 121-146). So the clean-slate half is DONE. What remains absent is the registerUnloadingHandler API itself — a JS callback invoked before teardown. CLOSEABLE: add bridge method nullock.onUnload(cb) that appends to an m_onUnloadHandlers QList<QJSValue>; in reload()/rebuildEngine(), before m_engine.reset(), invoke each handler (owner thread, guarded against re-entrancy) then clear the list. Add a small guard test that an onUnload handler fires on reload.
  - evidence: extensions_api.cpp:93-107 rebuildEngine() destroys+recreates the engine; reload() 121-146 calls it — the 'globals persist' concern is resolved; no onUnload/registerUnloadingHandler invokable exists (hpp:191-212)
- **#133 [extender] (hours) [verified] — [B31] BApp Store: version-compatibility gating ('requires a later version')**
  - plan: Confirmed absent: the complete catalog entry read (marketplace_logic.cpp:91-101) parses id/name/summary/version/author/url/sha256/categories/hooks/declaredPermissions — no minApiVersion/requires field. Nothing prevents installing an extension written against a different API generation. Close: (1) define a compile-time kExtensionApiVersion constant, (2) add QString requiresApiVersion to CatalogEntry + parse it in parseCatalog, (3) in auditInstall (marketplace_logic.cpp:232) compare via the already-present UpdateLogic::compareSemver (already included at marketplace_logic.cpp:3) and set a blocker when requiresApiVersion > current, (4) surface it in the install response + a 'requires a later version' badge in app.jsx. The semver primitive and the audit gate both already exist; this is a focused add + a marketplace_logic_test case (Tests/marketplace_logic/marketplace_logic_test.cpp).
  - evidence: marketplace_logic.cpp:91-101 entry read (no version-requirement field); auditInstall at :232-290 has no compat check; UpdateLogic::compareSemver already imported at marketplace_logic.cpp:3 and used at :330
- **#135 [extender] (hours) [verified] — [B33] BApp Store: update notification for installed extensions (available-updates indicator, Reinstall to update)**
  - plan: Prior gap partly STALE: the indicator + reinstall-to-update UI now EXISTS. merge() emits InstallState::UpdateAvailable when compareSemver(installed, catalog) < 0 (marketplace_logic.cpp:329-334), control_server maps it to 'update-available' (:5381), and the Marketplace UI shows a yellow 'update' badge (app.jsx:284) with '(have X)' and an 'Update' button (app.jsx:353-364). BUT it is unreachable in practice: installedVersion comes from parseInstalledVersion() reading a '// nullock:version' directive, and NONE of the 14 shipped extensions (extensions/*.js) carries that directive, so every one merges as Installed, never UpdateAvailable. Also install-builtins still overwrites unconditionally with no version compare (control_server.cpp:5331-5336). Close: add a '// nullock:version X.Y.Z' comment line to each of the 14 extensions/*.js (their catalog entries already declare version) — that alone makes the whole existing update path light up. Optionally make install-builtins version-aware. Cheap; primitives (merge state, badge, Update button) all already built.
  - evidence: marketplace_logic.cpp:329-334 UpdateAvailable branch; ui-v2/app.jsx:284 update badge + :353-364 Update button; extensions/*.js (14 files) grep 'nullock:version' -> 0 matches; control_server.cpp:5331-5336 install-builtins unconditional QFile::remove+copy
- **#12 [intruder] (hours) [verified] — Processing rule: Match/replace**
  - plan: Confirmed partial: current match-replace is a literal QString::replace(find,repl) with a US-delimited arg — no regex, no capture back-references. Add a new op 'match-replace-regex' in intruder_rules.cpp:applyRule using QRegularExpression with QString-based back-reference substitution (translate $1/\1), add it to operations(), and add an intruder_rules_test case (literal-vs-regex, backref, invalid-pattern no-op). Backend-small + test, hours. Full GUI parity then rides the item-10 rules editor.
  - evidence: intruder_rules.cpp:28-39 match-replace: sep=arg.indexOf(0x1f) then s.replace(find,repl) — QString::replace(QString,QString) is literal; no QRegularExpression path in the file.
- **#13 [intruder] (hours) [verified] — Processing rule: Modify case**
  - plan: Confirmed partial: only full uppercase/lowercase. Add 'capitalize' (first char upper, rest lower) and 'to-lower-except-first' ops as branches in intruder_rules.cpp:applyRule (code-point-aware first char, mirroring the reverse-op's toUcs4 care for non-BMP), add to operations(), add test cases. Backend-small + test, hours. GUI exposure via the item-10 editor.
  - evidence: intruder_rules.cpp:15-16 only value.toUpper()/toLower(); operations() 61-62 lists only uppercase/lowercase.
- **#19 [intruder] (hours) [verified] — Resource pool: throttle / delay**
  - plan: Fixed inter-dispatch delay confirmed (interruptibleSleep(throttleMs) in the dispatch loop); no random variance, the one Burp feature missing. Add a throttleJitterMs field to Intruder + a clamp in intruder_pool_logic.cpp, and in the dispatch loop (intruder.cpp:518) sleep throttleMs + rand()%jitter (use QRandomGenerator). Wire a THROTTLE·JITTER input in tabs.jsx and a jitterMs key in control_server.cpp:3296-3300, plus an intruder_pool_logic test for the clamp. Small, self-contained.
  - evidence: intruder.cpp:518 (if(throttleMs>0) interruptibleSleep(throttleMs,...)); intruder_pool_logic.cpp:11-15 clampThrottleMs, :22-25 rpsToDelayMs (fixed only, no variance); tabs.jsx:599-603 THROTTLE·MS.
- **#21 [intruder] (hours) [verified] — Save/Resume attack**
  - plan: Confirmed: loadRun() rebuilds config + result rows via beginResetModel/endResetModel and emits change signals only — it never re-fires incomplete rows (intruder.cpp:281-333). saveRun already persists each row's raw m_combo (intruder.cpp:264-278), which is exactly what resume needs. Add Intruder::resume(): guard on !m_running, collect indices where !m_attacks[i]->m_complete, build a combosCopy from their m_combo, and drive the existing runWorker/fireOne path but iterating only the pending indices (fireOne already maps an explicit row index to m_attacks[row], so a sparse index list works). Expose /api/intruder/resume + a RESUME button in tabs.jsx, add an intruder_persist/engine-level test. The firing infrastructure fully exists; this is a focused wiring extension.
  - evidence: intruder.cpp:281-333 loadRun rebuilds rows, no start()/runWorker call for pending rows; intruder.cpp:264-278 saveRun serializes r.combo per row; intruder.cpp:512-517 dispatch loop + fireOne(row,combo) index mapping reusable for a pending-only pass; control_server.cpp:1674-1676 export endpoint; tabs.jsx:607-608 SAVE/LOAD buttons (no RESUME).
- **#28 [intruder] (hours) [verified] — Payload type: Character substring**
  - plan: Confirmed absent. Add a pure generator IntruderGenerators::substrings(const QString& value, int from, int length) that emits progressive substrings (Burp semantics: for each start offset, substrings of growing length), add "substring" to types(), wire it in control_server.cpp expandGen (3183-3199), add a UI spec, and add an intruder_generators test. The generator framework + capping already exist; this is a small self-contained generator.
  - evidence: intruder_generators.cpp:89-91 types() (no substring); intruder_rules.cpp:55-70 operations() has no substring; control_server.cpp:3183-3199 expandGen — trivially extensible.
- **#29 [intruder] (hours) [verified] — Payload type: Case modification**
  - plan: Confirmed absent as a payload TYPE (only global uppercase/lowercase RULES exist). Add IntruderGenerators::caseMods(const QString& word) returning the Burp set (as-is, lowercase, UPPERCASE, Capitalized, and optionally toggle-case), add "case" to types(), wire into expandGen + a UI spec + a generator test. Small; reuses QString::toUpper/toLower and existing generator plumbing.
  - evidence: intruder_generators.cpp:89-91 types() (no case); intruder_rules.cpp:15-16 only global uppercase/lowercase transforms; control_server.cpp:3183-3199 expandGen extensible.
- **#31 [intruder] (hours) [verified] — Payload type: Illegal Unicode**
  - plan: Confirmed absent as a generator (payload_forge only has a single canned unicode-CRLF string). Add IntruderGenerators::illegalUnicode(const QString& base) emitting overlong/illegal UTF-8 encodings (2-,3-,4-byte overlong forms of ASCII, plus a standard WAF-bypass set) as %-encoded byte strings, add "illegal-unicode" to types(), wire expandGen + UI + a test. Self-contained generator; the encoding tables are the only real work.
  - evidence: payload_forge.cpp:209-211 single canned "unicode-CRLF bypass" %E5%98%8D... string; intruder_generators.cpp:89-91 types() has no unicode generator.
- **#32 [intruder] (hours) [verified] — Payload type: Character blocks**
  - plan: Confirmed absent. Simplest of the batch: add IntruderGenerators::blocks(const QString& ch, int from, int to, int step) emitting `ch` repeated at increasing sizes (QString(n, ch)), capped at kMaxCount and by cumulative char volume (mirror the brute() kMaxChars guard). Add "blocks" to types(), wire expandGen (3183-3199), UI spec, and a generator test.
  - evidence: intruder_generators.cpp:89-91 types() lists only numbers/brute/dates; :54 existing kMaxChars pattern to reuse for the volume cap.
- **#33 [intruder] (hours) [verified] — Payload type: Null payloads**
  - plan: Confirmed absent. Goal: fire the base request N times (session/CSRF/timing). Watch a real footgun: parseSet (intruder.cpp:20-27) drops empty lines, so you cannot express 'N null payloads' as N blank textarea lines. Implement a dedicated 'null' generator that yields N sentinel entries AND handle them so applyPayloads leaves markers at their default — cleanest is a small mode flag/count on Intruder that bypasses set expansion and creates N rows with a null combo (engine already treats a null combo value as 'use marker default', intruder_engine.cpp:64,143). Add control key + UI count field + a test. Small but touches the engine glue, hence medium confidence.
  - evidence: intruder_engine.cpp:64 (null => marker default) confirms the primitive exists; intruder.cpp:20-27 parseSet drops empty lines (blocks the naive approach); intruder_generators.cpp:89-91 types() has no null generator.
- **#34 [intruder] (hours) [verified] — Payload type: Character frobber**
  - plan: Add QStringList frobber(const QString &base) to intruder_generators.{hpp,cpp}: iterate base by code point (toUcs4), emit one variant per position with that code point incremented by 1, capped at kMaxCount. Append "frobber" to types() (currently line 89-91). Add a branch to expandGen in control_server.cpp (~line 3197): if (t=="frobber") return IG::frobber(g.value("base").toString());. Add a case to Tests/intruder_generators/intruder_generators_test.cpp. Note: unlike numbers/brute/dates, frobber needs a base string arg, so the generate/set API must accept a "base" field (trivial).
  - evidence: intruder_generators.cpp:89-91 types() returns {numbers,brute,dates} only; no frobber fn in file. control_server.cpp:3183-3199 expandGen has numbers/brute/dates branches only.
- **#35 [intruder] (hours) [verified] — Payload type: Bit flipper**
  - plan: Add QStringList bitFlip(const QString &base): take base.toUtf8(), for each byte and each of its 8 bits emit a copy with that byte XOR (1<<k); output count = 8*len, already under kMaxCount. Add "bitflip" to types() and an expandGen branch reading g.value("base"). Test. Prior verdict rated this 'days/medium' — that is an overestimate; the generator itself is a trivial pure loop. The 'days' framing came from the CBC/padding-oracle USE CASE, not the generator, which is comparable to frobber.
  - evidence: intruder_generators.cpp:23-93 has no bit-flip fn; types() line 90 lists numbers/brute/dates. Bounded-by-kMaxCount pattern already established (line 30, 54).
- **#36 [intruder] (hours) [verified] — Payload type: Username generator**
  - plan: The 'common usernames' preset at tabs.jsx:342-346 is confirmed still a fixed static list, not a name->username generator. Add QStringList usernames(const QStringList &names): for each 'First Last' name emit deduped permutations (first, last, first.last, f.last, flast, firstl, lastf, first_last, etc.), capped at kMaxCount. Add "usernames" to types() and an expandGen branch (names from newline-split g.value("names")). Test.
  - evidence: tabs.jsx:342-346 'common usernames' fixed array (confirmed). intruder_generators.cpp:89-91 types() has no username generator.
- **#37 [intruder] (hours) [verified] — Payload type: ECB block shuffler**
  - plan: Add QStringList ecbShuffle(const QByteArray &base, int blockSize): split base into blockSize-byte blocks, enumerate block-order permutations (std::next_permutation), hex-encode each shuffled ciphertext, capped at kMaxCount to contain the factorial blow-up. Add "ecb-shuffle" to types() and expandGen (base+blockSize args). Test. Niche and permutation-count needs the cap, hence medium confidence and hours-to-day, but the primitive is a self-contained pure function like the others.
  - evidence: No ECB/block-shuffle code anywhere in intruder_generators.cpp or intruder_engine.cpp; types() line 90 unchanged.
- **#39 [intruder] (hours) [verified] — Processing rule: Substring / Reverse substring**
  - plan: In intruder_rules.cpp applyRule (before the Transcode fallthrough, ~line 39) add op "substring" with arg "from\x1flen" (US-delimited like match-replace) operating on value.toUcs4() for surrogate-safety, and "reverse-substring" counting the window from the end. Add both names to operations() (line 59-69). Add cases to Tests/intruder_rules/intruder_rules_test.cpp.
  - evidence: intruder_rules.cpp:9-47 applyRule op set (prefix/suffix/case/reverse/match-replace + Transcode); operations() 59-69 has no substring.
- **#41 [intruder] (hours) [verified] — Processing rule: Add raw payload**
  - plan: applyRule alone can't see the pre-processing value, but applyRules(value, rules) has it as its initial `value`. In intruder_rules.cpp applyRules (line 49-53) capture const QString raw = value; and special-case op=="add-raw" (append raw), plus optionally "add-raw-prefix", handled inside the loop rather than in applyRule. Add name(s) to operations(). Test the sequence base->hash->add-raw yields hash+raw.
  - evidence: intruder_rules.cpp:49-53 applyRules folds value->value with no access to the original; operations() 59-69 has no add-raw.
- **#42 [intruder] (hours) [verified] — Processing rule: Skip if matches regex**
  - plan: Rows are pre-built in start() (intruder.cpp:386-393) and rules applied at fire time (:449), so true removal is awkward. Simplest correct approach: add a skip-regex to the run config (m_skipRegex + setter + control_server /api/intruder/set field). In runWorker fireOne after applyRulesToCombo (intruder.cpp:449), if the skip regex matches any processed non-null value, mark the row complete with errorMessage 'skipped (rule)', release the inFlight permit, and return without client.send(). For Burp-exact 'removed' semantics instead, evaluate the predicate over combos in start() before beginResetModel and drop matching combos. Test via a _logic seam. Medium confidence because it touches the fire/row pipeline, not just the rule table.
  - evidence: intruder_rules.cpp:49-53 always returns a string (no drop signal); intruder.cpp:386-393 rows built up-front; :512-519 dispatch loop has no per-combo filter.
- **#44 [intruder] (hours) [verified] — Payload encoding (global 'URL-encode these characters')**
  - plan: Add m_urlEncodeChars (default Burp-like set, e.g. space,&,+,;,=,#,%,\r,\n) plus an enable toggle + setter, wired via control_server /api/intruder/set. In runWorker fireOne (intruder.cpp:449) and resend (:569), after applyRulesToCombo, percent-encode only the configured chars in each non-null combo value BEFORE applyPayloads (do not touch the whole request). Factor the char-set encode into a pure helper in intruder_rules or a _logic file and unit-test it. Currently only line-ending normalization happens (intruder.cpp:451-453).
  - evidence: intruder.cpp:451-453 only normalizes CRLF; url-encode is an opt-in rule op (intruder_rules.cpp:64), no always-on safety net.
- **#45 [intruder] (hours) [verified] — Grep - Payloads (flag reflected payloads)**
  - plan: Add a bool m_reflected field + ReflectedRole to IntruderAttack/roleNames (intruder.cpp:133-160). In fireOne the combo values are in scope: after building scan text (reuse grepScanText via computeGrep path), set reflected = any non-null/non-empty processed payload value is contained in the scan text. Pass reflected through the queued callback alongside matched/extracted (intruder.cpp:489-504) and resend (:587-603). Add a 'Reflected' column in tabs.jsx results table mirroring the Grep column (line 734 header + 749-751 cell). Persist in intruder_persist_logic if desired. Test.
  - evidence: intruder.cpp:72-85 computeGrep uses needles+spec only, no payload-value reflection check; data()/roleNames 133-160 have no reflected role.
- **#47 [intruder] (hours) [verified] — Resource pool: retries on network failure**
  - plan: Add m_maxRetries (0..N) + setter + control_server /api/intruder/set field. In fireOne (intruder.cpp:457) wrap client.send in a loop: while !result.ok (connect/timeout failures, not HTTP error statuses) and attempts <= maxRetries, optional interruptibleSleep backoff (helper already exists at :93), re-send with a fresh/reset HttpClient; record the final result. Mirror in resend(). Extract the retry decision into a _logic function and unit-test it. Primitives (send loop, interruptibleSleep, stop flag) all exist.
  - evidence: intruder.cpp:466-480 handles 429 back-off only; :481-499 records size/err and marks complete on any other outcome with no retry counter.
- **#203 [platform] (hours) [verified] — Collaborator: DNS interaction capture**
  - plan: Two gaps, both still present. (1) DNS hits are emit-and-forget: DnsSink has only `int m_hitCount` and no ring. Mirror OastServer: add `QList<OastHit> m_hits` + QMutex + `hitsSince(qint64)` in dns_sink.hpp/.cpp, append in onDatagram alongside the emit, then surface DNS hits in the existing /api/oast/poll handler (or a /api/oast/dns-poll sibling). (2) buildResponse hardcodes an A answer regardless of QTYPE; add a QTYPE branch in DnsLogic/buildResponse for AAAA/TXT/CNAME/NS (A-record storage/listability is the load-bearing half and is the hours-scoped slice; full record-type matrix is a bit more). Add a dns_logic unit test for the new parse/store path.
  - evidence: Src/Core/APIs/Oast/dns_sink.cpp:114 `++m_hitCount; emit hitReceived(hit);` (nothing retained); dns_sink.hpp:51/67 `int m_hitCount = 0;` is the ONLY hit state (contrast oast_server.hpp m_hits ring); dns_sink.cpp:63-64 `resp.append(char(0xC0)).append(char(0x0C)); ...TYPE A` with no QTYPE branch.
- **#208 [platform] (hours) [verified] — Project templates / scan configuration presets applied at project creation**
  - plan: create-from-template still applies only inScope/outOfScope/notes then returns, silently dropping `rules` and `extensionsEnabled` that the template schema (and the /templates list summary) advertise. Rules is trivially closeable: parse tplObj.value("rules").toArray() into QList<MatchReplaceRule> and call the EXISTING projectStore->setRules() (project_store.cpp:377). extensionsEnabled is slightly larger: there is no per-extension enable endpoint (only /api/extensions/install-builtins and /reload), so enabling a named extension means writing it into the extensions enabled-set/store then reload — verify that primitive before promising it. Add a control_logic test asserting rules land after create-from-template.
  - evidence: Src/BackEnd/Control/control_server.cpp:4984-4993 applies inScope/outOfScope/notes then `return okJson(...)` — rules & extensionsEnabled never read; templates/projects/web-app-pentest.json declares `"extensionsEnabled": ["identity_rotate.js"]`; setter EXISTS at Src/Core/Storage/project_store.cpp:377 `void ProjectStore::setRules(...)`; no per-extension enable endpoint (only control_server.cpp:5314 install-builtins, :5778 reload).
- **#222 [platform] (hours) [verified] — Diagnostics / crash reporting for support**
  - plan: Handler is real and installed (SetUnhandledExceptionFilter + POSIX sigaction, symbolisation, local-only 'review before sharing'), but there is no in-app path to the crash directory and recordNonFatal() has zero callers. To close: (1) add a control endpoint e.g. GET /api/diagnostics that returns CrashReporter::crashDir() and a listing of crash-*.txt files; (2) add a small Diagnostics panel/section in ui-v2 (chrome.jsx settings area) showing the path + file list + an 'open folder' hint; (3) sprinkle a handful of recordNonFatal() calls at the catch/fail-open sites (e.g. replay error, MITM handshake abort) so non-fatals are captured. Files: Src/BackEnd/Control/control_server.cpp (new endpoint), Src/Core/Utils/crash_reporter.cpp:160 (already defined), ui-v2/chrome.jsx.
  - evidence: Src/App/app.cpp:565 `CrashReporter::install();`; crash_reporter.cpp:38/140 crashDir(), :160 `void recordNonFatal(...)` defined; grep across Src/ui-v2 for recordNonFatal shows ONLY the definition (:160) and declaration (hpp:37) — no callers. No 'diagnostics'/crashDir reference in any ui-v2/*.jsx.
- **#225 [platform] (hours) [verified] — AI-assisted issue explanation / triage (Explain This, false-positive assessment)**
  - plan: Endpoint exists: POST /api/triage/finding {rowId,kind,severity,summary,evidence} builds a senior-analyst prompt (inlines the captured request/response via rowId, capped 8KB each) and calls a local Ollama /api/generate, model qwen2.5:14b, with a heuristic seed-list fallback when Ollama is unreachable. Gaps: no UI entry point, no persistence of the verdict, hard external dep off by default. To close the wiring part: (1) add a 'Triage with AI' action on a finding row that POSTs to /api/triage/finding and shows the result; (2) persist the returned verdict on the finding record (add a triage/notes column to the findings store — this is the backend-small piece). Note the true AI value still depends on the unbundled Ollama+14B model, so accept graceful heuristic fallback. Files: ui-v2 (findings panel action), control_server.cpp:4785 (return verdict for persistence), findings store.
  - evidence: control_server.cpp:4785-4830 `// POST /api/triage/finding`; :4792 `QString ollama="http://127.0.0.1:11434"`; :4793 `QString model="qwen2.5:14b"`; :4805-4813 rowId pulls requestRawById/responseRawById capped .left(8*1024); heuristic fallback path around :4543 `ollama unreachable; returned seed list`. grep 'triage' in ui-v2/*.jsx ui-v2/*.js -> empty (no UI).
- **#239 [platform] (hours) [no-verdict] — Reporting: XML issue report (burp_issues shape)**
  - plan: Confirmed: export handlers are nmap-xml (:5790), sarif (:5848), sbom (:5917), postman (:6051) -- no burp_issues-shaped XML. All primitives exist: m_wiring.scanner->findings(0) yields the full enriched Finding list (severity/kind/summary/host/url/cwe/confidence), and the nmap-xml handler already hand-builds XML by string concatenation with escaping. Add GET /api/export/burp-xml that iterates findings and emits <issues><issue><serialNumber><type><name><host><path><severity><confidence><issueBackground>... mirroring Burp's schema, HTML-escaping every target-derived field (reuse the toHtmlEscaped pattern from report/html). Add it to the isReadPath allowlist (:1687 startsWith '/api/export/' already covers it) and a UI download button beside the SARIF one (app.jsx:994). A few hours.
  - evidence: control_server.cpp:5790 (nmap-xml builder), :5848 (sarif iterates findings(1000)), :5917/:6051 (sbom/postman); allowlist :1687 covers /api/export/*; app.jsx:994 (sarif download button)
- **#253 [proxy] (hours) [verified] — CA certificate: leaf caching across restarts**
  - plan: Confirmed stale: on-disk leaf reuse (cert_authority.cpp:199-217) only tests LeafCert::valid() (cert_authority.hpp:15, non-empty check), never expiry; leaves are minted -days 365 (:268) and never re-minted, and an expired-cert handshake then trips markMitmBlocked (proxy_server.cpp:441). Fix in leafCertFor at :199-217: parse QSslCertificate(cached.certPem, QSsl::Pem).expiryDate(); if it is past or within ~30 days, fall through to the mint path (:220+) instead of returning the cached leaf. Add a clearLeaves() method to CertAuthority (rm caDir/leaves/* and m_cache.clear()) exposed via a /api/mitm/clear-leaves control endpoint + a ui-v2 button next to Clear blocklist (app.jsx:483). All primitives exist.
  - evidence: cert_authority.cpp:199-217 (disk reuse, only valid() gate), cert_authority.hpp:15 (valid()==non-empty), cert_authority.cpp:268 (-days 365), proxy_server.cpp:441 (expired handshake -> markMitmBlocked); no clear-leaves endpoint (grep of control_server.cpp)
- **#258 [proxy] (hours) [verified] — TLS pass through > Automatically add entries on client TLS negotiation failure**
  - plan: Confirmed present: markMitmBlocked fires from 5 call sites on client cert-pin refusal / upstream handshake failure and persists (proxy_server.cpp:441, 509, 523, 574, 656; persistBlocklist :1287). Gap is the missing on/off checkbox (Burp has one) and per-entry removal (covered by #257). Add a QAtomicInt m_autoBlockMitm (default 1) with setter, gate the AUTO markMitmBlocked calls behind it (the client-refused :441 and upstream-timeout :509 sites -- leave explicit ones), expose /api/mitm/auto-block {on} + a ui-v2 toggle. Contained backend-small change, hours.
  - evidence: proxy_server.cpp:441/509/523/574/656 (markMitmBlocked auto call sites), :1259-1280 (persist), no auto-block toggle in proxy_server.hpp:171-174 or control_server.cpp mitm endpoints
- **#259 [proxy] (hours) [verified] — Handling invalid upstream TLS certificates (Burp accepts by default so self-signed / expired lab targets stay testable)**
  - plan: Confirmed absent: upstream is VerifyPeer (proxy_server.cpp:471) and the sslErrors handler only records errors into tlsErrors, never calling ignoreSslErrors (:486-491); a self-signed/expired origin fails the handshake at :494 and is then markMitmBlocked (:509). grep confirms ignoreSslErrors is never called anywhere in Src/. Fix: add a bool m_acceptInvalidUpstream (CLI --accept-invalid-upstream-certs + /api/mitm/accept-invalid endpoint + ui-v2 toggle). When set, in the sslErrors lambda call upstream->ignoreSslErrors() (still push errs into tlsErrors for the history/diagnostics), and skip markMitmBlocked for cert-only failures so one bad-cert host is not permanently demoted. High-impact, hours, all primitives present.
  - evidence: proxy_server.cpp:471 (setPeerVerifyMode VerifyPeer), :486-491 (sslErrors records only), :494/:509 (handshake fail -> markMitmBlocked); grep: ignoreSslErrors appears only in a comment at :482, never called
- **#271 [proxy] (hours) [verified] — HTTP/1.1 keep-alive on the proxied (cleartext) connection**
  - plan: Confirmed: the cleartext leg (Connection::run, proxy_server.cpp:274-337) reads exactly one request, forwards, writes the response, then m_client->disconnectFromHost() with no loop — pipelined follow-up bytes are dropped. The MITM leg already implements a correct keep-alive loop with close-honoring at proxy_server.cpp:697-792. Refactor run() to mirror it: open one upstream QTcpSocket, wrap read-request/forward/read-response/write in a while loop bounded by socket state + isShuttingDown(), re-read the next request via readRequestFrom(m_client, req), and reuse the Connection:close / HTTP/1.0-default break logic from lines 776-786. Add a proxy_logic/control_logic test for the connection-persistence decision. Expose an on/off 'keep-alive if server supports it' toggle for the desync/smuggling use case.
  - evidence: proxy_server.cpp:297-337 (single-shot cleartext run(), ends in disconnectFromHost, no loop); MITM keep-alive loop proxy_server.cpp:697-792; close/HTTP1.0 decision proxy_server.cpp:776-786.
- **#273 [proxy] (hours) [verified] — 'Don't send items to Proxy history or live tasks, if out of scope' (as an option)**
  - plan: The BEHAVIOR is present but hardwired ON with no toggle, so it does not match Burp's optional checkbox — reclassify to partial. Out-of-scope items are always excluded from history (proxy_server.cpp:285-287) and always denied MITM (runTunnel, proxy_server.cpp:348-357). Add a ProxyServer bool (default keep current behavior) + a /api/proxy/opt endpoint + UI checkbox that, when enabled, still emits requestReceived/responseReceived for out-of-scope hosts (log-everything-filter-later). IMPORTANT: gate only history LOGGING with this flag — leave the no-MITM-for-out-of-scope privacy default untouched, since Burp's option controls logging only, not decryption. Conflating the two (as the code does today) is the semantic subtlety to preserve.
  - evidence: proxy_server.cpp:285-287 (inScope gate on requestReceived emit); proxy_server.cpp:348-357 (out-of-scope never MITM'd); scope precedence proxy_server.cpp:1368-1374; no toggle for logging out-of-scope exists.
- **#279 [proxy] (hours) [verified] — Proxy on/off toggle preserving the configured listener port**
  - plan: Confirmed: /api/proxy/toggle calls m_wiring.proxy->start() with no arguments (control_server.cpp:3015-3016), which hits the default overload start(LocalHost, 8080) (proxy_server.hpp:102-103) and walks the 8080-based fallback list (proxy_server.cpp:1741-1743) — so a proxy launched on a custom --proxy-port rebinds to 8080 (or next free) after an off/on toggle, diverging from where the browser/PAC/extension point. Fix: add m_bindAddress/m_bindPort members to ProxyServer, set them on the first successful start(), and have the no-arg start()/toggle reuse them instead of the 8080 default. Pairs with #281 (bind address). Add /api/proxy/port + a UI port field so the port is settable without a relaunch.
  - evidence: control_server.cpp:3013-3018 (toggle -> start() no args); proxy_server.hpp:102-103 (default start port 8080); proxy_server.cpp:1741-1743 (8080-anchored fallback list); no persisted last-bound port member.
- **#281 [proxy] (hours) [verified] — Proxy listener: Bind to address (loopback / all interfaces / specific IP)**
  - plan: Confirmed absent: proxy.start is always called with QHostAddress::LocalHost (app.cpp:783-785), the default is also LocalHost (proxy_server.hpp:102), and --listen binds only the control API (app.cpp:1035-1055) — nothing lets the PROXY bind off-loopback, so no phone/VM/second machine can be proxied. Add a --proxy-bind=ADDR flag (parse identically to the existing --listen handling at app.cpp:1036-1049), pass the resolved QHostAddress into proxy.start(), store it as the ProxyServer bind member (shared with #279), and add a UI 'bind to' selector (loopback / all / specific IP). SECURITY: binding the proxy off-loopback creates an open relay — gate it behind explicit opt-in with a warning, mirroring the control API's ctlLoopback guard (app.cpp:1049-1055).
  - evidence: app.cpp:783-785 (proxy.start hardcodes LocalHost); proxy_server.hpp:102 (default LocalHost); app.cpp:1035-1055 (--listen applies to control API only, with its own off-loopback guard).
- **#283 [proxy] (hours) [verified] — Proxy listener > Request handling > Redirect to host / Redirect to port**
  - plan: Confirmed absent: the upstream target comes only from rewriteHostPort (proxy_server.cpp:231-251) and CONNECT target parsing (proxy_server.cpp:340-343); there is no host/port pinning. Add a small redirect table (source host[:port] -> target host:port, with wildcard support) on ProxyServer, applied in Connection::run just before upstream.connectToHost (proxy_server.cpp:297-298) and in runTunnel before the target is handed to runMitmTunnel/runBlindTunnel (proxy_server.cpp:340-363). Add a rules-style UI. Pairs naturally with #284 (force TLS).
  - evidence: proxy_server.cpp:297-298 (upstream.connectToHost(req.host, req.port) with no redirect); rewriteHostPort proxy_server.cpp:231-251; CONNECT target parse proxy_server.cpp:340-343; no redirect map in proxy_server.hpp.
- **#284 [proxy] (hours) [verified] — Proxy listener > Request handling > Force use of TLS**
  - plan: Confirmed absent: the cleartext upstream is a plain QTcpSocket (proxy_server.cpp:297-298), never a QSslSocket; the only TLS upstream is inside runMitmTunnel (proxy_server.cpp:451-493). Add a per-listener/per-redirect 'force TLS upstream' flag; when set, the cleartext run() path uses connectToHostEncrypted + waitEncrypted with VerifyPeer and setPeerVerifyName(host) — reusing the exact upstream-TLS setup already present at proxy_server.cpp:458-493 — instead of the plain connect. This gives HTTP-in / HTTPS-out. Best implemented together with #283 so a redirect entry can also flip TLS on.
  - evidence: proxy_server.cpp:297-298 (plain QTcpSocket upstream on the cleartext leg); only QSslSocket upstream is the MITM one at proxy_server.cpp:451-493; no force-TLS flag anywhere in Src/BackEnd/Proxy.
- **#286 [proxy] (hours) [verified] — Per-host leaf certs for IP-address and IPv6 targets (IP SANs)**
  - plan: Confirmed broken. The SAN ext file is single-entry, DNS-only: cert_authority.cpp:243-247 writes `subjectAltName = DNS:%1`. An IPv4 literal (e.g. 192.168.1.1) passes isValidHostForCert (all digits+dots) but gets a DNS: SAN, which clients reject for an IP connection -> handshake fails -> host is auto-added to the MITM blocklist and blind-tunnelled thereafter (runTunnel proxy_server.cpp:354-363). IPv6 is rejected outright at cert_logic.cpp:25-29 (':' is not in the keep-set). Fix, ~half a day: (1) in cert_logic.cpp add an isIpLiteral(host) helper (QHostAddress parse) and permit ':' when the whole host is a valid IPv6 literal; keep sanitize() mapping ':' -> '_' for the on-disk leaf filename (already does). (2) In cert_authority.cpp choose the SAN type per host: `IP:%1` when isIpLiteral else `DNS:%1`. (3) Add a cert_logic unit test for IPv4/IPv6/hostname classification. Optional stretch: multi-SAN. Primitives all exist; pure focused change.
  - evidence: cert_authority.cpp:243-247 (single DNS-only SAN); cert_logic.cpp:17-33 (isValidHostForCert rejects ':' , accepts IPv4 as a bare 'hostname'); proxy_server.cpp:354-363 (handshake-fail path routes to blind tunnel)
- **#293 [proxy] (hours) [verified] — Automatically update Content-Length when the request/response body is edited**
  - plan: Confirmed and deliberate: resolveForwardBytes (intercept_logic.cpp:5-18) returns currentText.toUtf8() verbatim on any edit; the header comment (intercept_logic.hpp:33-36) explicitly keeps a stale Content-Length so operators can hand-craft smuggling requests. Plan: add a pure InterceptLogic::recomputeContentLength(editedText) that splits headers/body on CRLFCRLF, and if a Content-Length header exists and differs from the body length, rewrites it (leave chunked/TE alone; leave it untouched if the operator deleted the header). Gate it behind an 'auto-fix Content-Length' flag defaulting ON (Burp's default) but respect an explicit off — so the smuggling workflow survives. Touch intercept_logic.cpp/.hpp (+ unit test), thread the toggle through InterceptController, add a checkbox in tabs.jsx. A few hours.
  - evidence: intercept_logic.cpp:5-18 (edit path returns toUtf8 with no CL recompute); intercept_logic.hpp:33-36 (stale-CL-on-edit is intentional, no toggle)
- **#294 [proxy] (hours) [verified] — Automatically fix missing/superfluous trailing newline at end of request**
  - plan: Confirmed: the entire edited-forward path is `return currentText.toUtf8();` (intercept_logic.cpp:17) — no trailing-CRLF normalization. Plan: in the same recompute helper as #293, for a request with no body, ensure the header block ends in exactly one CRLFCRLF (strip extra trailing blank lines / add the missing terminator). Behind an 'auto-fix trailing newline' toggle defaulting ON. Pure logic + unit test in intercept_logic; hours. Low impact but trivial to close alongside #293.
  - evidence: intercept_logic.cpp:5-18 (25-line file; no newline fixup anywhere)
- **#300 [proxy] (hours) [verified] — Proxying plaintext ws:// WebSocket connections**
  - plan: Confirmed broken. readResponse correctly early-returns a 101 with the leftover bytes in resp.body (proxy_server.cpp:864-868), but the PLAINTEXT run() path (:319-337) has NO 101 special-case: it falls through to resolveResponseForClient -> serializeResponse (:920-936), which strips Transfer-Encoding, appends a bogus Content-Length, writes it, then disconnectFromHost — so a ws:// upgrade is re-framed and the socket closed; nothing is relayed/logged/repeatable. The MITM tunnel already does it right (:750-758). Fix, a few hours: in run(), immediately after readResponse, add `if (resp.statusCode == 101) { m_client->write(serializeUpgradeResponse(resp)); waitWritten(...); if Upgrade==websocket runWebSocketRelay(m_client, &upstream, req.host, req.port) else runRawRelay(m_client, &upstream); return; }` — mirroring :750-758. The stack-local `upstream` lives through the relay's QEventLoop, so passing &upstream is safe. Add a plaintext-ws smoke check.
  - evidence: proxy_server.cpp:319-337 (plaintext run() — no 101 branch; goes to serializeResponse+disconnect); proxy_server.cpp:920-936 (serializeResponse strips TE, adds Content-Length); proxy_server.cpp:750-758 (correct 101 handling exists in the MITM path)
- **#304 [repeater] (hours) [verified] — Repeater: Host header and target stay in sync when you edit either**
  - plan: Confirmed: setHost only assigns t.host (repeater.cpp:53-58) and never touches the `Host:` line inside t.requestText; send() transmits requestText verbatim against t.host (repeater.cpp:125-136), so editing the target host leaves a stale Host header and the request lands on an unintended vhost. Fix as sync-with-override (not forced rewrite): in Repeater::setHost, if the current requestText contains a `Host:` header whose value equals the OLD host, rewrite that one line to the new host; if it differs (deliberate desync) leave it. Symmetrically, when setRequestText parses a changed Host header, update t.host. ~1 focused change in repeater.cpp plus a control_logic/_logic unit test asserting the desync-preserving behaviour.
  - evidence: D:/dev/Nullock/Src/Core/Networking/repeater.cpp:53-58 setHost (sets t.host only), :73-78 setRequestText (sets text only), :125-136 send normalizes+sends requestText verbatim against t.host.
- **#305 [repeater] (hours) [verified] — Repeater: response metadata display (status, byte length, elapsed ms)**
  - plan: Confirmed absent: SendResult carries no size/timing field (networking.hpp:23-35), and Repeater stores only statusLine (repeater.cpp:140-143); the snapshot emits statusLine but no size/elapsed (control_server.cpp:1488, 1498). The primitives already exist: a QElapsedTimer readClock is started in HttpClient::send (networking.cpp:208-209) and byte length is rawResponse.size(); the proxy pane already renders both via fmtSize/fmtMs (proxy.jsx:272). Fix: add `int elapsedMs` (and reuse rawResponse.size()) to SendResult, populate elapsedMs from readClock in send(), store responseBytes/responseMs on RepeaterTab, emit them in the repeater snapshot + per-tab objects, and render them in the repeater UI. Focused backend change + snapshot + small frontend.
  - evidence: D:/dev/Nullock/Src/Core/Networking/networking.hpp:23-35 SendResult (ok/outcome/errorMessage/rawResponse/parsed only); repeater.cpp:140-143 stores only statusLine; networking.cpp:208-209 `QElapsedTimer readClock; readClock.start();`; snapshot control_server.cpp:1488,1498; proxy pane renders size+ms proxy.jsx:272.
- **#307 [repeater] (hours) [verified] — Repeater: out-of-scope send guard**
  - plan: Confirmed exceeds Burp (Burp has no repeater scope guard): /api/repeater/send checks blocksScope(host) and returns scopeBlocked before dispatch (control_server.cpp:3129-3132); CLI uses the same endpoint. Two residual gaps: (a) the Qt desktop Send button invokes Repeater::send() directly, bypassing the control-server guard; (b) a narrow TOCTOU — the guard reads the active tab host, then send is posted Qt::QueuedConnection (control_server.cpp:3137), so a scripted client could activate a different tab in between. Both close by moving the scope check to the send site: inject the scope predicate into Repeater (or a callback) and check inside Repeater::send() against the tab actually being sent. Backend-small + a control_logic test. Note the reachable paths (browser+CLI) are already guarded, so this is defense-in-depth.
  - evidence: D:/dev/Nullock/Src/BackEnd/Control/control_server.cpp:3129-3132 scope guard on /api/repeater/send, :3137 `invokeMethod(..."send", Qt::QueuedConnection)`; Src/Core/Networking/repeater.cpp:116-153 send() has no scope check.
- **#308 [repeater] (hours) [verified] — Repeater: headless / scriptable driving of the tool**
  - plan: Confirmed exceeds Burp: full CLI cmd_repeater covers set\|send\|show\|response\|clear\|load\|tabs\|tab{add,close,activate,rename,duplicate} (bin/nullock:275-356) with correct busy-poll on send (:293-297). Two small defects: (1) `repeater tabs` reports `hasResponse: (.responseText != "")` (bin/nullock:333) but the per-tab snapshot objects never include responseText (control_server.cpp:1493-1499 emit name/host/port/tls/statusLine only), so hasResponse is always truthy — root cause confirmed at the snapshot end. Fix: add `to["responseText"]` (or a `hasResponse` bool) to the per-tab snapshot object. (2) `repeater load` id-vs-index bug (see item 303). Both are hours. The separate large item is task #8 'Native cross-platform CLI' — bin/nullock is a bash script needing Git Bash on the owner's Windows box; that is not closeable now.
  - evidence: D:/dev/Nullock/bin/nullock:275-356 cmd_repeater, :293-297 busy-poll, :333 `hasResponse:(.responseText != "")`; control_server.cpp:1493-1499 per-tab snapshot omits responseText; load id/index bin/nullock:325-328 + repeater.cpp:165-167.
- **#316 [repeater] (hours) [verified] — Decoder: URL encode / decode**
  - plan: Basic parity present: urlEncode uses toPercentEncoding (leaves the unreserved set), urlDecode via fromPercentEncoding (transcode.cpp:30-35), dispatched :246-247, in operations() :231 and UI OPS (app.jsx:1545-1546). Missing Burp's 'URL encode all characters' / key-char-only modes. Close by adding a `url-encode-all` op: `toPercentEncoding("", "-._~0-9A-Za-z...")` with an empty exclude set / explicit include so every byte is percent-encoded; register in operations() + apply() + the DecoderTab OPS array. Backend-small + one OPS entry.
  - evidence: D:/dev/Nullock/Src/Core/Networking/transcode.cpp:30-35 urlEncode/urlDecode, :246-247 dispatch, :231 operations(); ui-v2/app.jsx:1545-1546 OPS.
- **#317 [repeater] (hours) [verified] — Decoder: HTML encode / decode**
  - plan: Present: single-pass htmlDecode resolves numeric (&#dd; / &#xhh;) plus named lt/gt/quot/apos/amp only (transcode.cpp:51-93); htmlEncode escapes &<>"' (transcode.cpp:37-50). &nbsp;/&copy;/etc. pass through. Close by adding the full HTML5 named-entity table to the named-entity branch (transcode.cpp:80-88) — a static QHash<QString,QString> lookup keeps the single-pass no-rescan invariant intact. Backend-small (data table). Note the proxy pane's html-decode is a different browser-textarea implementation with the full set (cross-ref A23) — unifying on this table also resolves that disagreement.
  - evidence: D:/dev/Nullock/Src/Core/Networking/transcode.cpp:37-50 htmlEncode, :51-93 htmlDecode (numeric + 5 named entities), :80-88 named-entity branch.
- **#320 [repeater] (hours) [verified] — Decoder: Hash operations (MD2/MD4/MD5/SHA family)**
  - plan: Confirmed: only md5/sha1/sha256/sha512 are dispatched (transcode.cpp:256-259) and in operations() (:236-237) / UI OPS (app.jsx:1548). Missing MD4, SHA-224, SHA-384 and SHA-3 — each is one QCryptographicHash enum value (Md4, Sha224, Sha384, Sha3_224/256/384/512) added via one hashOp dispatch line + operations() entry + OPS entry. Same registry feeds Intruder payload processing (intruder_rules.cpp apply(op,value)), so the win doubles. Close now; hours. (The `in.toUtf8()` binary-input limitation persists and is the same QString-vs-bytes issue as items 311/319 — not part of this quick win.)
  - evidence: D:/dev/Nullock/Src/Core/Networking/transcode.cpp:256-259 md5/sha1/sha256/sha512 only, :177-179 hashOp, :236-237 operations(); ui-v2/app.jsx:1548 OPS.
- **#337 [repeater] (hours) [verified] — Repeater: per-tab notes / annotations**
  - plan: Confirmed absent and cheap. Add QString notes to RepeaterTab; a setNotes(index,text) Q_INVOKABLE + /api/repeater/tab/notes endpoint (mirror rename at control_server.cpp:3167-3172); include notes in the per-tab snapshot (1487+); add a small notes textarea/field in tabs.jsx RepeaterTab. Hours.
  - evidence: repeater.hpp:19-27 struct RepeaterTab has no notes/comment field; snapshot + endpoints (control_server.cpp:3145-3178) carry only name/host/port/tls/status; tabs.jsx RepeaterTab has no notes UI.
- **#343 [repeater] (hours) [verified] — Repeater: automatic "Update Content-Length" when the body is edited**
  - plan: Confirmed absent: send() normalizes CRLF but never touches Content-Length (grep in repeater.cpp finds only response-framing hits in networking.cpp). Fix in Repeater::send() (or a helper): after splitting headers/body, if a Content-Length header exists and an autoUpdateContentLength toggle is on (default on), rewrite it to the body's UTF-8 byte length; add a per-tab bool so the tester can deliberately desync (Burp parity). Add the toggle to tabs.jsx and the snapshot. Backend-small + tiny frontend, hours. High value (impact blocker) and compounds with the send-to-Repeater body/CL mismatch.
  - evidence: repeater.cpp:124-136 normalizes CRLF then m_client.send; no Content-Length recompute (all networking.cpp Content-Length handling :277-327 is response framing); no CL toggle in struct/endpoints.
- **#345 [repeater] (hours) [verified] — Repeater: "Unpack gzip / deflate" in the response view**
  - plan: In Src/Core/Networking/repeater.cpp::send (line 139) the response is set as `t.responseText = QString::fromUtf8(result.rawResponse)` with no Content-Encoding handling. result.parsed is a Nullock::Proxy::HttpResponse (networking.hpp:34) which per content_decode.hpp:19-21 already carries decodedBody / bodyForInspection(). Fix: read the Content-Encoding response header (from result.parsed.headers) and, when gzip/x-gzip/deflate, call Nullock::Proxy::decodeContentEncoding(enc, body) (content_decode.hpp:22, impl content_decode.cpp:58) on the body segment, then rebuild responseText from status line + headers + decoded body. The Core->BackEnd/Proxy include edge already exists (repeater.hpp:4 includes proxy_server.hpp from Src/BackEnd/Proxy, same dir as content_decode.hpp), so it is genuinely one include away. Add a repeater_logic/_logic test asserting a gzip body round-trips to text. Caveat: brotli/zstd stay mojibake (content_decode.hpp:12-16 unsupported).
  - evidence: repeater.cpp:139 QString::fromUtf8(result.rawResponse) with no Content-Encoding branch; content_decode.hpp:22 + content_decode.cpp:58 decodeContentEncoding exists; only include sites proxy_server.cpp:4 and :914; repeater.hpp:4 already includes proxy_server.hpp; networking.hpp:33-34 rawResponse + parsed(HttpResponse).
- **#355 [repeater] (hours) [verified] — Decoder: Octal encode / decode**
  - plan: Pure string transform. Add "octal-encode"/"octal-decode" to transcode.cpp operations() (line 228-238) and matching branches in apply() (:240-260) — encode each UTF-8 byte to \ooo, decode \ooo / octal escapes back. Add both to the DecoderTab OPS array (app.jsx:1544-1549). Because Intruder payload processing shares this registry (intruder_rules.cpp:45 Transcode::apply), it lights up there too for free. Add a transcode _logic test. Hours.
  - evidence: transcode.cpp:228-238 operations() list has no octal; apply() :240-260 no octal branch; DecoderTab OPS app.jsx:1544-1549 matches (no octal).
- **#356 [repeater] (hours) [verified] — Decoder: Binary encode / decode**
  - plan: Same shape as octal (355). Add "binary-encode"/"binary-decode" to transcode.cpp operations() (:228-238) + apply() (:240-260) — encode bytes to space/none-separated 8-bit groups, decode groups back to bytes. Add to DecoderTab OPS (app.jsx:1544-1549). Shared with Intruder registry. Add a _logic test. Hours.
  - evidence: transcode.cpp:228-238 operations() has no binary op; apply() :240-260 no binary branch; DecoderTab OPS app.jsx:1544-1549 has none.
- **#357 [repeater] (hours) [verified] — Decoder: Gzip encode / decode**
  - plan: Add "gzip-decode"/"gzip-encode" to transcode.cpp operations()/apply(). Decode can reuse the working Nullock::Proxy::decodeContentEncoding("gzip", bytes) (content_decode.cpp:58), but note the layering: transcode is Src/Core, content_decode is Src/BackEnd/Proxy — either link it in or lift the zlib call into Core using Qt's bundled z_-prefixed zlib (see the Qt6Core bundled-zlib memory note). Encode needs a new deflate-with-gzip-header path (qCompress is NOT gzip — it prepends a 4-byte length; must emit a real gzip stream via zlib). Add to DecoderTab OPS (app.jsx:1544-1549). Still hours. Note: brotli/zstd remain unsupported (content_decode.hpp:12-16).
  - evidence: transcode.cpp:228-238 operations() has no gzip; transcode.cpp:1-8 includes only QByteArray/QCryptographicHash/QJson*/QRegularExpression; working gzip decode at content_decode.cpp:58 (BackEnd/Proxy, different layer).
- **#78 [scanner] (hours) [verified] — [A30] Access-control / authz testing via multi-identity replay**
  - plan: Present. /api/authz-test replays a captured row as each supplied identity, overlays per-identity headers, and flags divergence (control_server.cpp:4592-4691). Confirmed limits: identities are manual header sets (no auto-login - though Burp Auth Analyzer also uses manually-defined sessions, so this is near parity), divergence is status/size-only (distinctStatuses.size()>1 \|\| distinctSizes.size()>1, control_server.cpp:4667) which false-positives on dynamic pages, and it is API-only (no ui-v2 panel; grep found no authz reference in ui-v2). Closeable in hours: (a) backend-small FP guard - replay each identity twice and subtract natural same-identity size variance before flagging; (b) frontend - add an Authz panel. Full auto-login/session-acquisition would be days but is not required for Burp parity.
  - evidence: control_server.cpp:4592 /api/authz-test; :4645 client.send per identity; :4667 divergent = statuses>1 \|\| sizes>1; ui-v2 grep: no authz reference
- **#79 [scanner] (hours) [verified] — [A31] Hidden parameter discovery (param miner)**
  - plan: Present. Standalone /api/paramminer runs the full default wordlist capped at 2000 with status+reflection signals and emits findings (control_server.cpp:6787-6855, cap at :6811). Confirmed gaps: the deep-audit inline param-mine uses only the first 60 entries (control_server.cpp:505-506 defaultWordlist().mid(0,60)) as a speed tradeoff, and /api/paramminer has no GUI button (grep found no paramminer reference in ui-v2). Closeable in hours: raise/parametrize the deep-audit cap (accept an optional depth from the include set) and add a Param-miner GUI action+button in real-data.js/app.jsx.
  - evidence: control_server.cpp:506 defaultWordlist().mid(0,60) (deep audit); :6810-6811 standalone full list capped 2000; ui-v2 grep: no paramminer reference
- **#144 [sequencer] (hours) [verified] — Live capture option: "Ignore token length differences"**
  - plan: The core analysis (Shannon, char-class, hamming, LCS, sequential) already runs over the full variable-length list -- only the positional and bit-level cross-sample tests require a fixed modal width and silently drop off-width tokens (sequencer_logic.cpp:262 gate `if (modeW<8 \|\| modeC<kDeepMinN \|\| modeC*2<tokens.size()) return r;`), surfacing a bare `applicable:false` at :546 that is indistinguishable from corpus-too-small or width<8 or var-cols<4. Closeable win: in positionalEntropy()/bitLevelTests(), record the count of tokens dropped for non-modal width and a `notApplicableReason` enum (too-few, width-too-small, no-strict-majority, off-width-dropped), and emit them in posObj/bitObj so the analyst sees why the test disappeared and how many tokens were excluded. A genuine 'tolerate variable width in the positional test itself' (align/pad columns) is larger and NOT part of this hours-scoped fix.
  - evidence: sequencer_logic.cpp:257-268 (modal-width selection + drop gate), :262 (silent early return -> applicable=false), :544-556 (caller emits only applicable + fields, no dropped count / reason); core tests at :433-539 already consume the raw variable-length list.
- **#146 [sequencer] (hours) [verified] — Sample summary (count, shortest/longest length) before analysis**
  - plan: analyzeTokens emits `n` (:435) and MEAN length `avgLen` (:445-446) only; the minimum length is computed at :125-126 inside longestCommonSubstring and discarded, and max length is never computed. Closeable: in analyzeTokens after computing totalLen, add a single pass for min/max token length and emit `result["minLen"]/["maxLen"]` (a few lines, right beside :443-446). Then surface count + min/max in the new Sequencer UI (item 145) as a pre/post summary. Note there is no separate capture stage, so 'before analysis' collapses to 'shown alongside the result' -- honest given the one-shot analyze model.
  - evidence: sequencer_logic.cpp:435 (`result["n"]`), :443-446 (avgLen = mean only), :125-126 (minLen computed then discarded inside LCS); no maxLen anywhere.
- **#148 [sequencer] (hours) [verified] — Effective entropy = per-position charset sizes summed across positions**
  - plan: Effective entropy is currently ONE global alphabet rate times a length: effectiveBits = shannonEntropy(whole concatenated corpus) * varLen (:461-464, combinedBits from :452). A token that is hex in cols 0-7 and fixed-case alpha in 8-31 gets the blended average in both regions. But the per-column Shannon entropies are ALREADY computed at :272-282 into r.columnEntropy and are one summation from being Burp's per-position estimate -- they are instead spent only on the -20 biased flag. Closeable: when pos.applicable, compute `perPositionBits = sum(columnEntropy)` and emit it as `shannon["perPositionEffectiveBits"]`, and prefer it over the global product in the keyspace grade for fixed-width corpora (fall back to the global estimate for variable-width). Focused change in analyzeTokens + a _logic test on a mixed-charset corpus.
  - evidence: sequencer_logic.cpp:461-464 (global-rate * varLen), :452 (combinedBits over whole corpus), :272-282 (per-column entropy already computed into r.columnEntropy), :624 (columnEntropy currently only feeds the biased flag).
- **#152 [sequencer] (hours) [verified] — Token bit-conversion across encodings**
  - plan: Confirmed defects in decodeCorpusBytes (:304-333): (a) an ODD-length hex corpus fails `(t.size()%2)!=0` (:311) so allHex=false, then b64Re `^[A-Za-z0-9+/_-]+={0,2}$` (:307) matches the same hex chars, so it decodes as base64 and reports scheme="base64" for a hex corpus; (b) base32/base36/alphanumeric all match b64Re and are relabelled base64; (c) any token with a char outside [A-Za-z0-9+/_-] (JWT '.', decimal separators, '%'-encoding) fails both -> returns {} (:333) -> bitLevel applicable:false, zero bit-level analysis. Closeable now: stop mislabelling -- only claim scheme="base64" when the corpus is NOT valid hex and actually base64-decoded; add a base32 branch; and split JWT-shaped tokens on '.' and base64url-decode segments. Odd-length hex should be reported as an honest 'undecodable width' rather than silently reinterpreted. Focused edit + _logic test. (Fully arbitrary alphabet->bits is a stretch beyond this.)
  - evidence: sequencer_logic.cpp:306-311 (hex even-length + regex gate), :307/:320-332 (b64Re accepts hex/base32/base36 chars, scheme forced to base64), :333 (non-[A-Za-z0-9+/_-] corpus -> {}), :363 (empty -> applicable:false).
- **#153 [sequencer] (hours) [verified] — Sequential / counter token detection**
  - plan: parseToken (:38-55) handles only a bare decimal or (0x)hex string; call sites pass the RAW list (:533 looksSequential, :534 looksMonotonic). So a prefixed counter (sess_1001), suffixed, zero-padded-with-separator, or base64-wrapped counter reports looksSequential:false/looksMonotonic:false. Closeable and cheap: commonAffixLen(tokens,true/false) ALREADY exists (:77-92) and is already used elsewhere -- strip the common prefix+suffix inside decodeNumeric()/looksSequential()/looksMonotonic() before parseToken, so `sess_1001` parses on its variable region `1001`. Optionally attempt a base64/base64url decode-then-parse for wrapped counters. Emit a `counterBasis` note (raw\|affix-stripped\|decoded) so the analyst gets the right reason. Focused edit + _logic test.
  - evidence: sequencer_logic.cpp:38-55 (parseToken, no affix strip), :59-67 (decodeNumeric over raw tokens), :533-534 (raw list passed), :77-92 (commonAffixLen already available).
- **#154 [sequencer] (hours) [verified] — Structural / keyspace grading against a modern standard (NIST SP 800-63B)**
  - plan: The grading is genuine -- effectiveBitsPerToken is thresholded at 60/124 (:594-595) against the 64/128-bit marks, which Burp does not do -- but it is unlabelled: effectiveBitsPerToken is emitted (:469) while the thresholds and the standard name live only in comments (:583-585,:588-593) and the keyspace verdict is blended into a score with ~6 unrelated deductions, so the user cannot recover 'this token is below the 128-bit target' from the output. Closeable: add an explicit `keyspaceGrade` field to `shannon` (or result): {below-64\|below-128\|meets-128}, plus `keyspaceThresholdsBits:[60,124]` and `standard:"NIST SP 800-63B"`. Pure output-shaping, no logic change; makes this a clean exceed-over-Burp. ~1 hour + assert in the existing sequencer test.
  - evidence: sequencer_logic.cpp:594-598 (60/124 threshold deductions), :469 (effectiveBitsPerToken emitted), :583-593 (standard/threshold rationale in comments only, never emitted as a field).
- **#158 [sequencer] (hours) [verified] — Rule action: "Set a specific cookie or parameter value" (static or derived)**
  - plan: Two confirmed defects. (1) A purely STATIC rule ('always set X-Debug:1') can never fire: applyToRequest returns at session_rules.cpp:143 `if (rs.isEmpty() \|\| vars.isEmpty()) return;` -- until some unrelated extract rule has populated the host bag, the injection loop is never reached even for a rule with a fully-literal injectTemplate. (2) FAIL-OPEN: substitute() (session_rules_logic.cpp:76-90) leaves an unresolved `{{name}}` LITERAL in place when the var is missing (:85 skips past it), and applyToRequest's guard `if (value.isEmpty()) continue;` (:154) does not catch a non-empty literal -- so `{{token}}` is injected verbatim into the header/cookie/form body/query. Closeable: (a) let a rule whose injectTemplate contains no `{{...}}` placeholder fire regardless of vars (compute value first, allow static); (b) make substitute report whether any placeholder was left unresolved (or scan the result for `{{...}}`) and SKIP that injection (fail-closed). Both are focused edits in session_rules.cpp + session_rules_logic.cpp with a _logic test. ~hours.
  - evidence: session_rules.cpp:143 (vars.isEmpty() early return blocks static rules), :150-154 (templ built, value substituted, only empty-value skipped); session_rules_logic.cpp:85 (missing var -> literal left, offset advanced).
- **#163 [sequencer] (hours) [verified] — Per-host isolation of captured session values (exceeds Burp)**
  - plan: Confirmed exceeds on the egress path: applyToRequest reads ONLY this request's host bag (session_rules.cpp:139-142) and applyToResponse stores under the extracting host (:126-128), so a host-A secret can never inject into a host-B request -- stronger than Burp's global macro variables. The one caveat: variables() deliberately flattens every host's bag into a single namespace for the snapshot (session_rules.cpp:28-36, 'later hosts win on a name collision'), so a same-named var from a different host overwrites in the READ/introspection view. Isolation holds where it matters (egress) but not in the polled snapshot JSON. Closeable nicety (not required for the exceed): change variables() / the snapshot builder (control_server.cpp:1606) to emit host-qualified keys (host -> {var:val}) so the introspection surface preserves the isolation the injection path enforces. ~hours.
  - evidence: session_rules.cpp:139-142 (per-host inject), :126-128 (per-host store), :28-36 (variables() flattens, later host wins); control_server.cpp:1606 (snapshot consumes the flattened bag).
- **#165 [sequencer] (hours) [verified] — Cookie jar transport safety (do not replay a Secure cookie over cleartext)**
  - plan: The gate is still a port heuristic, not a TLS check: injectableOverTransport returns `port == 443` for Secure cookies. Confirmed HttpRequest (proxy_server.hpp:34-44) has NO tls/secure/scheme field, and session_manager.cpp:131 passes only req.port. Fix: (1) add `bool tls = false;` to Nullock::Proxy::HttpRequest; (2) set it true at the two places requests are constructed on a TLS connection (proxy MITM path and HttpClient/session replay) — grep for HttpRequest{ / .port = construction sites; (3) change SessionLogic::injectableOverTransport signature to take `bool overTls` and return `!c.secure \|\| overTls`; (4) update the call at session_manager.cpp:131 to pass req.tls and add a session_manager_logic test: Secure cookie + tls=false on port 8443 -> skip, Secure + tls=true on 8443 -> inject, cleartext(tls=false) on 443 -> skip. This closes both the false negative (HTTPS on 8443/4443 silently dropping Secure cookies) and the false positive (cleartext to 443 passing).
  - evidence: session_manager_logic.cpp:118-121 (`if (!c.secure) return true; return port == 443;`); session_manager.cpp:131 (`injectableOverTransport(c, req.port)`); proxy_server.hpp:34-44 (HttpRequest struct — method/target/host/port/path/headers/body/timestamp, no tls flag).
- **#176 [sequencer] (hours) [verified] — Reliability rating of the estimate (trust given sample size)**
  - plan: Confirmed absent: the deep-test gates fire silently (kDeepMinN=20 at sequencer_logic.cpp:226, size<kDeepMinN early-returns at :254 and :359, cat.size()<32 at :367) and disclosure is only the applicable booleans (:546, :561); a single token still returns verdict 'looks-random'/score 100. Fix is self-contained in analyzeTokens: compute a reliability tier from n and the distinct count — e.g. n<8 'insufficient', n<20 'low' (deep tests inactive), n<100 'moderate', >=100 'good' — and add result['reliability'] = { rating, sampleSize: n, deepTestsActive: n>=kDeepMinN, note }. Also stamp a reason when applicable is false (e.g. positional['reason']='corpus < 20 tokens' / 'no modal fixed width', bitLevel['reason']='not hex/base64' / '< 256 bits'). Add a sequencer_test asserting n=1 yields rating 'insufficient'. No signature change to the endpoint; JSON-only surface (visible via /api/sequencer/analyze and CLI until a Sequencer UI exists).
  - evidence: sequencer_logic.cpp:226 (kDeepMinN=20), :254 (positional early return), :359/:367 (bitLevel gates), :546 & :561 (only boolean applicable disclosure), :611-617 (distinctTokens/score); n=1 path returns score 100 verdict looks-random.
- **#179 [sequencer] (hours) [verified] — FIPS poker test (non-overlapping 4-bit groups, chi-square over 16 buckets)**
  - plan: Confirmed absent: the existing two-bit test (sequencer_logic.cpp:386-406) is a 4-bucket/3-dof serial test — a strictly weaker relative. Add a sibling nibble-poker loop in bitLevelTests over the same `cat` byte buffer: accumulate counts[16] over non-overlapping 4-bit groups (each byte -> two nibbles), require groups>=~40, chi = sum((counts[k]-exp)^2/exp) with exp=groups/16, flag chi>30.578 (15 dof, p<0.01). Add pokerChi/pokerFail to BitLevelResult (struct at :336-347), emit under bitLevel JSON, deduct in the score block. Add a sequencer_test with a corpus that never emits certain hex digits (passes 2-bit, fails poker). Pure backend + test.
  - evidence: sequencer_logic.cpp:386-406 (two-bit non-overlapping counts[4], exp=pairs/4, chi>11.345 '3 dof'); BitLevelResult :336-347 (twoBit members only, no poker).
- **#180 [sequencer] (hours) [verified] — FIPS runs test (counts of runs length 1..6+ against expected ranges)**
  - plan: Confirmed absent: BitLevelResult (sequencer_logic.cpp:336-347) has monobit/twoBit/serial only, no runs member, and nothing between :371-425 computes runs. Add a runs pass over the decoded bit stream (walk `cat` bit by bit as at :389-395): tally run lengths bucketed 1,2,3,4,5,6+ per bit value, compare against FIPS 140-2 expected intervals (scaled to the actual bit count, or use a chi-square against geometric-expected counts). Add runsFail to the struct, emit in JSON, deduct in score. Add a sequencer_test. Pure backend + test.
  - evidence: sequencer_logic.cpp:336-347 (BitLevelResult — no run-length member); :371-425 (monobit/twoBit/serial only, nothing else).
- **#181 [sequencer] (hours) [verified] — FIPS long runs test (flag any run of identical bits exceeding a threshold)**
  - plan: Confirmed absent (no run-length member in BitLevelResult :336-347; no 'longRun'/'FIPS' identifiers in the file). Cheapest of the three FIPS tests: while walking the bit stream for the runs test (item 180), track the maximum run length and flag if it exceeds a threshold (FIPS 140-2 uses >=26 for 20000 bits; scale to bit count, or a fixed conservative cap). Best implemented together with 180 in one bit-walk. Add longRunLen/longRunFail to the struct + JSON + a test. Pure backend.
  - evidence: sequencer_logic.cpp:336-347 (no run member); grep poker\|runs\|longRun in sequencer_logic.cpp returns 0 substantive hits (only the kDeepMinN comment).
- **#183 [sequencer] (hours) [verified] — Compression test (ZLIB-compress the bit sequence; ratio bounds entropy)**
  - plan: Confirmed absent (no zlib/compress/deflate/qCompress reference in sequencer.cpp/.hpp/sequencer_logic.cpp). The plumbing already exists: `cat` (sequencer_logic.cpp:365-366) is the materialised decoded byte buffer. Add a compression pass in bitLevelTests: `QByteArray z = qCompress(cat, 9);` (Qt's built-in zlib — no vendoring, avoids the Qt6Core z_-prefix concern since qCompress is exposed directly), ratio = double(z.size())/cat.size(); flag ratio below a floor (well-behaved random bytes barely compress, ratio ~1.0+overhead; structured/padded tokens compress notably). Guard cat.size()>= a minimum so the ~ small-input zlib header overhead doesn't dominate. Add compressionRatio/compressFail to BitLevelResult (:336-347), emit in JSON, deduct in score, and a sequencer_test with padded/structured tokens that pass the bit-frequency tests. Pure backend + test.
  - evidence: grep zlib\|compress\|deflate\|qCompress over sequencer.cpp/.hpp/sequencer_logic.cpp: 0 hits; sequencer_logic.cpp:365-366 (`QByteArray cat; for (...) cat += b;` — the exact buffer a compression test consumes).
- **#185 [sequencer] (hours) [verified] — Sequencer analysis report export (HTML / XML report of the token analysis)**
  - plan: request_export.cpp has no sequencer hits; cmd_report (bin/nullock build/html/json) has no sequencer branch; nothing enters the findings store. The analyze result is already a complete QJsonObject (analyzeTokens, sequencer_logic.cpp:433). Closeable slice: add a /api/sequencer/report endpoint (or a --format html\|json arg to the existing analyze handler in control_server.cpp near :4651) that renders the analyze JSON into a static HTML table (score/verdict, shannon, charClass, hamming, positional.columnEntropy, bitLevel pass/fail) — a pure string-builder, ~2-3h, mirrors the existing report HTML style. HONEST SCOPE CAVEAT: routing a weak-token result INTO the findings store / baseline-diff (so it tracks alongside scan findings) is a larger, separate change — do the standalone HTML/JSON export now, leave findings-store integration as a follow-up.
  - evidence: request_export.cpp grep sequencer = 0; control_server.cpp:4651 handler region (analyze only); analyzeTokens returns full QJsonObject sequencer_logic.cpp:433-637.
- **#186 [sequencer] (hours) [verified] — Sample-size guidance and FIPS conformance threshold (warn under ~100 tokens, note 20,000 for formal FIPS compliance, cap the sample)**
  - plan: Confirmed: analyzeTokens (sequencer_logic.cpp:433-637) emits result["n"]=tokens.size() but has NO sample-size warning, NO FIPS statement, NO cap — the only early return is the empty-corpus case (:436-440). Closeable in ~1-2h: in analyzeTokens after computing n, add result["sampleWarning"] when n<100 ("below recommended minimum, statistical tests unreliable"), result["fipsConformant"]=(n>=20000) with an explanatory note field, and a hard cap — reject n over a constant (e.g. 100000) with a `truncated`/`capped` field rather than silently building the 64 MB `combined` QString at :449-451 on the request thread. Add a sequencer test asserting the warning/FIPS fields for n=50 / n=20000. NOTE: moving the whole analyze onto the async/offload set (kActivePaths, control_server.cpp:~2944) is a separate, larger change; the hard cap makes the synchronous path safe enough on its own.
  - evidence: sequencer_logic.cpp:435 result["n"]=tokens.size(); :436-440 only empty-case early return; :449-451 unbounded `combined` build; no warning/fips/cap field anywhere in :433-637.
- **#188 [sequencer] (hours) [verified] — Rule scope — Parameter scope (apply only when named parameters are present)**
  - plan: SessionRule (session_rules.hpp:40-71) has no parameter field, and matches() strips the query before matching (session_rules.cpp:65-66 via stripQuery, session_rules_logic.cpp:123-126). Closeable: add `QString paramScope` (comma-list of names) to SessionRule; in SessionRules::matches (session_rules.cpp:46-67) — which already receives the full req.path WITH query — parse the query segment (do NOT reuse the stripped copy) and require every named param to be present before returning true; leave stripQuery for the pathGlob comparison only. Add the field to control_server (de)serialization (:1599/:3851) and a session_rules_logic test. ~2-4h. Low impact but a genuine wiring/guard-sized change with all primitives present.
  - evidence: session_rules.hpp:40-71 (no param field); session_rules.cpp:65-66 matches uses stripQuery(path); session_rules_logic.cpp:123-126 stripQuery discards query. Note req.path still carries the query, so the data is available.
- **#194 [sequencer] (hours) [verified] — Cookie jar honors cookie domain scope (Domain attribute, host-only vs domain cookies, subdomain matching)**
  - plan: Confirmed: parseSetCookie's attribute switch (session_manager_logic.cpp:67-71) has arms for path/expires/httponly/secure/samesite but NO domain arm — the Domain attribute is parsed off then dropped, and the jar keys strictly on host (session_manager.hpp:65 QHash<host,HostSession>). Closeable in ~3-5h: (1) add `QString domain; bool hostOnly` to CapturedCookie (session_manager_logic.hpp:14-23); (2) add a `domain` arm in the switch (:67-71) setting c.domain and hostOnly=false; (3) add a pure domainMatches(cookieDomain, reqHost) helper (RFC 6265 5.1.3: exact or dot-suffix) mirroring the existing pathMatches at :94-116; (4) in injectInto (session_manager.cpp:130-136) gate each cookie on domainMatches in addition to pathMatches/transport — a domain cookie from www.example.com then correctly rides api.example.com. Requires jar lookup to consider domain-cookie candidates across hosts (iterate m_byHost for domain matches, or maintain a domain index). Add a session_manager_logic test. The copyTo manual button (session_manager.cpp:177) becomes unnecessary. All primitives (pathMatches pattern) already present.
  - evidence: session_manager_logic.cpp:67-71 attribute switch (no domain arm); session_manager_logic.cpp:94-116 pathMatches (template for domainMatches); session_manager.cpp:130-136 injectInto (transport+path gates only); session_manager.hpp:65 host-keyed jar; session_manager.cpp:177 copyTo workaround.
- **#195 [sequencer] (hours) [verified] — Cookie jar expiry handling (Expires / Max-Age respected; expired cookies dropped, session cookies distinguished)**
  - plan: Confirmed stub: Expires is captured as an opaque string (session_manager_logic.cpp:68 `c.expires = val`), never parsed to a QDateTime; there is NO Max-Age arm at all (grep Max-Age in session_manager_logic.cpp = 0). Expired cookies are replayed forever; Max-Age=0 deletion is not understood; session-vs-persistent is indistinguishable. Closeable in ~3-4h: (1) parse Expires (QDateTime::fromString with the RFC 1123/HTTP-date formats) and add a `maxAge` arm to the switch (:67-71); (2) add fields `QDateTime expiresAt; bool sessionCookie` to CapturedCookie, computing expiresAt = now+maxAge (Max-Age wins over Expires per RFC), sessionCookie = neither present; (3) in injectInto (session_manager.cpp:130) skip cookies where expiresAt is valid && past; treat Max-Age<=0 / past-Expires as deletion; (4) surface sessionCookie/expiresAt in the control_server cookie JSON (control_server.cpp:~4849 co["expires"]) and UI. Add a session_manager_logic test for Max-Age=0, past/future Expires, and no-expiry=session. Pure Qt6::Core logic, all in the testable _logic TU.
  - evidence: session_manager_logic.cpp:68 expires stored as raw string (never QDateTime); grep Max-Age in session_manager_logic.cpp = 0 matches; session_manager.cpp:130-132 inject filter set is transport+path only (no expiry check); control_server.cpp:4849 co["expires"]=c.expires.
- **#403 [target] (hours) [no-verdict] — Content discovery: file-extension bruteforce + filename mutation rules**
  - plan: Confirmed absent: candidate construction is a single `base + '/' + rel` (content_discovery.cpp:159); Request has host/port/tls/basePath/headers/wordlist/maxRequests and NO extensions field (content_discovery.hpp:33-41); the only extensions present are baked into individual wordlist entries (cpp e.g. 'wp-login.php', '.env.local', 'backup.tar.gz'). Closeable in hours, backend-small: add `QStringList extensions` to Request; in the word loop, for each word also probe word+ext for each extension (skip if the word already has an extension), counting each against the budget. Beware the maxRequests cap interaction — expand candidates AFTER truncation or the cap will clip the matrix. Verifiable with a control_logic-style candidate-expansion test.
  - evidence: content_discovery.hpp:33-41 Request struct (no extensions field); content_discovery.cpp:159 single candidate `base + '/' + rel`; extensions only baked into wordlist entries.
- **#125 [extender] (day) [verified] — [B23] Installed tab: per-extension 'Loaded' checkbox to enable/disable without removing**
  - plan: CLOSEABLE (wiring + small guard). loadAll() unconditionally evaluates every *.js in the dir (extensions_api.cpp:148-177, entryInfoList at 151) — there is no enable/disable concept. The documented `extensionsEnabled` stub is confirmed inert: it is echoed into the template LIST response (control_server.cpp:4944) but create-from-template applies only inScope/outOfScope/notes (4984-4991), never it. Approach: persist a disabled-set (e.g. <appdata>/extensions/.disabled JSON list); loadAll() skips files whose basename is in the set; add /api/extensions/enable and /disable endpoints that mutate the set and call reload(); surface a per-script enabled flag in the boot payload (control_server.cpp:1230-1233) and render a checkbox in the ui-v2 Extensions card (app.jsx:642-656). Backend-small + a frontend checkbox; verify by toggling and confirming loadedScripts changes.
  - evidence: extensions_api.cpp:148-177 loadAll evaluates all *.js unconditionally; control_server.cpp:4944 echoes extensionsEnabled but 4984-4991 never applies it; templates/projects/api-pentest.json:13 declares it
- **#127 [extender] (day) [verified] — [B25] Installed tab: Up/Down buttons to control invocation order**
  - plan: CLOSEABLE. Confirmed no ordering control; loadAll() takes files in QDir default (name-alphabetical) order (extensions_api.cpp:151) and dispatches handlers in registration order (367). NOTE the prior gap's 'non-deterministic' wording is slightly wrong — the order IS deterministic (alphabetical), just not user-controllable. Approach: persist an order list (<appdata>/extensions/.order); sort the loadAll file list by it (unlisted files fall back to alphabetical); add /api/extensions/reorder; render Up/Down buttons in the ui-v2 Extensions card. Backend-small + frontend. Lower impact than B23/B22.
  - evidence: extensions_api.cpp:151 entryInfoList default (alphabetical) order, no explicit order control; 367 handler loop runs in registration order; no reorder endpoint/UI
- **#128 [extender] (day) [verified] — [B26] Installed tab: Add… dialog (choose extension type, file, output/error destination)**
  - plan: Reclassify stub->partial: two install paths now exist — curated marketplace install (control_server.cpp:5418-5461, id-only input) and /api/extensions/install-builtins which copies shipped extensions/*.js into the user dir (5314-5337). What is still absent is the Burp Add… dialog's core: install an ARBITRARY user-chosen local .js. CLOSEABLE: add POST /api/extensions/add-local {filename, source}, validate filename via safeScriptFileName, parse permissions (ExtensionPerms::parsePermissions) and surface a confirm for mutating grants, write via QSaveFile into extensionsDir, then reload(); add a <input type=file>/paste box in the ui-v2 Extensions card. NOTE the Burp-isms 'extension type' and 'output/error destination' do not map — Nullock has a single JS runtime and a unified extension log — so those parts are roadmap-N/A, not work.
  - evidence: control_server.cpp:5418-5440 install accepts only a catalog id; 5314-5337 install-builtins copies shipped scripts; marketplace.cpp:113-117 derives filename from catalog id, never user-supplied; no arbitrary-local-file add endpoint or UI file picker
- **#16 [intruder] (day) [verified] — Grep - Match**
  - plan: Functionally present and GUI-reachable: needles input (regex-or-literal), scanned over headers+decoded body off-thread, single any-hit ✓ column. Below Burp only in granularity — Burp shows one boolean column per configured expression. To reach full parity add a grepMatchEach(text, needles)->QList<bool> in intruder_grep.cpp, surface a per-needle bit-vector on the result row (new role + persist field), and render dynamic columns in tabs.jsx. Backend-small + frontend, ~1 day. Not required for basic functionality — it already flags matches today.
  - evidence: intruder_grep.cpp:19-36 grepMatch returns single bool (any needle hit); intruder.cpp:82-83 computeGrep sets one matched flag; tabs.jsx:568-576 comma-separated needles input, 749-751 single ✓/· column; control_server.cpp:3268-3277 grepMatch wiring.
- **#216 [platform] (day) [verified] — Burp REST API (service toggle, bind address, named API keys, scan launch + status)**
  - plan: Stays 'exceeds' (token auth, off-loopback opt-in with fail-loud, scan launch/status, exports for targets). Remaining gap: a SINGLE shared bearer token, not multiple revocable named keys, and no self-describing OpenAPI for its own endpoints. Named-keys slice is closeable (~a day, backend-small): replace the single m_apiToken with a small keystore (label -> secret, created/revoked), have isTokenAuthorized check membership, and add a management endpoint to mint/list/revoke. Self-OpenAPI is a separate small generator. Low impact, so this is optional polish.
  - evidence: Src/App/app.cpp:1031-1033 single `apiToken` from env/flag -> `controlServer.setApiToken(apiToken)`; Src/BackEnd/Control/control_server.cpp:1067 `ControlLogic::isTokenAuthorized(authHdr, m_apiToken)` checks one shared token; :941 loopback-or-token gate.
- **#230 [platform] (day) [verified] — Collaborator: interaction persistence across restarts**
  - plan: Confirmed: OastServer keeps hits in a capped in-memory ring (m_hits, evicted past kMaxHits), DNS hits are emitted and never stored, and ProjectStore persists nothing OAST-related. An overnight callback dies on restart. To close: add an oast_hits table to ProjectStore (SQLite, same engine as history), write on each OastServer::hitReceived and dns_sink hitReceived, and reload on project open so /api/oast/poll can serve historical hits. Also route DNS hits into the same store + poll so they survive (partially overlaps #229's DNS-visibility gap). Focused backend change — the storage engine and hit structs already exist. Files: Src/Core/Storage/project_store.cpp/.hpp (new table + append/load), oast_server.cpp:~191 (persist on append), dns_sink.cpp:114 (persist on hit).
  - evidence: oast_server.cpp ~191 `{QMutexLocker; m_hits.append(hit); while(m_hits.size()>kMaxHits) m_hits.removeFirst();}` over hpp `QList<OastHit> m_hits; // ring`. dns_sink.cpp:114-115 `++m_hitCount; emit hitReceived(hit);` (no store). grep 'finding'/oast in project_store.cpp = none.
- **#234 [platform] (day) [verified] — Annotations: per-item Notes column + colour highlighting on any message table**
  - plan: Confirmed absent: ProxyModel::Roles has no note/colour role, and there is no highlight/annotate/rowColor storage anywhere in Src/FrontEnd or Src/Core/Storage; the only per-row marking is a transient diff marker in proxy.jsx. But every primitive exists (SQLite ProjectStore keyed by row id, a stable row id, a JSX history grid), so this is a focused ~1-day feature: (1) add a row_annotations table (rowId -> note TEXT, color INT) to ProjectStore with get/set; (2) add NoteRole+ColorRole to ProxyModel and control endpoints GET/POST /api/annotation; (3) in ui-v2 proxy grid, render an editable Notes cell and a right-click colour-swatch menu that tints the row. Files: project_store.cpp/.hpp, proxy_model.hpp, control_server.cpp, ui-v2/proxy.jsx.
  - evidence: proxy_model.hpp:14-24 Roles have no note/color. grep -rani 'highlight\|annotat\|rowColor\|markRow' Src/FrontEnd Src/Core/Storage -> no product hits (only proxy.jsx:869 'Security annotations' comment, styles.css:975 payload-preview highlight). Transient diff marker only at ui-v2/proxy.jsx.
- **#235 [platform] (day) [verified] — Project files: scan issues persisted in the project and restored on reopen**
  - plan: Confirmed absent and correctly flagged blocker: no saveFindings/loadFindings anywhere, ProjectStore::saveMetadata serialises only v/name/notes/created/updated/inScope/outOfScope/rules (no findings), and on open only traffic is replayed — so every finding evaporates on restart and the baseline diff then reports previously-baselined issues as FIXED. Primitives all exist (PassiveScanner holds the findings list + identity keys used for baseline/diff; ProjectStore is SQLite). Focused ~1-day close: (1) add a findings table to ProjectStore, write on PassiveScanner::findingsChanged (upsert by the existing identity key), (2) on project open load findings back into the scanner before the baseline diff runs so FIXED is computed against the restored set, (3) expose via the existing findings snapshot. Files: Src/Core/Storage/project_store.cpp/.hpp (findings table + save/load), Src/App/app.cpp open path (load before diff), PassiveScanner wiring.
  - evidence: grep 'saveFindings\|loadFindings\|persistFindings\|findings.json\|findings.ndjson' Src -> none. project_store.cpp:294-305 saveMetadata writes only name/notes/inScope/outOfScope/rules — no findings. On open only traffic entries are replayed (app.cpp open path). Baseline/diff uses in-memory scanner findings only.
- **#238 [platform] (day) [no-verdict] — Configuration import/export as JSON (and Burp's configuration library / named config presets)**
  - plan: No /api/config* branch exists in the ~180-branch dispatcher; project templates (/api/project/templates :4918, create-from-template :4951) are read-only seeds that touch only three fields. The tunable config that DOES exist is coherent and already serializable: scope globs + notes + MatchReplaceRules (all in ProjectMeta), the active theme, and the Intruder config. Add POST /api/config/export that emits a single JSON bundle {scope, rules, theme, intruder} and POST /api/config/import that applies it (reusing ProjectStore::setInScope/setRules and the existing intruder load path), plus two buttons in ui-v2 via the real-data.js post() helper. That minimal export/import-as-JSON is a day. Burp's full named-preset 'configuration library' (many saved presets, partial-config merge) is the stretch and stays out of the one-day scope.
  - evidence: control_server.cpp endpoint dispatcher has no /api/config (templates read-only at :4918/:4951); ProjectMeta fields in project_store.hpp:15-23; intruder load at control_server.cpp:3361
- **#245 [platform] (day) [no-verdict] — Dashboard event log (suite-wide errors, warnings, info)**
  - plan: The only user-facing log is extension-scoped: ExtensionsApi::recentLog(40) surfaced in the snapshot (control_server.cpp:1226; extensions_api.hpp:84). There is no app-wide event log -- a failed SQLite history index open, a TLS error, a dropped filter are all silent. Closing this is a bounded, mirror-an-existing-pattern job: add an app-scope ring-buffer log sink modelled on ExtensionsApi::recentLog (a QMutex + bounded QStringList with severity), expose it as a snapshot field (eventLog[]), instrument the top error sites (HistoryIndex open failure, proxy TLS handshake failures already flowing through app.cpp, project open/save errors, ProxyModel row-cap discard), and render a panel in ui-v2. The infrastructure + main sites is ~1 day; it need not instrument every site to be a real, useful event log.
  - evidence: control_server.cpp:1226 (recentLog(40) is extensions-only), extensions_api.hpp:84 (recentLog signature); eventLog/appLog grep -> no app-wide sink
- **#248 [platform] (day) [no-verdict] — Performance feedback / memory usage display / low-memory warnings and memory settings**
  - plan: No memory instrumentation: GetProcessMemoryInfo/getrusage/GlobalMemoryStatus/proc-self grep over Src is empty. The one related control is the ProxyModel window cap (--max-rows default 10000) which discards rows silently. Closing it: add a small cross-platform RSS reader (GetProcessMemoryInfo on Win, getrusage/proc-self-statm on POSIX), expose rssBytes + rowCount + maxRows + a droppedRows counter as snapshot fields, render a memory/throughput stat in ui-v2, and surface a visible indicator when the row cap discards live rows (proxy_model currently drops with no signal). A day, with the snapshot mechanism and max-rows plumbing already present.
  - evidence: GetProcessMemoryInfo/getrusage/GlobalMemoryStatus grep over Src -> empty; --max-rows cap in proxy_model (silent discard); snapshot mechanism at control_server.cpp:1224-1227
- **#249 [platform] (day) [no-verdict] — Clickbandit (clickjacking PoC generator) and Burp Infiltrator**
  - plan: No clickjacking PoC generation: clickbandit/infiltrator grep hits only docs/roadmap (the parity register itself), not code. Detection exists (header_logic clickjacking-missing finding) and, crucially, the exact pattern to mirror exists: csrfPoc() in request_export.cpp:50 (and header request_export.hpp:19) is a self-contained PoC-HTML string generator with a matching endpoint /api/csrf/poc (:3908). Add clickjackPoc(url) that emits a standalone HTML page framing the target in an <iframe> with a positioned decoy overlay + opacity control, expose it as POST /api/clickjack/poc, and add a UI button. A day, backend-small. Infiltrator is deprecated in current Burp -- correctly out of scope; do not build it.
  - evidence: clickbandit/infiltrator grep -> only docs/roadmap; Src/Core/Networking/request_export.cpp:50 (csrfPoc generator to mirror), request_export.hpp:19; control_server.cpp:3908 (/api/csrf/poc endpoint)
- **#252 [proxy] (day) [verified] — Proxy listener > Certificate > Generate CA-signed per-host certificate (per-host leaf minting)**
  - plan: Confirmed: leaf carries exactly one 'DNS:<host>' SAN and /CN=<host> (cert_authority.cpp:243-247, 256). Two closeable improvements: (a) IP-literal hosts get a bogus DNS SAN and browsers reject it -- when isValidHostForCert/host parses as a QHostAddress, emit 'IP:<host>' instead of 'DNS:<host>' in the ext file at cert_authority.cpp:243-247 (minutes). (b) Upstream-cert cloning (multi-SAN/OU/serial parity with Burp) requires reordering the MITM flow: in runMitmTunnel, open the upstream TLS connection FIRST (currently at proxy_server.cpp:493), read upstream->sslConfiguration().peerCertificate(), extract its SubjectAltNames + subject, then pass them into leafCertFor so the ext file reproduces the full SAN set. This is a contained ~day change since the upstream socket and cert are already available on that path.
  - evidence: cert_authority.cpp:243-247 (subjectAltName = DNS:%1 only), :256 (-subj /CN=+host), :262-271 (x509 -req sign); upstream peerCertificate available at proxy_server.cpp:493 connectToHostEncrypted
- **#257 [proxy] (day) [verified] — TLS pass through list (manual host/IP-range + port entries, 'Apply to out-of-scope items')**
  - plan: Confirmed: public API is read (blockedHosts, proxy_server.hpp:173) + clear-all (clearMitmBlocked, :174) only; matching is exact QSet::contains (proxy_server.cpp:1257). No pre-add, no per-entry delete, no wildcards/ranges. Closeable core (the primary Burp-parity gap): add addMitmBlocked(host)/removeMitmBlocked(host) on ProxyServer (mirror markMitmBlocked/persistBlocklist at :1259-1304, reuse m_blockMutex + persistBlocklist), expose via /api/mitm/add-blocked and /api/mitm/remove-blocked next to the existing /api/mitm/clear-blocked (control_server.cpp:5773), and turn the ui-v2 blocklist chip into an editable list (app.jsx:483). That is a ~day wiring job on existing infra. IP-range/CIDR + host:port + 'apply to out-of-scope' matching would upgrade isMitmBlocked (:1255-1258) to glob/CIDR and is follow-on beyond the day.
  - evidence: proxy_server.hpp:171-174 (only isMitmBlocked/markMitmBlocked/blockedHosts/clearMitmBlocked), proxy_server.cpp:1255-1258 (exact QSet contains), control_server.cpp:5773-5774 (clear-blocked only), app.jsx:483 (Clear blocklist btn)
- **#264 [proxy] (day) [verified] — Match and replace rules (8 sections; literal-or-regex; per-rule enable; ordered list)**
  - plan: Confirmed: 6 sections (ReqUrl/ReqHeader/ReqBody/RespHeader/RespBody/RespStatus, proxy_server.hpp:75-82) vs Burp's 8 -- missing 'request param name' and 'request param value'. Match is regex-only (applyOne always uses compiledFind, :1443-1448); no literal type, so metacharacters must be hand-escaped. Per-rule enable exists (hpp:83) and rules are an ordered QList (parity). Closeable: (a) add a `bool literal` to MatchReplaceRule and, when set, compile QRegularExpression::escape(find) at :1406 -- trivial. (b) Add ReqParamName/ReqParamValue sections applying find/replace to individual query params (parse req.path query) and form-urlencoded body params, then reserialize + fixContentLength. Plus a ui-v2 section-dropdown entry (app.jsx:817). Contained backend-small + small frontend, ~day. Note the separate chunked-body framing limitation: fixContentLength only rewrites an existing CL header (:1455-1462), so a body rule on a chunked response still leaves framing wrong -- worth fixing in the same pass by converting chunked->CL on rewrite.
  - evidence: proxy_server.hpp:75-82 (6-section enum), proxy_server.cpp:1443-1448 (applyOne regex-only), :1455-1462 (fixContentLength rewrites existing header only), hpp:83 enabled flag present
- **#289 [proxy] (day) [verified] — SOCKS proxy chaining (SOCKS4/5, DNS-over-SOCKS)**
  - plan: Confirmed absent (no SOCKS/QNetworkProxy in Src). But Qt makes SOCKS5 cheap: QNetworkProxy(QNetworkProxy::Socks5Proxy, host, port[, user, pass]) applied via socket->setProxy() on each upstream socket does remote DNS by default (satisfies 'do DNS lookups over SOCKS'). Plan: (1) add ProxyServer::upstreamProxy() config (host/port/optional creds) with a control endpoint + UI field; (2) call setProxy() on the three upstream sockets before their connect calls (proxy_server.cpp:297-298, 368-370, ~451/:493); (3) manual verify through an SSH `-D` dynamic tunnel. SOCKS5 only; SOCKS4/no-remote-DNS toggle are minor add-ons. Confidence medium because it needs live testing against a real SOCKS endpoint, but the primitive is one Qt call per socket.
  - evidence: grep socks/QNetworkProxy across Src -> none; upstream socket creation at proxy_server.cpp:297-298 (plaintext), 368-370 (blind), 451-493 (encrypted) — none call setProxy
- **#292 [proxy] (day) [verified] — Intercept: 'Do intercept > Response to this request' (per-message opt-in)**
  - plan: Confirmed absent (intercept row is DROP/FORWARD only, tabs.jsx:318-321). Tractable because request and its response are handled sequentially on the SAME worker: run() pends the request (proxy_server.cpp:305-312) then later reaches resolveResponseForClient (:331-332); runMitmTunnel likewise (:706 req pend region -> :763-768). Plan: (1) add a per-connection bool forceInterceptThisResponse, set from a new InterceptController::forwardAndInterceptResponse() (mirror forward(), stash the flag on the resolved PendingRequest and read it back through the done semaphore like `decision`); (2) in resolveResponseForClient, OR that flag into the responsesEnabled gate for this one response; (3) add a '↦ FORWARD + hold response' button to tabs.jsx and the control dispatch. Contained to intercept.cpp/.hpp + proxy_server.cpp + one JSX button; ~1 day with a _logic test. Confidence medium: the flag has to ride the same cross-thread handoff, so care needed.
  - evidence: tabs.jsx:313-322 (only DROP/FORWARD); intercept.hpp:94-96 (forward/drop/forwardAll — no per-response opt-in); proxy_server.cpp:305-332 (request then response pended in the same worker)
- **#299 [proxy] (day) [verified] — HTTP history annotations: per-item highlight colours + comments + filter-by-annotation**
  - plan: Confirmed: no annotation fields exist. proxy_model.hpp Entry has only id/request/response (grep for highlight/comment/color -> none), and the control-server snapshot rows carry id/host/method/url/path/status/mime/params/tls/ip/ts/port/size/reqSize/elapsed and nothing else. Plan (a contained CRUD feature, ~1 day across layers): (1) add QString comment + int highlight to the Entry / history store; (2) add setComment/setHighlight control endpoints keyed by row id; (3) emit both in the snapshot row builder in control_server.cpp; (4) persist them in project_store so they survive reload; (5) frontend: a colour swatch + comment field on the history row/DetailPane and a filter-by-colour/has-comment control. No new engine, just plumbing a colour + string end to end.
  - evidence: proxy_model.hpp Entry (id/request/response only — annotation grep empty); control_server.cpp snapshot row keys (no comment/highlight)
- **#333 [repeater] (day) [verified] — Repeater: per-tab request history with back/forward (< >) navigation**
  - plan: Confirmed absent: RepeaterTab has no history container and send() overwrites responseText/statusLine in place. Primitives largely exist (tabs already snapshotted each send). Add QList<{request,response,status}> history + int cursor to RepeaterTab; in send() push the sent request+response and advance the cursor; add Q_INVOKABLE historyPrev/historyNext that restore requestText/responseText; expose historyIndex/historyCount in the snapshot (control_server.cpp:1487+); add < > buttons in tabs.jsx RepeaterTab target-row. Backend-small + small frontend, ~1 day. High-value (impact blocker).
  - evidence: repeater.hpp:19-27 struct RepeaterTab has no history field; repeater.cpp:138-147 overwrites t.responseText/statusLine on each send; UI has no nav (tabs.jsx:188-219 target-row, only CLEAR/SEND).
- **#350 [repeater] (day) [verified] — Repeater: tab state persisted in the project file and restored on reopen**
  - plan: Only store is repeater.hpp:101 QList<RepeaterTab> m_tabs (in-memory); clearAll (repeater.cpp:107-114) wipes on project switch and there is no serializer in Src/Core/Storage (only history_index/history_logic/project_logic/project_store). ProjectStore ALREADY persists scope/rules/notes to project.json (project_store.hpp:37 saveMetadata, :82-88 scope setters) and streams history on load. Add: (1) Repeater::serialize()->QJsonArray of {name,host,port,tls,requestText} and Repeater::restore(QJsonArray); (2) call serialize() from ProjectStore save path and write under a "repeater" key in project.json; (3) a restore signal (mirror entryLoaded/streamExistingHistory at project_store.hpp:114,126) wired in control_server to Repeater::restore. Primitives all exist; ~a day incl. a round-trip _logic test. Confidence medium pending ProjectStore's exact save hook.
  - evidence: repeater.hpp:101 m_tabs only store; repeater.cpp:107-114 clearAll wipe wired to historyShouldClear; Storage dir has no repeater file; project_store.hpp:37 saveMetadata + :82-88 scope persistence proves project.json persistence primitives exist.
- **#59 [scanner] (day) [verified] — [A11] Active audit: SQL injection (error/boolean/blind time-based)**
  - plan: Present and robust: error-based with breaker/balanced corroboration + baseline-error subtraction (sql_injection.cpp:95-129) and blind TIME-based with SLEEP/SLEEP(0)-control/reproduce guards across 5 DBMS (sql_injection.cpp:34-45,138-180); the GUI per-row probe battery independently does error + time-blind (control_server.cpp:2561-2597). The one genuine gap vs Burp = boolean/differential blind (no 'boolean' technique in the file). Closeable in ~a day: add a boolean phase to sql_injection.cpp firing matched TRUE (' OR 1=1-- -) vs FALSE (' OR 1=2-- -) pairs and confirming a stable content/length divergence that the neutral baseline lacks — the send/queryWith/response-compare primitives already exist. Backend-small + a _logic test.
  - evidence: sql_injection.cpp:123 error-based hit; :172 time-based hit; :95-100 breaker/balanced probes; no 'boolean' technique anywhere; control_server.cpp:677 srr.timeBased = include.contains("blind"); control_server.cpp:2572-2576 GUI-battery time-blind payloads
- **#83 [scanner] (day) [verified] — [A35] Retest an individual issue**
  - plan: Add POST /api/findings/retest {id}. All primitives exist: (1) look up the Finding by id via scanner->findings(0); (2) it carries rowId -> rebuild an AuditTarget from the originating history row exactly as /api/audit/all's fromHistory branch does (control_server.cpp:6734-6757: requestAt(i)/responseAt(i)->wasTls, method, path, body, Content-Type); (3) map finding.kind to a tester include-set (e.g. kind starts 'xss'->{xss}, 'sqli'->{sqli}, 'idor'->{idor}...) and call runDeepAudit(scanner, t, include, reports); (4) return the fresh sub-report + whether the same kind re-fired. Honest limitation: Finding stores no exact insertion-point/payload, so this re-runs the tester FAMILY against the same URL, not the byte-identical original payload. That matches ~90% of Burp's 'retest' value; a byte-exact retest would require persisting the request/insertion-point on each Finding (a larger change). Files: control_server.cpp (new route + kind->include map, likely a control_logic helper + _logic test).
  - evidence: control_server.cpp:4189 `/api/baseline/diff` (only current re-test path, whole-baseline); :6643 `/api/audit/run` and :6696 `/api/audit/all` both accept include[] and call runDeepAudit; :6734-6757 fromHistory rebuilds AuditTarget from requestAt/responseAt; no per-finding retest route (grep retest/rerun -> none)
- **#86 [scanner] (day) [verified] — [A38] Scan speed vs accuracy tradeoff settings**
  - plan: Partial confirmed. /api/audit/all today exposes throttleMs (0..60000) + limit (1..200) + include[] (which testers -> a crude depth lever), but the sweep is a single SERIAL QtConcurrent loop over targets with no worker-count/concurrency and no named fast/normal/thorough presets. Two closeable pieces: (1) preset layer (hours): add a 'profile' param that expands to a canned {include set, throttleMs} — fast={fingerprint,headers}, normal=default battery, thorough=all testers incl. slow OOB/time-based — via a small ControlLogic map + _logic test. (2) concurrency (rest of the day): replace the serial for-loop at control_server.cpp:6768 with a bounded QThreadPool/QtConcurrent::mapped capped at a 'concurrency' param (clamp 1..8), mirroring the intruder's setMaxConcurrency pattern already at :3294. Files: control_server.cpp audit/all handler + control_logic. Keep partial until both land.
  - evidence: control_server.cpp:6707 `int throttleMs = ...toInt(150); int limit = ...toInt(50)`; :6768 `for (const AuditTarget &t : targets){ runDeepAudit(...); QThread::msleep(throttleMs);}` serial; intruder already has concurrency :3294 `setMaxConcurrency`; portscan has presets :9140 but the vuln-audit engine has none
- **#87 [scanner] (day) [verified] — [A39] Live scanning of proxy traffic (live active audit tasks)**
  - plan: Partial confirmed: live PASSIVE scanning is real (proxy responseReceived -> PassiveScanner::onResponseReceived, app.cpp), but there is no live ACTIVE audit task — the user must manually POST /api/audit/all or hit 'Probe all rows'. Closeable minimal version (day, backend-small): add an opt-in live-active toggle that connects proxy->responseReceived to a handler which, for each new in-scope request that carries a query/body and has not been seen, fires runDeepAudit off-thread (throttled, dedup-keyed on method+host+path+param-names). Primitives all exist: scope (blocksScope), runDeepAudit, off-thread QtConcurrent, the same fromHistory target-build. HONEST caveats that make this a scoped-day not a trivial-hour: must exclude the scanner's OWN outbound traffic to avoid a feedback loop, need a bounded in-flight cap and a dedup LRU, and a kill switch. A full Burp-style live-audit task with per-insertion-point queueing is larger and overlaps B4. Files: app.cpp (wire signal behind a flag) + control_server.cpp (toggle route + dedup set).
  - evidence: Src/App/app.cpp: PassiveScanner wired to proxy responses (onResponseReceived) — live passive only; grep live-active/autoAudit/liveActive across Src+ui-v2 -> no matches; runDeepAudit exists control_server.cpp:472; audit is manual via :6643/:6696
- **#88 [scanner] (day) [verified] — [A40] Vulnerable/outdated JS library detection**
  - plan: Partial confirmed. Client-side JS lib version detection DOES exist and correlates to CVEs: http_fingerprint_logic.cpp extracts versions from asset paths for jQuery (lib-jquery), jQuery UI (lib-jquery-ui), Bootstrap (lib-bootstrap) via regex, and control_server.cpp:433/:7317 calls CveDatabase::lookup(cveKind, name+version); cve_database.cpp:361-380 holds curated jQuery CVEs (CVE-2019-11358, 2020-11022/11023). Gap is COVERAGE + SOURCE, exactly as the prior note: only ~3 libraries, a hand-curated bundled table, no retire.js-style hash catalogue or live feed. Closeable expansion (day, generator): (1) add version regexes for the retire.js top set (angular/angularjs, react, vue, lodash, moment, handlebars, dompurify, d3, ...) in http_fingerprint_logic.cpp add(...) block; (2) generate matching cve_database.cpp entries by transcribing retire.js's jsrepository.json known-vuln ranges into the existing {kind,cve,desc,range,url} row shape. A true content-hash catalogue (retire.js hash matching for renamed/inlined bundles) is the larger piece and stays out of scope. Keep partial.
  - evidence: http_fingerprint_logic.cpp:142 `jquery[.\-]?([0-9.]+)...` add("jQuery",v,"body","lib-jquery"); :146 Bootstrap lib-bootstrap; control_server.cpp:433 `CveDatabase::lookup(t.cveKind, t.name+" "+t.version)`; cve_database.cpp:361-380 lib-jquery CVE rows; passive_scanner.cpp:1500-1540 framework detect is info-only (no version->CVE)
- **#91 [scanner] (day) [verified] — [B3] Scan configuration library (saved named configs / presets)**
  - plan: Absent confirmed for the vuln-audit engine: include[]+throttle are rebuilt inline per request; there is NO saved/named config store or import/export (portscan has ad-hoc presets at control_server.cpp:9140 but the audit engine has none). Closeable (day): reuse the existing JSON-to-disk persistence pattern already used for the baseline store (loadBaseline/saveBaseline) to add a named scan-config store: POST /api/scan-config/save {name, include[], throttleMs, concurrency, limit}, GET /api/scan-config/list, /api/scan-config/get, /api/scan-config/delete, persisted to a scan_configs.json in the app data dir; ship 3 built-in presets (fast/normal/thorough — dovetails with item 86). Then /api/audit/all accepts {config:'name'} to expand server-side. Small ui-v2 dropdown to pick/save. Files: control_server.cpp (routes + a ControlLogic (de)serialize helper + _logic test), ui-v2/app.jsx (config picker). Overlaps item 86's preset layer — build once.
  - evidence: control_server.cpp:6704 `QSet<QString> include;` built inline in /api/audit/all; :6643 /api/audit/run same; grep savedConfig/scanProfile/named config across Src+ui-v2 -> only ACCENT_PRESETS (UI theme) and portscan preset; baseline persistence pattern exists loadBaseline (control_server.cpp:4192)
- **#93 [scanner] (day) [verified] — [B5] Issue suppression / false-positive marking**
  - plan: Absent confirmed and high-value/closeable. Today triage is all-or-nothing: /api/findings/clear wipes everything (control_server.cpp:5783) and the only per-finding route is /api/triage/finding (AI suggestion, control_server.cpp:4791) which persists NO state. There is no false-positive mark, per-kind suppression, or annotation. Closeable (day): (1) add a stable finding key (reuse keyOf(kind,host,url,summary) already defined for baseline diff) and persist a suppression map {key -> {status:'false-positive'\|'ignored', note, ts}} to a suppressions.json via the existing loadBaseline/saveBaseline-style disk helper; (2) routes POST /api/findings/mark {rowId\|key,status,note}, /api/findings/unmark, and include the suppressed set in the /api/findings/grouped + report/SARIF responses (filter or tag, don't delete); (3) ui-v2/app.jsx: per-row 'Mark false positive'/'Ignore' + note field beside the existing 'Clear all' button (app.jsx ~:838). Findings enrich/export already key off kind so tagging rides through cleanly. Files: control_server.cpp (2 routes + persistence + filter in grouped/report), a ControlLogic key/serialize helper + _logic test, ui-v2/app.jsx.
  - evidence: control_server.cpp:5783 `if (path == "/api/findings/clear")` -> scanner->clear() (only mutation); :4791 /api/triage/finding is advisory-only, no persisted state; :4021 /api/findings/grouped read-only; keyOf() already exists for baseline diff (:4200); no false-positive/suppress route (grep -> none)
- **#149 [sequencer] (day) [verified] — Character-level: per-position chi-square with a p-value per position**
  - plan: Current test is per-position Shannon entropy compared to the STRONGEST sibling column (ref = max of variable-column entropies, :284-285), flagging columns whose entropy is <0.5*ref (:290-296). Its blind spot is real: a generator biased UNIFORMLY across every column has no strong sibling to stand out against and scores clean, and it self-disables below n=20 / width<8 / <4 variable columns / ref<2.0 all behind one applicable:false (:254,:262,:290). Closeable: the per-column character counts already exist (cnt hash :273-274). Add a per-column chi-square goodness-of-fit against the uniform-over-observed-alphabet expectation, plus a p-value via a hand-rolled regularized lower incomplete gamma (~40 lines, standard), emitting per-column {chiSquare, pValue, pass} and an overall pass. This fixes the absolute-vs-relative gap. ~1 day incl. a _logic test; needs the incomplete-gamma helper which Qt does not provide.
  - evidence: sequencer_logic.cpp:272-296 (Shannon-vs-strongest-sibling, no chi-square/p-value), :284-285 (ref = max sibling entropy -> uniform bias invisible), :254/:262/:290 (four silent self-disable gates behind applicable:false).
- **#150 [sequencer] (day) [verified] — Bit-level: FIPS monobit at every bit position independently**
  - plan: Current monobit is ONE p-value over the whole concatenated bitstream (:372-382 over `cat`), so a single stuck bit in a 128-bit token is diluted 1:128 and never reaches p<0.01. The per-token decoded byte arrays already exist (`per`, :362) but are flattened into `cat` at :365-366, discarding token boundaries. Closeable: add a per-bit-position pass that keeps `per` un-flattened -- require equal-length decoded tokens (guard on a single decoded length), then for each bit index compute the 1-proportion across tokens and its monobit p-value, flag any position with p<0.01. Restructure bitLevelTests to run this alongside the existing aggregate test; emit `perPositionMonobit:[{pos,pValue,fail}]`. The decode and monobit math already exist; ~1 day incl. test.
  - evidence: sequencer_logic.cpp:362 (`per` = per-token bytes), :365-367 (flattened to `cat`, boundaries lost), :372-382 (single aggregate monobit); no per-position iteration anywhere.
- **#156 [sequencer] (day) [verified] — Rule scope -- URL scope (all / suite scope / custom include-exclude list)**
  - plan: Each SessionRule has exactly one hostGlob + one pathGlob, positive-match only (session_rules.hpp; matched at session_rules.cpp:65-66 via globToRx in session_rules_logic.cpp:12-25). No include/exclude list, no 'use suite scope' checkbox, no protocol/port -- 'everything except /logout' is inexpressible. Separately, an UNCONFIGURABLE suite-scope gate is imposed upstream: proxy_server.cpp:1493 `if(!isInScope(req.host)) return;` sits BEFORE injectInto (:1530) and applyToRequest (:1536), so an out-of-scope host gets no session handling regardless of hostGlob (defensible but undocumented). Closeable core win: add hostExclude/pathExclude glob fields to SessionRule and reject on exclude match in matches() (session_rules.cpp) with matching plumbing in /api/session-rules/set and the CLI JSON; that delivers the include/exclude expressiveness. Full suite-scope-checkbox + protocol/port UI is larger. ~1 day incl. _logic test.
  - evidence: session_rules.hpp SessionRule (single hostGlob/pathGlob, no exclude); session_rules.cpp:65-66 (positive match only); session_rules_logic.cpp:12-25 (globToRx, no negation); proxy_server.cpp:1493 (scope gate before session hooks at :1530/:1536).
- **#162 [sequencer] (day) [verified] — Cookie jar viewer: list + add / edit / remove individual cookies**
  - plan: Viewer is read + clear-host + clear-all + copy-host only; the backend mutation surface is exactly setAutoInject/clearHost/clearAll/copyTo (session_manager.cpp:146-191) -- no per-cookie add/edit/delete -- and the viewer hides Path and Expires (app.jsx:2113-2131 renders name/value/{HO,S,SameSite} in a 3-col grid), the two attributes that decide whether a cookie injects (path) and whether it is stale (expires). Closeable full-stack: (1) add SessionManager::upsertCookie(host,CapturedCookie)/removeCookie(host,name) (mirrors existing merge logic at :72-88) + control endpoints /api/sessions/cookie/{set,delete}; (2) in app.jsx add an editable row (edit value, delete button) and an 'Add cookie' form, and add Path + Expires columns from the already-captured c.path/c.expires. Primitives (full CapturedCookie struct, per-host store) exist; ~1 day incl. a session_manager_logic/control test.
  - evidence: session_manager.cpp:146-191 (mutation surface = autoInject/clearHost/clearAll/copyTo only), :72-88 (merge logic to mirror); ui-v2/app.jsx:2113-2131 (name/value/flags grid, no Path/Expires, no edit/add/delete).
- **#178 [sequencer] (day) [verified] — Character transition analysis (char at position i in token k vs token k-1)**
  - plan: Confirmed absent: positionalEntropy (sequencer_logic.cpp:252-298) builds a per-column MARGINAL histogram across the whole sample with no reference to token order; hammingDistance (:69-75) counts differing positions between consecutive tokens, not P(char_k\|char_{k-1}) per position. Add a new test over the modal-fixed-width corpus (reuse the modal-width selection at :256-265): for each column c, build a contingency table of (token[k-1][c], token[k][c]) over consecutive pairs and run a chi-square test of independence; flag columns whose transition distribution deviates significantly. Gate behind kDeepMinN and a per-column minimum count. Emit result['charTransition'] = { applicable, weakColumns, ... } and deduct in the score block (:624-629). Add a sequencer_test with a per-position-correlated generator (uniform marginals) to prove it catches what positionalEntropy misses. Self-contained backend + test; JSON-only surface.
  - evidence: sequencer_logic.cpp:252-298 (positionalEntropy: per-column marginal, `for (const QString &t : fw) cnt[t[c].unicode()]++`, no order); :69-75 (hammingDistance = count of differing positions between consecutive tokens).
- **#196 [sequencer] (day) [verified] — Cookie jar and session rules persist in the project file / user settings across restarts**
  - plan: Confirmed absent: neither SessionManager (m_byHost, session_manager.hpp:65) nor SessionRules (m_rules, session_rules.hpp:104) declares save/load; both are stack locals in app.cpp; project_store.cpp mentions cookie/session ONLY in redaction header lists (:236-248, :486-499), never persistence. Closeable in ~1 day: (1) add toJson()/fromJson() to SessionManager (serialize HostSession: host, autoInject, CapturedCookie fields) and SessionRules (serialize QList<SessionRule> — the control_server already has field-by-field (de)serialization at :1599/:3851 to reuse); (2) add sessionState + sessionRules sections to project_store's save/load schema; (3) in app.cpp load on startup and connect sessionsChanged/rulesChanged to a debounced save. HONEST NOTE: it touches two subsystems + the project-store schema + app wiring, so it is a full day at the upper edge of 'closeable', not a couple hours; primitives (project_store JSON I/O, rule serialization) all exist.
  - evidence: session_manager.hpp:65 & session_rules.hpp:104 (no save/load); project_store.cpp cookie/session hits only in redaction lists (236-248, 486-499); control_server.cpp:1599/:3851 existing per-field rule (de)serialization to reuse.
- **#366 [target] (day) [verified] — Site map tree roots keyed by protocol://host:port**
  - plan: Re-key the site map on origin, not bare host. In Src/FrontEnd/GUI/Models/Proxy/site_map_model.cpp change Entry to carry {port, tls} and build the lookup key as (tls?"https":"http")+"://"+host+((tls&&port==443)\|\|(!tls&&port==80)?"":":"+port). The source primitives already exist: ProxyModel::portAt(row) (proxy_model.hpp:38) and tlsAt(row) — use them in rebuild()/onSourceRowsInserted() instead of only HostRole. Add Origin/Port/Scheme role names. Update the sitemap serializer at control_server.cpp:1446-1459 to emit origin/port/scheme. Update the ui-v2/proxy.jsx SiteMap render (181-212) to key/label on origin. The openapi export at control_server.cpp:4683 already proves the exact scheme://host(:port) key can be built from available fields.
  - evidence: site_map_model.hpp:40-44 struct Entry {host,count,anyTls} — no port/scheme. site_map_model.cpp:63-73 & 83-96 key purely on HostRole. control_server.cpp:1453-1456 serializes only host/count/tls. ui-v2/proxy.jsx:181-208 renders a flat host list (one row per host, tls dot, count). ProxyModel does expose portAt() (proxy_model.hpp:38, cpp:198-200) so the data is present.
- **#385 [target] (day) [no-verdict] — Content discovery: thread count / concurrency and request throttling controls**
  - plan: Add throttleMs and concurrency ints to ContentDiscovery::Request (content_discovery.hpp). In discover(): honor throttleMs with a wait between get() calls (trivial), and fan the word loop out over a bounded set of N HttpClients (QtConcurrent::mapped or a small thread pool), merging Hits and preserving the requestsSent budget; keep the twin-token calibration serial (it must run first). Plumb both dials from control_server.cpp:8501 with qBound. Add a _logic/coverage test for the throttle/merge. HttpClient is per-call and stateless, so concurrency is safe.
  - evidence: content_discovery.cpp:99-104 one HttpClient, cpp:154-176 strictly serial `for (w : words) { get(full); }`. The Request struct carries host/port/tls/basePath/wordlist/headers/maxRequests only — no threads/throttle field.
- **#393 [target] (day) [no-verdict] — Per-item Comment and Highlight (six colours) on site map / history, driving filters and reporting**
  - plan: Confirmed absent: the rows table has no comment/colour/tag column (history_index.cpp:75-88: id/ts/method/host/port/path/status/size/tls/mime/req_json/resp_json), and grep for comment/highlight/annotation in history_index is empty. Closeable in ~1 day: (1) ALTER TABLE migration adding `comment TEXT` + `highlight INTEGER` (the table is CREATE TABLE IF NOT EXISTS, so add an idempotent PRAGMA-guarded ALTER on open); (2) add CommentRole/HighlightRole to ProxyModel + an in-memory setter; (3) a POST /api/history/annotate {id,comment,color} control endpoint; (4) ui-v2 proxy.jsx inline comment cell + a six-swatch highlight picker, row background from highlight. Filter-by-colour is a small add that folds into item 392. Medium confidence because the schema migration must be verified against existing project DBs.
  - evidence: Src/Core/Storage/history_index.cpp:75-88 (full rows schema, no annotation columns); grep comment\|highlight\|annotation in history_index.* -> empty.
- **#399 [target] (day) [no-verdict] — Target > Issue definitions: browsable library of every issue type the scanner can report**
  - plan: Confirmed absent as a browsable library, but the data is one accessor away. FindingEnricher's public API is per-finding only (finding_enricher.hpp:13-29: enrich/hasMapping/confidenceForKind) with no listKinds. Crucially the mappings live in a static, enumerable table: finding_enricher.cpp:21-22 `const QHash<QString, Mapping> &table()` with ~150 entries (each Mapping = CWE, OWASP, CVSS score+vector, compliance, fix summary; e.g. cpp:129,212,327), plus a family-prefix list at :379-380. Closeable in ~1 day: (1) add FindingEnricher::listDefinitions() returning table().keys() joined with Mapping fields; (2) a GET /api/issue-definitions endpoint enumerating them; (3) a small ui-v2 'Issue definitions' browse view. No detection logic needed — pure exposure of an existing table.
  - evidence: finding_enricher.hpp:13-29 (per-finding API, no listKinds); finding_enricher.cpp:21-22 static QHash<QString,Mapping> table(), entries :129/:212/:327, family list :379-380; grep listKinds\|/api/kinds -> empty.
- **#402 [target] (day) [no-verdict] — Content discovery: recursive descent into discovered directories**
  - plan: Confirmed absent: content_discovery.cpp discover() is a single flat pass — base is assigned once (cpp:90 `const QString base = normBase(req.basePath)`) and one flat `for (const QString &w : words)` loop (cpp:154-176) builds candidates as `base + '/' + rel` with no re-descent. Closeable in ~1 day, backend-small: after the flat pass, collect directory-like hits (3xx to a trailing-slash Location, or 200 index-looking) and re-invoke discover() on each with an incremented depth, guarded by a max-depth and the existing requestsSent/maxReq budget and m_seen-style dedup to avoid loops. The classify()/calibration/get primitives are reusable per level. Add a maxDepth field to Request. Verifiable with a nested-dir fixture.
  - evidence: content_discovery.cpp:90 base assigned once, :154-176 flat loop, :159 full=base+'/'+rel (no recursion); content_discovery.hpp:33-41 Request has no depth field.
- **#405 [target] (day) [no-verdict] — Engagement tools > Analyze target (static vs dynamic URL counts, unique parameter names, per-URL entry points)**
  - plan: Confirmed absent: grep target-analyz/analyzeTarget/uniqueParam/entry-point across Src/ui-v2 -> nothing. But the exact history-walk primitive exists next door: /api/openapi/export already iterates captured history building a nested QMap<host, QMap<path, QMap<method, OpInfo>>> (control_server.cpp:4664-4736). Closeable in ~1 day, backend-small + small frontend: add /api/target/analyze that walks the same history and returns counts of static vs dynamic URLs (dynamic = has query/params), the set of distinct parameter names (params are already parsed — ParamsRole/req parsing), and per-host entry-point paths; render as a small stats panel. Medium confidence on the exact static/dynamic heuristic and where param names are most cheaply enumerated.
  - evidence: grep targetAnalyzer\|uniqueParam\|entry point -> none; control_server.cpp:4664-4736 /api/openapi/export walks history into QMap<host,QMap<path,QMap<method,OpInfo>>> (reusable pattern).

### infra (1)

- **#220 [platform] (day) [verified] — Container/Docker distribution for headless + CI**
  - plan: Dockerfile exists (multi-stage, aqtinstall Qt 6.7.3, non-root uid 10001) but is self-declared unvalidated, bases are tag-pinned not digest-pinned, and no CI job ever builds it. To close: (1) pin `FROM ubuntu:22.04@sha256:...` on both build and runtime stages and pin QT_VERSION already done; (2) add a `build-docker` job to .github/workflows/ci.yml (or a new docker.yml) that runs `docker build -t nullock .` on push so it stays green; (3) optionally push to GHCR on tag via release.yml. The recipe already mirrors the green Linux CI build, so validation is mostly iterating the build once. Files: Dockerfile:16-17,23,47; .github/workflows/ci.yml or new docker.yml.
  - evidence: Dockerfile:14-17 `has not yet been validated with docker build ... Pin Qt/base image digests`; :23 `FROM ubuntu:22.04 AS build`; :47 `FROM ubuntu:22.04 AS runtime` ARG QT_VERSION=6.7.3; :52 `useradd -m -u 10001 nullock`. .github/workflows/ contains ci.yml, release.yml, pages.yml, nullock-scan-example.yml — none reference docker (grep -rln docker .github/ empty).
